# v1.12 Risk Register

Severity logic (consistent with the prior stabilization train):

- **S0** — catastrophic: silent data corruption, reachable data deletion, false verify success,
  snapshot retention violation, or path traversal outside the intended root.
- **S1** — critical: crash-consistency failures, serious JSON automation breakage, unsafe filename
  trust, and migration parity failures.

| ID | Risk | Severity | Area | Phase target | Status | Notes |
|---|---|---|---|---|---|---|
| CK-112-R001 | Engine `Verify` does not honor engine-owned DB | S1 | engine/verify | Phase 1 | fixed | Fixed in v1.12.1: `DefaultEngine.Verify` now delegates to `maintenance.VerifyCommandWithDBAndContainersDir(Config.DB, ...)` instead of the global path that called `db.ConnectDB()`. Regression tests: `internal/engine/verify_db_ownership_test.go`. |
| CK-112-R002 | Existing candidate mutating request/result structs are too thin | S1 | engine contracts | Phase 2 | fixed | Fixed in Phase 2: `internal/engine/candidates.go` contracts expanded to represent real command behavior (store/restore/remove/gc/snapshot create-list-show-stats-diff-delete-restore/repair/recovery), with shared neutral types (`OperationWarning`, `BatchSummary`, `ExecutionMode`, `SnapshotQuery`) and enums for restore/remove modes and destination modes. Recovery re-modeled from an incorrect restore-like placeholder to a corrective report. Renderer/backend neutrality enforced by `TestCandidateContractFieldTypesAreNeutral`; representability proven by per-operation construction tests in `internal/engine/contracts_test.go`. Deferrals (batch `InputPath` ownership, no error taxonomy, no interface methods/routing) documented in `engine-baseline.md`. No routing performed; activation gated by later phases. |
| CK-112-R003 | Catalog facade may accidentally become SQLite-specific | S1 | catalog/db | Phase 4 | fixed | Fixed in Phase 4: implemented catalog methods now have dual-backend contract tests (`internal/catalog/backend_contract_test.go`) covering `FindLogicalFile`, `FindPhysicalFilesForLogicalFile`, `FindSnapshot`, `ListSnapshots`, `LoadReachabilityRoots`, and the deferred `ErrNotImplemented` methods. SQLite runs unconditionally; PostgreSQL runs when `COLDKEEP_TEST_DB` is set (CI provides a `postgres:16` service and sets it) and skips with an explicit reason otherwise. The baseline uncovered and fixed a real timestamp-bind incompatibility in `ListSnapshots` (pre-formatted RFC3339 string vs go-sqlite3's space-separated storage); the fix binds `time.Time` directly. Dialect rules documented in `sqlite-postgres-baseline.md`. PostgreSQL execution must remain enabled in CI/pre-release (Phase 12). |
| CK-112-R004 | CLI behavior drift during routing | S1 | CLI/engine | all migration phases | open (partial) | Phase 5: snapshot list/show/stats/diff parity proven. Phase 6: gc dry-run/live parity proven by `internal/engine/gc_engine_test.go` and `cmd/coldkeep/gc_engine_routing_test.go`. Phase 7: restore-by-ID live and dry-run paths routed with CLI parity guards (`cmd/coldkeep/restore_engine_routing_test.go`). Phase 8: single-file store routed with CLI parity guard (`cmd/coldkeep/store_engine_routing_test.go`). Phase 9: remove-by-ID live and dry-run routed with CLI parity guards (`cmd/coldkeep/remove_engine_routing_test.go`). Phase 10: verify command execution path routed via seam with parity guards (`cmd/coldkeep/verify_engine_routing_test.go`) and dependency boundary tightened (`internal/engine/dependency_guard_test.go`). Remaining deferred operations (stored-path remove modes, repair, recovery, store-folder, stored-path/snapshot restore) open. |
| CK-112-R005 | JSON output drift during routing | S1 | CLI/rendering | all migration phases | open (partial) | Phase 5: JSON envelope and field shape for snapshot commands verified. Phase 6: gc JSON shape verified by `TestGCDryRunEngineRoutingJSON`. Phase 7: restore batch JSON envelope remains unchanged for routed restore-by-ID live and dry-run paths (`TestRestoreByIDEngineRoutingJSON`, `TestRestoreByIDDryRunEngineRoutingJSON`). Phase 8: store JSON envelope remains unchanged for routed single-file store (`TestStoreByFileEngineRoutingJSON`). Phase 9: remove batch JSON envelope remains unchanged for routed remove-by-ID live and dry-run paths (`TestRemoveByIDEngineRoutingJSON`, `TestRemoveByIDDryRunEngineRoutingJSON`). Phase 10: verify system/file JSON envelope and fields remain unchanged under routed verify path (`TestVerifySystemEngineRoutingJSON`, `TestVerifyFileEngineRoutingJSON`). Remaining deferred operations open. |
| CK-112-R006 | Restore safety validation could remain CLI-only | S0/S1 | restore/catalog | Phase 7 | mitigated (partial) | Phase 7: `Engine.Restore` added and active for file-ID mode; CLI restore-by-ID live and dry-run execution now route through engine using injected DB/container dependencies. Engine tests cover dry-run behavior, non-write guarantees, and deferral boundary (`internal/engine/restore_engine_test.go`). Stored-path restore and snapshot restore remain direct paths; `LoadRestorePlanMetadata` remains `ErrNotImplemented`. Traversal/symlink safety remains enforced in existing storage/pathsafe paths; full catalog-backed restore-plan safety remains open for later phase closure. |
| CK-112-R007 | GC reachability may be split between old/new paths | S0/S1 | GC/catalog | Phase 6 | mitigated | Phase 6: GC dry-run and live both route through a single engine path (`DefaultEngine.GarbageCollect` → `maintenance.RunGCWithDB`). Packed and legacy root handling is unchanged and contained entirely inside `RunGCWithDB`. GC must never delete reachable data invariant preserved: live GC refused on SQLite; test `TestGCLiveRefusedOnSQLite` confirms. `LoadGCPlanMetadata` remains `ErrNotImplemented` (reachability catalog API deferred; not needed for current GC correctness). |

## Notes

- Risks discovered during a phase are added here, not silently fixed out of scope.
- A risk is only closed with evidence (test, parity proof, or documented accepted deferral).
- No release-tracker row is closed without evidence.
