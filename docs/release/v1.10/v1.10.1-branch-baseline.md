# v1.10.1 Branch Baseline

Status: Complete  
Owner phase: v1.10.1 Phase 0 — Branch Baseline & Scope Lock

## Release

v1.10.1 — CLI Correctness & Contract Stabilization

## Starting Point

v1.10.1 starts from the released v1.10.0 baseline.

## Branch

Feature branch:

```text
feature/v1.10.1-cli-contract-stabilization
```

## Baseline Checks

| Check | Result | Notes |
|---|---|---|
| main updated with `git pull --ff-only` | Pass | `main` was verified up to date with `origin/main`; pull result was `Already up to date.` |
| Working tree clean before branch | Pass | `git status` was clean before `feature/v1.10.1-cli-contract-stabilization` was created. |
| `v1.10.0` tag exists | Pass | `git tag --list "v1.10.0"` returned `v1.10.0`. |
| `v1.10.0` tag is ancestor of `main` | Pass | `git merge-base --is-ancestor v1.10.0 main` succeeded. |
| Branch created from `main` | Pass | `feature/v1.10.1-cli-contract-stabilization` is based on `main`. |

## Baseline Commit

`3b2fdbe3fed7e1b527648929b14e3ec5bcc93a3c`

## Baseline Tag

`v1.10.0`

## Scope Statement

v1.10.1 is the first remediation release after the v1.10.0 baseline/freeze release.

It is limited to CLI/JSON contract correctness and directly related regression coverage.

No implementation has started in Phase 0.