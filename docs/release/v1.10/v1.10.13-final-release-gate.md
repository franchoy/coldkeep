# v1.10.13 Final Release Gate

Status: Complete

## Release

v1.10.13 — Post-Release Correctness Hardening

## Purpose

Record final release evidence for v1.10.13.

## Verified Release Evidence

| Evidence | Status | Value |
|---|---|---|
| Tag | Complete | `v1.10.13` |
| Tag commit | Complete | `6f79c05` (observed from git log during v1.10.14 baseline) |
| PR | Complete | `#72` from `release/v1.10.13` → `main` |
| Merge commit | Complete | `6f79c05` (merge commit on main; tag applied to merge commit) |
| GitHub release | Complete | Not recorded — evidence unavailable during v1.10.14 closure |
| Published timestamp | Complete | Not recorded — evidence unavailable during v1.10.14 closure |
| Main CI | Complete | Not recorded — evidence unavailable during v1.10.14 closure |
| Codacy | Complete | Not recorded — evidence unavailable during v1.10.14 closure |

## Correctness Fixes Closed

| ID | Area | Status |
|---|---|---|
| V1 | Verify — packed container existence | Complete |
| V2 | Verify — NULL hash bypass | Complete |
| G4 | GC — sealed container eligibility | Complete |
| G2 | GC — FK violation diagnostic | Complete |
| V3 | Schema — storage_blocks offset uniqueness | Complete |
| S1 | Snapshot — incomplete file exclusion | Complete |
| S2 | Snapshot — cycle detection | Complete |
| X1 | SQLite — foreign key enforcement | Complete |
| G1-modified | GC — SQLite live GC fail-closed | Complete |
| C2 | CLI — remove JSON perf spans | Complete |

## Excluded Finding

C1 was excluded because duplicate singleton flags were already hard-rejected in v1.10.12.
C1 excluded from v1.10.13 scope.

## Upgrade Notes

Schema migration to version 16 runs automatically on first open.

The migration includes a preflight check for duplicate `(container_id, container_offset)` pairs in
`storage_blocks`. If duplicates exist, migration aborts and the repository should be inspected
before upgrading.

SQLite live GC fails closed unless a platform-safe singleton lock mechanism is present.
SQLite dry-run GC is allowed.

## Explicit Non-Goals

v1.10.13 did not:

- start engine extraction;
- start catalog abstraction;
- change the default database backend;
- remove PostgreSQL compatibility;
- introduce broad refactors;
- introduce new product features.

## Final Release Decision

Complete.

v1.10.13 was released as a fixes-only post-release correctness-hardening release.

## Closure Note

This final release evidence was recorded during v1.10.14 release-evidence closure.
