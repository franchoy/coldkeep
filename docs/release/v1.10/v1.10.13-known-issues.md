# v1.10.13 Known Issues

Status: Active

## Deferred Issues

The following issues were identified during the v1.10.12 adversarial audit but are deferred beyond v1.10.13.

### v1.10.14 — Release Evidence Closure

The following items are deferred to v1.10.14:

- v1.10.12 stale final release evidence cleanup;
- v1.10.13 final release evidence creation;
- README roadmap note update;
- CHANGELOG missing v1.10.7–v1.10.13 entries;
- issue-tracker source-of-truth clarification;
- v1.11 handoff gate documentation.

### v1.11.0 — Architecture

- Engine facade boundary introduction;
- Catalog abstraction implementation;
- SQLite-safe GC singleton lock (currently fail-closed in v1.10.13);
- Default database backend migration.

## Retracted Findings (Not Issues)

The following were investigated and confirmed as false alarms during the v1.10.12 adversarial audit:

| ID | Finding | Reason Retracted |
|----|---------|-----------------|
| G3 | Dry-run misleads for packed GC | Dry-run calls containerHasLivePhysicalUnits — confirmed correct |
| R1 | Recovery sealing+sealed size check | sealed=TRUE means seal completed; clearing sealing=FALSE without size check is correct |
| S3 | Orphaned snapshot_path rows | Both ResolveSnapshotPaths and insertSnapshotFilesByPathIDNoReturningBatch are in same tx; rollback is clean |
| S4 | DeleteSnapshot SQLite concurrent gap | Absorbed into X1 (SQLite FK enforcement); SQLite currently test-only |
