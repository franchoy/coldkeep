# v1.10.1 Local Validation

Status: Complete

## Release

v1.10.1 — CLI Correctness & Contract Stabilization

## Purpose

This document records local validation and CI simulation for v1.10.1 before release-candidate preparation.

Phase 11 does not add new remediation scope.

If validation fails, only release-blocking fixes may be made, and validation must be rerun.

## Baseline

| Field | Value |
|---|---|
| Branch | feature/v1.10.1-cli-contract-stabilization |
| Commit | e9fcd77b08ddf2237de6434838fe86eb9d728ab4 |
| Date | 2026-05-16T08:45:09Z |
| OS | Linux 6.14.0-37-generic #37~24.04.1-Ubuntu SMP PREEMPT_DYNAMIC Thu Nov 20 10:25:38 UTC 2 x86_64 GNU/Linux |
| Go version | go version go1.23.1 linux/amd64 |
| Working tree clean before validation | yes |

## Scope Confirmation

Phase 11 validates the v1.10.1 branch after completed Phases 3-10.

It does not intentionally change:

- production code
- tests
- CI workflows
- dependencies
- issue tracker rows (except release-blocking closure_proof backfill for fixed linked rows)
- remediation matrix rows
- storage / GC / restore / packed-storage internals

## Targeted Regression Validation

| Command | Result | Notes |
|---|---|---|
| `go test ./... -run 'UnexpectedPositional|PositionalArgs|ExtraArg|FlagToken|ValueFlag|FlagValue|Requires.*Value|Boolean|Bool|Reverse|False|InvalidBoolean|Duplicate|Singleton|Repeated|Flag.*Duplicate|JSONShorthand|JsonShorthand|OutputJSON|OutputJson|OutputMode|FormatJSON|JSONOutput|JsonOutput|MachineReadable|SingleJSON|NoHuman|Stdout|Stderr|PreStateful|Stateful|BeforeRepo|BeforeInit|BeforeKey|NoMutation|InvalidUsage|CLI' -count=1` | pass | all targeted suites green |

## Formatting Validation

| Command | Result | Notes |
|---|---|---|
| `gofmt -l cmd/coldkeep/main.go cmd/coldkeep/init.go cmd/coldkeep/main_test.go` | pass | no output |
| `git diff --check` | pass | no output |

## Static Validation

| Command | Result | Notes |
|---|---|---|
| `go vet ./...` | pass | no diagnostics |

## Full Test Validation

| Command | Result | Notes |
|---|---|---|
| `go test ./...` | pass | full suite green |

## Race Validation

| Command | Result | Notes |
|---|---|---|
| `go test -race ./...` | pass | full race suite green |

## Pre-Release Checklist / Local CI Simulation

| Command / Check | Result | Notes |
|---|---|---|
| `scripts/pre_release_checklist.sh` | not-present | script does not exist in this checkout |
| `scripts/smoke.sh` | exception-documented | failed with `coldkeep binary is required on PATH` in this shell |
| `scripts/audit_ci_enforcement.sh` | pass | local + remote enforcement audit passed |
| `go vet ./...` | pass | local CI static validation |
| `go test ./...` | pass | local CI test validation |

## Closure Data Validation

| Command | Result | Notes |
|---|---|---|
| `python3 closure consistency check` | pass after corrective rerun | initial run found 47 fixed linked tracker rows missing `closure_proof`; backfilled from matrix closure proof and reran to green |

## Validation Conclusion

Result: ready-with-documented-exception

Blocking failures: none

Documented exceptions: `scripts/pre_release_checklist.sh` is not present in this checkout; `scripts/smoke.sh` failed in this environment because `coldkeep` is not on PATH.

## Phase 11 Completion Statement

The v1.10.1 local validation and CI simulation phase is complete.

Phase 11 completed:

- targeted v1.10.1 CLI/JSON regression validation
- formatting validation
- static validation
- full Go test validation
- race validation or documented deferral
- pre-release checklist / local CI simulation recording
- closure consistency validation

Phase 11 did not:

- add new remediation scope
- change production behavior intentionally
- change CI workflows
- change dependencies
- alter v1.10.1 closure decisions
