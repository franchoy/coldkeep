# v1.10.1 Safe Test Strategy

**Phase**: 2  
**Purpose**: Define safe test strategy for Phase 3-9 implementation  
**Status**: Complete  

## Test Coverage Analysis

### Current State
- **Total existing tests identified**: 11 test entries across 5 matrix rows
- **Fully covered**: 2 matrix rows (M029, M086) - 100% coverage
- **Gaps identified**: 3 matrix rows (M028, M030, M033) - 0% coverage, need regression test creation

### Coverage by Matrix Row

| Matrix ID | Test Class | Existing Tests | Coverage % | Status |
|-----------|------------|----------------|------------|--------|
| CK-110-M029 | unexpected-positional-args | 2 | 100% | Ready for Phase 3 |
| CK-110-M086 | flag-value-validation | 6 | 100% | Ready for Phase 4 |
| CK-110-M028 | boolean-explicit-false | 0 | 0% | **Gap: needs test creation before Phase 5** |
| CK-110-M030 | duplicate-singleton-flags | 0 | 0% | **Gap: needs test creation before Phase 6** |
| CK-110-M033 | pre-stateful-validation | 0 | 0% | **Gap: needs test creation before Phase 9** |

## Test Implementation Strategy

### Phase 3: Reject Unexpected Positional Arguments (M029)

**Status**: Ready - existing tests provide baseline coverage

**Existing Test Coverage**:
- `TestRunRemoveCommandStoredPathRejectsPositionals` - validates remove fails with positional args
- `TestRunRestoreCommandStoredPathRejectsPositionals` - validates restore fails with positional args

**Safe Implementation Approach**:
1. **Pre-implementation verification**: Run existing tests to establish baseline
   - Command: `go test ./cmd/coldkeep -run TestRun.*CommandStoredPathRejectsPositionals -v`
   - Expected: All tests pass
   
2. **Expand coverage scope** (non-blocking for Phase 3):
   - Future regression tests needed for: list, search, inspect, verify, all snapshot subcommands, repair, config, simulate
   - Can be deferred to future phase if Phase 3 focus is stored-path mode only
   
3. **Implementation gates**:
   - Must not break existing tests during implementation
   - Must verify behavior with both stored-path and input modes (if applicable)

### Phase 4: Reject Invalid Flag-Value Combinations (M086)

**Status**: Ready - existing tests provide coverage

**Existing Test Coverage**:
- `TestResolveTraceOptionsRejectsConflictingTraceFlags` - trace flag conflicts
- `TestValidateNonNegativeIntegerFlagUsesLastValue` - integer flag handling
- `TestValidateNonNegativeIntegerFlagRejectsLimitAboveMaximum` - integer bounds
- `TestRunSnapshotCommandRestoreRejectsInvalidQueryFlagValues` - snapshot restore --query
- `TestRunSnapshotCommandShowRejectsInvalidQueryFlagValues` - snapshot show --query
- `TestRunSnapshotCommandDiffRejectsInvalidQueryFlagValues` - snapshot diff --query

**Safe Implementation Approach**:
1. **Pre-implementation verification**: Run all 6 tests to establish baseline
   - Command: `go test ./cmd/coldkeep -run TestResolveTraceOptions|TestValidateNonNegative|TestRunSnapshotCommandShow|TestRunSnapshotCommandRestore|TestRunSnapshotCommandDiff -v`
   - Expected: All tests pass
   
2. **Expand coverage scope** (non-blocking):
   - Future tests for: --flag=token pattern specifically
   - Tests for other commands using value flags beyond snapshot subcommands
   
3. **Implementation gates**:
   - Flag validation logic must pass all existing matrix row tests
   - Value validation must work consistently across all commands

### Phase 5: Boolean Explicit False Handling (M028)

**Status**: BLOCKED - No existing tests. Requires test creation before implementation.

**Test Gap**: Complete absence of tests for `--keep-metadata=false` vs `--keep-metadata` parsing

**Affected Commands**: 
- snapshot create
- snapshot list
- snapshot delete
- snapshot stats

**Required Regression Tests** (Must be created in Phase 2 post-work or Phase 5 pre-work):

1. **Test: Boolean flag with explicit false value**
   - Input: `coldkeep snapshot create ... --keep-metadata=false`
   - Expected: Explicit false value (not default true) should be honored
   - Coverage: All snapshot subcommands with boolean flags

2. **Test: Boolean flag without explicit value**
   - Input: `coldkeep snapshot create ... --keep-metadata` (without =false)
   - Expected: Flag presence means true, absence means false
   - Coverage: All snapshot subcommands

3. **Test: Boolean flag with invalid values**
   - Input: `coldkeep snapshot create ... --keep-metadata=maybe`
   - Expected: Error with clear message about valid values (true/false/1/0)
   - Coverage: Validation of boolean flag values

4. **Test: Default boolean value without flag**
   - Input: `coldkeep snapshot create ...` (--keep-metadata omitted)
   - Expected: Default value applies (verify correct default for each flag)
   - Coverage: Default behavior verification

**Safe Implementation Approach**:
1. Create regression tests FIRST (TDD approach for M028)
   - Write tests for --keep-metadata=false behavior
   - Verify tests fail with current implementation
   
2. Implement flag parsing changes in Phase 5
   - Implement --keep-metadata=false parsing
   - Run tests to verify fix
   - Ensure no regressions in other commands
   
3. Implementation gates:
   - All new regression tests must pass
   - No breaking changes to existing boolean flag behavior

### Phase 6: Duplicate Singleton Flag Error (M030)

**Status**: BLOCKED - No existing tests. Requires test creation before implementation.

**Test Gap**: Complete absence of tests for duplicate flag detection (--flag ... --flag error)

**Affected Commands**:
- list, search, inspect, verify
- snapshot create, snapshot list, snapshot delete
- repair, config, simulate

**Required Regression Tests** (Must be created in Phase 2 post-work or Phase 6 pre-work):

1. **Test: Singleton flag specified twice**
   - Input: `coldkeep list ... --config /path/1 --config /path/2` (if config is singleton)
   - Expected: Error with message "duplicate flag: --config"
   - Coverage: All known singleton flags

2. **Test: Singleton flag with duplicate values**
   - Input: `coldkeep search ... --filter pattern --filter pattern`
   - Expected: Error (or uses last value, depending on semantics)
   - Coverage: Verify singleton semantics are enforced

3. **Test: Non-singleton flags allow duplicates**
   - Input: Command with non-singleton flags specified multiple times
   - Expected: Either uses last value or accumulates (depending on flag type)
   - Coverage: Ensure correct flags are enforced as singleton

**Safe Implementation Approach**:
1. Identify all singleton flags in Phase 6 analysis
   - Inventory which flags should reject duplicates
   - Document singleton flag list in matrix row details
   
2. Create regression tests FIRST
   - Test duplicate detection for each singleton flag
   - Verify tests fail with current implementation
   
3. Implement duplicate flag detection in Phase 6
   - Add validation logic to flag parser
   - Run tests to verify fix
   - Ensure non-singleton flags still work correctly
   
4. Implementation gates**:
   - All new tests must pass
   - No breaking changes to multi-value flags
   - Clear error messages for duplicate singleton flags

### Phase 9: Pre-Stateful CLI Validation (M033)

**Status**: BLOCKED - No existing tests. Requires test creation before implementation.

**Test Gap**: Complete absence of tests for CLI validation before repository initialization (fail-fast)

**Affected Commands**: All commands requiring repository (list, search, inspect, verify, snapshot *, repair)

**Required Regression Tests** (Must be created in Phase 2 post-work or Phase 9 pre-work):

1. **Test: Invalid flag fails before repository stat check**
   - Input: `coldkeep list --invalid-flag` (invalid flag)
   - Expected: Error about invalid flag BEFORE attempting to read repository state
   - Verify: Error classification as usage error (not storage/verify error)
   - Timing: Should fail in milliseconds (no repo access)

2. **Test: Valid flags pass pre-stateful check**
   - Input: `coldkeep list --format json` (valid flag, bad/missing repo)
   - Expected: Error about missing/invalid repo (not about flags)
   - Verify: Validates flags before repository errors
   - Timing: Should fail after flag validation, before repo stat

3. **Test: Ordering - flag validation before repo initialization**
   - Input: Invalid flag + invalid repository path
   - Expected: Error message about invalid flag (not repository)
   - Coverage: Verify ordering of validation layers

4. **Test: Repository-dependent flag validation**
   - Input: `coldkeep verify --fix` (--fix may require specific repo state)
   - Expected: CLI-level flag validation passes, then repo check happens
   - Coverage: Ensure flags validated before stateful operations

**Safe Implementation Approach**:
1. Analyze CLI parsing flow in Phase 9 analysis
   - Identify where repository access happens
   - Identify where flag validation happens
   - Determine reordering needed for fail-fast
   
2. Create regression tests FIRST
   - Test invalid flags with/without bad repository
   - Verify error ordering and classification
   
3. Implement pre-stateful validation in Phase 9
   - Reorder validation: flags first, then repository access
   - Ensure errors classified correctly (usage vs. storage)
   - Run tests to verify fix
   
4. Implementation gates**:
   - All new tests must pass
   - No breaking changes to valid flag handling
   - Error messages must be clear about validation stage

## Test Execution and Validation

### Pre-implementation Gates
For each phase:
1. Run baseline tests: `go test ./cmd/coldkeep -run Test<Phase> -v`
   - Verify all existing tests pass
   - Document baseline output
   
2. Run cross-module tests: `go test ./... -run Test<Feature> -v`
   - Ensure no regressions in storage, batch, or other modules
   - Verify only CLI-related changes
   
3. Run full CLI test suite: `go test ./cmd/coldkeep -v`
   - All 50+ tests in main_test.go must pass

### Post-implementation Validation
1. Regression testing
   - All baseline tests must still pass
   - New tests for M028, M030, M033 must pass
   
2. Cross-command validation
   - Test affected commands with new validation
   - Verify error messages and exit codes
   
3. JSON output validation
   - Verify error JSON schema for all new validation errors
   - Confirm alignment with existing error patterns

### Test Strategy Summary

| Phase | Matrix Row | Test Status | Strategy |
|-------|-----------|-------------|----------|
| 3 | M029 | Ready | Use existing 2 tests as baseline, expand post-implementation |
| 4 | M086 | Ready | Use existing 6 tests as baseline, expand post-implementation |
| 5 | M028 | Blocked | **Create tests first (TDD), then implement** |
| 6 | M030 | Blocked | **Create tests first (TDD), then implement** |
| 9 | M033 | Blocked | **Create tests first (TDD), then implement** |

**Key Risk Mitigations**:
- M028, M030, M033 use TDD (tests before code) to ensure safety
- All phases run full CLI test suite post-implementation
- Cross-module testing prevents storage/batch regressions
- Clear error message validation prevents usability regressions

---

**Phase 2 Status**: Safe test strategy defined. Ready for Phase 3 kickoff with baseline tests confirmed.
