# v1.10.14 Scope — Release Evidence Closure & v1.11 Handoff Cleanup

Status: Complete

## Release

v1.10.14 — Release Evidence Closure & v1.11 Handoff Cleanup

## Purpose

v1.10.14 closes the v1.10.x stabilization train and prepares the repository for v1.11 behavior-preserving engine facade work.

This is a documentation and release-control release only.

## Core Invariant

v1.10.14 may close release evidence and prepare handoff documentation, but it must not change runtime behavior or start v1.11 implementation work.

## In Scope

- close stale v1.10.12 release evidence;
- close v1.10.13 release evidence;
- update README roadmap state;
- update CHANGELOG through v1.10.13;
- clarify the v1.10 issue tracker source of truth;
- document Codacy false-positive handling;
- create the v1.11 handoff gate;
- create the v1.10.14 final release gate.

## Out of Scope

v1.10.14 must not:

- change Go source files;
- change tests;
- change CI;
- change scripts;
- change dependencies;
- change runtime behavior;
- change CLI behavior;
- change JSON behavior;
- change schema or migration behavior;
- start engine extraction;
- create `internal/engine`;
- start catalog abstraction;
- create `internal/catalog`;
- change the default database backend;
- remove PostgreSQL compatibility;
- implement SQLite-first behavior;
- perform v1.11 implementation work.

## Release Position

v1.10.13 was the final correctness-hardening release in the v1.10.x train.

v1.10.14 is the final evidence-closure and handoff cleanup release before v1.11.

v1.11 may begin only after v1.10.14 is complete.

## v1.11 Handoff Rule

The first v1.11 phase must be behavior-preserving engine facade work.

v1.11 must wrap current behavior first, prove CLI/JSON/exit-code parity, and only then begin lifting logic.
