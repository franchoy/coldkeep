# v1.10.1 Matrix Inventory

Status: Complete  
Owner phase: v1.10.1 Phase 1 — Extract v1.10.1 Matrix Rows

## Release

v1.10.1 — CLI Correctness & Contract Stabilization

## Purpose

This document records the remediation matrix rows selected for v1.10.1.

v1.10.1 is limited to CLI and JSON contract stabilization.

This inventory is the source of truth for the implementation phases that follow.

## Selection Rules

A matrix row belongs to v1.10.1 if it primarily concerns one of:

- unexpected positional arguments
- CLI arity validation
- CLI value flags accepting another flag token as value
- boolean flags treating explicit `false` as `true`
- duplicate singleton flags
- JSON shorthand consistency
- JSON mode and output-mode contract
- mixed human output in machine-readable mode
- validation before repository initialization where the defect is caused by CLI misuse

## Exclusion Rules

A matrix row does not belong to v1.10.1 if the fix primarily requires:

- packed-storage internals
- GC, refcount, or reachability logic
- restore or recovery internals
- filesystem abstraction
- filesystem fault injection
- cross-platform CI expansion
- Codacy hard blocking
- dependency or toolchain upgrade
- repository format change
- engine extraction
- broad CLI redesign
- unrelated refactor

## Phase 1 Boundary

Phase 1 identifies inventory only.

Phase 1 does not:

- implement fixes
- close issue rows
- close remediation-matrix rows
- change issue or matrix status
- add closure proof

# Inventory Summary

Total candidate rows reviewed: 31
Included rows: 5
Excluded/deferred/needs-split rows: 26

## Included Rows By Phase

### Phase 3 — Unexpected Positional Arguments

| Matrix ID | Severity | Domain | Title |
|---|---|---|---|
| `CK-110-M029` | S2 | cli | Reject ignored extra positional arguments |

### Phase 4 — Value Flags Reject Flag Tokens

| Matrix ID | Severity | Domain | Title |
|---|---|---|---|
| `CK-110-M086` | S2 | cli | Reject flag-token values for value flags |

### Phase 5 — Boolean Explicit False Semantics

| Matrix ID | Severity | Domain | Title |
|---|---|---|---|
| `CK-110-M028` | S2 | cli | Normalize explicit false boolean flag handling |

### Phase 6 — Duplicate Singleton Flags

| Matrix ID | Severity | Domain | Title |
|---|---|---|---|
| `CK-110-M030` | S2 | cli | Singleton flags must reject duplicate occurrences instead of last-win behavior |

### Phase 9 — Pre-Stateful Validation Ordering

| Matrix ID | Severity | Domain | Title |
|---|---|---|---|
| `CK-110-M033` | S2 | validation | CLI validation must happen before repository initialization when possible |

## Non-Included Rows

See `docs/release/v1.10/v1.10.1-exclusion-notes.md` for rationale on excluded (15), deferred (7), and needs-split (4) rows.

Key principles:

- **Excluded (15 rows)**: Out of v1.10.1 scope (packed-storage, GC, restore/recovery, filesystem, CI, Codacy, tooling, security scanner review, complexity review, etc.)
- **Deferred (7 rows)**: Related to CLI/JSON but intentionally moved to later v1.10.x releases (snapshot normalization, numeric validation, benchmarks, scripts, restore mode validation)
- **Needs-split (4 rows)**: Partially in v1.10.1; require decomposition (JSON shorthand + machine output, CLI contract backlog umbrella, snapshot backlog umbrella, verify backlog umbrella)

# Phase 1 Completion Statement

The v1.10.1 matrix and issue inventories have been extracted.

Phase 1 completed:

- candidate matrix row extraction from remediation-matrix.csv
- manual inclusion/exclusion/defer/needs-split review
- phase owner assignment for 5 included rows
- raw issue inventory generation (33 issues from issue-tracker.csv)
- exclusion notes creation with scope protection rules
- human-readable inventory summary

Phase 1 did not:

- implement fixes
- change production code (cmd/, internal/, tests/)
- change test files
- change scripts
- change CI workflows (.github/)
- close issue tracker rows
- close remediation matrix rows
- change working tree status files (issue-tracker.csv, remediation-matrix.csv remain unchanged)
- add closure proof
