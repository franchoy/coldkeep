# v1.10.3 PR Notes

## Title

v1.10.3 — Packed Storage Metadata Integrity

## Summary

This PR finalizes the v1.10.3 fixes-only release focused on packed-storage metadata integrity guardrails, corruption regression coverage, tracker/matrix closure accounting, and local validation evidence.

This phase is documentation-only and release-candidate preparation only. It does not open a PR and does not change runtime behavior.

## Scope

Included:

- packed metadata integrity invariant documentation and validation evidence
- packed manifest/index and bounds/hash/read-safety guardrail evidence
- packed compatibility guardrail evidence
- corruption regression matrix evidence
- tracker/matrix closure outcomes for selected v1.10.3 rows
- local validation and CI-equivalent simulation evidence
- release-candidate documentation handoff (PR notes, release notes, changelog)

Excluded:

- engine extraction
- catalog abstraction
- new product features
- GC reachability corrections
- broad restore/recovery rewrites
- CI policy changes
- implementation refactors for deferred test-complexity findings

## Phase Evidence and Commits

| Phase | Commit | Scope |
|---:|---|---|
| 8 | `8e41780` | tracker/matrix closure update for CK-110-M035/M060/M061 |
| 9 | `447337c` | local validation and CI-equivalent simulation record |
| 10 | pending (this change) | release-candidate documentation and handoff |

## Matrix Closure Outcomes

- CK-110-M035: accepted (3 linked production-complexity findings; S2 tracking retained)
- CK-110-M060: deferred (14 linked test-complexity findings; out of fixes-only scope)
- CK-110-M061: deferred (2 linked test-only scanner findings; non-production invariants)
- Total linked issues finalized: 19

## Validation

Passed:

- packed-storage targeted validation command set
- package-local packed validation command set
- `go vet ./...`
- `go test ./...`
- `go test -race ./...`
- `go run golang.org/x/vuln/cmd/govulncheck@latest ./...` (no reachable vulnerabilities)
- CI-equivalent local checks (`audit_ci_enforcement.sh`, `check_smart_quotes.sh`, `check_versioned_row_writers.sh`)
- closure artifact validation (report/summary/status/checklist presence and integrity)

Documented local exception:

- `scripts/audit_ci.py` is not present in this repository and was recorded as skipped with rationale.

## This Does Not

- change production code
- change implementation tests
- change tracker/matrix closure rows
- change CI workflows
- change dependencies
- add packed-storage behavior
