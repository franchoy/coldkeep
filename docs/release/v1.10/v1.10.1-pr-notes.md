# v1.10.1 PR Notes

## Title

v1.10.1 — CLI Correctness & Contract Stabilization

## Summary

This PR completes the v1.10.1 stabilization release focused on CLI correctness, JSON output contracts, validation ordering, and official tracker/matrix closure.

This release is part of the v1.10 Reliability Freeze, CI Hardening & Correctness Burn-down train.

## Scope

Included:

- unexpected positional argument rejection
- value-flag token rejection
- explicit boolean false handling
- duplicate singleton flag rejection
- JSON shorthand consistency
- JSON output contract validation
- pre-stateful validation ordering
- tracker/matrix closure proof
- local validation and CI simulation record

Excluded:

- validation/security hardening for v1.10.2
- packed storage metadata integrity for v1.10.3
- GC reachability fixes for v1.10.4
- restore/recovery hardening for v1.10.5
- CI/Codacy integration phases
- engine extraction
- catalog abstraction
- new product features

## Implementation Commits

| Phase | Commit | Scope |
|---:|---|---|
| 3 | `682408c` | positional-argument hardening |
| 4 | `8e47ef0` | value-flag token rejection |
| 5 | `83dd220` | explicit false boolean flags |
| 6 | `14daec5` | duplicate singleton flags |
| 7 | `dbab178` | JSON shorthand output mode |
| 8 | `2b1a514` | machine-readable JSON output |
| 9 | `e065596` | pre-stateful validation ordering |
| 10 | `5d13cf2`, `e9fcd77` | tracker/matrix closure |
| 11 | `dd836af` | local validation record |

## Validation

Passed:

- targeted v1.10.1 CLI/JSON regression suite
- `go vet ./...`
- `go test ./...`
- `go test -race ./...`
- CI audit script
- closure consistency validation

Documented local exceptions:

- `pre_release_checklist` script not present
- smoke script failed in the local shell because `coldkeep` was not on `PATH`

These exceptions are documented in `v1.10.1-local-validation.md`.
