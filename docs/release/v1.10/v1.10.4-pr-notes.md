# v1.10.4 PR Notes

## Title

v1.10.4 — GC Reachability & Deletion Correctness

## Summary

This PR finalizes the v1.10.4 fixes-only release focused on GC reachability and deletion correctness evidence, tracker/matrix closure accounting, and local validation evidence.

This phase is documentation-only and release-candidate preparation only. It does not open a PR and does not change runtime behavior.

## Scope

Included:

- GC reachability/deletion correctness invariant documentation and validation evidence
- Phase 1-9 evidence consolidation and regression mapping
- tracker/matrix closure outcomes for selected v1.10.4 rows
- local validation and CI-equivalent simulation boundary evidence
- release-candidate documentation handoff (PR notes, release notes, changelog)

Excluded:

- engine extraction
- catalog abstraction
- new product features
- broad restore/recovery rewrites
- packed-storage metadata feature work
- CI policy changes

## Phase Evidence and Commits

| Phase | Commit | Scope |
|---:|---|---|
| 10 | `fbba3c5` | tracker/matrix closure update for CK-110-M014/M036/M079 |
| 11 | `93d21d9` | local validation and CI-equivalent simulation record |
| 12 | `f16410a` | release-candidate documentation and handoff |

## Matrix Closure Outcomes

- CK-110-M014: fixed (2 linked issues closed)
- CK-110-M036: fixed (22 linked issues closed)
- CK-110-M079: fixed (11 linked issues closed)
- Total linked issues closed: 35

## Validation

Passed:

- `go vet ./...`
- `go test ./internal/maintenance ./internal/retention -count=1`
- `go test -race ./internal/maintenance ./internal/retention -count=1`
- `go test ./... -run 'GC|Snapshot|Retention|Repair|Physical|Orphan|Report|DryRun|Destructive|Candidate|Parity|Concurrency|Mutation' -count=1`

Documented local exception boundary:

- Full `go test ./...` and full race suite remain affected locally by known integration timeout at `TestConcurrentStoreMultiChunkFilesAtomicCompletion`.

## Pre-PR Code Quality / Codacy-Risk Gate (Required)

Placement in release flow:

- Phase 12 complete
- pre-release checklist
- pre-PR code quality / Codacy-risk pass
- fix real findings (if any)
- commit fixes
- confirm clean tree
- open PR

Required command bundle for v1.10.4:

- `git status --short`
- `go vet ./...`
- `go test ./internal/maintenance ./internal/retention -count=1`
- `go test -race ./internal/maintenance ./internal/retention -count=1`
- `go test ./... -run 'GC|Snapshot|Retention|Repair|Physical|Orphan|Report|DryRun|Destructive|Candidate|Parity|Concurrency|Mutation' -count=1`
- `golangci-lint run ./...`
- `staticcheck ./...`
- `gosec ./...`
- `git fetch origin`
- `git diff --name-only origin/main...HEAD`
- `git diff --stat origin/main...HEAD`
- `git diff origin/main...HEAD -- '*.go'`

Tool bootstrap if missing:

- `go install honnef.co/go/tools/cmd/staticcheck@latest`
- `go install github.com/securego/gosec/v2/cmd/gosec@latest`
- `go install github.com/mgechev/revive@latest`

Optional style pass:

- `revive ./...`

Policy:

- Fix real correctness, security, static-analysis, and high-signal maintainability issues.
- Do not perform broad refactors, style churn, or unrelated cleanup in this release gate.
- Commit any real fixes from this gate separately after Phase 12.

## This Does Not

- change production code
- change test code
- change tracker/matrix closure rows
- change CI workflows
- change dependencies
