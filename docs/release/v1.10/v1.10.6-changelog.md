# Changelog — v1.10.6

## v1.10.6 — CI / Codacy / Copilot Workflow Hardening

### Added

- CI-specific Copilot instructions.
- Critical-path coverage prompt.
- Codacy passive-mode policy.
- Scanner suppression / acceptance policy.
- CI/Codacy release gate boundary policy.
- CI workflow delta review.
- Local validation and workflow consistency evidence.

### Changed

- Release process documentation now distinguishes:
  - release-blocking CI outcomes,
  - advisory CI outcomes,
  - release-blocking Codacy findings,
  - advisory Codacy findings,
  - baseline scanner findings,
  - false positives,
  - test-only accepted findings,
  - environment-sensitive local validation outcomes.
- Release guidance now emphasizes critical-path coverage over global coverage percentages.
- Scanner findings now require decision, rationale, release impact classification, and evidence/follow-up path.

### Validation

- `go vet ./...` passed.
- `go test ./... -count=1` passed.
- `go test -race ./... -count=1` passed.
- Phase 8 documentation validation passed.
- Scope guard passed.

### Compatibility

- No product behavior changes.
- No production code changes.
- No test implementation changes.
- No CI workflow behavior changes.
- No Codacy configuration changes.
- No dependency changes.
