# v1.10.7 Phase 4 — Invariant Coverage Map

Status: Complete

## Purpose

Phase 4 maps package coverage evidence to named correctness invariants.

This keeps v1.10.7 aligned with the correctness-first principle that coverage exists to protect data-safety behavior, not to chase raw percentages.

## Core Invariant

v1.10.7 coverage must be interpreted through correctness invariants, not raw package percentages.

## Artifacts

```text
docs/release/v1.10/v1.10.7-invariant-coverage-map.csv
docs/release/v1.10/v1.10.7-invariant-coverage-map.md
```

## Invariants Mapped

Tier 1:

* `CK-INV-GC-REACHABILITY`
* `CK-INV-RESTORE-DETERMINISM`
* `CK-INV-VERIFY-FAIL-CLOSED`
* `CK-INV-RECOVERY-FAIL-CLOSED`
* `CK-INV-SNAPSHOT-ROOTS`
* `CK-INV-PACKED-LEGACY-PARITY`
* `CK-INV-PHYSICAL-METADATA-REUSE`

Tier 2:

* `CK-INV-CATALOG-COMPAT`
* `CK-INV-CONTAINER-PLACEMENT`
* `CK-INV-RETENTION-POLICY`

Tier 3:

* `CK-INV-CLI-JSON-CONTRACT`
* `CK-INV-BENCHMARK-ARTIFACTS`

## Evidence States

* `covered`
* `partial`
* `observe-only`

## Phase 4 Findings

The main low-visibility future hardening priorities are:

1. `internal/recovery` fail-closed behavior.
2. `internal/maintenance` orchestration paths.
3. Restore determinism subpaths inside `internal/storage`.
4. Container placement and encrypted/compressed payload accounting.
5. Packed-only / legacy-only / mixed-repository invariant mapping.

## Non-Goals

Phase 4 does not:

* add tests;
* change Go code;
* change the coverage script;
* add thresholds;
* compare coverage against previous baselines;
* change CI workflows;
* change dependencies.

## Validation

Phase 4 validation confirms:

* invariant map CSV exists;
* invariant map Markdown exists;
* all required Tier 1 invariants are mapped;
* evidence states are valid;
* every Tier 1 invariant has a release action;
* no threshold enforcement was added;
* no Go code, tests, CI workflows, script behavior, or dependencies changed.

## Handoff

Next phase:

Phase 5 — Soft Threshold Policy
