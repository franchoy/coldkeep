# v1.11.0 Checklist

Release: v1.11.0 — Behavior-Preserving Engine Facade Baseline
Status: In progress

## Phase Completion

- [x] Phase 0 — Documentation Alignment and Engine-Facade Scope Declaration
- [x] Phase 1 — Engine Facade Contract Skeleton
- [x] Phase 2 — Wrapper-Only Implementation
- [x] Phase 3 — CLI Routing for Low-Risk Commands
- [ ] Phase 4 — Engine-Level Validation Ownership
- [ ] Phase 5 — Dependency-Direction Guard
- [ ] Phase 6 — SQLite/PostgreSQL Compatibility Baseline
- [ ] Phase 7 — Mutating Operation Facade Candidates
- [ ] Phase 8 — v1.11.0 Final Release Gate

## Documentation

- [x] `docs/release/v1.11/` exists.
- [x] v1.11.0 scope declaration exists.
- [x] v1.11.0 phase status document exists.
- [x] v1.11.0 checklist exists.
- [x] v1.10.15 stale final-release-gate status line is aligned.
- [x] v1.10.x remains closed; no v1.10.17 is introduced.

## Scope Lock

- [x] v1.11.0 is declared as behavior-preserving engine facade baseline.
- [x] No runtime behavior change is included.
- [x] No CLI output change is included.
- [x] No JSON shape change is included.
- [x] No exit-code change is included.
- [x] No storage format change is included.
- [x] No schema behavior change is included.
- [x] No default database backend change is included.
- [x] PostgreSQL compatibility remains explicitly preserved.
- [x] SQLite-first direction is documented as future/local-first direction, not a Phase 0 implementation.

## Phase 3 — CLI Routing for Low-Risk Commands

- [x] `coldkeep stats` routes through `engine.DefaultEngine.Stats`.
- [x] `coldkeep inspect` is NOT routed (user directive: stats only in Phase 3).
- [x] `coldkeep verify` is NOT routed (user directive: stats only in Phase 3).
- [x] `StatsRequest` carries `Trace observability.TraceOptions` for full parity.
- [x] `runObservabilityStatsPhase` var signature unchanged (test seam preserved).
- [x] `runStatsCommand` function body unchanged.
- [x] No CLI rendering changes are introduced.
- [x] No JSON shape changes are introduced.
- [x] No exit-code changes are introduced.
- [x] No storage/schema changes are introduced.
- [x] No PostgreSQL compatibility is removed or weakened.
- [x] `internal/engine` does not import `cmd/coldkeep`.
- [x] No domain packages import `internal/engine`.
- [x] `go vet ./internal/engine/...` passes.
- [x] `go vet ./cmd/coldkeep/...` passes.
- [x] `go test ./internal/engine/... -v` passes (all 9 subtests).
- [x] `go test ./cmd/coldkeep/...` passes (existing stats mocks unaffected).
- [x] `go test ./...` passes (full suite green).

## Phase 2 — Wrapper-Only Implementation

- [x] `DefaultEngine.Stats` delegates to `observability.Service.Stats`.
- [x] `DefaultEngine.Inspect` delegates to `observability.Service.Inspect`.
- [x] `DefaultEngine.Verify` delegates to `maintenance.VerifyCommandWithContainersDir`.
- [x] `Config.DB` is required; `New` returns an error when DB is nil.
- [x] No business logic is moved from domain packages into the engine.
- [x] No CLI commands are routed through the engine.
- [x] No runtime behavior changes are introduced.
- [x] No JSON output changes are introduced.
- [x] No exit-code changes are introduced.
- [x] No storage/schema changes are introduced.
- [x] No PostgreSQL compatibility is removed or weakened.
- [x] `internal/engine` does not import `cmd/coldkeep`.
- [x] No domain packages import `internal/engine`.
- [x] Phase 2 compile/test validation passes.

## Phase 1 — Engine Facade Contract Skeleton

- [x] `internal/engine/` exists.
- [x] Engine interface exists.
- [x] Low-risk read-oriented request/result types exist.
- [x] `DefaultEngine` skeleton exists.
- [x] No CLI commands are routed through the engine.
- [x] No business logic is moved.
- [x] No runtime behavior changes are introduced.
- [x] No JSON output changes are introduced.
- [x] No exit-code changes are introduced.
- [x] No storage/schema changes are introduced.
- [x] No database-default changes are introduced.
- [x] No PostgreSQL compatibility is removed or weakened.
- [x] Engine package does not import `cmd/coldkeep`.
- [x] Phase 1 compile/test validation passes.

## Architecture Boundary

- [x] Engine facade work is allowed only from Phase 1 onward.
- [x] Wrapper-first rule is documented.
- [x] Parity-before-lifting rule is documented.
- [x] Catalog facade is deferred to v1.12 unless explicitly re-scoped.
- [x] Daemon/API/UI work is deferred to v2.x.
- [x] Network/NAS/cloud/multi-user work is deferred to v3.x.
- [x] Dependency-direction rules are documented (`internal/engine` must not import `cmd/coldkeep`).

## Validation

- [x] `git diff --check` passes (no whitespace errors).
- [x] No Go code changed.
- [x] No tests are required for this phase (docs-only).
- [x] Docs build or markdown checks pass if available.

## Release Gates — Phase 1 Prerequisites

- [x] Phase 0 checklist is complete and reviewed.
- [x] Scope is approved before Phase 1 begins.
- [x] `internal/engine` must not exist until Phase 1 is explicitly started.

## Release Gates — Engine Facade (Phases 1–7)

- [x] `internal/engine` request/result types are defined (Phase 1).
- [x] `DefaultEngine` shell exists with no logic (Phase 1).
- [x] Wrapper methods delegate to existing packages without moving logic (Phase 2).
- [x] Selected CLI commands route through engine with proven before/after parity (Phase 3).
- [x] Parity evidence covers: human output, JSON output, exit codes, error behavior (Phase 3).
- [ ] Correctness invariants are present at engine entry points (Phase 4).
- [ ] Dependency-direction is enforced by CI or test (Phase 5).
- [ ] SQLite and PostgreSQL behavior evidence is recorded (Phase 6).
- [ ] Mutating wrappers exist and are wrapper-only (Phase 7).

## Release Gates — v1.11.0 Final (Phase 8)

- [ ] `internal/engine` exists and `DefaultEngine` wraps selected operations.
- [ ] Selected CLI commands route through engine.
- [ ] No user-visible behavior changed.
- [ ] JSON output unchanged.
- [ ] Exit codes unchanged.
- [ ] Repository, storage, and schema unchanged.
- [ ] PostgreSQL compatibility preserved.
- [ ] No catalog abstraction introduced accidentally.
- [ ] Dependency direction is enforced.
- [ ] Release documentation includes parity evidence for each promoted command.
- [ ] `go vet ./...` passes.
- [ ] `go test ./...` passes.
- [ ] PR opened, CI green, PR merged, release tagged.
