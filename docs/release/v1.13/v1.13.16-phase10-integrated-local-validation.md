# v1.13.16 Phase 10 Integrated Local Validation and Fixture Recovery

**Mode:** BUILD  
**Phase 10 status:** Complete
**Execution:** Complete local certified
**Phase 11:** Next, not started; Build not authorized

## Original Phase 10 authority and stop

Phase 10 was authorized as source-neutral complete local candidate validation
from `b23a144c574952d622dd2c2606ec0feca3ffeb91`. It allowed no product, test,
script, workflow, schema, module, dependency, or configuration mutation and
would have permitted one eight-document evidence commit only after every local
gate passed.

The run correctly set `COLDKEEP_LONG_RUN=1` for the selected PostgreSQL
concurrency/race profile. Twelve of thirteen selected top-level tests passed
and none skipped. `TestRefCountContainmentStressMatrix` failed at iteration 1
in all four children: `plain_w1`, `plain_w4`, `aesgcm_w1`, and `aesgcm_w4`.
No Phase 10 evidence commit or push was created.

The retained historical failure evidence is under
`/tmp/coldkeep-v11316-phase10-r2-evidence.pTt3Mi` when present. Its partial
passing results cannot certify the corrected candidate.

Two invocation-only events are explicitly non-findings: an initial omission
of `COLDKEEP_DB_AUTO_BOOTSTRAP=true`, and a concurrent plain/AES package
launch that collided on a harness-owned scratch database. Future PostgreSQL
package profiles must include required auto-bootstrap context and run serially.

## CK-V11316-012

CK-V11316-012 is an S2 CI/test reliability blocker now in state
`IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION`. Its classification is
`TEST_FIXTURE_SELECTION_BASE_CONTRACT_DRIFT`.

The stress fixture creates a stable `inputDir`, creates a new `base` below it
for every iteration, and intentionally retains current mappings for `c` and
`b-overlap`. Beginning with iteration 1, those earlier mappings remain below
`inputDir/iter-0000` while the fixture supplies `inputDir/iter-0001` as the
next full snapshot selection base.

Product behavior is correct and immutable. Full snapshot creation enumerates
all current completed physical mappings and fails closed if any mapping cannot
be represented below the explicit selection base. No source in
`internal/snapshot`, Engine, CLI, or another product domain may change.

## Exact recovery

Phase 10R1 authorized exactly one semantic line in exactly one test path:

```text
tests/integration/integration_test.go
TestRefCountContainmentStressMatrix
SelectionBase: base,
SelectionBase: inputDir,
```

The stable `inputDir` is the capture root containing every mapping created and
retained by the subtest. This preserves full-snapshot semantics, prior-
iteration accumulation, codec and worker profiles, removals, GC, verification,
restore/hash checks, refcount assertions, normal iteration count, and
`COLDKEEP_LONG_RUN` gating.

Direct recovery requires one genuine PostgreSQL 16.15 race execution at the
normal iteration count, with all four children passing and zero skips, plus
immutable snapshot-domain preservation. A genuine failure stops without
retry or another correction.

## Phase 10R1 result and Phase 10R2 stop

The Phase 10R1 correction was committed locally as
`5a029107f45697894a709b3592999f2f6175692a`. Its direct PostgreSQL race proof
passed all four codec/worker children with zero skips. The restarted Phase 10
concurrency profile then passed all thirteen selected tests in both plain and
AES-GCM with zero skips.

The later complete adversarial profile did not certify the candidate. Both
codec children of
`TestAdversarialG1UnrelatedContainerDamagePreservesHealthyRestore` self-skipped
because the anchor and damaged file ended up in the same packed container.
The package passed, but that required isolation proof did not execute.

CK-V11316-013 is therefore an S2 CI/test reliability blocker now in state
`IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION`, classified as
`G1_CLI_CONTAINER_ISOLATION_FIXTURE_CONFIGURATION_DRIFT`. The test changes the
container maximum in the Go test process, while its Store operations execute
in a separate CLI process. The CLI resolves container size from
`COLDKEEP_CONTAINER_MAX_SIZE_MB`, and the test environment did not supply it.
Production packing, container, storage, and restore behavior are correct and
immutable.

## Exact Phase 10R2 recovery

Phase 10R2 authorized exactly one test path and only within
`TestAdversarialG1UnrelatedContainerDamagePreservesHealthyRestore`:

```text
tests/adversarial/g1_deterministic_restore_adversarial_test.go
```

The ineffective in-process `SetContainerMaxSize` override is removed, the CLI
environment receives `COLDKEEP_CONTAINER_MAX_SIZE_MB=1` before the first
Store, and the same-container self-skip becomes a hard fixture failure. File
payloads, codecs, corruption details, restore assertions, filler count, Store
and Restore helpers, packed product behavior, and the codec matrix remain
unchanged. No product, script, workflow, schema, migration, module, or
dependency path is authorized.

## Phase 10R2 recovery and complete restart

Prospective authority commit
`5f367cfed3f72f14686d6e7734f98a50c06a9ca7` has parent exactly the R1 fix
and changes the eight Phase 10 authority documents only. Test fix
`6f0d7693e4621c0f2eb528354c46d523ea94ec02` has that authority commit as
parent and changes only the named G1 adversarial test.

Direct recovery used exact Go 1.26.7 and a fresh pinned PostgreSQL 16.15
service. The single genuine execution passed both plain and AES-GCM children
with zero failures and zero skips; each child passed the non-skippable runtime
container-ID inequality assertion. Immutable container, packed storage, and
restore race preservation passed. The retained direct summary is
`/tmp/coldkeep-v11316-phase10r2-direct-evidence.zKXng3/DIRECT-G1-RECOVERY-SUMMARY.txt`
with SHA-256
`96d4d6df2c11b76bed0d4c7dd425dab49a89d8241a837c92202e6f61010da66c`.

The complete Phase 10 matrix then restarted from the committed R2 fix using
fresh evidence. PostgreSQL package profiles were serialized, every fixture
used explicit auto-bootstrap context and a fresh database identity, and every
required selected long-run family passed with zero skips. The complete result
included:

- static/structural, exact lint 2.9.0, vet, build, and focused packages;
- SQLite fresh/upgrade/relocation/reopen and PostgreSQL 16.15 backend proof;
- plain/AES-GCM short and full integration, migration, smoke, adversarial,
  seven-selector G6, and three-selector long-run profiles;
- 13/13 concurrency per codec, refcount 4/4 per profile with zero skips, and
  complete adversarial 70/70 per codec with G1 passing both children and zero
  skips;
- simulation 4/4, legacy/packed/hybrid, wrong-key atomic failure, and
  JSON/stdout/stderr/exit contracts;
- exactly one canonical full Go and one canonical full race, package race in
  both codecs, critical coverage 3/3, release/governance/Phase 3/Python/CI
  enforcement, manual Phase 6 lifecycle, and restart/reopen proof.

No genuine candidate failure occurred. Discarded launches were limited to
sandbox or invocation/evidence-assertion errors and contributed no passing
result to certification. Product runtime diff from the Phase 10 source head is
empty; the complete test diff is exactly the R1 integration fixture and R2 G1
fixture paths. All Phase 10R2-owned PostgreSQL, storage, manual, binary,
cache/temp, and driver resources were removed after evidence capture. The
retained final evidence root is
`/tmp/coldkeep-v11316-phase10r2-final-evidence.1DpVsv`; its validation summary
SHA-256 is
`b515775f3817788a6da070e131d986e89b5df6ad5255b2d68ebdeefdcab89c5f`.

CK-V11316-012 and CK-V11316-013 are implemented pending exact-head hosted
certification. Phase 11 is Next but not started, and Phase 11 Build, hosted
execution, push, tag, release, CK-V11316-007 remediation, and v1.x technical
correctness closure remain unauthorized.

```text
PHASE_10: COMPLETE
PHASE_10_EXECUTION: COMPLETE_LOCAL_CERTIFIED
PHASE_11: NEXT
PHASE_11_BUILD: NOT_AUTHORIZED
FINDINGS_CONFIRMED: 13
FINDINGS_CLOSED: 10/13
CK-V11316-007: OPEN
CK-V11316-012: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
CK-V11316-013: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
V1_X_TECHNICAL_CORRECTNESS_CLOSURE: WITHHELD
```
