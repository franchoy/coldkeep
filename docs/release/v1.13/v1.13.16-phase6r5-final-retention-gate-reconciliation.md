# v1.13.16 Phase 6R5 Final Retention Gate Reconciliation

**Release:** v1.13.16 — Snapshot Retention Integrity, Observability Truth, and Final v1.x Closure
**Phase:** 6R5 — Final smoke and corruption-adversarial retention gate reconciliation
**Mode:** BUILD
**Status:** Locally certified candidate; exact-head hosted certification pending

## Entry evidence and classification

Exact predecessor head `ce7271a068aea1656d7239c5e282752928ed2e31`
failed CI run `33334609160`. Both smoke profiles encoded the stale assertion
that snapshot retention blocks stored-path unlink. Both G15 profiles parsed the
generic stderr process envelope instead of the authoritative failed batch item
in stdout, and did not complete the required conservative-state proof. CodeQL
run `33334609155` passed Actions, Go, Python, and Aggregate; open CodeQL alerts
remain zero.

The accepted Phase 6R4 diagnosis established no product runtime defect.
Snapshot-retained stored-path unlink succeeds, while by-ID logical deletion is
blocked with `SNAPSHOT_RETAINED_DELETE_BLOCKED`. Corrupt snapshot metadata
causes standard verify to fail closed; dry-run and live GC succeed
conservatively without mutating the retained target; snapshot restore remains
byte-identical. This classification is frozen for Phase 6R5.

```text
FAILED_HEAD: ce7271a068aea1656d7239c5e282752928ed2e31
FAILED_CI: 33334609160
CODEQL: 33334609155 PASS
OPEN_CODEQL_ALERTS: 0
SMOKE_ROOT: stale stored-path retention lifecycle
G15_ROOT: batch-item output parsing and incomplete conservative-state proof
PRODUCT_RUNTIME_DEFECT: NOT_ESTABLISHED
PRODUCT_RUNTIME_CHANGE: FORBIDDEN
PHASE_6: WITHHELD_PENDING_FINAL_GATE_RECONCILIATION
PHASE_7: NOT_AUTHORIZED
FINDINGS_CLOSED: 1/8
```

## Exact implementation authority

Only these executable test/gate paths may change:

- `scripts/smoke.sh`
- `tests/adversarial/g14_g17_snapshot_retention_adversarial_test.go`

No product runtime, schema, migration, workflow, dependency, module, public
wire-model, or third executable path is authorized. Product runtime paths
changed must remain zero. Discovery that another executable or product path is
required stops this phase.

## Frozen smoke contract

The v1.3 smoke lifecycle must capture alpha's logical ID, stored path, and
content hash from Store JSON. Snapshot 1 and snapshot 2 must both contain the
stable snapshot-relative alpha member before the stored path is unlinked
exactly once. That unlink must succeed, remove the current physical mapping,
and preserve the logical recipe and both memberships.

Subsequent by-ID removal must exit 3 and the exact stdout batch item for alpha
must report `SNAPSHOT_RETAINED_DELETE_BLOCKED`. Dry-run and live GC must
preserve snapshot-only alpha, and snapshot restore after live GC must reproduce
alpha's hash. After deleting snapshot 1 the same by-ID guard must remain under
snapshot 2. After deleting snapshot 2, dry-run and live GC must reclaim the
rootless alpha recipe/content while beta and gamma remain valid current roots.
Existing Store, snapshot show/diff/query/restore/delete, pattern, filtered
diff, GC, multi-snapshot, and final-root coverage must remain non-vacuous.

## Frozen G15 contract

G15 continues to call by-ID `remove <logical-id>`. Its assertion must parse
stdout first, identify the exact requested target item, require exit 3, failed
item state, and invariant `SNAPSHOT_RETAINED_DELETE_BLOCKED`, and treat stderr
only as the generic process envelope.

The existing `tests/utils/removestate` fingerprint must prove exact valid-
target equality after the blocked remove, GC dry-run, and live GC. Standard
verify must still fail with the accepted snapshot-graph integrity codes after
both GC modes, injected corrupt rows should remain observable where practical,
and a post-live-GC restore from `g15-valid-snap` must match the pre-corruption
Store hash byte for byte.

## Validation and commit boundary

Phase 6R5 uses isolated disposable storage and PostgreSQL state under pinned Go
1.26.7 with `GOTOOLCHAIN=local`. It requires smoke, focused G15, G14–G17, the
complete adversarial and PostgreSQL correctness profiles in plain and AES-GCM,
migration compatibility, race, full Go, vet, Bash syntax, the repository
ShellCheck command, release-state development/auto, governance, the full
Python suite, CI enforcement, and a clean detached committed-tree gate.

Exactly three commits are authorized:

1. `docs: authorize v1.13.16 final retention gate reconciliation`
2. `test: reconcile smoke and adversarial retention contracts`
3. `docs: record v1.13.16 final retention gate reconciliation`

The evidence commit may represent Phase 6 only as a candidate pending exact-
head hosted certification. A hosted failure withholds Phase 6R5 and Phase 6;
no retry or fourth Phase 6R5 commit is implicitly authorized. Phase 7 is a
separate future operation and is not authorized here.

## Local certification evidence

Authority commit `2870ef1e8504e8ac42640d41c05fff6b8fd4b008` and test commit
`d7deb4097d681751f7373ac7f11ef6bb8bb18790` preserve the exact three-commit
boundary. The test commit changes only the two authorized gate paths and no
product runtime path.

Plain and AES-GCM smoke passed against separate disposable databases and
storage roots. Alpha's stored path was unlinked exactly once after both
snapshots contained its stable member; its mapping disappeared, both by-ID
guards returned the exact failed stdout item, live-GC snapshot restore matched
the Store hash, and final live GC removed the rootless alpha recipe while beta
and gamma remained current and byte-readable.

Focused G15 and G14–G17, complete adversarial, PostgreSQL correctness, and the
two required migration-compatibility tests passed with race detection in both
codecs. G15 proved exact `removestate` equality after blocked remove, dry-run,
and live GC; verify continued reporting accepted snapshot-graph corruption
after both GC modes; injected invalid-lifecycle, missing-graph, and inserted
orphan references remained; and restore from `g15-valid-snap` was byte-
identical.

The clean detached test commit passed full Go, vet, Bash syntax, the repository
ShellCheck command, release-state development/auto in supported detached
release-PR context, governance, all 215 Python tests, CI enforcement, and diff
checks under pinned Go 1.26.7.

```text
AUTHORITY_SHA: 2870ef1e8504e8ac42640d41c05fff6b8fd4b008
TEST_SHA: d7deb4097d681751f7373ac7f11ef6bb8bb18790
FAILED_PREDECESSOR_HEAD: ce7271a068aea1656d7239c5e282752928ed2e31
FAILED_PREDECESSOR_CI: 33334609160
PREDECESSOR_CODEQL: 33334609155 PASS
SMOKE_STALE_CONTRACT: CORRECTED
G15_BY_ID_PRODUCT_GUARD: PRESERVED
G15_OUTPUT_PARSING: CORRECTED
G15_VALID_TARGET_MUTATION: NONE
G15_GC_DRY_RUN: PASS_AND_NON_MUTATING
G15_LIVE_GC: PASS_AND_CONSERVATIVE
G15_CORRUPTION_POST_GC: STILL_DETECTED
G15_POST_GC_SNAPSHOT_RESTORE: BYTE_IDENTICAL
PRODUCT_RUNTIME_CHANGE: NONE
LOCAL_VALIDATION: PASS
CK-V11316-001: IMPLEMENTED_RETENTION_PROOF_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
CK-V11316-002: CLOSED_CERTIFIED
CK-V11316-008: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
CK-V11316-003..007: OPEN
FINDINGS_CLOSED: 1/8
PHASE_6: CANDIDATE_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
PHASE_7: NOT_AUTHORIZED
```
