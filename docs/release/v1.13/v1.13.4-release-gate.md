# Coldkeep v1.13.4 Release Gate

**Release:** `v1.13.4 — Read-Side Dependency Direction and Result Ownership Review`  
**Branch:** `release/v1.13.4`  
**Status:** Blocked during Phase 10 execution

## Purpose

This document records the final release-gate evidence for `v1.13.4`.

Phase 9 created this document as a draft. Phase 10 began execution and stopped
at the first required blocker rather than claiming a green gate without
PostgreSQL-backed validation.

## Scope under validation

`v1.13.4` includes:

- read-side ownership inventory
- `stats` result ownership review
- `inspect` dependency-direction review
- `snapshot show` ownership review
- `snapshot diff` ownership review
- renderer/JSON assembly boundary review
- dependency-direction guard tests
- backend compatibility and package dependency review
- final ownership summary
- `v1.13.5` handoff

## Expected code impact

Current expected impact before Phase 10:

- production code: unchanged
- tests: changed only by Phase 7 guard tests
- docs: changed
- scripts/CI: unchanged
- runtime behavior: unchanged

Phase 10 must verify this remains true.

## Required final validation

Phase 10 must execute:

`PRE_RELEASE_CHECKLIST.md Profile A`

## Required Phase 10 behavior

If all required validation passes:

- update this release gate with executed commands and results
- update `v1.13.4-phase-list.md`
- commit the final release-gate evidence
- push the branch
- open a PR from `release/v1.13.4` to `main`

If any required validation fails or is blocked without an equivalent
substitute:

- stop
- do not open the PR
- update this document with the exact failed or blocked command
- update `v1.13.4-phase-list.md`
- commit and push the blocked evidence if docs changed
- remediate narrowly only after the blocker is understood
- rerun the final gate

## Profile A execution record

### Branch and repository state

- Commands run:
  - `git status -sb`
  - `git branch --show-current`
  - `git fetch origin`
  - `git log --oneline --decorate -n 24`
- Result: `PASS with environment note`
- Evidence:
  - branch was `release/v1.13.4`
  - working tree was clean
  - head was `2539ec7`
  - local tracking ref still matched `origin/release/v1.13.4`
  - `git fetch origin` remained blocked by the environment with
    `error: cannot open '.git/FETCH_HEAD': Read-only file system`

### Static and formatting checks

Not run because the gate stopped earlier at PostgreSQL reachability.

### Targeted tests

Not run because the gate stopped earlier at PostgreSQL reachability.

### Full test suite

Not run because the gate stopped earlier at PostgreSQL reachability.

### Race validation

Not run because the gate stopped earlier at PostgreSQL reachability.

### Codec-specific validation

Not run because the gate stopped earlier at PostgreSQL reachability.

### PostgreSQL-backed validation

- Environment preparation attempted:
  - `export GOCACHE=/tmp/coldkeep-gocache`
  - `export GOLANGCI_LINT_CACHE=/tmp/coldkeep-golangci`
  - `mkdir -p "$GOCACHE" "$GOLANGCI_LINT_CACHE"`
  - `docker compose up -d coldkeep_postgres`
  - exported:
    - `COLDKEEP_TEST_DB=1`
    - `COLDKEEP_DB_AUTO_BOOTSTRAP=true`
    - `COLDKEEP_SCHEMA_PATH=db/schema_postgres.sql`
    - `DB_HOST=127.0.0.1`
    - `DB_PORT=5432`
    - `DB_USER=coldkeep`
    - `DB_PASSWORD=coldkeep`
    - `DB_NAME=coldkeep`
    - `DB_SSLMODE=disable`
    - `COLDKEEP_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef`
    - `COLDKEEP_STORAGE_DIR="$PWD/.ci-storage/manual-checks"`
- Result: `BLOCKED`
- Blocking command:
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
- Evidence:
  - `docker compose up -d coldkeep_postgres` reported the container started
  - `pg_isready` returned `127.0.0.1:5432 - no response`
  - `psql "host=127.0.0.1 port=5432 user=coldkeep password=coldkeep dbname=coldkeep sslmode=disable" -t -A -c "SELECT 1;"` returned `psql: error:`
- Additional note:
  - deeper Docker inspection with `docker compose ps` and `docker compose logs`
    was attempted but could not be approved after the escalation review hit its
    current usage limit, so the gate stopped at the verified reachability
    blocker instead of inferring PostgreSQL health

### Integration validation

Not run because the gate stopped earlier at PostgreSQL reachability.

### Adversarial validation

Not run because the gate stopped earlier at PostgreSQL reachability.

### Smoke matrix

Not run because the gate stopped earlier at PostgreSQL reachability.

### Benchmark matrix

Not run because the gate stopped earlier at PostgreSQL reachability.

### Legacy compatibility

Not run because the gate stopped earlier at PostgreSQL reachability.

### Cross-platform approximation

Not run because the gate stopped earlier at PostgreSQL reachability.

### Snapshot release gate

Not run because the gate stopped earlier at PostgreSQL reachability.

### CI enforcement audit

Not run because the gate stopped earlier at PostgreSQL reachability.

### Final branch state

- `git diff --check` was not yet run in Phase 10 because the gate stopped at
  the first required blocker.
- branch remained clean before documentation updates.
- no PR was opened.

## Final decision placeholder

`BLOCKED`

Reason:

- `PRE_RELEASE_CHECKLIST.md` Profile A cannot be called green without reachable
  PostgreSQL-backed validation.
- the gate stopped at the first required blocker:
  `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
- no further Profile A sections were claimed or executed past that blocker.

## Phase 9 note

No PR was opened. Phase 10 remains blocked until PostgreSQL is reachable and
the full Profile A gate can be rerun from the beginning.
