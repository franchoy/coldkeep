# v1.13.9 Phase 15 — Repair and Recovery Boundary Honesty Scope Definition

Starting SHA: `f6d5d4ea1ccbef0ae5955217309d33ec3d91bcf3`

## Summary

Phase 15 closes the remaining tracker-definition gap after Phase 14 decided the
repair/recovery ownership boundary but left the next follow-up only
recommended, not explicitly named in the `v1.13.9` tracker.

This phase is docs-only. It does not implement repair or recovery execution,
does not activate engine `Repair*` or `Recover*` methods, and does not change
any runtime behavior.

Its purpose is to define the exact next implementation phase for `v1.13.9`
using the checked-in Phase 14 decision and release documents as the source of
truth.

## Current confirmed ownership state

Active `v1.13.9` repair/recovery ownership remains:

- `doctor` stays CLI-owned orchestration over `internal/recovery`,
  schema/version checks, verify, and audit seams
- `repair` stays CLI-owned orchestration over explicit
  `internal/maintenance` repair primitives
- `internal/recovery`, `internal/maintenance`, `internal/verify`, and
  storage/container corrective helpers remain lower-layer ownership
- engine `Repair*` and `Recover*` shapes remain candidate-only and are not
  active interface methods

## Defined next implementation phase

The next implementation phase after Phase 15 is:

`Phase 16 — Repair and Recovery Boundary Honesty Proof`

That phase is expected to stay narrow and evidence-oriented. It may prove only
the smallest boundary claims required by the Phase 14 decision, such as:

- explicit unsupported-boundary proof for engine repair/recovery candidates
- tests showing `doctor` and `repair` remain CLI-owned in `v1.13.9`
- comments or release evidence that clarify lower-layer corrective ownership

Phase 16 must not begin broad repair or recovery execution redesign by default.

## Allowed future Phase 16 areas

If Phase 16 proceeds, the intended implementation surface is limited to:

- `cmd/coldkeep`
- `internal/engine` candidate-only boundary evidence
- release documents and the canonical Phase 16 artifact
- lower-layer ownership tests or comments only if needed to prove honest
  unsupported boundaries

## Forbidden in Phase 15

Phase 15 does not:

- add engine `Repair*` or `Recover*` methods
- route `doctor` or `repair` through engine
- change repair, recovery, or verify behavior
- change create/delete/restore behavior
- change CLI text or JSON output
- change parser behavior
- change schemas or migrations
- change storage format
- change workflows
- begin Phase 16 implementation
- open or reopen a pull request

## Acceptance criteria

Phase 15 is complete when:

- the checked-in `v1.13.9` tracker defines an explicit Phase 15 title
- the checked-in `v1.13.9` tracker defines this artifact as the canonical
  Phase 15 record
- the checked-in `v1.13.9` tracker explicitly marks Phase 15 docs-only
- the checked-in `v1.13.9` tracker explicitly names the next implementation
  phase after Phase 15
- the checked-in Phase 15 artifact keeps the next phase bounded to
  boundary-honesty or unsupported-surface proof
- no runtime code changes land

## Validation

Because this phase is docs-only, the local validation gate is intentionally
narrow:

- `git diff --check`
- release-doc consistency grep across:
  - `docs/release/v1.13/v1.13.9-phase-list.md`
  - `docs/release/v1.13/v1.13.9-validation-checklist.md`
  - `docs/release/v1.13/v1.13.x-release-train.md`
  - `docs/release/v1.13/v1.13.9-repair-recovery-boundary-honesty-scope-definition.md`

## Handoff

Phase 16 is authorized after this phase.

Phase 16 must remain a bounded Build pass and must not infer repair/recovery
activation from memory; it should use this Phase 15 definition and the checked-in
Phase 14 decision as the starting contract.
