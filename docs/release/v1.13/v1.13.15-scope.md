# Coldkeep v1.13.15 Scope

**Release:** v1.13.15 — Final v1.x Security, Reproducibility, and Operational Closure
**Status:** Published; post-publication closure pending
**Branch:** release/v1.13.15-post-publication-closure

## Objective

Close v1.x on a supported, patched, vulnerability-scanned toolchain; preserve
the Windows native-string corrections and reproducible source-only release
contract; reconcile current authority; and complete enforceable tag,
publication, and final post-publication closure without changing the product.

## Current phase

Phase 9S merged normally through PR #118 as exact protected main
`6a2417e8189631b018779c2fd24fc559ed761f3f` and passed full recertification.
Phase 9 then created annotated tag object
`38b48ef60e1cd8cc9a6966bfaa1fda074fdf6f12`, which peels to that immutable
product baseline, and published stable immutable release `378768097` at
`2026-08-28T21:17:21Z`.

Tag CI, Linux/macOS/Windows tag-native installation, tag-triggered CodeQL,
zero-alert pagination, and the immutable-release attestation passed. Phases
0–9 are Complete. Phase 10 is Next and owns only the authorized documentation,
governance/provider authority, protected merge, exact-final-main proof, branch
cleanup, and read-only terminal audit. V2 implementation has not started.

## Frozen baselines

- Language floor: go 1.25.0.
- Certified toolchain: Go 1.26.7 with GOTOOLCHAIN=local.
- Windows dependency: golang.org/x/sys v0.44.0.
- Scanner: govulncheck v1.7.0 in ordinary mode.
- Distribution: source-only; no prebuilt binaries.
- Source-install OS families: Linux, macOS, Windows.
- Product images: Linux amd64 and arm64.
- Canonical release body: `v1.13.15-release-body.md`, exactly 1,815 bytes at
  SHA-256
  477796fc1c44151ddc77825559c48876c49ab742540586a190abc2c878eea357.
- v1.13.14 historical publication identity remains immutable.

## Root invariants

1. Windows native strings are length-safe and cannot panic at rename limits.
2. Every release-critical path uses one supported, patched, scanned toolchain.
3. Repository and hosted release authority are current, truthful, and
   enforceable.
4. A clean clone can be developed, built, and installed through documented,
   tested contracts.
5. Release tags are independently peeled from the public remote before their
   semantic module version and source origin are certified.

## Final planned v1.x closure boundary

v1.13.15 is the final planned v1.x release. Phase 9 publication alone does not
close v1.x. Only Phase 10 post-publication reconciliation, exact-final-main
recertification, complete temporary-branch cleanup, and the read-only terminal
audit may return V1_X: FULLY_CLOSED.

A future v1 maintenance release is permitted only for a newly discovered
critical correctness or security defect under a separately planned and
explicitly authorized maintenance train. V2 planning review may follow
closure; v2 implementation still requires a separate plan.

## Exclusions

No v2 implementation, SQLite-first product/default behavior, public API
expansion, storage schema or format change, migration, broad refactor,
prebuilt-binary distribution, historical v1.13.14 rewrite, dependency
movement, workflow change outside the exact Phase 9S certification paths, or
weakening of tag creation protection is authorized.

Scope expansion, unexpected dependency movement, identity drift, private
security impact, ambiguous registry identity, or unsupported governance
semantics requires a return to Plan mode.
