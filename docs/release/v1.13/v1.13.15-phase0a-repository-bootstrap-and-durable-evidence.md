# Coldkeep v1.13.15 Phase 0A — Repository Bootstrap and Durable Evidence

**Mode:** `BUILD`
**Status:** Complete on this bounded local commit
**Branch:** `release/v1.13.15`
**Starting baseline:** `5337f955467e67e4cce9bcdae5904a4b2c6af670`

## Preconditions

An authenticated observation identified `franchoy` user ID `7415595` and
confirmed hosted `main`, fetched `origin/main`, and local `main` at the literal
starting baseline. No v1.13.15 branch, tag, release, or open pull request
collision existed. The working tree was clean before branch creation.

The exact-head CI and CodeQL workflows succeeded. The current release-tag
ruleset is active but contains the malformed include `refs/tags/"v*"`, has no
bypass actor, and therefore remains finding `CK-V11315-015`; it is evidence,
not an entry blocker. Direct alert inventory and an exact-head Codacy result
were unavailable and are not represented as passing facts.

## Changes

This commit adds only new `v1.13.15-*` release-control and evidence files under
`docs/release/v1.13/`. It changes no pre-existing file and no runtime,
dependency, workflow, container, version, governance setting, branch on the
remote, tag, release, or pull request.

The Phase 0A v1.13.14 baseline uses two layers:

- provenance, including the observation timestamp; and
- a bounded immutable identity with a separately computed SHA-256.

The whole baseline file has a second SHA-256 for tamper detection. The baseline
file ends in LF; the identity hash covers the canonical compact `identity`
serialization without a trailing LF. Later observations compare only that
canonical identity and its hash.

## Self-effective acceptance

The phase becomes complete when this allowlisted change is committed locally
and the post-commit tree is clean. It is not pushed in Phase 0A.

```text
PHASE_0A: COMPLETE_ON_THIS_LOCAL_COMMIT
PHASE_0_AGGREGATE: NEXT
PHASE_0B: NEXT
PHASE_1: NOT_STARTED
REMOTE_MUTATION: NONE
SOURCE_IDENTITY: 1.13.14
RELEASE_STATE_VALIDATOR: INTENTIONALLY_DEFERRED
```
