# Coldkeep v1.13.15 Phase 5 — Integrated Local Closure

**Mode:** `BUILD`
**Status:** Complete
**Observed:** `2026-08-27`
**Exact candidate:** `ec8717f40485e272042ee63ff05f265598a50efc`

## Candidate identity and bounded corrections

The integrated gate began from Phase 4 commit
`8ebeda21c2dda890b0407061ae022188a56b6dee`. Whole-repository execution
exposed three stale negative fixtures in
`scripts/audit_ci_enforcement_test.go`: their mutations still described the
pre-Phase-3 Required Gate, so the intended failure was not exercised. The
bounded validation-only corrections are:

- `23e4a27293a23e7658916888667329282d619b3d` — current cross-platform
  dependency fixture and diagnostic;
- `dab879ca03c6d4106a8e66b88e705691a58b453f` — current forbidden paired-job
  dependency fixture; and
- `ec8717f40485e272042ee63ff05f265598a50efc` — current cross-platform success
  assertion fixture.

Each fixture now proves that its mutation occurred before invoking the audit.
Focused regressions and the complete `scripts` package passed after correction.
No product, dependency, workflow, container, or public-contract file changed.

The exact candidate inventory is:

```text
tracked entries: 1479
git ls-tree stream SHA256: 1995a0bc91cecc0751aab2d597d8013b8d0b246025e142936fcc234a192db714
worktree: clean
```

## Certified quality and security gate

All commands used Go `1.26.7` with `GOTOOLCHAIN=local`. The exact candidate
passed:

- module tidy-diff, Go formatting, diff integrity, shell syntax, and
  ShellCheck;
- the complete Python discovery suite (`165/165`);
- governance, Phase 3 container-contract, development release-state, and local
  CI-enforcement validators;
- pinned `golangci-lint v2.9.0` with zero findings;
- `go vet ./...`;
- complete non-race and race whole-repository sweeps; and
- ordinary-output `govulncheck@v1.7.0 ./...` with zero reachable
  vulnerabilities. One vulnerability exists in a required module but is not
  called by Coldkeep.

The final whole-repository sweeps included the long-running packages rather
than substituting focused selectors:

| Sweep | Scripts | Adversarial | Integration | Result |
|---|---:|---:|---:|---|
| `go test -count=1 ./...` | 193.795s | 323.430s | 262.126s | Pass |
| `go test -race -count=1 ./...` | 194.759s | 345.310s | 330.703s | Pass |

## PostgreSQL, codec, and layout closure

A dedicated task-owned PostgreSQL 16.15 service supplied a fresh database for
every tier. The plain matrix passed short correctness, full stress, dedicated
long-run selectors, deterministic G6, full G1-G17 adversarial coverage, and
snapshot/legacy selectors. Recorded elapsed times were 211.029s, 276.090s,
56.579s, 15.926s, 305.782s, and 4.891s respectively.

The AES-GCM matrix passed the same tiers with a generated development-only key.
Its short correctness and stress tiers took 207.071s and 283.645s; the full
adversarial tier took 314.152s and the final snapshot selector took 3.478s.
No key value was retained. The full repository sweeps then repeated the
database-backed package surface under both normal and race execution.

The service, network, and `ckv11315p5_pgdata` test volume were removed after
validation.

## Benchmark evidence

All four hard integrity profiles passed with checksummed raw samples and
aggregates:

```text
none-w1: PASS
none-w4: PASS
zstd-w1: PASS
zstd-w4: PASS
```

Timing retained advisory-only authority. `none-w1` and `zstd-w1` were within
their references; `none-w4` produced six valid informational crossings and
`zstd-w4` produced two. The exact-SHA bundle was atomically promoted to the
ignored evidence root:

```text
.release-evidence/v1.13.14/ec8717f40485e272042ee63ff05f265598a50efc/
staging directories: 0
inventory: PASS
```

An initial attempt used a generated base64 key and failed closed before
evidence promotion because Coldkeep requires a 32-byte hex key. The rerun used
a generated 64-character hex key and passed; this was an operator-environment
correction, not a candidate change.

## Containers and source installation

The static container contract passed. Product images built for Linux amd64 and
arm64 and both executed `coldkeep version 1.13.15`. The host initially lacked
arm64 binfmt support; QEMU registration was restored with the test-support
image observed at digest
`sha256:400a4873b838d1b89194d982c45e5fb3cda4593fbfd7e08a02e76b03b21166f0`,
after which the already-built arm64 image smoked successfully.

The development container rebuilt, started its development-only PostgreSQL,
ran `post-create.sh` twice, and proved Go 1.26.7, `GOTOOLCHAIN=local`, and GCC.
Its task-owned containers, network, and volume were removed by the gate.

Local Linux `go install ./cmd/coldkeep` passed at the exact candidate with
CGO enabled, Go 1.26.7, GCC 13.3.0, and embedded VCS revision
`ec8717f40485e272042ee63ff05f265598a50efc`. The installed binary reports
Coldkeep 1.13.15 and `x/sys v0.44.0`.

The native Linux/macOS/Windows Phase 3 source-install proof remains applicable:
`git diff 8d643301..ec8717f4 -- go.mod go.sum cmd internal db` is empty, so no
module or build input changed after those three native rows passed. Public
exact-revision installation remains Phase 6 work; tagged installation remains
Phase 9 work.

## Historical identity preservation

An authenticated GitHub reobservation reconstructed the canonical Phase 0A
identity object from the actual release, annotated tag object, peeled commit,
published tree, and Phase23R manifest. It matched exactly:

```text
tag object: a996b25b562de69749f41c3af56626aeb5d44e33
peeled target: caac44d459609f89f2c971cb7b07a8678bd52d2c
release ID: 377376616
identity SHA256: 03d1aa57b46c2e340fc4d33876d0f197c558a4c106aea3e2798c30914914dbd6
baseline whole-file SHA256: 139d93f855d0185996fff54fc5c2008a26844ee6d33ec0aef484bc6778ecf4cc
V1_13_14_IDENTITY: PASS
```

The original Phase 0A baseline was not rewritten.

## Harness observations

The first cleanup preflight used a temporary tool directory beginning with
`/tmp/coldkeep-`, which collided with the established broad test-storage glob.
Cleanup stopped on a read-only module-cache entry; the task-owned directory was
removed and the exact tools were reinstalled under `/tmp/ckv11315p5-tools`.
No repository evidence or candidate file was changed. Failed intermediate
whole-repository attempts also left isolated test databases; only those
task-owned databases were force-dropped before the final fresh sweeps.

```text
PHASE_5: COMPLETE
PHASE_6: NEXT
V1_13_14_IDENTITY: UNCHANGED
V1_13_15_IMPLEMENTATION: LOCAL_CANDIDATE_CERTIFIED
V2_IMPLEMENTATION: NOT_STARTED
```
