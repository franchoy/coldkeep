# v1.13.16 Phase 4 Snapshot Capture-root Implementation Authority

**Release:** v1.13.16 — Snapshot Retention Integrity, Observability Truth, and Final v1.x Closure
**Phase:** 4 — Next
**Mode:** BUILD

## Authority boundary

Phase 3 RED evidence is frozen. Its local harness at
`internal/engine/snapshot_create_path_contract_test.go` remains byte-identical
at 17,788 diff bytes and SHA-256
`6b006f9024e8ea45b3551d02d8f9d2cdeef961238924b073473c8b6a947c444c`.
It may execute but must not be edited, staged, committed, or pushed.

CK-V11316-001 is the Phase 4 implementation root. Phase 4 owns the explicit
create-time `SelectionBase`, CLI CWD handoff, physical selector resolution,
capture-root containment, relative member derivation, full-snapshot
containment failure, and lossless source representability. CK-V11316-002
remains open Phase 5 scope; Phase 4 does not intentionally implement complete
per-selector miss accounting or final selector atomicity/backend closure.

## Frozen compatibility

- `physical_file.path` remains the existing canonical absolute host-native
  physical identity.
- `snapshot_path.path` remains normalized relative member identity.
- `SnapshotQuery` and `SnapshotRestoreSelection` do not gain `SelectionBase`.
- Existing snapshot show, query, pattern, regex, diff, and restore semantics
  continue to operate on persisted relative members without process CWD.
- The CLI adds no capture-root flag; it obtains CWD once for snapshot creation
  and hands it to the Engine explicitly.
- Schema and database formats do not change; schema version remains 16.
- No broad snapshot, restore, storage, or physical-path refactor is authorized.

## Exact implementation allowlist

Production paths:

- `internal/engine/candidates.go`
- `internal/engine/snapshot_create.go`
- `internal/engine/snapshot_selector.go`
- `internal/snapshot/snapshot.go`
- `cmd/coldkeep/main.go`
- optional focused file `internal/snapshot/snapshot_create_path.go`

Compatibility/adaptation test paths:

- `internal/engine/snapshot_create_engine_test.go`
- `internal/snapshot/snapshot_test.go`
- `cmd/coldkeep/snapshot_create_engine_routing_test.go`

The implementation must remain host-native and compile on supported platforms.
It does not parse foreign Windows roots on Linux, change Store physical-path
normalization, add dependencies, introduce schema movement, or begin Phase 5.

## Required disposition

Phase 4 must make R1, R2, R3, R6, R7, and POSIX R8 GREEN while preserving
R9–R13. R4 and R5 remain expected RED Phase 5 evidence unless correct shared
path work makes them incidentally GREEN. CK-V11316-001 remains open after
implementation pending Phase 6 retention proof; no finding closes in Phase 4.

```text
PHASE_3: COMPLETE
PHASE_4: NEXT
CK-V11316-001: OPEN_RED_REPRODUCED
CK-V11316-002: OPEN_RED_REPRODUCED
FINDINGS_CLOSED: 0/7
SCHEMA_VERSION: 16
```
