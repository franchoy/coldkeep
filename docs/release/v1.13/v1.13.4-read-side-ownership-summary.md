# Coldkeep v1.13.4 Read-Side Ownership Summary

**Release:** `v1.13.4 — Read-Side Dependency Direction and Result Ownership Review`  
**Phase:** Phase 9  
**Status:** Complete pending Phase 10 release-gate execution  
**Branch:** `release/v1.13.4`

## Purpose

This document summarizes the final `v1.13.4` read-side ownership decisions
after Phases 1–8.

The reviewed surfaces are:

- `stats`
- `inspect`
- `snapshot show`
- `snapshot diff`

This is a pre-release summary. `v1.13.4` is not PR-ready until Phase 10
executes the final `PRE_RELEASE_CHECKLIST.md` Profile A gate.

## Release shape

- `v1.13.4` is primarily a documentation and guard-test release.
- Phase 7 added narrow CLI guard tests.
- No production code changed so far.
- No public behavior changed so far.
- No human output changed so far.
- No JSON output changed so far.
- No JSON error envelopes changed so far.
- No exit-code behavior changed so far.
- No routing changed so far.
- No schema, storage, or repository format changed so far.
- Final release readiness still depends on Phase 10.

## Phase summary

| Phase | Output | Result |
| --- | --- | --- |
| Phase 0 | Scope docs and stale `v1.13.3` cleanup | complete |
| Phase 1 | Ownership inventory | complete |
| Phase 2 | `stats` result ownership review | complete |
| Phase 3 | `inspect` dependency-direction review | complete |
| Phase 4 | `snapshot show` ownership review | complete |
| Phase 5 | `snapshot diff` ownership review | complete |
| Phase 6 | Renderer/JSON boundary review | complete |
| Phase 7 | Dependency-direction guard tests | complete |
| Phase 8 | Backend compatibility review | complete |
| Phase 9 | Final summary and `v1.13.5` handoff | complete |
| Phase 10 | Final release gate and PR opening | pending |

## Final ownership decisions

### `stats`

Decision:

- keep current ownership unchanged for `v1.13.4`
- treat `engine.StatsResult` wrapping `observability.StatsResult` as a
  documented transitional seam
- observability owns concrete aggregation
- renderer owns human output and success JSON output
- CLI owns public error envelopes
- broad result-shape migration remains deferred

### `inspect`

Decision:

- keep direct `CLI -> observability` routing unchanged for `v1.13.4`
- observability owns inspect logic and the production result type
- public targets remain:
  `repository`, `file`, `logical-file`, `snapshot`, `chunk`, and `container`
- `EntityPhysicalFile` remains internal-only
- renderer owns human output and success JSON output
- CLI owns public error envelopes
- engine-owned inspect seam decision remains deferred

### `snapshot show`

Decision:

- keep mixed ownership unchanged for `v1.13.4`
- metadata and stats remain engine-backed through CLI helpers
- file listing remains snapshot-package-backed
- CLI owns success JSON assembly
- renderer owns human output
- CLI owns public error envelopes
- preserve `matched_file_count` and `total_snapshot_file_count`

### `snapshot diff`

Decision:

- keep split ownership unchanged for `v1.13.4`
- CLI owns validation, richer query parsing, summary fast-path selection,
  filter post-processing, count projection, and success JSON assembly
- engine owns the provisional diff request/routing seam
- snapshot package owns core detailed diff logic
- renderer owns human output
- CLI owns public error envelopes
- preserve `matched_entry_count`, `total_diff_entry_count`, summary/detailed
  behavior, and `entries` inclusion/omission behavior

## Guard coverage added in v1.13.4

Phase 7 added the following narrow CLI guard tests:

- `TestReadSideJSONOwnershipFamiliesRemainDistinct`
  - protects renderer-envelope success JSON for `stats` / `inspect` versus
    CLI-envelope success JSON for `snapshot show` / `snapshot diff`
- `TestReadSideJSONErrorsRemainCLIEnvelopeOwned`
  - protects the CLI-owned JSON error envelope family across representative
    read-side failures
- `TestInspectPublicTargetsRemainDocumentedSet`
  - protects the public inspect target list, the `logical-file` alias, and
    `physical-file` remaining unsupported publicly
- `TestReadSideOutputDoesNotLeakTaxonomyHelperNames`
  - protects representative success and error payloads from leaking internal
    taxonomy helper terminology

## Backend compatibility decision

Phase 8 concluded:

- SQLite compatibility remains preserved.
- PostgreSQL compatibility remains preserved.
- No SQLite-only assumptions were introduced.
- The strongest dual-backend anchor remains
  `internal/catalog/backend_contract_backends_test.go`.
- PostgreSQL validation remains environment-gated via `COLDKEEP_TEST_DB`.
- Temporary per-test PostgreSQL database isolation remains important.
- Final confidence still depends on Phase 10 Profile A validation.

## Public contract preservation

`v1.13.4` preserves:

- CLI behavior
- human output
- JSON success output
- JSON error envelopes
- exit codes
- command routing
- engine routing
- schema and migrations
- storage format
- repository format
- backend defaults
- SQLite compatibility
- PostgreSQL compatibility

## Deferred seams

| Seam | Status | Recommended future handling |
| --- | --- | --- |
| `stats` engine wrapper over observability result | Transitional | Defer broad result migration |
| Direct `inspect` CLI-to-observability path | Transitional | Defer engine inspect seam decision |
| `snapshot show` mixed engine/snapshot ownership | Transitional | Defer broad consolidation |
| `snapshot diff` split CLI/engine/snapshot ownership | Transitional | Defer broad query/result migration |
| Split success JSON ownership | Transitional | Preserve until a dedicated output-contract release |
| CLI-owned JSON error envelopes | Stable public contract | Preserve |
| Backend parity evidence partly indirect | Accepted for `v1.13.4` | Revisit when ownership migrates |

## Release readiness status

`v1.13.4` is not PR-ready until Phase 10 completes the final release gate.

If Phase 10 finds a correctness blocker, do not open the PR. Record the
blocker in the release gate and phase list, remediate narrowly, and rerun the
gate.

## Phase 9 conclusion

- `v1.13.4` ownership decisions are complete.
- `v1.13.4` is ready for Phase 10 final validation.
- No production code changed in Phase 9.
- No tests changed in Phase 9.
