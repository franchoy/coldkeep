# Phase 12R1R22 — CK-V11316-014 required-proof reliability remediation

**Mode:** BUILD
**Status:** Authorized — local implementation and certification only
**Base:** `cf2351cac16a95a4d34ddaadf622eb329432ebc2`

## Finding and policy disposition

CK-V11316-014 is repository-confirmed as an open S2 CI/test reliability
finding. The previously unresolved question is resolved prospectively as:

```text
AGGREGATION_POLICY: STAGE_SCOPED_AFTER_SUCCESSFUL_PREFLIGHT
CK_V11316_014: OPEN
FINDINGS_CONFIRMED: 15
FINDINGS_CLOSED: 13/15
CK_V11316_015: CLOSED_CERTIFIED
CK_V11316_007: OPEN_UNCHANGED
V1_X_TECHNICAL_CORRECTNESS_CLOSURE: WITHHELD
PHASE_13: NOT_AUTHORIZED
```

This policy is an owner decision for the remediation, not proof that an
unambiguous historical public guarantee already existed. Standard/full
prerequisite failure remains fail-fast and prevents entry to downstream deep
verification. After successful preflight, the existing deep loop continues to
collect independent errors it can safely continue past. Existing immediate
returns for unusable discovery, invalid authority, cancellation, or other
unsafe continuation remain unchanged. No public promise is made that one
invocation reports every defect, returns an exact cross-version count, orders
diagnostics in a particular way, or aggregates across prerequisite and deep
stages.

Historical attribution remains append-only: commit
`aba89fe221d3b119210215c815d81d624e5940ba` replaced the prior count assertion
with a first-error expectation; `f6ddb130cd2c5952a556c6104eb594d435c684c3`
later changed the error taxonomy.

## Exact implementation authority

The implementation/enforcement commit may modify only:

- `internal/verify/verify_system.go`
- `internal/verify/verify_system_deep_aggregation_test.go`
- `tests/integration/integration_test.go`
- `scripts/check_required_test_events.py`
- `scripts/test_check_required_test_events.py`
- `scripts/audit_ci_enforcement.sh`
- `scripts/audit_ci_enforcement_test.go`
- `.github/workflows/ci.yml`
- `PRE_RELEASE_CHECKLIST.md`

The verification change is limited to a private, per-invocation
`ContainerReader` supplied after real full preflight. The normal entry point
must use `FilesystemContainerReader` and preserve its current checks, query and
error order, stopping behavior, aggregation, logging, cancellation, public
output, and exit behavior. No exported testing API, environment bypass,
process-global injection hook, copied collector, or production mutation is
authorized.

The integration proof must separately establish: simulation failures do not
skip; a source-consistent physical gap reaches and triggers only the existing
packed occupancy error; and two pre-existing packed corruptions are rejected
by real CLI preflight. A private production-pipeline test begins from a fixture
whose full preflight genuinely passes, injects exactly two distinct copied-
buffer faults in the downstream reader, proves both targeted failures were
collected, and proves persistent files and catalog bytes remain unchanged.
The controlled count of two belongs only to that fixture.

## Required execution evidence

Required Go JSON evidence is bound to exact package and test identities in
three profiles:

- `integration-correctness-plain` —
  `github.com/franchoy/coldkeep/tests/integration`
- `integration-correctness-aes-gcm` —
  `github.com/franchoy/coldkeep/tests/integration`
- `ck014-internal-verify` —
  `github.com/franchoy/coldkeep/internal/verify`

Each required test needs a preceding `run`, one non-contradictory terminal
`pass`, and successful completion of its expected package. The original Go
process, the Go-to-`tee` evidence capture, and the checker are independent
requirements:

```text
GO_PROCESS_EXIT_ZERO
AND EVIDENCE_CAPTURE_EXIT_ZERO
AND COMPLETE_VALID_EVENT_EVIDENCE
AND REQUIRED_PROOFS_PASS
```

Both local Profile A and the existing hosted correctness-matrix job must
snapshot the complete `PIPESTATUS` array immediately after each pipeline.
They run the checker for diagnostic completeness, return the original Go
status first, otherwise fail on capture status, and only then use the checker
status. Go stderr is retained separately from machine-readable JSON. A failed
invocation cannot borrow an earlier event file. Required skip, fail, missing
selection, wrong package, contradictory terminal evidence, malformed or
incomplete input, and unknown profiles fail. Unrelated optional skips retain
their existing policy.

## Commit and execution boundary

Exactly two local commits are authorized:

1. `docs: authorize v1.13.16 required-proof reliability remediation`
2. `test: restore v1.13.16 stage-scoped verification proof`

The second commit must contain the private seam, implementation tests,
required-event checker, local/hosted wiring, and enforcement together. No
artificial uncompilable RED commit is required. Deterministic baseline evidence
must distinguish current skip/enforcement defects from newly restored
aggregation coverage; it must not claim an established production aggregation
regression.

After the second commit, complete attached and fresh-detached exact-head local
certification under Go 1.26.7, PostgreSQL 16.15, both codecs, the canonical
Profile A workload, exact lint, governance, and dynamic Python discovery.
Retain separate external logs, JSON events, stderr, pipeline statuses,
checksums, and resources. Any required failure, identity drift, unexpected
path, behavior change, or unestablished fixture premise stops execution.

No push, hosted execution, evidence/closure commit, CK-V11316-014 closure,
CK-V11316-007 remediation, Phase 12 completion, Phase 13 work, tag, or release
is authorized.
