# v1.13.16 Phase 6R9 — Critical-Coverage Throughput Assertion Isolation Recovery

**Mode:** BUILD
**Status:** `BRANCH_WORKTREE_LOCAL_PASS_COMMITTED_AND_DETACHED_PENDING`
**Finding:** `CK-V11316-010`

Phase 6R9 is the current v1.13.16 implementation phase. It is authorized only
to isolate the two Phase 1 compatibility-matrix throughput floor assertions
from Go coverage instrumentation while preserving the real store and restore
operations, correctness assertions, and diagnostic measurements.

The one-file implementation and branch-worktree local validation are complete.
Committed-tree certification, detached certification, push, and hosted evidence
remain pending at this evidence checkpoint.

## Accepted entry state

- Branch: `release/v1.13.16`
- Accepted head: `f58be125eee09c6c1cfb76b911ada18e7f232a69`
- Phase 6R7 corrected detached execution passed, including all 12 retained
  PostgreSQL integration selectors.
- The accepted Phase 6R7 head was pushed exactly once.
- Hosted CI run `33407340364`, attempt 1, evaluated the accepted head and
  completed with 35 of 36 jobs successful. The sole failure was `critical
  coverage report` job `99539998681`; `Required Gate` job `99544626406`
  succeeded.
- Hosted CodeQL run `33407340517`, attempt 1, evaluated the accepted head and
  succeeded for Python, Actions, Go, and the aggregate gate.
- Preceding CI run `33365683050`, attempt 1, remains failed and is retained as
  chronology evidence only.
- No newer branch push exists, and code scanning reports zero open alerts.

The failed step was `Generate critical coverage report`. Package
`github.com/franchoy/coldkeep/internal/storage` failed in
`TestPhase1CompatStoreThroughputWithinTolerance`: the 4 MiB operation took
`4.390023167s`, measured `0.91 MB/s`, and was below the unchanged `5.00 MB/s`
floor. No Store error, mismatch, panic, race, timeout, filesystem error, or
corruption was reported. The adjacent restore throughput test executed
afterward without failing. The command stopped before `go tool cover` and CSV
generation.

## Failure classification

The accepted failure is a combination: primary gate composition, secondary
measurement stability, with Go coverage instrumentation as the contributing
condition. It is not a product-runtime regression and not a hosted CI
infrastructure defect. The real operations completed and correctness remained
valid; only their numeric wall-clock floors were unsuitable for enforcement in
the instrumented coverage binary.

## Finding state

`CK-V11316-010` is open as an S2 CI/test reliability blocker. The report-only
critical-coverage job combines a single-sample wall-clock assertion, atomic
coverage, and multi-package hosted-runner contention. The denominator is now 10
confirmed findings and 1 of 10 closed. `CK-V11316-009` remains implemented
pending exact-head hosted certification. Phase 6 remains withheld.

## Authorized implementation

Exactly one executable/test file may change:

- `internal/storage/phase1_compat_matrix_test.go`

The file must gain exactly one private helper named
`skipPhase1ThroughputFloorUnderCoverage`, accepting `*testing.T`, operation
name, measured MB/s, active threshold, and elapsed duration. The helper must
call `t.Helper()`, contain the file's only `testing.CoverMode()` call, return
for an empty mode, and call `t.Skipf` for a non-empty mode.

Its diagnostic must include the operation, exact coverage mode, measured MB/s,
threshold, elapsed duration, confirmation that the real operation completed,
and confirmation that only the numeric wall-clock floor is not enforced.

Store order is fixed: real store, elapsed calculation, throughput calculation,
diagnostic log, helper call, threshold comparison.

Restore order is fixed: setup store, timed restore, elapsed calculation,
throughput calculation, diagnostic log, `bytes.Equal` correctness assertion,
helper call, threshold comparison.

There may be no warm-up, retry, threshold, payload, codec, worker, timeout,
workflow, script, product-runtime, module, dependency, or database change.

## Frozen and allowed documentation surfaces

All product runtime, threshold definitions, scripts, workflows, module files,
database surfaces, and source/test files other than the one file above are
frozen. The retained PostgreSQL 12-of-12 result remains authoritative and the
service must not be reprovisioned.

Only these current-authority documents may change:

- `docs/release/v1.13/README.md`
- `docs/release/v1.13/v1.13.16-phase-list.md`
- `docs/release/v1.13/v1.13.16-phase6-retention-certification.md`
- `docs/release/v1.13/v1.13.16-phase6r9-critical-coverage-throughput-recovery.md`
- `docs/release/v1.13/v1.13.16-release-state.md`
- `docs/release/v1.13/v1.13.16-remediation-tracker.md`
- `docs/release/v1.13/v1.13.16-scope.md`
- `docs/release/v1.13/v1.13.16-source-test-allowlist.md`
- `docs/release/v1.13/v1.13.16-validation-checklist.md`

Historical phase documents remain immutable.

## Required commit and validation contract

Exactly three commits must follow the accepted head:

1. `docs: authorize v1.13.16 critical coverage throughput recovery`
2. `test: isolate throughput assertions from coverage reporting`
3. `docs: record v1.13.16 critical coverage throughput recovery`

The local result-semantic matrix requires ordinary selectors 20 times with the
unchanged `5.0 MB/s` floors and zero skips; race selectors 10 times with the
unchanged `1.0 MB/s` floors and zero skips; atomic-coverage selectors 10 times
with both real operations executed and exactly the two expected skips; and one
full Phase 1 atomic-coverage run with only those two test names skipped and all
other Phase 1 tests passed. Three independent critical-coverage reports must
each reach `go tool cover`, emit a valid 17-row CSV, and leave the tree clean.

The full package, plain/AES-GCM quality, repository ordinary/race, vet, module,
format, shell, exact golangci-lint 2.9.0, authority, governance, Phase 3,
215-test Python, and CI-policy gates must pass. The committed tree repeats this
matrix with permitted selector counts 5/3/5. A fresh detached worktree at the
evidence SHA repeats the matrix using `GOFLAGS=-buildvcs=false` only for Go and
repository scripts that invoke Go; its first `go list -json ./...` must produce
exactly 51 complete package records with empty stderr, and its engine dependency
guard must pass.

The prior isolated PostgreSQL matrix is retained as `PASS_12_OF_12`; it is not
reprovisioned or rerun because this phase changes no runtime, DB, schema,
migration, Store, container, adversarial, integration, or backend path.

Neither failed CI run `33407340364` nor `33365683050` may be retried. After all
local and detached gates pass, exactly one push must trigger fresh exact-head CI
and CodeQL. CI must pass 36 of 36 jobs, CodeQL must pass 4 of 4, alerts must be
zero, and direct quality/status truth must be recorded. A failure is evidence
for an immediate stop, not authorization for retry or another commit.

Stop before mutation or push if identity or topology changes, an allowlist or
frozen surface would be exceeded, a threshold changes, execution/correctness is
bypassed, skip semantics differ, any critical report or required gate fails,
lint is not exact, Python is not 215 of 215, committed and detached results
differ, PostgreSQL retention is invalidated, or the remote moves.

Until fresh exact-head hosted CI passes 36 of 36 jobs, CodeQL passes 4 of 4,
and alert/status truth is clean, Phase 6 remains withheld, Phase 7 remains
unauthorized, and release tagging and publication remain prohibited.

## Branch-worktree implementation evidence

- Authority: `6be80e391c7c19a42ea9547aa83c815298311dd5`
- Test: `9439c433ae09588378c7c3e4c1afda1baaa1e1bb`
- Executable path: `internal/storage/phase1_compat_matrix_test.go` only
- Test-file SHA-256:
  `9eb713c373e9b9ef4c987901d1bb0aa6c1bb10f7f596570e11d0ba45c9a197fb`
- Helper: `skipPhase1ThroughputFloorUnderCoverage`
- `testing.CoverMode()` occurrences: 1, inside that helper
- Helper call sites: 2
- Threshold files and values: unchanged (`5.0 MB/s` ordinary, `1.0 MB/s`
  race)
- Ordinary selectors: 20 store passes and 20 restore passes; zero skips and
  failures. Store measured 12.40–13.98 MB/s; restore measured 84.40–105.71
  MB/s.
- Race selectors: 10 store passes and 10 restore passes; zero skips, failures,
  and race reports. Store measured 7.43–8.79 MB/s; restore measured 47.37–59.44
  MB/s.
- Atomic selectors: 10 store runs/skips and 10 restore runs/skips; zero target
  passes and failures. Every diagnostic reported exact mode `atomic`, measured
  MB/s, active `5.00 MB/s` threshold, elapsed duration, completed real
  operation, and only the numeric wall-clock floor not enforced.
- Full Phase 1 atomic coverage: 15 tests started, 13 passed, and exactly the two
  throughput test names skipped. DB insert bounds, restore determinism, hash,
  and parity passed.
- Coverage profiles were non-empty and valid; `go tool cover` completed.
- Critical reports 1–3 each completed and produced 17-row CSVs with exact
  header, unique populated inventory, and valid coverage values. External CSVs
  `critical-1.csv`, `critical-2.csv`, and `critical-3.csv` each hash to
  `3aed0bee231113609b3689fa136623c5c9f30abbdc5e1b62c6fa456b1801bc47`.
- Full storage ordinary/race, plain/AES-GCM race quality, full Go/race, vet,
  module diff, gofmt, shell, validation matrix, row writers, and CI policy
  passed.
- Exact golangci-lint 2.9.0 binary SHA-256 was
  `9d3691b2e2410f1fe06b6cb72c734e4cd15eebbcb5d5c5f08ac0901c693fdbd9`;
  the candidate wrapper reported PASS with zero issues.
- Release-state development/auto, governance, Phase 3 contracts, snapshot
  names, benchmark inventory, linearity, and exactly 215 Python tests passed.
- The worktree and index were clean after validation. The Phase 6R7 test hash
  remained `9d2765acd0e08a84e9bfe5afda66dd5fd6ca08893c1218fbd7bbfb588eed5dac`.
- The prior isolated PostgreSQL result remains retained at 12 of 12.

`CK-V11316-010` is implemented pending exact-head hosted certification.
Committed-tree and detached certification must still pass before the one
authorized push.
