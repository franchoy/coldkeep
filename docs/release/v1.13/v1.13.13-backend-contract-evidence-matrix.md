# Coldkeep v1.13.13 Backend Contract Evidence Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `4 — SQLite/PostgreSQL Backend Audit`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `02740d5af8a52173c593407b56d32e8c1481475e`

Source establishes ownership and semantics. Local and hosted tests supplement
that proof; a generic green job is never substituted for a named claim. Local
PostgreSQL was unavailable and no service was provisioned solely for Phase 4.

| Contract / claim | Source evidence | Local SQLite/default evidence | Local PostgreSQL outcome | Required exact-head hosted evidence | Skip ownership | Evidence strength | Finding / classification |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Backend fixtures/detection | `internal/db/backend.go`; `internal/testutil/backendtest` | fixture isolation passes | all `/postgres` fixtures skip without configured DB | backend fixture setup in required PostgreSQL step | Infrastructure | Strong for SQLite; hosted required for PG | PROVEN |
| Schema version 16 | migration constant, SQLite endpoint, both schema files | `SCH001`, `SCH003`, migration metadata pass | PG selectors skipped locally | named fresh/current PG schema events | Infrastructure | Three-source derivation plus execution | PROVEN |
| Fresh bootstrap/idempotency | `RunMigrations`; `EnsurePostgresSchema` | SQLite bootstrap/rerun selectors pass | PG bootstrap selectors skip | `TestPostgresFreshBootstrapCreatesPhaseOneV8Schema`; `SCH001/postgres` | Infrastructure | Strong when named hosted event maps | PROVEN_WITH_EXPLICIT_BOUND |
| Supported migration chains | ordered SQLite migrations; transactional PG schema | legacy and `SCH008`–`SCH011` SQLite paths pass | 18 named PG/schema skips in focused migration run | legacy pre-v6, v7, v11, metadata-boundary named events | Infrastructure | Strong source + fixtures | PROVEN |
| Fresh/upgraded semantic parity | schemas plus migration guards | current constraints/data preservation pass | PG parity unavailable locally | required PG schema package contracts | Infrastructure | Strong for supported paths | PROVEN |
| Transaction commit/rollback | shared `database/sql` transaction paths | cross-backend SQLite subtest passes under race | `/postgres` skips | `TestBackendTransactionCommitRollbackAcrossBackends/postgres` | Infrastructure | Strong named contract | PROVEN |
| Conditional locks/release | backend lock suffix helpers | intentional clause omission paths pass | lock subtests skip | `TestBackendForUpdateLockReleaseAcrossBackends/postgres` | Infrastructure | Strong bounded capability proof | PROVEN_WITH_EXPLICIT_BOUND |
| `NOWAIT` / `SKIP LOCKED` | PG-only suffixes and error handling | omission is source-proven | subtest skips | `TestBackendNowaitAndSkipLockedAcrossBackends/postgres` | Infrastructure | Strong bounded PG proof | PROVEN_WITH_EXPLICIT_BOUND |
| Lock cancellation | context propagation and rollback | SQLite/common cancellation behavior passes | subtest skips | `TestBackendBlockedLockCancellationAcrossBackends/postgres` | Infrastructure | Strong named contract | PROVEN |
| Required-row cardinality | `internal/db/mutation_rows.go` | SQLite subtest passes | subtest skips | `TestMutationRowsAffectedContractAcrossBackends/postgres` | Infrastructure | Strong named contract | PROVEN |
| Catalog logical/physical facades | `logical_files.go`; `physical_files.go` | SQLite contract passes | `/postgres` skips | named Catalog facade events | Infrastructure | Implementation/equivalence strong; typing/adoption debt inherited | `P2-ARCH-001` |
| Current files/configuration | Catalog and Engine production routes | Catalog/Engine SQLite contracts pass | `/postgres` skips | named Catalog/Engine events | Infrastructure | Strong shared contract | PROVEN |
| Snapshot lookup/graph | `snapshots.go`; `graph.go` | SQLite lookup/graph passes | `/postgres` skips | named Catalog graph/snapshot events | Infrastructure | Strong except inherited typing | PROVEN / `P2-ARCH-001` |
| Snapshot label filter | `snapshots.go`: `label LIKE $2` | SQLite probe: `Alpha LIKE %alpha%` is true | source/manual PG semantics: `LIKE` case-sensitive | existing same-case hosted test cannot disprove divergence | Test design gap, not infrastructure | Source proves semantic mismatch | `P4-BE-001`, BLOCKER |
| Placement planning | `placement.go` tagged union | SQLite contract passes | `/postgres` skips | `TestCatalogContractChunkPlacementsAcrossBackends/postgres` | Infrastructure | Strong named production contract | PROVEN |
| Restore planning | `restore_plan.go`; storage adoption | SQLite contract and restore suites pass | `/postgres` skips | `TestCatalogContractRestorePlansAcrossBackends/postgres` | Infrastructure | Strong plan proof; install roots inherited | PROVEN / `P3-COR-001`, `002` |
| G6 coherent GC statement | `gc_plan.go` one compound `UNION ALL` | all five deterministic mutation cases pass under race | five `/postgres` cases skip | all five named G6 `/postgres` events in required/adversarial jobs | Infrastructure | Source + MVCC rule + deterministic hosted test | PROVEN |
| GC plan/dry-run | Catalog plan, `gc.BuildPlan`, Engine | plan and dry-run contracts pass | `/postgres` skips | Catalog plan and Engine dry-run named events | Infrastructure | Strong shared read-only contract | PROVEN |
| SQLite live GC refusal | `maintenance.acquireGCAdvisoryLock` | three explicit refusal/dry-run tests pass | N/A | not substituted by PG live execution | Intentional capability | Strong source and local behavior | PROVEN_WITH_EXPLICIT_BOUND |
| PostgreSQL live GC | dedicated advisory session and transaction path | N/A | gated live tests skip | named live-GC/advisory events in required PG jobs | Infrastructure | Strong only at exact hosted SHA | PROVEN_WITH_EXPLICIT_BOUND |
| Snapshot mutation lifecycle | shared Engine/snapshot transactions | SQLite lifecycle subtest passes under race | `/postgres` skips | `TestEngineMutationSnapshotLifecycleAcrossBackends/postgres` | Infrastructure | Strong shared outcome | PROVEN except label query |
| Store/remove/restore mutations | shared Engine/storage paths | SQLite contracts pass | `/postgres` skips | named Engine mutation events | Infrastructure | Strong route evidence; restore roots inherited | PROVEN_WITH_EXPLICIT_BOUND |
| Stats/inspect/snapshot reads | shared Engine routes | SQLite read contracts pass | `/postgres` skips | named Engine read events | Infrastructure | Strong named contract | PROVEN except label filter |
| Verify | shared verify routes | SQLite corruption and backend contracts pass | `/postgres` skips | `TestEngineReadVerifyAcrossBackends/postgres` | Infrastructure | Route proof strong; packed file-deep defect inherited | `P3-COR-003` |
| Recovery | shared recovery/Engine path | SQLite idempotency/fault paths pass | `/postgres` skips | named recovery backend events | Infrastructure | Strong when hosted events map | PROVEN |
| Repair | shared topology/Engine path | SQLite repair contracts pass | `/postgres` skips | named repair backend events | Infrastructure | Strong when hosted events map | PROVEN |
| Doctor | ordered Engine composition | SQLite backend contract passes | `/postgres` skips | `TestEngineDoctorAcrossBackends/postgres` | Infrastructure | Strong named route | PROVEN |
| Container row lock | conditional backend lock helper | SQLite bounded path passes | `/postgres` skips | `TestContainerRowLockIntegrationAcrossBackends/postgres` | Infrastructure | Strong bounded capability | PROVEN_WITH_EXPLICIT_BOUND |
| Packed/legacy/mixed behavior | common placement/read/GC/verify paths | compatibility and maintenance suites pass where supported | PG maintenance suites skip locally | required PG package plus adversarial plain/AES-GCM | Infrastructure | Strong aggregate except inherited file-deep root | `P3-COR-003` |
| Compression variants | common storage codec paths | six feature-gated tests skip without opt-in | N/A | adversarial plain/AES-GCM is supplemental | Feature gate | Not a backend gap | EXPLICIT_BOUND |
| Darwin restore path | platform-specific restore test | one Darwin-only skip on Linux | N/A | cross-platform phase later | Platform | Legitimate environment limit | EXPLICIT_BOUND |
| Unsupported codec fixtures | SQLite schema `CHECK` rejects fixture creation | two verify tests skip because invalid row cannot be seeded | N/A | no backend claim missing | Schema guard | Stronger fail-closed schema, not lost proof | PROVEN |
| SQLite lock/concurrent-store exclusions | explicit test capability bounds | two skips | N/A | Phase 5 owns broader coordination | Intentional capability | Honest bounded evidence | PROVEN_WITH_EXPLICIT_BOUND |

## Local JSON event totals

| Evidence file | Scope | Pass events | Skip events | Fail events |
| --- | --- | ---: | ---: | ---: |
| `/tmp/coldkeep-phase4-evidence/local-backend.json` | 11 backend/subsystem package trees | 1,883 | 109 | 0 |
| `/tmp/coldkeep-phase4-evidence/focused-race.json` | G6, transactions, locks, cardinality, container, snapshot, GC | 37 | 13 | 0 |
| `/tmp/coldkeep-phase4-evidence/migrations.json` | schema/bootstrap/migration selectors | 57 | 18 | 0 |

Package-level pass events are included in Go's JSON totals. Every skip event
was retained with package and test identity. The focused 13 skips and migration
18 skips are entirely PostgreSQL infrastructure gates; none is silently
treated as a pass.

## Hosted evidence acceptance

At the exact committed Phase 4 SHA, the final handoff must record:

- branch CI and `CI Required Gate` success;
- the exact `correctness-matrix (plain)` PostgreSQL job and its named required
  package-contract events;
- adversarial plain and AES-GCM G6 execution;
- CodeQL and `CodeQL Aggregate` success; and
- one immutable SHA shared by every cited run.

Until that exact-head evidence completes, `PHASE_4` cannot be set to `PASS` in
the external handoff even though this evidence document records the frozen
audit verdict.
