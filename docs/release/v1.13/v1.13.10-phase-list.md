# Coldkeep v1.13.10 Phase List

**Release:** `v1.13.10 — v1.x Closure Integrity and CI Runtime Hygiene`
**Status:** Ready for release
**Branch:** `release/v1.13.10`

## Phase 0 — v1.13.9 Post-Release Documentation Closure and v1.13.10 Baseline

**Status:** Complete

Close the released v1.13.9 documentation state, reconstruct its missing Phase
7 and Phase 8 tracker sections from canonical evidence, record the canonical
release gate, reconcile the v1.13.x train, and establish these v1.13.10
trackers. This phase is documentation-only and does not change runtime code,
schemas, repository or storage formats, backend defaults, workflows, or
scripts.

Acceptance requires truthful v1.13.9 merge/tag/release evidence, one active
v1.13.10 definition, preserved explicit v2.0/later-v2.x deferrals, and a
clean documentation-only diff within the authorized file scope.

Phase 0 is complete.

## Phase 1 — v1.13.10 Release Identity Activation

**Status:** Complete

## Objective

Activate the v1.13.10 executable and release-gate identity.

## Files changed

- `internal/version/version.go`
- `internal/version/version_test.go`
- `PRE_RELEASE_CHECKLIST.md`
- release tracker files

## Intended behavior change

Only `coldkeep version 1.13.10` and equivalent JSON version output.

## Explicit non-changes

- no CLI parser, output-shape, or exit-code change;
- no engine, storage, snapshot, restore, GC, verification, or repair/recovery change;
- no schema, migration, workflow, CI, or backend-default change.

## Validation

Commands run successfully:

```bash
gofmt -w internal/version/version.go internal/version/version_test.go
go test -count=1 ./internal/version
go test -count=1 ./cmd/coldkeep -run 'TestRunCLIVersionRejectsUnexpectedPositionalArgs'
go build -o "$phase1_tmp/coldkeep" ./cmd/coldkeep
bash scripts/check_smart_quotes.sh
scripts/validate_validation_matrix.sh
bash scripts/check_versioned_row_writers.sh
scripts/audit_ci_enforcement.sh --local-only
git diff --check
```

The built CLI reported `coldkeep version 1.13.10`; JSON output reported
`1.13.10` in one valid envelope.

Phase 1 is complete. Phase 2 — v1.13.x Release-Train Reconciliation is next.

## Phase 2 — v1.13.x Release-Train Reconciliation

**Status:** Complete

## Objective

Reconcile the original proposed release numbering with actual v1.13.9
completion and explicit v2.x deferrals.

## Result

- Former v1.13.10–v1.13.13 numbers are retired.
- Decision, inventory, and readiness work is mapped honestly.
- Substantive unimplemented hardening remains explicit.
- No additional mandatory v1.x runtime phase was created.
- One authoritative active v1.13.10 definition remains.

## Files changed

- `docs/release/v1.13/v1.13.10-release-train-reconciliation.md`
- `docs/release/v1.13/v1.13.x-release-train.md`
- `docs/release/v1.13/v1.13.10-scope.md`
- `docs/release/v1.13/v1.13.10-phase-list.md`
- `docs/release/v1.13/v1.13.10-validation-checklist.md`
- `docs/release/v1.13/README.md`

## Explicit non-changes

- no source, tests, workflow, script, schema, migration, or version change;
- no v1.13.9 evidence rewrite or deferred implementation;
- no PR, tag, or release operation.

## Validation

Commands run successfully:

```bash
rg -n \
  'v1\.13\.10|v1\.13\.11|v1\.13\.12|v1\.13\.13|superseded|retired|absorbed|partial|deferred|planned final|full v1\.13|swap order|backend parity|thin.wrapper|v2\.0|v2\.x|release blockers|v1\.x follow-ups' \
  docs/release/v1.13
git diff --check
bash scripts/check_smart_quotes.sh
scripts/validate_validation_matrix.sh
bash scripts/check_versioned_row_writers.sh
scripts/audit_ci_enforcement.sh --local-only
```

The Phase 2 checklist records the semantic review, repository policy checks,
and exact documentation diff review.

Phase 2 is complete. Phase 3 — Engine Contract Documentation Truthfulness is
next.

## Phase 3 — Engine Contract Documentation Truthfulness

**Status:** Complete

The exact active Engine method set is recorded in
`v1.13.10-engine-contract-documentation-truthfulness.md`. Repair and Recover
remain candidate-only, while active read-side contracts remain truthful about
their bounded and provisional workflow ownership. Source comments and private
test terminology now align with that boundary without an interface,
contract-field, or runtime behavior change. Targeted engine and CLI tests,
vet, semantic searches, documentation validators, and exact diff review
passed.

Phase 3 is complete. Phase 4 — Release-State Validator Contract is next.

## Phase 4 — Release-State Validator Contract

**Status:** Complete

Phase 4 created the canonical contract
`v1.13.10-release-state-validator-contract.md`. It froze the dependency-free
validator's four lifecycle states, local-versus-manual evidence boundary,
source-of-truth hierarchy, bounded current-versus-historical parsing model,
CKRS001–CKRS019 rule catalogue, deterministic human and JSON interfaces, exit
codes, fixture requirements, CI integration, and exact Phase 5 boundary.

The contract resolves the immutable-tag evidence problem by requiring
complete pre-tag evidence in the tagged tree and a later documentation closure
commit before the next version becomes active. External publication, CI,
Codacy, and remote branch-deletion facts remain manual rather than being
misrepresented as locally verifiable.

Phase 4 acceptance criteria:

- the canonical Phase 4 artifact contains every required contract section;
- the lifecycle model covers development, pre-release, merged-not-tagged, and
  released Git contexts without network access;
- the parsing model is structurally bounded and does not scan historical prose
  as current state;
- stable CKRS rules, deterministic output, exit codes, and all mandatory
  fixture cases are frozen;
- Phase 5 may implement only the validator, its isolated tests, checklist and
  CI integration, CI-audit enforcement, and normal active tracker updates;
- no validator, script, workflow, runtime, historical-document, or release
  publication change lands in Phase 4; and
- the contract verdict is `RELEASE-STATE VALIDATOR CONTRACT FROZEN`.

Phase 4 is complete.

## Phase 5 — Release-State Validator Implementation and CI Integration

**Status:** Complete

Phase 5 implemented `scripts/validate_release_state.py` and its 44-test
isolated temporary-repository suite. All CKRS001–CKRS019 rules, lifecycle
inference, bounded historical parsing, conservative completed-phase artifact
resolution, deterministic human/JSON output, and exit-code boundaries are
enforced without network access or repair behavior.

The quality job now fetches full history, runs fixture tests universally, and
runs blocking real-state validation only for release lifecycle contexts. The
local CI audit protects the checkout depth and both named validator steps; the
pre-release checklist includes the same early quality gate. No external/manual
fact is falsely automated and no upload-artifact migration was performed.

Phase 5 is complete.

## Phase 6 — GitHub Actions Node 24 Artifact Migration

**Status:** Complete

Phase 6 migrated the five existing CI upload steps in `.github/workflows/ci.yml`
from `actions/upload-artifact@v5` to `actions/upload-artifact@v7`. The canonical
artifact is `v1.13.10-github-actions-node24-artifact-migration.md`; it records
the exact inventory, semantic-preservation proof, and explicit non-changes.
The local CI audit now requires exactly five v7 uses and zero v5/v6 uses.

The workflow comparison changed only those five action-version lines. No
artifact input, job/trigger graph, validator behavior, runtime source, or
external release operation changed.

Phase 6 is complete. Phase 7 — v1.x Closure Summary and v2.0 Handoff Freeze is
next.

## Phase 7 — v1.x Closure Summary and v2.0 Handoff Freeze

**Status:** Complete

Phase 7 created the canonical
`v1.13.10-v1x-closure-summary-and-v2.0-handoff-freeze.md` artifact. It freezes
the final v1.x hard non-regression invariants, compatibility baselines,
permitted implementation evolution, capability/limitation matrix, intentional
ownership seams, early-v2.0 decision inputs, local-first v2.x scope, and
beyond-local v3.x boundary.

The artifact does not start v2.0, claim SQLite is the current default, claim
complete engine ownership for mixed workflows, activate Repair or Recover,
claim recursive Store is active, overstate backend parity or portability, or
claim v1.13.10 is released. Current architecture and reconciliation documents
now match the active snapshot-mutation and v2/v3 boundary truth.

Phases 0 through 8 are complete. The canonical local pre-release gate records
the passed cleanup-commit evidence; the evidence-restoration candidate requires
its own clean exact-head gate before one pull request from
`release/v1.13.10` to `main` is authorized.

## Phase 8 — Final Local Release Gate and PR Authorization

**Status:** Complete

Completed the full local Profile A pre-release gate, including both codec
matrices, PostgreSQL integration and adversarial coverage, snapshot and
legacy-compatibility gates, benchmark thresholds, critical coverage reporting,
release-state enforcement, and exact authorized-diff review on the benchmark
cleanup commit `eb38a58c9f4b2213a823c61c81ad98612c3abb63`. The initial
documentation candidate `4300b9bf0fdb532b469230c26b443c3a09fdec66` had a
repeated `none/w1` benchmark threshold failure, so PR authorization was
withheld. Diagnosis established a scratch-database cleanup defect; the bounded
cleanup correction changed benchmark infrastructure and PostgreSQL lifecycle
tests only, not thresholds, baselines, workloads, schema, migration, or normal
runtime behavior.

The fresh cleanup-commit gate passed. This evidence-restoration documentation
commit still requires the complete gate on its own clean exact head before one
release pull request is authorized. No merge, tag, publication, external CI,
CodeQL, Codacy, or branch-cleanup result is claimed at this phase.
