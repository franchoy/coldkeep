# v1.13.16 Phase 6 Snapshot Retention Certification

**Release:** v1.13.16 — Snapshot Retention Integrity, Observability Truth, and Final v1.x Closure
**Phase:** 6 — End-to-end snapshot retention, Remove, GC, and restore certification
**Mode:** BUILD
**Status:** Complete certified
**Finding:** CK-V11316-001

## Entry authority

Phase 5 is externally complete at evidence commit
`4ba598fb57a3594d094ed3297c3fb5549dbc401f`. Exact-head CI run
`33307923059` passed all 36 jobs, including Required Gate. CodeQL run
`33307923075` passed Actions, Go, Python, and Aggregate, and the repository had
zero open CodeQL alerts. CK-V11316-002 is closed certified. The permanent
Phase 3 R1-R13 harness remains tracked unchanged at Git blob
`58370a557b5972b76b40248ac5725d47ce9cdc77`.

```text
PHASE_5: COMPLETE
PHASE_6: COMPLETE
PHASE_6R7: COMPLETE_CERTIFIED
PHASE_6R9: COMPLETE_CERTIFIED
PHASE_6R9R2: COMPLETE_CERTIFIED
PHASE_6R10: CURRENT_AUTHORITY_RECONCILIATION
PHASE_7: NEXT_PLAN_NOT_STARTED
CK-V11316-001: CLOSED_CERTIFIED
CK-V11316-002: CLOSED_CERTIFIED
CK-V11316-009: CLOSED_CERTIFIED
CK-V11316-010: CLOSED_CERTIFIED
CK-V11316-011: CLOSED_CERTIFIED
FINDINGS_CONFIRMED: 11
FINDINGS_CLOSED: 6/11
```

## Certification invariant

For content owned by a committed snapshot, removing the current
`physical_file` mapping must not make any required logical file, chunk, block,
or container GC-reclaimable. After actual supported GC execution, restoring
from that snapshot must reproduce the original bytes exactly.

The canonical proof must enter through production Store with a real source
beneath an explicit capture root, create the snapshot through the Engine using
a relative explicit selector, remove the current mapping through the normal
Remove route, build the normal GC plan, execute GC, and restore through the
snapshot Engine/domain route. Direct SQL may inspect catalog state but may not
rewrite source identity, membership, Remove state, or GC reachability.

The proof will use three deliberately distinct content classes:

- snapshot-only retained content: current mapping removed, snapshot retained;
- truly unreachable control content: current mapping removed, no other root;
- current-live control content: current mapping preserved.

Before and after GC, assertions must establish the exact current mappings,
snapshot membership, absence of pins and second snapshots, plan inclusion or
exclusion, physical reclamation of the unreachable control where the active
storage form permits it, preservation of live and snapshot roots, successful
GC completion, and byte-identical post-GC snapshot restore.

## Backend and storage coverage

The permanent test must use isolated temporary roots and fresh repositories.
SQLite must certify the actual catalog and reachability implementation through
Store, snapshot creation, Remove, and GC planning. Live GC execution and
post-GC restore must be certified against disposable PostgreSQL databases in
both plain and AES-GCM profiles using `COLDKEEP_DB_AUTO_BOOTSTRAP=true`.
Supported loose/direct and packed/container-backed paths must be identified
from existing helpers and covered without inventing a storage mode or changing
codec behavior.

Any inability to satisfy the required retention invariant through an existing
supported route is a failing proof, not authority to change product code.

## Exact implementation boundary

No production `.go` file is authorized. The preferred implementation is the
single dedicated test:

- `internal/maintenance/v11316_snapshot_retention_integration_test.go`

Existing tests may be modified only when materially necessary, and only at:

- `internal/storage/remove_test.go`
- `internal/gc/plan_test.go`
- `internal/maintenance/gc_phase6_test.go`
- `internal/snapshot/snapshot_test.go`
- `internal/storage/restore_phase3_integration_test.go`

No schema, migration, dependency, public CLI, container/block format,
snapshot-path identity, selector accounting, Remove semantics, GC semantics,
or restore semantics change is authorized. If a production defect or another
required path is discovered, Phase 6 stops for separate authorization.

## Required validation and commit boundaries

The implementation must pass focused SQLite and PostgreSQL plain/AES-GCM
coverage, relevant `-race` suites, the unchanged Phase 3 R1-R13 harness, all
modified packages and vet, and a detached clean committed-tree full repository
gate using pinned Go 1.26.7 with `GOTOOLCHAIN=local`. Release-state,
governance, the complete Python suite, local CI enforcement, and diff checks
must also pass.

Exactly three Phase 6 commits are authorized:

1. `docs: authorize v1.13.16 snapshot retention certification`
2. `test: certify snapshot retention through remove and gc`
3. `docs: record v1.13.16 snapshot retention certification`

CK-V11316-001 remains open until the final evidence commit passes exact-head
hosted CI, Required Gate, CodeQL, and zero-open-alert certification. Phase 7 is
not authorized by this document.

## Phase 6R2 correction override

The first Phase 6 execution stopped when the preserved production-path test
reached `SNAPSHOT_RETAINED_DELETE_BLOCKED` through stored-path unlink. The
separate [Phase 6R2 authority](v1.13.16-phase6r2-snapshot-only-retention-correction.md)
supersedes the test-only implementation boundary above without rewriting its
historical authorization. Phase 6 remains the aggregate `Next` lifecycle
pointer required by the release-state validator, but its proof is operationally
withheld until Phase 6R2 corrects and certifies CK-V11316-008. The root count is
eight and the closed count remains `1/8` until exact-head hosted
certification.

## Phase 6R2 certification result

The correction and permanent proof are committed at:

- product: `e8cc9b3650ab9548ce46e7df18e9c6f3483e2574`;
- tests: `854a3187adc66dcc04d7e95f41f8925cdcd608bf`.

The test SHA passed the full local matrix in a clean detached worktree with
pinned Go 1.26.7. SQLite, PostgreSQL plain, PostgreSQL AES-GCM, race, full Go,
vet, release-state development/auto, governance, the complete Python suite,
CI enforcement, and diff checks all passed. The canonical A/B/C lifecycle
proved snapshot-only retention with byte-identical post-GC restore, reclamation
of an unreachable control, and preservation of a current-live control across
legacy and packed placement. Multiple-snapshot deletion and active-pin release
transitions were also certified. Exact-head hosted predicates remain pending;
Phase 7 is not authorized.

## Phase 6R3 compatibility reconciliation

Exact-head evidence commit `6bcac449df7a952ec7b95f0813c4d725497341d9`
failed CI run `33331967865` because
`tests/integration/v18_migration_compatibility_integration_test.go` retained
two pre-Phase-6R2 expectations: stored-path unlink refusal under snapshots and
GC-dead classification from current liveness plus pins without snapshot roots.
Both PostgreSQL profiles reported the same two stale assertions. Product
runtime defect is not established and product changes are forbidden. The
[Phase 6R3 authority](v1.13.16-phase6r3-migration-compatibility-reconciliation.md)
permits exactly that test file to adopt the frozen snapshot-only contract and
then requires full local and exact-head hosted recertification.

Authority `a72602ed988978476bd500c130d428a8c4471cfe` and test
`81df6a497a2ae07ecba177fb645e1b4bf556fa15` completed the authorized local
reconciliation. The test commit changes no product runtime path and only the
single compatibility test file. Exact affected tests and the complete
integration suite passed the hosted-relevant short/race profile on disposable
PostgreSQL plain and AES-GCM. A clean detached test-commit worktree passed full
Go, vet, release-state development/auto, governance, all 215 Python tests, CI
enforcement, and diff checks with pinned Go 1.26.7. Phase 6 is a locally
certified candidate pending exact-head hosted certification; Phase 7 is not
authorized.

## Phase 6R5 final gate reconciliation

Exact-head CI `33334609160` at predecessor
`ce7271a068aea1656d7239c5e282752928ed2e31` failed both smoke profiles on the
stale expectation that snapshot retention blocks stored-path unlink, and both
G15 profiles on generic stderr parsing before the authoritative stdout batch
item. CodeQL `33334609155` passed all four jobs with zero open alerts.

The accepted Phase 6R4 diagnosis established no product runtime defect:
stored-path unlink is allowed while snapshots retain the logical recipe;
by-ID logical deletion remains blocked; corrupt-graph dry-run and live GC are
successful and conservative; and post-GC snapshot restore is byte-identical.
The [Phase 6R5 authority](v1.13.16-phase6r5-final-retention-gate-reconciliation.md)
permits only `scripts/smoke.sh` and the G14–G17 adversarial test to reconcile
those contracts. Phase 6 is withheld pending this final gate reconciliation,
and Phase 7 is not authorized.

Phase 6R5 authority `2870ef1e8504e8ac42640d41c05fff6b8fd4b008` and test
`d7deb4097d681751f7373ac7f11ef6bb8bb18790` completed the two-path
reconciliation with no product runtime changes. Both codec profiles passed the
full smoke lifecycle, focused G15 and G14–G17, complete adversarial,
PostgreSQL correctness, migration compatibility, and race gates. The clean
detached test commit passed full Go, vet, Bash/ShellCheck, release-state,
governance, all 215 Python tests, CI enforcement, and diff checks. Phase 6 is a
candidate pending exact-head hosted certification; Phase 7 remains
unauthorized.

## Phase 6R7 single-connection verify test recovery

Final exact-head CI `33365683050` at
`bd45a0e611d3dacb726490c3beb5fac5cd598f34` failed quality/plain in
`TestVerifySystemDeepPackedSQLiteSingleConnection` with `store packed fixture:
context deadline exceeded`; deep verification was not reached. CI
`33303282081` attempt 1 had already failed at the identical Store boundary
under atomic coverage. Phase 6R6 therefore superseded the one-off-transient
classification and classified the defect as `COMBINATION`, with primary
`TEST_FIXTURE_ISOLATION_DEFECT` and contributing
`TEST_TIMEOUT_POLICY_DEFECT`. No product or CI-infrastructure defect is
established.

The [Phase 6R7 authority](v1.13.16-phase6r7-single-connection-verify-test-recovery.md)
permits only `internal/verify/verify_system_single_connection_test.go`. It
requires a default-timeout fixture-builder subprocess followed by successful
and deliberately blocked one-connection verification subprocesses initialized
with the existing 250 ms DB operation timeout and bounded by the existing
10-second outer process deadline. Authority
`ce815419c0e72d341da6892eec01662de734292f` and test
`7399b38b22c2f19fe0e12e1c0a8ec06cadc7ebce` completed that isolated
one-file test recovery. Targeted 100/50/50 repetition, exact local lint 2.9.0,
full Go/race/vet, 215 Python tests, and the complete isolated PostgreSQL 16.15
plain/AES-GCM preservation matrix passed; the unrelated port-5432 service was
unchanged. CK-V11316-009 is implemented pending exact-head hosted
certification, findings remain 9 confirmed and 1/9 closed, CI `33365683050`
was not retried, Phase 6 remains withheld, and Phase 7 remains unauthorized.

Evidence commit `96f19cbd333b0120ab1e18a65c6309eb6d9a2210` captured those
branch-worktree and isolated PostgreSQL results before detached certification
completed. Its earlier detached-complete implication is superseded. The later
detached attempt passed exact lint, targeted ordinary/race/atomic coverage, and
full `internal/verify` ordinary/race, then stopped at
`TestEngineDependencyDirection`. Phase 6R7R3 proved nested Go VCS stamping in
the linked worktree as a detached validation environment defect and authorized
`GOFLAGS=-buildvcs=false` only for source-neutral corrected detached execution.
Corrected detached execution passed, including all 12 retained PostgreSQL
selectors, and head `f58be125eee09c6c1cfb76b911ada18e7f232a69` was pushed exactly
once. Hosted CI `33407340364` passed 35 of 36 jobs and failed only critical
coverage; CodeQL `33407340517` passed all four jobs with zero open alerts.

## Phase 6R9 authority checkpoint

CK-V11316-010 records a primary gate-composition defect, secondary
measurement-stability defect, with coverage instrumentation as a contributing
condition. It is not a product-runtime regression or hosted CI infrastructure
defect. Phase 6R9 may change only
`internal/storage/phase1_compat_matrix_test.go` and current-authority docs to
isolate the two numeric throughput floors after real execution and correctness
checks. Runtime, thresholds, scripts, workflows, dependencies, and database
surfaces remain frozen. Authority `6be80e391...` and test `9439c433...`
completed the one-file implementation. The complete branch-worktree local
matrix passed: ordinary/race floors were enforced, atomic coverage produced
only the two expected post-execution skips, all three critical reports
completed, exact lint found zero issues, and 215 Python tests passed.
CK-V11316-010 is implemented pending exact-head hosted certification;
committed-tree and detached certification remain pending. Findings are 10
confirmed and 1/10 closed, Phase 6 remains withheld, and Phase 7 remains
unauthorized.

## Phase 6R9R2 authority checkpoint

Phase 6R9 branch-worktree and clean committed-tree certification passed, but
detached certification stopped only in the unchanged benchmark-evidence
signal-cleanup subtest. Phase 6R9R1 classified CK-V11316-011 as a combination:
primary `TEST_FIFO_SIGNAL_SYNCHRONIZATION_DEFECT`, secondary
`TEST_CHILD_PROCESS_LIFECYCLE_BOUNDING_DEFECT`, with the exact original
blocking parent call not recovered. The retained FIFO/staging residue and
bounded 11-of-240 versus 140-of-140 differential diagnostics establish the
test-harness mechanism. No production-script, product-runtime,
detached-worktree, or CI-infrastructure defect is established.

[Phase 6R9R2](v1.13.16-phase6r9r2-benchmark-signal-test-recovery.md)
authorizes only `scripts/phase14_release_gate_test.go` to add a five-second
owned FIFO/child lifecycle, exact exit-130 proof, and a bounded negative
missing-readiness regression. Production scripts, workflows, runtime,
dependencies, database surfaces, and the Phase 6R9 commits remain frozen.
Authority `4105e857...` and test `8784873e...` completed the one-file harness.
The prohibited unsharded command was not rerun; its 123/123 completed
iterations remain supporting evidence for the separately sharded ordinary
200/200, race 100/100, atomic 100/100, and negative 50/50 branch results.
Parent ordinary 20/20 and race 10/10, complete scripts ordinary/race,
preserved Phase 6R9 throughput semantics, critical coverage 3/3, storage,
quality, full Go/race/vet, exact lint 2.9.0, policy, and 215 Python tests passed
with no signal residue. CK-V11316-011 is implemented pending exact-head hosted
certification; findings remain 11 confirmed and 1/11 closed. Clean committed-
tree and detached certification remain pending. Phase 6 remains withheld,
Phase 7 remains unauthorized, and v1.x technical correctness closure remains
withheld.

## Phase 6R10 current certification result

The [Phase 6R10 reconciliation](v1.13.16-phase6r10-hosted-certification-reconciliation.md)
supersedes the historical pending-state statements above without rewriting
their chronology. Certified executable head
`fe308d8ae51fef3f003953fd42368e2712d9f415` passed CI `33560172954` attempt
1 with all 36 jobs successful, CodeQL `33560172936` attempt 1 with all four
jobs successful, and zero open code-scanning alerts. Critical-coverage artifact
`9821142625`, `v1.10.7-critical-coverage-report`, was successfully finalized
and was not expired when re-read.

The accepted branch, clean committed-tree, detached, and isolated PostgreSQL
12/12 evidence remains authoritative. Hosted CI certified the whole unchanged
candidate; the hosted workflow did not directly select the benchmark signal
subtest. CK-V11316-001 and CK-V11316-008 through CK-V11316-011 are closed
certified, CK-V11316-002 remains closed certified, and CK-V11316-003 through
CK-V11316-007 remain open. Findings are 11 confirmed and 6/11 closed.

Phase 6 is complete. Phase 7 is next in PLAN mode but is not started, and its
Build is not authorized. The documentation-only Phase 6R10 authority head must
pass fresh exact-head hosted certification before owner review may authorize
Phase 7 Plan execution. V1.x technical correctness closure remains withheld.
