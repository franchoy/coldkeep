# Coldkeep v1.13.7 Scope

**Release:** `v1.13.7 — SQLite-First Repository Portability Baseline`
**Status:** Released
**Branch:** `release/v1.13.7`

## Goal

Establish the SQLite-first local repository portability baseline while
preserving PostgreSQL compatibility.

The release will inventory current repository initialization and
backend-selection behavior, review SQLite catalog placement, define repository
portability expectations, classify existing evidence, and add only narrowly
justified portability guards.

## Core direction

SQLite-first does not mean SQLite-only.

SQLite is the intended local repository direction.

PostgreSQL remains supported and mandatory in compatibility validation and the
final release gate.

## In scope

- repository initialization inventory
- backend-selection inventory
- SQLite catalog-location review
- repository-root and portability assumptions
- WAL/SHM/checkpoint considerations
- repository move/reopen contract
- portability evidence classification
- SQLite/PostgreSQL compatibility review
- minimal portability smoke-test design
- narrow test-only guards if justified
- backend-direction documentation consistency
- v1.13.8 handoff preparation

## Out of scope

- daemon
- API
- UI
- scheduling
- NAS
- cloud/object storage
- distributed storage
- network/server mode
- multi-user mode
- broad schema rewrite
- broad migration rewrite
- storage format change
- repository format change
- PostgreSQL compatibility removal
- SQLite-only engine/catalog assumptions
- public CLI behavior changes unless explicitly required
- JSON shape changes
- error-envelope changes
- exit-code changes
- broad refactors

## Compatibility requirements

- existing repositories remain readable
- SQLite behavior remains supported
- PostgreSQL behavior remains supported
- no backend default change without explicit scope and validation
- no storage or repository format change
- public CLI and output contracts remain stable unless explicitly scoped

## Release process

All phases remain on `release/v1.13.7`.

One PR is opened only after:

- every phase is complete
- the full `PRE_RELEASE_CHECKLIST.md Profile A` gate passes
- PostgreSQL-backed validation passes
- release evidence is recorded

## Scope outcome

The planned contract, evidence, compatibility, guard, and documentation work
is complete.

During guard implementation, a real schema-reopen migration defect was
discovered and remediated narrowly for SQLite and PostgreSQL.

The release remains within scope:

- no backend-default transition
- no normal SQLite local runtime
- no CLI portability feature
- no storage or repository format change
- PostgreSQL compatibility preserved

The remaining requirement is the full Profile A release gate.

## Release-gate outcome

The final Profile A pre-PR gate passed.

The release implementation and documentation are complete.

One pull request may now be opened from `release/v1.13.7` to `main`.

The release remains unmerged, untagged, and unreleased.

## Release closure

`v1.13.7` completed its controlled release process:

- Profile A passed locally.
- Pull request #99 passed all required checks.
- The pull request was merged into `main`.
- Post-merge `main` CI passed.
- The release branch was deleted.
- Annotated tag `v1.13.7` was created from the released `main` state.
- The GitHub release was published.

### Provenance

- Final pull-request head: `5f5506179a6395304c90f4b6fd938b5d8d259305`
- Merge commit: `3813dc32f85c72641dd516dfa41c439d2b4c916c`
- Released main commit: `3813dc32f85c72641dd516dfa41c439d2b4c916c`
- Tag target: `3813dc32f85c72641dd516dfa41c439d2b4c916c`
- Pull request: <https://github.com/franchoy/coldkeep/pull/99>
- GitHub release: <https://github.com/franchoy/coldkeep/releases/tag/v1.13.7>
- Released at: `2026-06-20T06:28:24Z`

The post-release review identified inherited corrective work for
`v1.13.8` Phase 0:

- release/version/documentation reconciliation;
- preservation of valid zero-reference logical-file state across migration;
- release-train ownership and numbering reconciliation.
