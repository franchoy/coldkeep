# Coldkeep v1.13.14 Scope

**Release:** `v1.13.14 — Final v1.x Correctness Remediation and Closure Certification`
**Status:** Active
**Branch:** `release/v1.13.14`

## Objective

Correctly revalidate v1.x after the confirmed post-release inventory. Phase 0
reconciled publication truth and activated release identity. Phase 1 confirmed
and froze the complete finding, rejection, and owner-phase scope without
changing runtime behavior. Phase 2 froze implementation designs, regression
contracts, compatibility impacts, and source/test allowlists for every numbered
row without implementing or closing one. Phase 3 added only deterministic,
nil-by-default remediation test seams and closed no row. Phase 4 implemented
execution-derived verification accounting and closed only `CK-V1-AUD-001`.
Phase 5 completed caller-context and cancellation ownership and closed only
`CK-V1-AUD-002`. Phase 6 propagated single-file Store finalization failures,
preserved writer-owned quarantine, and closed only `CK-V1-AUD-007` plus its
historical root-equivalent `CK-110-1728`.
Phase 7 made rollback cleanup and quarantine fail closed, retained poisoned
container identity through durable quarantine failure, and closed only
`CK-V1-AUD-008`. Phase 8 preserved snapshot restore overwrite through the
Engine adapter, unified Show/Diff/Restore selector normalization, and closed
only `CK-V1-AUD-006` and `CK-V1-AUD-009`. Phase 9 made SnapshotDiff Limit and
its raw/matched/summary populations truthful, populated metadata FileCount from
persisted snapshot membership without N+1 queries, and closed only
`CK-V1-AUD-010` and `CK-V1-AUD-012`. Phase 10 froze the complete scheduler,
transaction, active-accounting, partial-result, physical-byte,
filesystem-failure, and overflow design for `CK-V1-AUD-003` and
`CK-V1-AUD-004` without implementing or closing either finding. Phase 11
implemented that design: Engine Workers now bound maintenance-owned execution,
physical `Stat` supplies accurate dry/live bytes, active removals are included,
filesystem and overflow errors are surfaced, and partial results remain
truthful. Phase 11 closed only `CK-V1-AUD-003` and `CK-V1-AUD-004`.
Phase 12 made the documented ContainerDir default consistent across the frozen
Engine routes, corrected only the stale Store codec contract wording, and made
Store and Stats consume one Store-compatible packed-block target resolver while
preserving Store warnings and silent Stats behavior. Phase 12 closed only
`CK-V1-AUD-011`, `CK-V1-AUD-013`, and `CK-V1-AUD-014`.
Phase 13 corrected the stale positional search example and made the existing
five-code process/batch/JSON automation contract and init compression options
discoverable in root README and top-level help. Runtime parsing,
classification, JSON, search, init, and compression behavior remain
unchanged. Phase 13 closed only `CK-V1-AUD-015`, `CK-V1-AUD-016`, and
`CK-V1-AUD-017`; `CK-V1-AUD-005` remains partial. Phase 14 completed all four
release-process gates by isolating CLI test storage, constraining evidence
names to tracked Git source, implementing the exact-SHA benchmark-evidence
lifecycle, and adding branch-relative release linearity while preserving the
Phase 0 remote ruleset. Phase 15 independently executed every current frozen
primary regression, reconciled all 21 numbered rows, and preserved AUD-005 as
lifecycle-deferred to Phases 18/26 without changing production or test code.
Phase 16 then proved all 10 assigned rows across the exact applicable backend,
codec, compression, and storage-layout matrix. All 73 derived cells passed;
production and test code remained unchanged. Phase 17 then proved 17/17 deep
rows under caller cancellation/deadlines, Go race detection, deterministic
faults, G1–G17 adversarial execution, and native Linux/macOS/Windows coverage.
All 71 derived runtime cells passed and no production or test code changed.
Phase 18 then reconciled the active candidate truth across the authorized
current documentation surfaces. It changed no product, test, script, workflow,
schema, storage, dependency, governance, or benchmark-threshold contract and
did not close `CK-V1-AUD-005`.
Phase 19 then approved the complete candidate-side corrective evidence stack
to enter the final release lifecycle. It did not certify release readiness,
publication, or final published closure, and changed no product or test code.
Phase 20 then recorded the final corrective handoff and froze the 1,397-entry
candidate core at SHA-256
`984221fc5dc0e9d2d76d97cfb2c334bd7a52733682d818cb839d7c5caacac4da`
without changing any frozen core entry.

## Starting baseline

`f3b75dc0fbee44e4fe91eb2df1df724f9426640e`, exact `origin/main` and the
peeled target of `v1.13.13` at activation.

## Current phase

Phases 0–20 are Complete. Phase 21 is the sole Next phase and is
`NOT_STARTED_READY_FOR_SEPARATE_AUTHORIZATION`; it owns the independent full
local exact-candidate release gate. Phases 22–26 are Not started. The Phase 20
handoff and immutable candidate-core freeze become terminally active only after
exact-head hosted acceptance.
AUD-005 remains
lifecycle-partial: Phase 18 candidate truth is complete and Phase 26 final
publication truth is pending.

## Boundaries

v1.x normative scope remains complete. Correctness certification is under
corrective revalidation; exactly 17 findings and 4 process gates are confirmed
and frozen, with `16/17` findings closed and `4/4` gates complete. The eight
Phase 1 rejection outcomes remain excluded. v2 implementation is not
authorized.
