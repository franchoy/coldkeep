# Coldkeep v1.13.5 Phase List

**Release:** `v1.13.5 — Release Record Closure and Engine-Level Invariant Ownership`
**Branch:** `release/v1.13.5`
**Status:** Open

## Phase 0 — Post-release record closure and v1.13.5 scope reconciliation

**Status:** Complete

Close stale `v1.13.4` post-release documentation, record final `v1.13.4`
narrow production hardening, and create the `v1.13.5` scope, phase list, and
validation checklist.

Created:

- `v1.13.5-scope.md`
- `v1.13.5-phase-list.md`
- `v1.13.5-validation-checklist.md`

Updated stale `v1.13.4` release records so they reflect the completed release
rather than a pre-PR or pending Phase 10 state.

Recorded that the final `v1.13.4` release included narrow production hardening
for:

- database environment integer parsing
- `verify file` fileID bounds validation

Reconciled the `v1.13.5` scope as:

`v1.13.5 — Release Record Closure and Engine-Level Invariant Ownership`

No production code changed.
No tests changed.
No scripts or CI changed.
No runtime behavior changed.

Phase 0 acceptance criteria:

- `release/v1.13.5` created from `main`
- `v1.13.5-scope.md` created
- `v1.13.5-phase-list.md` created
- `v1.13.5-validation-checklist.md` created
- stale `v1.13.4` pre-release wording fixed
- final `v1.13.4` narrow production hardening recorded
- `v1.13.5` title reconciled
- `v1.13.x` release train updated if present
- docs validation passed
- branch pushed
- no production code changed
- no tests changed
- no runtime behavior changed

## Phase 1 — Engine-level validation and invariant ownership inventory

**Status:** Complete

Inventory current validation and invariant ownership for active engine-facing
operations.

Created `v1.13.5-engine-validation-inventory.md`.

Recorded current ownership across `Verify`, `Remove`, `Restore`, `Stats`,
`Inspect`, `SnapshotShow`, `SnapshotDiff`, and GC / maintenance invariant
propagation.

Documented:

- which request validation remains CLI-only
- which request validation already exists in engine
- which invariants are enforced below engine in domain packages
- how invariant metadata is created and propagated
- which unsupported/deferred seams remain explicit
- which tests currently protect each boundary

Phase 1 remained descriptive only. No ownership migration was performed.
No production code changed.
No tests changed.
No scripts or CI changed.
No runtime behavior changed.

## Phase 2 — Verify engine validation and invariant boundary review

**Status:** Complete

Review `Engine.Verify`, `VerifyRequest`, `VerifyResult`, request validation,
CLI-to-engine routing, and invariant/error propagation.

Created `v1.13.5-verify-invariant-boundary.md`.

The review documents the current `Verify` ownership boundary across:

- CLI validation
- engine request validation
- verify domain package validation
- `v1.13.4` `verify file` fileID bounds hardening
- invariant/error propagation
- public error-envelope ownership
- existing test protection
- candidate guard gaps for Phase 8

No ownership migration was performed.
No code changed.
No tests changed.
No public behavior changed.

## Phase 3 — Remove engine validation and snapshot-retention invariant review

**Status:** Complete

Review active remove engine paths, destructive-operation invariants,
snapshot-retained deletion blockers, and unsupported remove modes.

Created `v1.13.5-remove-invariant-boundary.md`.

The review documents the current `Remove` ownership boundary across:

- CLI validation
- engine request validation
- by-ID dry-run and live execution ownership
- storage-layer destructive-operation invariants
- snapshot-retention blocking ownership
- invariant metadata propagation
- unsupported stored-path engine seams
- existing test protection
- candidate guard gaps for Phase 8

No ownership migration was performed.
No code changed.
No tests changed.
No scripts or CI changed.
No public behavior changed.

## Phase 4 — Restore engine validation and restore-safety boundary review

**Status:** Complete

Review active restore engine paths, restore request validation, output-dir
validation, dry-run/live result ownership, and unsupported restore modes.

Created `v1.13.5-restore-invariant-boundary.md`.

The review documents the current `Restore` ownership boundary across:

- CLI validation
- engine request validation
- by-ID dry-run and live execution ownership
- storage-layer restore-safety invariants
- metadata application and strictness behavior
- invariant/error propagation
- unsupported stored-path engine seams
- existing test protection
- candidate guard gaps for Phase 8

No ownership migration was performed.
No code changed.
No tests changed.
No scripts or CI changed.
No public behavior changed.

## Phase 5 — GC and maintenance invariant propagation review

**Status:** Complete

Review invariant error propagation for GC, maintenance, and repair-adjacent
paths.

Created `v1.13.5-maintenance-invariant-propagation.md`.

The review documents the current GC and maintenance invariant boundary across:

- invariant metadata model
- GC dry-run/live safety paths
- GC integrity and catalog-consistency refusals
- repair/refcount orphan-row refusals
- recovery/quarantine adjacent behavior
- CLI public error classification
- JSON/human error-envelope ownership
- existing test protection
- candidate guard gaps for Phase 8

No ownership migration was performed.
No code changed.
No tests changed.
No scripts or CI changed.
No public behavior changed.

## Phase 6 — Read-side guard follow-up from v1.13.4

**Status:** Complete

Created `v1.13.5-read-side-guard-follow-up.md`.

The review documents the current read-side guard baseline across:

- `stats`
- `inspect`
- `snapshot show`
- `snapshot diff`
- success JSON envelope ownership
- JSON error-envelope ownership
- public inspect target boundaries
- taxonomy/helper invisibility
- count-field stability
- existing test protection
- candidate guard gaps for Phase 8

No ownership migration was performed.
No code changed.
No tests changed.
No public behavior changed.

## Phase 7 — Backend-neutral invariant coverage review

**Status:** Complete

Created `v1.13.5-backend-neutral-invariant-review.md`.

The review documents the backend-neutrality posture across:

- `Verify`
- `Remove`
- `Restore`
- `Stats`
- `Inspect`
- `SnapshotShow`
- `SnapshotDiff`
- GC / maintenance invariants
- SQLite/PostgreSQL compatibility
- existing backend coverage
- Phase 8 candidate guard backend classification

No backend migration was performed.
No ownership migration was performed.
No code changed.
No tests changed.
No public behavior changed.

## Phase 8 — Targeted guard implementation pass

**Status:** Complete

Created `v1.13.5-targeted-guard-implementation.md`.

Phase 8 reviewed the backend-neutral candidate guard gaps from Phases 2–7.

No new tests were added because all remaining candidates were already covered,
duplicate, over-brittle, backend-sensitive without a concrete gap, or not
worth freezing in this release.

No production code changed.
No tests changed.
No public behavior changed.

## Phase 9 — Final v1.13.5 decision summary and v1.13.6 handoff

**Status:** Complete

Created:

- `v1.13.5-engine-invariant-ownership-summary.md`
- `v1.13.5-v1.13.6-handoff.md`
- `v1.13.5-release-gate.md`

Phase 9 records the final `v1.13.5` decisions across:

- `Verify`
- `Remove`
- `Restore`
- GC / maintenance invariants
- read-side guards
- backend neutrality
- Phase 8 guard outcomes

Recommended next release:

`v1.13.6 — Catalog Contract Expansion and Backend Parity Review`

The `v1.13.5` release gate document remains pending Phase 10.

No code changed.
No tests changed.
No public behavior changed.

## Phase 10 — PRE_RELEASE_CHECKLIST.md Profile A and PR opening

**Status:** Blocked

The final release gate found a blocker.

See:

- `v1.13.5-release-gate.md`

No pull request was opened.
