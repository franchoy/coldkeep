# v1.13.16 Phase 5 Selector Atomicity Implementation Authority

**Release:** v1.13.16 — Snapshot Retention Integrity, Observability Truth, and Final v1.x Closure
**Phase:** 5 — Next
**Mode:** BUILD

## Entry authority

Phase 4 is externally complete at exact head
`6c11bc6245b301873c598fe784a4df3cbc5ba809`. CI run `33303282081`,
attempt 2, passed all 36 jobs, including critical coverage and the Required
Gate. Reused CodeQL run `33303282125` passed Actions, Go, Python, and Aggregate
with zero open alerts.

The Phase 3 harness at
`internal/engine/snapshot_create_path_contract_test.go` remains immutable Phase
5 input. Its deterministic untracked Git diff is 17,788 bytes with SHA-256
`6b006f9024e8ea45b3551d02d8f9d2cdeef961238924b073473c8b6a947c444c`.
Phase 5 must make its existing R4 and R5 assertions green without editing the
harness, then commit the unchanged file as a permanent regression suite.

## Required behavior

CK-V11316-002 owns fail-closed accounting for explicit snapshot-create
selectors. The implementation must:

- canonicalize and deduplicate selectors while preserving exact versus prefix
  kind;
- match every distinct selector independently against eligible COMPLETED
  sources in the existing physical `SelectionBase` namespace;
- require every selector to match at least one source;
- allow duplicate and overlapping selectors without duplicate snapshot
  members;
- return typed `not_found` with deterministic unmatched-selector reporting;
- roll back the snapshot, membership, and operation-created `snapshot_path`
  rows on any miss; and
- preserve preexisting dictionary rows and all Phase 4 capture-root,
  representability, collision, read, query, restore, and legacy contracts.

Full snapshots have no explicit selectors and remain outside this accounting
rule. Schema version 16, public APIs, storage formats, and dependencies remain
unchanged.

## Exact implementation boundary

Production paths:

- `internal/snapshot/snapshot.go`
- `internal/engine/snapshot_create.go`
- optional focused `internal/snapshot/snapshot_create_selector_accounting.go`

Existing test paths:

- `internal/snapshot/snapshot_test.go`
- `internal/engine/snapshot_create_engine_test.go`
- `internal/engine/snapshot_selector_backend_contract_test.go`
- `tests/integration/integration_test.go`

Permanent unchanged regression harness:

- `internal/engine/snapshot_create_path_contract_test.go`

No schema, migration, workflow, dependency, CLI, restore, query, GC, or Phase 6
change is authorized.

## Transaction and backend proof

The snapshot transaction must own source enumeration, selector accounting,
member derivation, collision validation, path resolution, membership writes,
and commit. SQLite and disposable PostgreSQL plain/AES-GCM tests must prove
that missing and mixed selectors return `not_found` with no snapshot,
snapshot-file, or operation-only path rows; duplicate and overlapping
selectors must succeed without duplicate membership; preexisting shared path
rows must survive failed creates.

The complete R1–R13 Phase 3 harness must be green. Phase 5 may not weaken or
reopen Phase 4 behavior to achieve that result.

## Commit and lifecycle boundary

Exactly three Phase 5 commits are authorized: authority documentation, one
product/test correction, and final evidence documentation. CK-V11316-002
remains open until the final evidence head passes exact-head CI, Required Gate,
CodeQL, and zero-open-alert certification. CK-V11316-001 remains open pending
Phase 6 retention proof. Phase 6 is not authorized by this document.

```text
PHASE_4: COMPLETE
PHASE_5: NEXT
PHASE_6: NOT_STARTED
CK-V11316-001: OPEN_IMPLEMENTED_HOSTED_RECOVERY_CERTIFIED_PENDING_PHASE6_RETENTION_PROOF
CK-V11316-002: OPEN_RED_REPRODUCED
FINDINGS_CLOSED: 0/7
SCHEMA_VERSION: 16
```
