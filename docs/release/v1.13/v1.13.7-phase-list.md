# Coldkeep v1.13.7 Phase List

**Release:** `v1.13.7 — SQLite-First Repository Portability Baseline`
**Branch:** `release/v1.13.7`
**Status:** In progress

## Phase 0 — Branch creation, post-v1.13.6 release-record cleanup, and v1.13.7 scope baseline

**Status:** Complete

Goal: establish the release branch and release frame for v1.13.7.

Outputs:

- branch created from released `main`
- v1.13.6 release record repaired
- v1.13.7 scope created
- v1.13.7 phase list created
- v1.13.7 validation checklist created
- v1.13.7 title reconciled
- v1.13.8 restore/remove candidate preserved

## Phase 1 — Current repository initialization and backend-default inventory

**Status:** Complete

Goal: inventory current repository initialization and backend-default behavior.

Created `v1.13.7-repository-initialization-inventory.md`.

The inventory records:

- repository initialization entry points
- end-to-end `init` routing
- backend-selection inputs
- current backend-default behavior
- SQLite catalog-path derivation
- PostgreSQL connection derivation
- schema bootstrap and migration paths
- existing repository-open behavior
- repository filesystem-layout assumptions
- existing test evidence
- documentation alignment questions

No backend default changed.
No production code changed.
No tests changed.
No runtime behavior changed.

## Phase 2 — SQLite local catalog placement contract review

**Status:** Pending

Goal: review how SQLite catalog placement behaves in local repositories today.

Outputs:

- SQLite catalog placement contract review
- placement assumptions and edge cases
- portability-sensitive observations

## Phase 3 — Repository portability contract definition

**Status:** Pending

Goal: define the repository move/reopen portability contract.

Outputs:

- portability contract document
- repository-root assumptions
- WAL/SHM/checkpoint handling expectations

## Phase 4 — Current portability evidence inventory

**Status:** Pending

Goal: classify existing evidence supporting repository portability claims.

Outputs:

- portability evidence inventory
- current validation coverage map
- documented evidence gaps

## Phase 5 — SQLite/PostgreSQL compatibility guard review

**Status:** Pending

Goal: review whether current compatibility evidence justifies narrow new guards.

Outputs:

- compatibility guard review
- recommendation on guard additions
- preserved PostgreSQL validation expectations

## Phase 6 — Repository portability smoke test design

**Status:** Pending

Goal: define a minimal smoke-test approach for portability validation.

Outputs:

- smoke-test design
- scope boundaries for the test
- expected validation evidence

## Phase 7 — Minimal repository portability guard implementation

**Status:** Pending

Goal: add only narrowly justified portability-focused guard coverage.

Outputs:

- minimal portability guard changes, if justified
- validation evidence for any added test-only coverage
- explicit no-change record if no guard is justified

## Phase 8 — Backend-default and documentation consistency review

**Status:** Pending

Goal: reconcile backend-direction wording with validated repository behavior.

Outputs:

- backend-default wording review
- documentation consistency updates
- confirmed SQLite-first and PostgreSQL-compatible posture

## Phase 9 — Final v1.13.7 portability baseline summary and v1.13.8 handoff

**Status:** Pending

Goal: summarize the v1.13.7 portability baseline and prepare the next handoff.

Outputs:

- final portability baseline summary
- v1.13.8 handoff
- draft release-gate frame

## Phase 10 — PRE_RELEASE_CHECKLIST.md Profile A and PR opening

**Status:** Pending

Goal: complete the full release gate and open the single PR for the release.

Outputs:

- full `PRE_RELEASE_CHECKLIST.md Profile A` evidence
- PostgreSQL-backed validation evidence
- PR opening after a fully green gate
