# Coldkeep v1.13.11 Scope

**Release:** `v1.13.11 — Safety and Backend Compatibility Gate Closure`
**Status:** Active
**Branch:** `release/v1.13.11`
**Starting baseline:** `423c57815580c39bee4f79ecd81570e9cfa9d273`

## Current phase

Phases 0–9 are Complete. Phase 10 is Next; its implementation is complete
locally and exact-head CI is pending. Five bounded contracts now cover shared transaction
outcomes, real PostgreSQL row-lock behavior, SQLite clause omission, blocked
query cancellation, cleanup/reuse, and production container NOWAIT/SKIP LOCKED
integration. Focused SQLite, repeat, and race profiles pass; no local
PostgreSQL test database is configured, so BKC-003 and BKC-015 remain
provisional. Phase 9 exact-head CI run
`30114444798` at `848e579b` proved the five shared Engine mutation/GC
contracts in plain correctness job `89551564893`, with quality and aggregate
required-gate job `89555865026` also green. No production divergence was found.
Phase 8 exact-head required CI run `30109561344` proved the scoped
list/show/stats/diff selector and tree
presentation contracts across SQLite and PostgreSQL, including both required
PostgreSQL selector events and a green aggregate gate. Phase 7 exact-head required CI
proved scoped Stats, Inspect, Verify, context/error, and non-mutation behavior
across SQLite and PostgreSQL after correcting SQLite deep verification's
single-connection row-lifecycle starvation. Phase 6 exact-head required CI
proved the scoped implemented catalog contracts across SQLite and PostgreSQL,
including deterministic snapshot-list ordering; it did not activate deferred
catalog APIs or establish engine/read-side parity. Phase 5 proved selected
schema/bootstrap/migration contracts with required PostgreSQL execution while
keeping broad backend schema equivalence intentionally conservative. Shared
packed-block partial rebuild now fails closed; automatic whole-block recovery
remains out of scope. Phase 5 does not establish full backend parity or
locking. Phase 7 does not establish selector/query parity; Phase 8 closed that
scoped work. Phase 9 closes only its active, uncontended mutation and GC dry-run
contract boundaries; Phase 10 owns transaction and row-lock semantics and does
not establish repository/process coordination or broad isolation equivalence.

## Included

- Backend compatibility claim classification and executable proof.
- SQLite/PostgreSQL shared fixtures and contract tests.
- CI activation of currently skipped PostgreSQL package tests.
- Selected engine read and mutation parity, including explicit
  unsupported-backend behavior.
- Snapshot selector preservation and determinism.
- Conservative same-host repository process locking, startup-recovery
  coordination, live-GC barrier behavior, and multi-process contention proof.
- Container range and header/catalog consistency, bounded decompression, exact
  JSON integer preservation, and targeted SQL affected-row/iterator handling.
- CI and release-evidence updates.

## Excluded

- Deferred catalog-method implementation; complete CLI thin-wrapper migration;
  broad engine/catalog architecture migration; recursive `Engine.Store`; and
  active Repair or Recover Engine methods.
- SQLite-default initialization or embedded portable SQLite catalog product
  behavior.
- Daemon, queue, API, UI, scheduling, NAS, cloud, distributed, or multi-user
  work; cross-machine locking; repository redesign; and storage-format redesign.

## Invariants

PostgreSQL compatibility remains required. The default runtime backend remains
unchanged. SQLite-first is a future local-product direction, not a v1.13.11
implementation. v2.0 work has not started.
