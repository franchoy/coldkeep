# Phase 12R1R38 — CK-015 hosted named-proof wiring recovery

## Authority and purpose

Phase 12R1R37 stopped before push because the locally certified CK-015
correction had no committed hosted route that executed and proved its named
SQLite and PostgreSQL obligations. That stop is accepted. It is a proof-wiring
gap, not a failure of the production correction or its existing tests.

Phase 12R1R38 authorizes exactly two append-only local commits from
`4e665a59af147a4bd096447b8f8c4de9fad331db`:

1. `docs: authorize v1.13.16 CK-015 hosted proof enforcement`; and
2. `ci: enforce named CK-015 lookup and PostgreSQL proofs`.

The first commit changes exactly the nine authority paths frozen below. The
second changes exactly the six implementation/enforcement paths frozen below.
No product source, storage test, database, schema, module, dependency, public
API, storage format, or benchmark producer enters scope.

CK-V11316-015 stays open. CK-V11316-014 stays closed certified,
CK-V11316-007 stays open, and the aggregate remains 15 confirmed and 13/15
closed. No push, hosted run, finding closure, Phase 12 completion, Phase 13,
merge, tag, or release is authorized.

## Frozen named-proof profiles

Both profiles bind events to exact package
`github.com/franchoy/coldkeep/internal/storage`. The existing checker semantics
remain unchanged: every required identity needs exactly one RUN followed by
exactly one PASS, no required SKIP or FAIL, and a successful terminal package
PASS.

`ck015-initial-lookup-sqlite` requires:

- `TestCKV11316015InitialLookupOperationalErrorStopsStoreBeforeFallbackSQLite`
- `TestCKV11316015InitialLookupPartialScanErrorStopsStoreBeforeFallbackSQLite`
- `TestCKV11316015InitialLookupErrNoRowsPreservesNewObjectStoreSQLite`
- `TestCKV11316015InitialLookupSupportedStatusRoutingSQLite`
- `TestCKV11316015InitialLookupSupportedStatusRoutingSQLite/completed`
- `TestCKV11316015InitialLookupSupportedStatusRoutingSQLite/aborted`
- `TestCKV11316015InitialLookupSupportedStatusRoutingSQLite/processing`

`ck015-initial-lookup-postgres` requires:

- `TestCKV11316015PostgresInitialLookupOperationalErrorStopsStoreBeforeFallback`
- `TestCKV11316015PostgresOpenLocalStorageRepairAndRecovery`
- `TestCKV11316015PostgresRepairPublisherLocksChunkBeforeAuthorityMutation`
- `TestCKV11316015PostgresRepairCompetitorWinsChunkLockBeforePublication`
- `TestCKV11316015PostgresSharedChunkHealingBetweenValidationAndPlanReclassifies`

## Hosted execution contract

Add two separate steps only to the existing `correctness-matrix` plain leg,
after the CK-014 internal proof and before the existing PostgreSQL internal-
package contracts. Each invokes `go test` with
`-race -count=1 -p=1 -parallel=1 -json ./internal/storage` and the complete
anchored selector for its profile.

The SQLite step explicitly uses `COLDKEEP_CODEC=plain`. The PostgreSQL step
also requires `COLDKEEP_TEST_DB=1`, maintenance database `postgres`, automatic
bootstrap, and the existing loopback `coldkeep` service settings. It preserves
the existing PostgreSQL image exactly:

```text
postgres:16.15-bookworm@sha256:bb3e1a57e5407e0a5280b4211980a5e537f4abd234a87014ac979849a78dd825
```

`COLDKEEP_SCHEMA_PATH` and inherited compression/long-run settings remain
unset for these wrappers. The fixtures are explicitly plain and make no AES
coverage claim. No job, Required Gate relationship, service, Go policy, or
check topology changes.

Each invocation refuses stale target files and retains six identity-bound
records below `${RUNNER_TEMP}/integration-diag`:

```text
<prefix>.json
<prefix>.go.stderr
<prefix>.checker.stdout
<prefix>.checker.stderr
<prefix>.status
<prefix>.metadata
```

The prefix binds candidate SHA, run ID, run attempt, job, and plain leg.
Metadata is credential-free and binds repository/ref, package, exact selector,
profile, Go version/toolchain, and the PostgreSQL image where applicable. The
wrapper snapshots the complete `PIPESTATUS` immediately after `go test | tee`,
records Go, capture, checker, and status-record-write results independently,
and returns the first nonzero result in that order. Success additionally
requires all six invocation-specific records to be readable regular files and
their status/metadata contents to agree. Empty stderr is valid; absent or
invalid JSON is not.

The existing correctness-matrix upload is strengthened in place to run under
`always()`, use `actions/upload-artifact@v7`, bind codec/SHA/run/attempt in the
artifact name, upload `${{ runner.temp }}/integration-diag/*`, retain for 14
days, and use `if-no-files-found: error`. A wildcard match from another proof
cannot satisfy an absent CK-015 record, and diagnostics are not fabricated on
failure.

## Local parity and fail-closed enforcement

Profile A gains two workflow-equivalent wrappers immediately after CK-014 and
before Phase 18. Each has its own stale-target refusal, JSON/stderr separation,
immediate full `PIPESTATUS`, independent statuses, and six-file completeness
gate. The PostgreSQL wrapper requires the same maintenance/bootstrap/DB
settings. The CI audit enforces the workflow step and each local wrapper
independently; an intact workflow or sibling wrapper cannot compensate for a
missing local contract.

Checker fixtures cover complete profiles, a skipped routing child despite a
passing parent, a missing PostgreSQL obligation, wrong package, contradictory
terminal events, and unrelated optional skips. The existing audit fixture
framework mutates placement, selectors, profiles, settings, stale-file
protection, stderr separation, status propagation and precedence, evidence
completeness, artifact retention, and Required Gate wiring. It includes the
case where unrelated diagnostic files remain while one CK-015 record is
absent.

## Exact path allowlists

The authority commit changes exactly:

- `docs/release/v1.13/v1.13.16-phase12r1r38-ck015-hosted-named-proof-wiring-recovery.md`
- `docs/release/v1.13/README.md`
- `docs/release/v1.13/v1.13.16-phase-list.md`
- `docs/release/v1.13/v1.13.16-release-state.md`
- `docs/release/v1.13/v1.13.16-remediation-tracker.md`
- `docs/release/v1.13/v1.13.16-scope.md`
- `docs/release/v1.13/v1.13.16-source-test-allowlist.md`
- `docs/release/v1.13/v1.13.16-validation-checklist.md`
- `docs/release/v1.13/v1.13.16-phase12r1r34-ck015-initial-lookup-error-propagation.md`

The implementation/enforcement commit changes exactly:

- `.github/workflows/ci.yml`
- `scripts/check_required_test_events.py`
- `scripts/test_check_required_test_events.py`
- `scripts/audit_ci_enforcement.sh`
- `scripts/audit_ci_enforcement_test.go`
- `PRE_RELEASE_CHECKLIST.md`

The R1R34 record receives only an append-only reconciliation note. Its original
no-workflow-change boundary remains historically correct; this later R1R38
authority does not act retrospectively.

## Validation and stop boundary

Before the implementation commit, require checker positives and negative
fixtures, the focused Go audit mutation table, shell syntax and ShellCheck,
formatting, exact golangci-lint 2.9.0, and applicable authority checks.
After the implementation commit, certify its clean exact SHA first attached
and then in a fresh detached worktree. Both contexts must prove all seven
SQLite and five PostgreSQL event identities, checker/audit fixtures, local and
workflow wiring, formatting, `go vet ./...`, exact lint, repository governance,
actual Python discovery inventory, lineage, whitespace, scope, frozen blobs,
and clean trees.

Use the absolute Go 1.26.7 executable with `GOTOOLCHAIN=local` and read-only
module policy. Detached Go execution adds `-buildvcs=false` from its first
invocation. Each context uses its own fresh task-owned PostgreSQL 16.15
container from the pinned local image with `--pull=never` and loopback-only
dynamic access. Execution evidence remains in a new mode-0700 external corpus.

A genuine mandatory failure after commit 2 is terminal: do not amend, add a
repair commit, or retry without owner review. Stop after external reporting for
separate candidate acceptance and exact-SHA push authorization.

```text
PHASE_12R1R37=STOPPED_PRE_PUSH
PHASE_12R1R38=AUTHORIZED_LOCAL_BUILD
CK_V11316_015=OPEN
CK_V11316_014=CLOSED_CERTIFIED
CK_V11316_007=OPEN_UNCHANGED
FINDINGS_CONFIRMED=15
FINDINGS_CLOSED=13/15
PUSHES=0
HOSTED_CERTIFICATION=NOT_STARTED
V1_X_TECHNICAL_CORRECTNESS_CLOSURE=WITHHELD
PHASE_13=NOT_AUTHORIZED
```

## Later Phase 12R1R39 enforcement-gap authority note

A later repository-native review of candidate `236a6198...` confirmed that the
complete CI audit accepted sixteen isolated mutations that disabled CK-015
evidence completeness, stale-target refusal, immediate `PIPESTATUS` capture,
or Go-failure propagation. This does not rewrite R1R38's two-commit authority,
runtime wrappers, earlier proof executions, or historical evidence.

Phase 12R1R39 is separate prospective authority for one nine-document
authority commit followed by one two-file audit/test correction commit. It
keeps CK-V11316-015 open, CK-V11316-014 closed certified, CK-V11316-007 open,
and findings at 15 confirmed with 13/15 closed. Candidate acceptance, push,
hosted certification, finding closure, Phase 12 completion, and Phase 13 remain
unauthorized.
