# v1.13.16 Phase 13R1R1 CK-015 SQLite test-harness recovery

## Authority and boundary

**Mode:** BUILD

**Base:** `86440a383f6bffbe0f7c9ee787a15ef96d2f030b`

**Push:** Not authorized

**Hosted execution:** Not authorized

Phase 13R1 exact-head CI `35436261398` attempt 1 failed in
`TestCKV11316015SharedProcessingChunkWaitersConvergeWithoutDeadlock` with
SQLite `database is locked`. Required Gate correctly failed. CodeQL
`35436261317` passed 4/4, and the Phase 13R1 governance/help corrections passed
their scoped hosted proofs. The failed attempt remains preserved and must not
be rerun under this authority.

The accepted diagnosis archive is 871166 bytes with SHA-256
`38f450494b83ba7dae32a632ff341b8b16857ebd7e11380dc04ca950af6ef424`;
all 1702 manifest entries reverified at entry. It establishes:

- the storage tree, failing test blob, Store/repair implementation, database
  implementation, `go.mod`, and `go.sum` are byte-identical between the
  previously certified base and Phase 13R1 candidate;
- the failure reproduced locally with the same assertion, path, and error once
  among 600 AES-labelled focused executions (599 pass, one fail); and
- the 100-execution diagnostic overrun is retained as a qualification and does
  not change the classification.

The fixture uses WAL and `_busy_timeout=5000` with up to eight open
connections. After CK-015 PROCESSING waiting has converged, two deferred read
transactions may attempt to upgrade to writes and SQLite may return
`SQLITE_BUSY`. Coldkeep's supported simulated SQLite path deliberately uses one
shared connection. The accepted classification is therefore
`PREEXISTING_CK015_TEST_HARNESS_RELIABILITY_RECOVERY_REQUIRED`, not a product
regression, Phase 13R1 regression, or reason to reopen CK-V11316-015.

## Frozen correction

The sole test implementation path is:

- `internal/storage/store_atomic_repair_test.go`

Within `TestCKV11316015SharedProcessingChunkWaitersConvergeWithoutDeadlock`,
the fixture database must be aligned to the supported SQLite capability with:

```go
fixture.repo.DB.SetMaxOpenConns(1)
fixture.repo.DB.SetMaxIdleConns(1)
```

Both Store goroutines, PROCESSING-to-COMPLETED orchestration, bounded waiting,
expected logical IDs, `AlreadyStored` assertions, and no-live-repair-state
checks must remain unchanged. Retries, skips, timeout inflation, assertion
weakening, and production changes are forbidden.

## Exact append-only commits

1. `docs: authorize v1.13.16 phase 13R1R1 test-harness recovery`
2. `test: align CK-015 contention fixture with supported SQLite`
3. `docs: record v1.13.16 phase 13R1R1 local evidence`

Commit 1 and Commit 3 change exactly the eight authority paths named in the
source/test allowlist. Commit 2 changes only the one test path above. No
production, runtime, workflow, schema, module, dependency, or storage
implementation path may change.

## Required local evidence

Attached evidence at Commit 2 must include exactly 1000 AES-labelled focused
repetitions and 200 plain controls, followed by fresh package-level plain/AES
quality commands, exact hosted-quality plain/AES commands, and the applicable
whole-repository gates under exact Go 1.26.7 with `GOTOOLCHAIN=local`. The full
Go sweep must use `-timeout=30m` from its first formal invocation.

One fresh detached checkout of Commit 2 must pass exactly 500 AES-labelled and
100 plain focused repetitions, both package-level and hosted-quality modes,
and the same applicable whole-repository gates. A failed prescribed batch is a
stop; it may not be extended or retried.

## Preserved lifecycle state

```text
PHASE_13R1_HOSTED_CERTIFICATION: FAILED_PRESERVED
HOSTED_RETRY: NOT_AUTHORIZED
CK_V11316_015: CLOSED_CERTIFIED_RETAINED
PHASE_12_TECHNICAL_CORRECTNESS: ESTABLISHED
PHASE_13R1R1: AUTHORIZED_LOCAL_BUILD
PHASE_13B: WITHHELD_PENDING_OWNER_ACCEPTANCE_AND_FRESH_HOSTED_CERTIFICATION
CK_V11316_007: OPEN
FINDINGS_CONFIRMED: 15
FINDINGS_CLOSED: 14/15
PHASE_14: NOT_AUTHORIZED
V1_X_FULL_CLOSURE: NOT_ESTABLISHED
```
