# Coldkeep v1.13.6 Phase List

**Release:** `v1.13.6 — Catalog Contract Expansion and Backend Parity Review`  
**Branch:** `release/v1.13.6`  
**Status:** In progress

## Phase 0 — Post-v1.13.5 release-record cleanup and v1.13.6 scope baseline

**Status:** Complete

Create the release branch, clean stale v1.13.5 post-release wording,
reconcile v1.13.6 scope, and create the v1.13.6 release frame.

Created:

- `v1.13.6-scope.md`
- `v1.13.6-phase-list.md`
- `v1.13.6-validation-checklist.md`

Updated the v1.13.5 post-release record to remove stale pre-gate wording and
reconciled the v1.13.6 title/scope in the v1.13.x release train.

No production code changed.
No tests changed.
No public behavior changed.
No runtime behavior changed.

## Phase 1 — Catalog contract baseline inventory

**Status:** Complete

Created `v1.13.6-catalog-contract-inventory.md`.

The inventory maps current catalog responsibilities across:

- schema/bootstrap/migration
- repository capabilities
- logical identity
- physical mapping
- snapshot graph
- snapshot membership
- path resolution
- chunk/file ordering
- block/container placement
- restore-plan assumptions
- reachability
- GC eligibility
- repair/refcount
- verification expectations
- stats/inspect read-side queries
- engine request/result boundaries

No production code changed.
No tests changed.
No public behavior changed.

## Phase 2 — Snapshot membership contract review

**Status:** Complete

Created `v1.13.6-snapshot-membership-contract.md`.

The review documents the current snapshot membership contract across:

- snapshot identity and lineage
- `snapshot_file` membership rows
- logical/physical membership relationships
- snapshot show/list/diff consumption
- snapshot restore consumption
- snapshot delete/retention consumption
- reachability and GC membership assumptions
- backend-sensitive observations for Phase 6
- coverage evidence to classify in Phase 7

No production code changed.
No tests changed.
No public behavior changed.

## Phase 3 — Logical/physical file mapping contract review

**Status:** Complete

Created `v1.13.6-logical-physical-mapping-contract.md`.

The review documents the current mapping contract across:

- logical content identity
- physical filesystem representation
- logical-to-physical relationships
- stored path and snapshot path identity
- store-time mapping and deduplication
- snapshot mapping interactions
- restore-time path reconstruction
- remove-time and retention mapping
- read-side mapping exposure
- verify, repair, and GC mapping assumptions
- backend-sensitive observations for Phase 6
- coverage evidence to classify in Phase 7

No production code changed.
No tests changed.
No public behavior changed.

## Phase 4 — Placement and restore-plan catalog contract review

**Status:** Complete

Created `v1.13.6-placement-restore-plan-contract.md`.

The review documents the current placement and restore-plan contract across:

- chunk identity
- logical file chunk ordering
- legacy container placement
- packed storage block placement
- block/chunk/container relationships
- codec, compression, and encryption metadata
- store-time placement
- restore descriptor generation
- deterministic restore ordering
- packed/legacy placement parity
- verify placement expectations
- read-side placement exposure
- GC/maintenance placement assumptions
- backend-sensitive observations for Phase 6
- coverage evidence to classify in Phase 7

No production code changed.
No tests changed.
No public behavior changed.

## Phase 5 — Reachability and GC eligibility contract review

**Status:** Complete

Created `v1.13.6-reachability-gc-eligibility-contract.md`.

The review documents the current reachability and GC eligibility contract
across:

- current/live roots
- snapshot roots
- snapshot-retained data
- logical/physical mapping roots
- file_chunk/chunk reachability
- packed block reachability
- legacy container reachability
- GC dry-run planning
- live GC deletion planning
- snapshot retention interactions
- repair/refcount refusal boundaries
- orphan/dangling row behavior
- invariant metadata and public rendering separation
- backend-sensitive observations for Phase 6
- coverage evidence to classify in Phase 7

No production code changed.
No tests changed.
No public behavior changed.

## Phase 6 — SQLite/PostgreSQL schema and query parity review

**Status:** Complete

Created `v1.13.6-sqlite-postgres-parity-review.md`.

The review documents SQLite/PostgreSQL parity across:

- backend selection and configuration
- schema bootstrap and migrations
- dialect and placeholder handling
- transaction and isolation assumptions
- FK and constraint behavior
- ordering, grouping, and aggregation
- string and path comparison
- snapshot membership
- logical/physical mapping
- placement and restore-plan assumptions
- reachability and GC eligibility
- repair/refcount refusal
- stats/inspect read-side queries
- verify catalog expectations
- backend-sensitive observations for Phase 7

No production code changed.
No tests changed.
No public behavior changed.

## Phase 7 — Existing catalog/backend test coverage classification

**Status:** Complete

Created `v1.13.6-catalog-backend-test-coverage.md`.

The review classifies existing evidence across:

- schema bootstrap and migrations
- repository capability derivation
- catalog logical/physical/snapshot/reachability lookups
- snapshot membership
- logical/physical mapping
- placement and restore-plan derivation
- reachability and GC eligibility
- repair/refcount refusal
- invariant metadata
- stats/inspect read-side queries
- verify catalog expectations
- SQLite/PostgreSQL parity
- candidate Phase 8 guard areas

No production code changed.
No tests changed.
No public behavior changed.
Phase 8 is expected to record a no-new-tests decision unless new evidence
appears.

## Phase 8 — Targeted catalog/backend guard implementation pass

**Status:** Complete

Created `v1.13.6-targeted-catalog-guard-implementation.md`.

Phase 8 records the Phase 7 no-new-tests decision.

No new catalog/backend guards were added because existing evidence is
sufficient for v1.13.6 and the remaining candidate areas are better treated as
deferred backend-sensitive or matrix-shaped parity concerns rather than narrow
Phase 8 guards.

No production code changed.
No tests changed.
No public behavior changed.

## Phase 9 — Final catalog/backend contract decision summary

**Status:** Complete

Created `v1.13.6-catalog-backend-contract-summary.md`.
Created `v1.13.6-v1.13.7-handoff.md`.
Created `v1.13.6-release-gate.md`.

Phase 9 closes the v1.13.6 review work, records the final contract decisions,
records the no-runtime-change/no-new-tests outcome, prepares the draft
release-gate record for Phase 10, and prepares the v1.13.7 handoff.

No production code changed.
No tests changed.
No public behavior changed.

## Phase 10 — PRE_RELEASE_CHECKLIST.md Profile A and PR opening

**Status:** Blocked

The full `PRE_RELEASE_CHECKLIST.md Profile A` gate did not pass.

Evidence is recorded in `v1.13.6-release-gate.md`.

The gate blocked on the required `aes-gcm` integration short-suite command:
`go test -race -count=1 -short ./tests/integration/...`

No PR was opened.
