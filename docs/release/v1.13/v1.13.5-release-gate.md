# Coldkeep v1.13.5 Release Gate

**Release:** `v1.13.5 — Release Record Closure and Engine-Level Invariant Ownership`
**Status:** Blocked
**Branch:** `release/v1.13.5`

## Purpose

This document records the final release gate for `v1.13.5`.

The gate remains pending until Phase 10 executes the required final validation
set and records the outcome.

## Release character

`v1.13.5` is currently a docs-only release.

Expected changed files before the final gate are release-record documents only.

## Pre-gate summary

- Phase 0 completed post-release `v1.13.4` record cleanup and opened the
  `v1.13.5` baseline release docs.
- Phase 1 completed the engine validation and invariant ownership inventory.
- Phase 2 completed the `Verify` boundary review, including the final
  `v1.13.4` `verify file` `fileID` hardening review.
- Phase 3 completed the `Remove` boundary and snapshot-retention review.
- Phase 4 completed the `Restore` boundary and restore-safety review.
- Phase 5 completed the GC and maintenance invariant propagation review.
- Phase 6 completed the read-side guard follow-up and confirmed no additional
  read-side Phase 8 guard was justified.
- Phase 7 completed the backend-neutral invariant review and confirmed no
  SQLite-only assumption was introduced.
- Phase 8 completed as a docs-only targeted guard decision pass because no
  justified missing backend-neutral guard remained.
- Phase 9 completed the final decision summary, `v1.13.6` handoff, and this
  draft release gate.

## Final gate requirement

Phase 10 must execute the full `PRE_RELEASE_CHECKLIST.md Profile A` gate.

Profile A must include PostgreSQL-backed validation.

No pull request may be opened until the final gate is green.

## Gate execution

**Date:** `2026-06-13`
**Branch:** `release/v1.13.5`
**Start commit:** `9dca35b`
**Gate:** `PRE_RELEASE_CHECKLIST.md Profile A`

## Environment notes

- `git fetch origin`: failed with known local environment limitation
  `error: cannot open '.git/FETCH_HEAD': Read-only file system`
- PostgreSQL reachability: failed
  `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
  returned `127.0.0.1:5432 - no response` after startup and retry waits
- Writable Go cache: `/tmp/coldkeep-gocache`
- Writable Go module cache: `/tmp/coldkeep-gomodcache`
- Writable golangci-lint cache: `/tmp/coldkeep-golangci`
- Tooling note:
  `go version go1.25.10 linux/amd64` resolved successfully with the writable
  cache setup

## Gate evidence

### 1. Branch and workspace

- Commands:
  - `git status -sb`
  - `git branch --show-current`
  - `git log --oneline --decorate -n 32`
  - `git tag --list "v1.13.4"`
  - `git fetch origin`
- Result: passed with one known local environment limitation
- Notes:
  - branch remained `release/v1.13.5`
  - working tree was clean
  - start commit was `9dca35b`
  - tag `v1.13.4` existed
  - `git fetch origin` failed only because `.git/FETCH_HEAD` is read-only in
    this local environment

### 2. Release-doc sanity

- Commands:
  - `git status -sb`
  - `git diff --check`
  - `rg -n "TODO|TBD|FIXME" docs/release/v1.13/v1.13.5*.md || true`
  - `rg -n "Pending Phase 10|PRE_RELEASE_CHECKLIST.md Profile A|PostgreSQL-backed validation|No pull request" docs/release/v1.13/v1.13.5-release-gate.md`
- Result: passed
- Notes:
  - no whitespace errors were present
  - no active `TODO` / `TBD` / `FIXME` markers were found outside the checklist
    example text in `v1.13.5-validation-checklist.md`
  - the release gate was still correctly documented as pending before
    execution

### 3. PostgreSQL setup and reachability

- Commands:
  - `docker compose up -d coldkeep_postgres`
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
- Result: failed
- Notes:
  - `docker compose up -d coldkeep_postgres` reported the service as started
  - host-side readiness probes continued to return
    `127.0.0.1:5432 - no response`
  - Phase 10 stopped here, before Section 2 quality checks and before the
    Section 3 CI-parity matrix, because PostgreSQL-backed validation is
    mandatory for Profile A

## Blocker

- Command:
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
- Failure:
  - PostgreSQL was not reachable on `127.0.0.1:5432` after container startup
    and retry waits
- Impact:
  - the required `PRE_RELEASE_CHECKLIST.md Profile A` gate could not proceed
  - PostgreSQL-backed validation was not completed
  - no PR can be opened for `v1.13.5`
- Next action:
  - fix the local PostgreSQL startup or host reachability issue
  - rerun Phase 10 from the start of the gate on a clean branch state

## Final decision

The `v1.13.5` Phase 10 `PRE_RELEASE_CHECKLIST.md Profile A` gate is blocked.

PostgreSQL-backed validation did not become reachable, so the required final
gate did not run to completion.

No pull request was opened.

No production code changed.
No tests changed.
No scripts or CI changed.
No public behavior changed.
No runtime behavior changed.
