# Coldkeep v1.13.5 Release Gate

**Release:** `v1.13.5 — Release Record Closure and Engine-Level Invariant Ownership`
**Status:** Passed
**Branch:** `release/v1.13.5`

## Purpose

This document records the final release gate for `v1.13.5`.

The initial gate was blocked, the blocker was diagnosed, and the full Profile A
gate was rerun from the clean gate point. The final gate status is recorded
below.

## Release character

`v1.13.5` is currently a docs-only release.

Expected changed files before the final gate are release-record documents only.

## Pre-gate summary

- Phase 0 completed post-release `v1.13.4` record cleanup and opened the
  `v1.13.5` baseline release docs.
- Phase 1 completed the engine validation and invariant ownership inventory.
- Phase 2 completed the `Verify` boundary review, including the final
  `v1.13.4` `verify file` `fileID` hardening review.
- Phase 3 completed the `Remove` boundary and snapshot-retention review.
- Phase 4 completed the `Restore` boundary and restore-safety review.
- Phase 5 completed the GC and maintenance invariant propagation review.
- Phase 6 completed the read-side guard follow-up and confirmed no additional
  read-side Phase 8 guard was justified.
- Phase 7 completed the backend-neutral invariant review and confirmed no
  SQLite-only assumption was introduced.
- Phase 8 completed as a docs-only targeted guard decision pass because no
  justified missing backend-neutral guard remained.
- Phase 9 completed the final decision summary, `v1.13.6` handoff, and this
  draft release gate.

## Final gate requirement

Phase 10 must execute the full `PRE_RELEASE_CHECKLIST.md Profile A` gate.

Profile A must include PostgreSQL-backed validation.

No pull request may be opened until the final gate is green.

## Gate execution

**Date:** `2026-06-13`
**Branch:** `release/v1.13.5`
**Start commit:** `9dca35b`
**Gate:** `PRE_RELEASE_CHECKLIST.md Profile A`

## Environment notes

- `git fetch origin`: failed with known local environment limitation
  `error: cannot open '.git/FETCH_HEAD': Read-only file system`
- PostgreSQL reachability: failed
  `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
  returned `127.0.0.1:5432 - no response` after startup and retry waits
- Writable Go cache: `/tmp/coldkeep-gocache`
- Writable Go module cache: `/tmp/coldkeep-gomodcache`
- Writable golangci-lint cache: `/tmp/coldkeep-golangci`
- Tooling note:
  `go version go1.25.10 linux/amd64` resolved successfully with the writable
  cache setup

## Gate evidence

### 1. Branch and workspace

- Commands:
  - `git status -sb`
  - `git branch --show-current`
  - `git log --oneline --decorate -n 32`
  - `git tag --list "v1.13.4"`
  - `git fetch origin`
- Result: passed with one known local environment limitation
- Notes:
  - branch remained `release/v1.13.5`
  - working tree was clean
  - start commit was `9dca35b`
  - tag `v1.13.4` existed
  - `git fetch origin` failed only because `.git/FETCH_HEAD` is read-only in
    this local environment

### 2. Release-doc sanity

- Commands:
  - `git status -sb`
  - `git diff --check`
  - `rg -n "TODO|TBD|FIXME" docs/release/v1.13/v1.13.5*.md || true`
  - `rg -n "Pending Phase 10|PRE_RELEASE_CHECKLIST.md Profile A|PostgreSQL-backed validation|No pull request" docs/release/v1.13/v1.13.5-release-gate.md`
- Result: passed
- Notes:
  - no whitespace errors were present
  - no active `TODO` / `TBD` / `FIXME` markers were found outside the checklist
    example text in `v1.13.5-validation-checklist.md`
  - the release gate was still correctly documented as pending before
    execution

### 3. PostgreSQL setup and reachability

- Commands:
  - `docker compose up -d coldkeep_postgres`
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
- Result: failed
- Notes:
  - `docker compose up -d coldkeep_postgres` reported the service as started
  - host-side readiness probes continued to return
    `127.0.0.1:5432 - no response`
  - Phase 10 stopped here, before Section 2 quality checks and before the
    Section 3 CI-parity matrix, because PostgreSQL-backed validation is
    mandatory for Profile A

## Blocker

- Command:
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
- Failure:
  - PostgreSQL was not reachable on `127.0.0.1:5432` after container startup
    and retry waits
- Impact:
  - the required `PRE_RELEASE_CHECKLIST.md Profile A` gate could not proceed
  - PostgreSQL-backed validation was not completed
  - no PR can be opened for `v1.13.5`
- Next action:
  - fix the local PostgreSQL startup or host reachability issue
  - rerun Phase 10 from the start of the gate on a clean branch state

## Final decision

The initial `v1.13.5` Phase 10 `PRE_RELEASE_CHECKLIST.md Profile A` attempt
was blocked.

PostgreSQL-backed validation did not become reachable from the sandboxed local
execution path, so the required final gate did not run to completion on the
first attempt.

No pull request was opened from the blocked attempt.

No production code changed.
No tests changed.
No scripts or CI changed.
No public behavior changed.
No runtime behavior changed.

## Gate rerun after blocker remediation

### Rerun execution

**Date:** `2026-06-14`
**Branch:** `release/v1.13.5`
**Start commit:** `8bcc3c9`
**Gate:** `PRE_RELEASE_CHECKLIST.md Profile A`

### Rerun diagnosis outcome

- `docker-compose.yml` was already correct for the expected gate path
- PostgreSQL inside `coldkeep_postgres` was healthy
- in-container readiness passed
- Docker port publishing was correct on `127.0.0.1:5432`
- host-side reachability passed from the elevated host/Docker-capable
  environment
- the original blocker was sandbox network isolation, not a Coldkeep defect

### Rerun environment notes

- `git fetch origin`: failed again with the known local environment limitation
  `error: cannot open '.git/FETCH_HEAD': Read-only file system`
- PostgreSQL readiness:
  - `docker compose exec -T coldkeep_postgres pg_isready -U coldkeep -d coldkeep`
    passed
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep` passed from the
    elevated host path
  - `PGPASSWORD=coldkeep psql -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep -c "select 1;"`
    passed from the elevated host path
- Writable Go cache: `/tmp/gocache-coldkeep-1781435327`
- Writable Go module cache: `/tmp/gomodcache-coldkeep-1781435327`
- Writable golangci-lint cache: `/tmp/golangci-coldkeep-1781435327`

### Rerun gate evidence

#### 1. Branch and workspace

- Commands:
  - `git status -sb`
  - `git branch --show-current`
  - `git log --oneline --decorate -n 32`
  - `git fetch origin`
- Result: passed with one known local environment limitation
- Notes:
  - branch remained `release/v1.13.5`
  - working tree was clean
  - rerun start commit was `8bcc3c9`
  - `git fetch origin` still failed only because `.git/FETCH_HEAD` is
    read-only in this local environment

#### 2. PostgreSQL setup and reachability

- Commands:
  - `docker compose ps`
  - `docker compose logs --tail=100 coldkeep_postgres`
  - `docker compose exec -T coldkeep_postgres pg_isready -U coldkeep -d coldkeep`
  - `pg_isready -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep`
  - `PGPASSWORD=coldkeep psql -h 127.0.0.1 -p 5432 -U coldkeep -d coldkeep -c "select 1;"`
- Result: passed
- Notes:
  - PostgreSQL was healthy in-container
  - host `127.0.0.1:5432` reachability worked from the elevated host path
  - the earlier blocked attempt was confirmed to be an environment-only
    sandbox reachability limitation

#### 3. Release-doc sanity

- Commands:
  - `git status -sb`
  - `git diff --check`
  - `rg -n "TODO|TBD|FIXME" docs/release/v1.13/v1.13.5*.md || true`
  - `rg -n "Pending Phase 10|PRE_RELEASE_CHECKLIST.md Profile A|PostgreSQL-backed validation|No pull request" docs/release/v1.13/v1.13.5-release-gate.md`
- Result: passed
- Notes:
  - no whitespace errors were present before the rerun
  - no active `TODO` / `TBD` / `FIXME` markers were found outside checklist
    example text
  - the release gate was still correctly documented as pending before the
    rerun began

#### 4. Quality-equivalent checks

- Commands:
  - `bash scripts/clean_test_storage.sh`
  - `go mod tidy && git diff --exit-code`
  - `gofmt -l $(git ls-files '*.go')`
  - `bash -n scripts/*.sh`
  - `bash scripts/check_smart_quotes.sh`
  - `shellcheck $(find scripts/ -maxdepth 1 -name '*.sh' ! -name 'critical_coverage.sh')`
  - `scripts/validate_validation_matrix.sh`
  - `bash scripts/check_versioned_row_writers.sh`
  - `golangci-lint run ./...`
  - `go vet ./...`
  - `COLDKEEP_CODEC=plain go test -race -count=1 ./cmd/... ./internal/...`
  - `COLDKEEP_CODEC=aes-gcm COLDKEEP_KEY="$COLDKEEP_KEY" go test -race -count=1 ./cmd/... ./internal/...`
  - focused chunk benchmark determinism tests
  - `go build ./...`
  - `scripts/audit_ci_enforcement.sh --local-only`
  - `go build -o coldkeep ./cmd/coldkeep`
- Result: passed
- Notes:
  - `golangci-lint` reported `0 issues.`
  - plain and `aes-gcm` cmd/internal race suites both passed
  - CI enforcement audit passed
  - binary build passed

#### 5. Required CI matrix

- Commands:
  - full Profile A integration matrix for `plain`
  - full Profile A integration matrix for `aes-gcm`
  - adversarial suites and focused `G14` through `G17`
  - per-codec smoke scripts
  - local legacy compatibility command
  - local cross-platform approximation
  - critical coverage report generation
  - `scripts/run_snapshot_release_gate.sh --count 1`
- Result: passed
- Notes:
  - short, full, long-run, and refcount integration suites passed for both
    codecs
  - adversarial suites passed for both codecs
  - smoke scripts passed for both codecs
  - legacy compatibility passed
  - cross-platform local approximation passed
  - snapshot release gate passed
  - critical coverage report completed successfully

#### 6. Benchmark interpretation

- Commands:
  - benchmark matrix for `none/zstd` with `workers=1` and `workers=4`
  - regression reports for all four combinations
- Result: passed
- Notes:
  - `benchmark-none-w1`, `benchmark-none-w4`, `benchmark-zstd-w1`, and
    `benchmark-zstd-w4` all completed successfully
  - `regression-report-none-w1.json`, `regression-report-none-w4.json`,
    `regression-report-zstd-w1.json`, and
    `regression-report-zstd-w4.json` each reported no regressions detected
  - no `workers=1` benchmark blocker appeared
  - no special `workers=4` local-variance exception was needed

#### 7. Final local checks

- Commands:
  - `git diff --check`
  - `git status -sb`
- Result: passed
- Notes:
  - final whitespace check passed
  - final branch state remained clean

## Final rerun decision

The clean rerun of the `v1.13.5` Phase 10
`PRE_RELEASE_CHECKLIST.md Profile A` gate passed.

PostgreSQL-backed validation completed successfully in the elevated
host/Docker-capable environment, and all required Profile A sections passed.

The branch is ready for pull request opening.

No production code changed.
No tests changed.
No scripts or CI changed.
No public behavior changed.
No runtime behavior changed.
