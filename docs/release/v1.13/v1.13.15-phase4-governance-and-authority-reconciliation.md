# Coldkeep v1.13.15 Phase 4 — Governance and Authority Reconciliation

**Mode:** `BUILD`
**Status:** Complete

## Current authority

- Root `AGENTS.md` is provider-neutral repository authority for the active
  v1.13.15 closure train and contains deterministic baseline commands.
- Copilot and CI instructions use the active v1.13.15 boundary.
- Generic regression and coverage prompts use the active boundary.
- The dedicated v1.10 phase prompt is explicitly `HISTORICAL_ONLY`; it retains
  provenance and cannot govern current work.
- Completed release evidence remains immutable historical material rather than
  a source of mutable current-phase authority.

## Lifecycle and roadmap reconciliation

The current root, security, contribution, checklist, architecture, series, and
release-train surfaces agree:

- v1.13.15 is the active final v1.x closure release;
- v1.13.14 remains immutable published history;
- v1.x completed the frozen Engine/Catalog correctness work and bounded
  SQLite/PostgreSQL compatibility;
- SQLite-first repository-local default product behavior belongs to v2.x; and
- v2 implementation has not started.

Older roadmap language implying that SQLite-first default productization had
to complete inside v1.x is preserved as historical provenance but superseded
on that timing point.

## Deterministic proof

`scripts/validate_governance.py` validates required active authority markers,
rejects known stale-current phrases, and classifies the historical v1.10 prompt
and completed release records separately. Its unit tests include negative stale
provider detection and historical-path classification.

```text
governance validator: PASS
governance negative tests: 4/4 PASS
complete Python discovery: 165/165 PASS
development release-state validator: PASS
local CI-enforcement audit: PASS
diff integrity: PASS
PHASE_4_IMPLEMENTATION: COMPLETE
GOVERNANCE_AUTHORITY: PASS
V2_IMPLEMENTATION: NOT_STARTED
```
