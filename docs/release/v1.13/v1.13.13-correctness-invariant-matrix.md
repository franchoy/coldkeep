# Coldkeep v1.13.13 Correctness Invariant Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `3 — Correctness and Invariant Audit`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `a401d973e04863aed0239d06f43d9c3979fbbb97`

This matrix classifies every frozen `V1R-COR-*` requirement against current
production source. Tests supplement the source trace and do not override a
source defect. `PROVEN_WITH_EXPLICIT_BOUND` means the exact documented v1
claim holds while a stated limitation lies outside it; it is not `PARTIAL`.

| Requirement ID | Invariant | Production implementation | Primary source paths | Tests/evidence | Backend/platform bounds | Historical issues related | Current source disposition | Phase 3 finding | Classification | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `V1R-COR-001` | Crash safety and no partial or inconsistent exposure | transaction-bound store/remove/snapshot/GC mutations; restore temp-write, hash, sync, close, rename | `internal/storage/store.go`; `internal/storage/remove.go`; `internal/storage/restore.go`; `internal/snapshot/snapshot.go`; `internal/maintenance/gc.go` | store interleaving/cancellation, restore fault-FS, snapshot rollback, GC fault-FS | per-file publication; snapshot multi-file restore is intentionally not command-atomic; Windows directory sync is unavailable | restore durability and transaction-hardening releases | ordinary rename after a non-atomic absence check can destructively replace a destination created in the final race window | `P3-COR-001` | **BLOCKER** | Pre-publication reconstruction is fail closed, but the no-overwrite publication guarantee is not. |
| `V1R-COR-002` | Reachability- and snapshot-safe GC | Catalog coherent metadata load, `gc.BuildPlan`, live liveness revalidation, pins, FK/cardinality guards | `internal/catalog/gc_plan.go`; `internal/gc/plan.go`; `internal/maintenance/gc.go`; `internal/retention/reachability.go` | G6 coherent-plan race test; reachability, packed/mixed, dry-run/live, snapshot retention suites | production live GC is PostgreSQL-only; Phase 4 owns parity and Phase 5 owns coordination proof | v1.13.12 G6; packed snapshot retention fixes | current, snapshot, and pin roots are conservative; corrupt/orphan metadata refuses planning or deletion | — | **PROVEN_WITH_EXPLICIT_BOUND** | Proven within the currently claimed backend/coordination boundary. |
| `V1R-COR-003` | Deterministic, byte-identical restore | ordered recipe and chunk index, placement validation, chunk/file hashes, exact size checks | `internal/catalog/restore_plan.go`; `internal/storage/restore.go`; `internal/storage/storage_block_reader.go` | compatibility determinism, concurrent-write determinism, cross-platform byte tests, packed restore fixtures | supported legacy, packed, and mixed repositories | historical packed restore and ordering defects | final whole-file SHA prevents partial or reordered bytes from becoming success | — | **PROVEN** | Selection and plan outputs are sorted/deduplicated before execution. |
| `V1R-COR-004` | Destination confinement against traversal and symlink escape | lexical validation, trusted-root containment, pre-operation `Lstat` component walks | `internal/pathsafe/pathsafe.go`; `internal/storage/restore.go`; `internal/snapshot/snapshot.go` | traversal, drive/UNC, separator, corrupt stored path, static symlink tests | Linux/macOS/Windows lexical handling; no handle-pinned destination traversal | stored-path and snapshot traversal fixes | static malicious paths and existing symlinks fail closed, but a checked parent can be replaced with a symlink before create/rename | `P3-COR-002` | **BLOCKER** | The source has check/use separation and no directory-handle-relative install primitive. |
| `V1R-COR-005` | Atomic, non-destructive overwrite/no-overwrite installation with authoritative exact destinations | temp file in destination directory, final `Stat`, ordinary rename; lower restore may append `originalName` for a directory | `internal/storage/restore.go`; `internal/engine/default_engine_routing_helpers.go`; `internal/snapshot/snapshot.go` | overwrite/no-overwrite and rename-failure tests; no final-window adversarial test | ordinary rename behavior differs by OS; no atomic no-replace primitive is used | `RESTORE_INSTALL_CONTRACT` | both atomic no-replace and exact planned-destination authority are violated | `P3-COR-001` | **BLOCKER** | One inherited root has subcases `P3-COR-001A` and `P3-COR-001B`. |
| `V1R-COR-006` | Fail-closed integrity verification | system/repository block pipeline plus file standard/full/deep levels | `internal/verify/verify_repository.go`; `internal/verify/verify_file.go`; `internal/maintenance/verify_command.go` | corruption-stage fixtures, system packed verification, file verification suites | all configured backends share the route; Phase 4 owns equivalence | packed verification parity work | file-level deep verification inner-joins legacy `blocks` and silently omits packed-only chunks | `P3-COR-003` | **BLOCKER** | System deep verification remains fail closed; the defect is the claimed file-deep path. |
| `V1R-COR-007` | Packed/legacy and mixed-repository compatibility | dual placement resolver, packed block reader, legacy decoder, common restore/GC/system verify | `internal/catalog/placement.go`; `internal/storage/restore.go`; `internal/storage/storage_block_reader.go`; `internal/verify/verify_file.go` | dual compatibility, packed/mixed restore, GC, snapshot and system verify suites | supported repository versions and codecs only | v1.8–v1.13 compatibility releases | restore, GC, retention, recovery, and system verification are compatible; packed file-deep verification is not | `P3-COR-003` | **BLOCKER** | One verification defect blocks the aggregate parity requirement. |
| `V1R-COR-008` | Snapshot immutability, retention, and delete safety | transaction-atomic create/delete, immutable product surface, graph validation, snapshot roots | `internal/snapshot/snapshot.go`; `internal/catalog/snapshot_graph.go`; `internal/catalog/gc_plan.go` | create rollback/immutability, lineage, selectors, delete, retention and restore tests | multi-file restore is sequential and not all-or-nothing; metadata strict failure may occur after file publication | snapshot graph/retention releases | snapshot rows cannot be mutated through production APIs; delete and roots preserve retained payload | — | **PROVEN_WITH_EXPLICIT_BOUND** | The documented per-file restore publication bound is explicit. |
| `V1R-COR-009` | Recovery and repair must not legitimize corrupt mappings | strict recovery/quarantine; topology-first refcount repair; exact affected-row checks | `internal/recovery/system_recovery.go`; `internal/maintenance/repair_refcounts.go`; `internal/maintenance/repair_chunk_refcounts.go` | recovery fault-FS/idempotency and repair orphan/cardinality tests | PostgreSQL executions skip locally without infrastructure; source contract is backend-neutral | v1.13.11 recovery/repair hardening | corrupt relationships are rejected before mutation; quarantined/orphan payload is not made authoritative | — | **PROVEN** | Optional non-strict recovery warnings do not make suspect payload reachable. |
| `V1R-COR-010` | Container, header, and range validation before allocation or reads | filename checks, header magic/version/CRC/max validation, overflow-safe range validation | `internal/container/format.go`; `internal/container/container.go`; `internal/container/payload.go`; `internal/storage/storage_block_reader.go` | header/range/physical-size/cardinality/fault-FS tests | supported header versions 0 and 1; platform `int` bound checked before allocation | v1.13.11 container bounds remediation | corrupt ranges, maxima, headers, filenames, and physical sizes fail before allocation/read | — | **PROVEN** | Persisted maximum and catalog maximum must agree. |
| `V1R-COR-011` | Bounded decompression | prevalidated expected size, absolute CKBL bound, zstd decoder memory/window cap, exact identity size | `internal/storage/compression/zstd.go`; `internal/storage/compression/none.go`; `internal/storage/storage_block_reader.go` | aggregate-frame, oversized-output, truncated-input, AES-GCM-before-decompress tests | supported `none` and `zstd` compression only | v1.13.11 bounded decompression remediation | aggregate output is bounded and size equality is mandatory | — | **PROVEN** | Encryption authentication and compressed hashes precede decompression. |
| `V1R-COR-012` | Exact numeric fidelity | tagged Engine values store integer decimal tokens; CLI projects to `int64` or `json.Number` | `internal/engine/observability_adapter.go`; `cmd/coldkeep/observability_engine_adapters.go` | exact numeric kinds, nested integer boundaries, large stats JSON tests | JSON numbers remain numbers; invalid negative/domain values are rejected at request boundaries | v1.13.11 JSON integer fidelity remediation | no correctness-critical integer is routed through `float64` in the stable projection | — | **PROVEN** | Evidence covers above `2^53`, `MaxInt64`, unsigned boundaries, and nested containers. |
| `V1R-COR-013` | Fail-closed required-row mutation cardinality and transaction behavior | `RequireRowsAffected`/`RequireExactlyOneRow`; transaction-local validation; commit-truthful results | `internal/db/mutation_rows.go`; `internal/storage`; `internal/snapshot`; `internal/maintenance`; `internal/recovery`; `internal/container` | required mutation cardinality and rollback suites | legitimate variable/zero-row cleanup, CAS, upsert and bulk operations remain explicit | v1.13.11 70-mutation inventory and 20 hardenings | required transitions reject zero/multiple rows; no new cardinality gap was found | — | **PROVEN** | Phase 4 will re-evaluate backend-specific transaction semantics. |
| `V1R-COR-014` | Correctness under the claimed concurrency boundary | transactional metadata, restore pins, coherent GC statement, repository coordination dependency | `internal/catalog/gc_plan.go`; `internal/storage/restore.go`; `internal/storage/store.go`; `internal/coordination` | focused race tests for G6, restore determinism and pin lifecycle | same-process/same-host/local-filesystem claim; Phase 5 owns native/advisory lock lifecycle | G6 and same-host coordination releases | correctness-sensitive paths are sound within the documented ownership assumption; destination-path attacker races are separately `P3-COR-002` | — | **PROVEN_WITH_EXPLICIT_BOUND** | Phase 5 must confirm the assumed lock boundary; no distributed claim is made. |
| `V1R-XPLAT-002` | Linux, macOS, and Windows path/restore compatibility with limitations explicit | portable lexical path checks and Go rename primitive; Windows directory sync skip | `internal/pathsafe`; `internal/storage/restore.go`; cross-platform hosted jobs | path-form tests and prior exact-head cross-platform CI | install semantics are not equivalent enough to satisfy atomic no-replace; no unclaimed OS promise | cross-platform restore hardening | lexical path behavior is covered, but install and symlink-race guarantees are blocked | `P3-COR-001`, `P3-COR-002` | **BLOCKER** | Phase 7 must design, and Phases 8–9 must remediate/prove, the platform contract. |

## Mechanical verdict

Three distinct root findings block applicable frozen requirements. There are
no `PARTIAL` requirement dispositions; intentional bounds remain explicit.

```text
V1_CORRECTNESS:
BLOCKED

CORRECTNESS_V1_BLOCKERS:
3

CORRECTNESS_PARTIALS:
0
```
