# Coldkeep v1.13.8 Release Gate

**Release:** `v1.13.8 — Restore / Remove Contract Split and Stored-Path Boundary`
**Phase:** Phase 12
**Status:** Released — pre-PR Profile A preserved and post-release closure recorded
**Branch:** `release/v1.13.8`
**Final executable commit:** `4a0edaba472c3848656ab2045baebf1c2fb4a3d4`

## Gate profile

`PRE_RELEASE_CHECKLIST.md Profile A — Pre-PR CI-parity gate`

## Scope freeze

No new product scope was introduced during the gate.

The final green run validated the already-accepted `release/v1.13.8` execution
surface, recorded gate artifacts under `/tmp/ckphase12-run-rwPMsb`, and kept the
working tree clean through the final executable checks.

## Executable remediation history

The final executable commit superseded earlier executable commits only when a
real repository blocker was fixed and recommitted:

| Commit | Type | Repository remediation |
|---|---|---|
| `d1f5b928cf10849b8fed29465f9324b88fffb25f` | test | Isolated parallel adversarial/integration PostgreSQL fixtures with dedicated test-main bootstrap helpers. |
| `f310d348dc4e65a4a669586d191ae05ddb697efa` | test | Reduced deep-verify cadence in the chunk-reuse adversarial harness to clear a real full-race timeout blocker. |
| `4a0edaba472c3848656ab2045baebf1c2fb4a3d4` | fix | Corrected `scripts/critical_coverage.sh` argument expansion so the ShellCheck/coverage gate passes honestly. |

Runner-only corrections during Phase 12 did **not** change the executable
commit. They included stale-binary selection, omitted smoke reset state, wrong
AES-GCM integration invocation, outdated benchmark-validator arguments,
multi-object JSON extraction, wrong legacy target selection, and one sandboxed
local-DB restart.

## Toolchain evidence

- Go: `go version go1.25.10 linux/amd64`
- Docker Compose: `Docker Compose version v5.1.4`
- PostgreSQL client: `psql (PostgreSQL) 16.14`
- `jq`: `jq-1.7`
- `shellcheck`: `ShellCheck - shell script analysis tool`
- `golangci-lint`: `v2.6.2`
- Python: `Python 3.12.3`
- Host/platform: `Linux 6.17.0-1017-oem x86_64 GNU/Linux`

## PostgreSQL and binary provenance

- `docker compose up -d coldkeep_postgres`: passed
- host-side PostgreSQL readiness (`SELECT 1`): passed
- schema version check: passed, `schema_version = 16`
- built binary path: `/tmp/ckphase12-run-rwPMsb/bin/coldkeep`
- binary SHA-256: `cd3308eaab5f84157d1f432a0553f7a6ecad964347112fd2880d27543efdb5f8`
- smoke provenance checks before each codec smoke leg:
  - `command -v coldkeep`: gate binary
  - `realpath "$(command -v coldkeep)"`: gate binary
  - `coldkeep version`: `coldkeep version 1.13.8`
  - `coldkeep version --output json`: extracted final JSON object reported `1.13.8`

## Quality-equivalent checks

| Check | Result |
|---|---|
| `bash scripts/clean_test_storage.sh` | Passed |
| `go mod tidy && git diff --exit-code` | Passed |
| `gofmt -l $(git ls-files '*.go')` | Passed |
| `bash -n scripts/*.sh` | Passed |
| `bash scripts/check_smart_quotes.sh` | Passed |
| ShellCheck (`scripts/`, excluding `critical_coverage.sh`) | Passed |
| `scripts/validate_validation_matrix.sh` | Passed |
| `bash scripts/check_versioned_row_writers.sh` | Passed |
| `golangci-lint run ./...` | Passed |
| `go vet ./...` | Passed |
| `COLDKEEP_CODEC=plain go test -race -count=1 ./cmd/... ./internal/...` | Passed |
| `COLDKEEP_CODEC=aes-gcm COLDKEEP_KEY=... go test -race -count=1 ./cmd/... ./internal/...` | Passed |
| chunk benchmark-contract tests | Passed |
| `go build ./...` | Passed |
| `scripts/audit_ci_enforcement.sh --local-only` | Passed |
| built CLI human version assertion | Passed |
| built CLI JSON version assertion | Passed |

## Required CI matrix and supplements

| Suite | Plain | AES-GCM |
|---|---:|---:|
| integration correctness (`-short`) | Pass | Pass |
| integration stress | Pass | Pass |
| integration long-run | Pass | Pass |
| ref-count containment | Pass | Pass |
| adversarial full | Pass | Pass |
| focused adversarial G14-G17 | Pass | Pass |
| smoke | Pass | Pass |

Additional required local gates:

- benchmark matrix (`none/zstd`, `workers=1/4`): all four runs passed
- benchmark regression validation: all four reports passed
- legacy compatibility
  - `TestPhase2PostMigrationStoreRestoreSnapshotRegressionIntegration`: passed
- local cross-platform approximation
  - `./internal/pathsafe`: passed
  - `./internal/storage -run "RestoreCrossPlatform|RestoreSeam"`: passed
- critical coverage report: passed
- snapshot release gate: passed
- zero-reference / PostgreSQL migration supplements: passed
- relocation guard: passed

## Full repository gates

| Gate | Result |
|---|---|
| `go test -count=1 ./...` | Passed |
| `go test -race -count=1 ./...` | Passed |
| `go vet ./...` | Passed |
| `golangci-lint run ./...` | Passed |
| `git diff --check` | Passed |
| final `git status -sb` before gate documentation | Clean |

## Profile A decision

Profile A passed on executable commit `4a0edaba472c3848656ab2045baebf1c2fb4a3d4`.

Live PostgreSQL validation passed.

The built CLI reported exactly `1.13.8` in both human and JSON modes.

## Pre-PR pull-request authorization

The release branch is authorized to receive its documentation-only gate record
and then open the single pull request to `main`.

No merge, tag, publication, branch deletion, or `v1.13.9` activation is
authorized by this phase.

## Pre-PR release status

- Not merged.
- Not tagged.
- GitHub release not published.
- `v1.13.9` remains planned and inactive.

## Immediate blockers

None found.

## Post-release closure

The sections above preserve the pre-PR Profile A evidence exactly as recorded
before the pull request was opened.

Final authoritative release evidence:

- Final pull-request head: `96395857cb01c1deecdda38d4beae2e48a755d22`
- PR merge commit: `584d6e8db8f54f4fdf5f4785aad698c71cad66dd`
- Released main commit: `584d6e8db8f54f4fdf5f4785aad698c71cad66dd`
- Annotated tag target: `584d6e8db8f54f4fdf5f4785aad698c71cad66dd`
- PR required checks: passed
- Post-merge `main` CI: passed
- GitHub release: published at `2026-06-28T09:53:45Z`
- Release branch: deleted remotely

This distinction preserves the provenance of both:

1. the local pre-PR Profile A gate;
2. the final released state validated by PR CI, post-merge CI, tag, and
   publication.

## Final release state

- Merged.
- Tagged as `v1.13.8`.
- GitHub release published.
- Release branch deleted.
- Released.
