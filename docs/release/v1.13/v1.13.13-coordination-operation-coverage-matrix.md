# Coldkeep v1.13.13 Coordination Operation Coverage Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `5 — Repository Coordination Audit`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `5a4f1f76ee807b5cf4075782d2a13c4e37bdbf67`

Every active Engine method and every production CLI/tooling surface is
accounted for. `Required` means the frozen v1 repository-wide policy requires
the outer lease. Engine methods intentionally do not acquire it themselves;
the CLI/application caller owns the complete lifetime for real/shared
repositories.

| Operation | Read/mutate/exclusive | Coordination required | Acquisition layer | Same-process reservation | Native lock | Postgres advisory lock | Lifetime | Release | Relevant test | Classification | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Engine `Stats` / CLI `stats` | read plus startup recovery mutation | YES | CLI outer wrapper | YES | YES | NO | recovery → session → Engine → cleanup | `WithLease` | CLI policy/lifecycle | PROVEN | Read command is conservatively exclusive. |
| Engine `Inspect` / CLI `inspect` | read plus startup recovery mutation | YES | CLI outer wrapper | YES | YES | NO | same | same | policy and routing | PROVEN | — |
| Engine `Verify` / CLI `verify` | read plus startup recovery mutation | YES | CLI outer wrapper | YES | YES | NO | all verify levels and cleanup | same | restore/verify/GC runtime contracts | PROVEN | Prevents mixed catalog/container views. |
| Engine `SnapshotList` | read plus startup recovery mutation | YES | CLI snapshot policy | YES | YES | NO | full command/session | same | snapshot policy | PROVEN | Includes tree/query modes. |
| Engine `SnapshotShow` | read plus startup recovery mutation | YES | CLI snapshot policy | YES | YES | NO | same | same | snapshot policy | PROVEN | — |
| Engine `SnapshotStats` | read plus startup recovery mutation | YES | CLI snapshot policy | YES | YES | NO | same | same | snapshot policy | PROVEN | — |
| Engine `SnapshotDiff` | read plus startup recovery mutation | YES | CLI snapshot policy | YES | YES | NO | same | same | snapshot policy | PROVEN | Summary and detailed routes share the lease. |
| Engine `SnapshotCreate` | mutation | YES | CLI snapshot policy | YES | YES | NO | recovery → mutation → transaction/session cleanup | same | snapshot policy/routing | PROVEN | — |
| Engine `SnapshotDelete` | preview/read or mutation | YES | CLI snapshot policy | YES | YES | NO | complete preview/execute path | same | snapshot policy/routing | PROVEN | Both modes remain exclusive. |
| Engine `SnapshotRestore` | repository read/pins plus destination writes | YES | CLI snapshot policy | YES | YES | NO | plan, pins, reads, install, metadata, unpin, cleanup | same | snapshot routing; G6 outer proof | PROVEN_WITH_EXPLICIT_BOUND | Restore correctness blockers remain Phase 3 findings. |
| Engine `GarbageCollect`, dry-run / CLI `gc --dry-run` | read/plan | YES | CLI `gc` policy | YES | YES | PostgreSQL session acquired by maintenance | recovery → plan → cleanup | outer lease after inner cleanup | policy and runtime tests | PROVEN | SQLite dry-run skips advisory lock but retains native lease. |
| Engine `GarbageCollect`, live / CLI `gc` | destructive exclusive mutation | YES | CLI `gc` policy | YES | YES | YES on PostgreSQL | recovery → advisory acquire → GC → advisory cleanup → session cleanup | inner advisory before outer native release | Linux G6 live-GC and advisory tests | PROVEN_WITH_EXPLICIT_BOUND | SQLite live GC refuses before mutation. |
| Engine `PlanGarbageCollection` / CLI `simulate gc` | shared repository read; connection may bootstrap/migrate schema | YES | Missing: CLI treats all `simulate` as bypass | NO | NO | NO | uncoordinated application session | session only | bypass policy and simulate routing tests | **BLOCKER** | `P5-COORD-001`; not an isolated simulation. |
| Engine `Store` / CLI `store` | mutation | YES | CLI `store` policy | YES | YES | NO | recovery → store → writer/DB cleanup | outer wrapper | policy, runtime, G6 contention | PROVEN | — |
| Engine `StoreFolder` / CLI `store-folder` | batch mutation | YES | CLI `store-folder` policy | YES | YES | NO | complete batch and cleanup | outer wrapper | policy/G6 | PROVEN | — |
| Engine `ListFiles` / CLI `list` | read plus startup recovery mutation | YES | CLI `list` policy | YES | YES | NO | recovery → query → cleanup | outer wrapper | policy and read routing | PROVEN | — |
| Engine `SearchFiles` / CLI `search` | read plus startup recovery mutation | YES | CLI `search` policy | YES | YES | NO | same | same | policy and read routing | PROVEN | — |
| Engine `GetConfiguration` / CLI `config get` | read; schema ensure may mutate | YES | CLI config policy | YES | YES | NO | session/schema ensure → read → cleanup | outer wrapper | config policy/routing | PROVEN | No separate startup recovery, but schema work remains inside lease. |
| Engine `SetConfiguration` / CLI `config set` | mutation | YES | CLI config policy | YES | YES | NO | schema ensure → mutation → cleanup | outer wrapper | config policy/routing | PROVEN | — |
| Engine `Repair` / CLI `repair` | corrective mutation | YES | CLI `repair` policy | YES | YES | NO | recovery → repair → cleanup | outer wrapper | policy/routing | PROVEN | Inherited architecture/correctness findings unchanged. |
| Engine `Recover` / startup recovery | corrective catalog/storage mutation | YES | Enclosing participating CLI operation | YES | YES | NO | after native acquire, before command dispatch | enclosing lease | ordering and production wrapper tests | PROVEN | Direct shared-repository callers must acquire explicitly. |
| Engine `Doctor` / CLI `doctor` | recovery/schema/verify composite mutation | YES | CLI `doctor` policy | YES | YES | NO | complete Engine composite and cleanup | outer wrapper | policy/doctor routing | PROVEN | Doctor owns its recovery internally. |
| Engine `Remove` / CLI by-ID remove | mutation | YES | CLI `remove` policy | YES | YES | NO | recovery → remove → cleanup | outer wrapper | policy/G6 | PROVEN | Dry-run and live share policy. |
| Engine `RemoveStoredPaths` | mutation | YES | CLI `remove` policy | YES | YES | NO | complete single/batch path | outer wrapper | stored-path routing | PROVEN | — |
| Engine `Restore` / CLI by-ID restore | repository read/pins plus destination writes | YES | CLI `restore` policy | YES | YES | NO | complete restore and cleanup | outer wrapper | runtime and routing tests | PROVEN_WITH_EXPLICIT_BOUND | Phase 3 install/confinement blockers unchanged. |
| Engine `RestoreStoredPath` | repository read/pins plus destination writes | YES | CLI `restore` policy | YES | YES | NO | complete restore and cleanup | outer wrapper | stored-path routing | PROVEN_WITH_EXPLICIT_BOUND | — |
| Schema bootstrap/migration for participating commands | schema mutation | YES | Enclosing CLI policy before `application.Open` | YES | YES | NO | inside outer lease | session closes before lease | production ordering tests/source | PROVEN | Opening and `EnsurePostgresSchema` occur in dispatch after acquisition. |
| CLI `init` | local `.env` creation only | NO | Explicit bypass | NO | NO | NO | current working directory only | ordinary file close | bypass policy/source | PROVEN_WITH_EXPLICIT_BOUND | Does not open DB, storage, Engine, or repository control namespace. |
| CLI `simulate store` | isolated temporary SQLite simulation | NO | Explicit bypass | NO | NO | NO | temp DB and simulated writer | `StorageContext.Close` | simulation source/tests | PROVEN_WITH_EXPLICIT_BOUND | No shared repository access. |
| CLI `simulate store-folder` | isolated temporary SQLite simulation | NO | Explicit bypass | NO | NO | NO | same | same | simulation source/tests | PROVEN_WITH_EXPLICIT_BOUND | — |
| CLI `benchmark chunkers` | synthetic computation | NO | Explicit bypass | NO | NO | NO | process-local benchmark | ordinary cleanup | benchmark source/tests | PROVEN_WITH_EXPLICIT_BOUND | No shared repository. |
| CLI `benchmark run` | isolated generated database/repository cases | NO | Explicit bypass | NO | NO | NO | uniquely named temporary DB and case directories | explicit terminate/drop/close cleanup | benchmark source/tests | PROVEN_WITH_EXPLICIT_BOUND | Does not target the configured production repository. |
| CLI help/version/invalid input | no repository access | NO | Explicit bypass | NO | NO | NO | parse/render only | none | policy tests | PROVEN | — |

## Mechanical result

```text
PRODUCTION_OPERATION_BYPASSES:
1

ARCHITECTURAL_ROOTS:
P5-COORD-001
```
