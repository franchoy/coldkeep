# Coldkeep v1.13.13 Phase 11 Independent Full Local Release Gate

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `11 — Independent Full Local Release Gate`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `ab6aa49f54b34ed8a4610058a16b63ce4eab7d29`

## 1. Purpose

Phase 11 independently executes the complete locally practical release gate on
the immutable v1.13.13 candidate. Phase 8/9 results explain the expected
contracts but are not substituted for fresh Phase 11 execution. This phase
changes documentation only after every mandatory local row passes. It does not
declare formal v1.x closure, merge, tag, publish, or begin Phase 12.

## 2. Starting SHA

The fetched entry state was exact:

```text
BRANCH: release/v1.13.13
LOCAL_HEAD: ab6aa49f54b34ed8a4610058a16b63ce4eab7d29
ORIGIN_HEAD: ab6aa49f54b34ed8a4610058a16b63ce4eab7d29
AHEAD_BEHIND: 0/0
WORKTREE: CLEAN
V1.13.13_TAG: ABSENT_LOCAL_AND_REMOTE
PHASES_0_10: COMPLETE
PHASE_11: NEXT
PHASES_12_16: NOT_STARTED
```

## 3. Environment inventory

```text
GO: go version go1.25.10 linux/amd64
GOOS: linux
GOARCH: amd64
CGO_ENABLED: 1
GIT: git version 2.43.0
PYTHON: Python 3.12.3
GOLANGCI_LINT: v2.6.2
POSTGRES_CLIENT: PostgreSQL 16.14
LOCAL_POSTGRES: AVAILABLE (PostgreSQL 16, healthy on 127.0.0.1:5432)
```

Relevant test variables were initially unset. PostgreSQL rows used the
documented `COLDKEEP_TEST_DB`, auto-bootstrap, schema-path, `DB_*`, codec, and
storage-directory controls. The AES-GCM key was present but never printed in
evidence. Task-specific Go and lint caches under `/tmp` avoided the read-only
user cache without changing repository content.

## 4. Authority loaded

The complete Phase 9 proof, closure matrix, and execution matrix; Phase 10
reconciliation; current-state truth matrix; active validation checklist and
phase list; `VALIDATION_MATRIX.md`; `PRE_RELEASE_CHECKLIST.md`; and current CI
workflow definitions were read before execution. They establish six technical
blockers closed, `P2-DOC-001` closed, zero known open v1 blockers, and formal
v1.x closure pending a later phase.

## 5. Repository cleanliness

The tracked/untracked worktree was clean at entry. A broader ignored-file audit
found old April/July test binaries and repository data, including root
`storage/containers` owned by `nobody:nogroup`; those pre-existing artifacts
were treated as user state and were not modified. Tests were isolated from
that state with documented task-owned storage paths.

The gate generated an ignored CLI binary, codec smoke stores, two smoke
working directories, one Python bytecode file, three cross-build binaries, and
one `cmd/coldkeep/storage/.../repository.lock`. Their exact creation times and
inventories were verified, the files were removed individually, and only
empty generated directories were removed. Pre-existing ignored state was
preserved.

## 6. Release-state gate

`git diff --check`, validation-matrix validation, all 48 release-state fixture
tests, development-state validation, and the remote CI-enforcement audit pass.
The validator reports `state: development`, `active_version: 1.13.13`, and
`violations: 0`.

## 7. Build gate

`go mod tidy -diff`, tracked-Go formatting, shell syntax, smart-quote,
ShellCheck, versioned-row-writer, and `go build ./...` checks pass. The built
CLI reports exactly `coldkeep version 1.13.13` in human output and `1.13.13`
in its JSON contract.

## 8. Vet gate

`go vet ./...` passes with no findings.

## 9. Lint gate

`golangci-lint v2.6.2 run ./...` passes from a clean task-specific cache with
`0 issues`. No lint configuration or source was changed.

## 10. Full Go test gate

`go test -count=1 ./...` passes all 51 discovered module packages with zero
package failures or package skips. PostgreSQL-specific backend subtests were
also executed explicitly later with the available local service.

## 11. Race gate

The preferred complete command, `go test -race -count=1 ./...`, passes. Fresh
codec package runs and the focused eight-package remediation run additionally
cover CLI, coordination, secure installation, storage, snapshot, Engine,
Catalog, and verify under the race detector.

## 12. Codec matrix

Plain and AES-GCM each pass the fresh `./cmd/... ./internal/...` race matrix,
short integration suite, full integration stress suite, dedicated long-run
suite, refcount-containment matrix, full adversarial suite, G14–G17 tail, named
round trip, focused Phase 9 proofs, and smoke gate. Storage, restore, verify,
snapshot, GC, packed storage, metadata, and integrity contracts therefore all
execute in both practical local codec modes.

## 13. Adversarial gate

The complete `COLDKEEP_LONG_RUN=1` G1–G17 suite passes under plain in 292.423s
and AES-GCM in 287.959s. The separate G14–G17 selectors pass under both codecs.
The named independent-process G6 selectors also report explicit PASS events
under both codec invocations; no matching skip is accepted.

## 14. Restore recheck

The fresh race recheck of `internal/fsx/secureinstall`, `internal/storage`,
`internal/snapshot`, `internal/engine`, and `cmd/coldkeep` passes. It preserves
exact destinations, atomic non-replacing publication, intentional overwrite,
retained-parent confinement, post-publication metadata, strict-metadata
compatibility, and the absence of an unsafe fallback. The broad, focused,
G5/adversarial, round-trip, smoke, and Linux cross-platform rows agree.

## 15. Verify recheck

Fresh `internal/catalog`, `internal/verify`, Engine, integration, and Phase 9
packed-verification proofs pass. Explicit SQLite and PostgreSQL subtests cover
legacy-only, packed-only, and mixed recipes, truthful placement attempts,
missing placement, malformed range, and corrupt packed data fail-closed
behavior.

## 16. Catalog/backend recheck

Aggregate query, scan, iteration, cancellation, and deadline error contracts
pass. Snapshot-label case equivalence, placement authority, and restore-plan
contracts explicitly pass on both SQLite and local PostgreSQL. The PostgreSQL
mutation-cardinality selector reports PASS rather than SKIP.

## 17. Coordination recheck

Fresh coordination package/CLI race tests, the Phase 9 simulate-GC PostgreSQL
integration proof, and the three independent-process G6 selectors pass.
Simulate-GC remains coordinated; isolated store simulations remain bypasses;
lease-before-application-open, real-holder contention, killed-holder release,
and reacquisition are preserved. The claim remains same-host/local-filesystem
only and makes no NFS, SMB, or distributed-lock assertion.

## 18. Cross-platform compile

The local Linux pathsafe and storage cross-platform approximation passes.
Secure-installer test binaries cross-compile for Darwin amd64, Darwin arm64,
and Windows amd64. These compile rows do not substitute for Phase 12 native
runtime execution.

## 19. Mandatory local benchmark/long-run gates

Both codecs pass short integration, full stress, dedicated long-run, actual
25-iteration refcount containment, full adversarial long-run, and smoke rows.
All four hard AES-GCM benchmark-integrity profiles (`none-w1`, `none-w4`,
`zstd-w1`, `zstd-w4`) return `BENCHMARK_INTEGRITY_PASS`; all generated checksum
manifests verify. The four timing observations are `ADVISORY`: each produces a
valid `NO_REGRESSION` report with comparator exit `0` and verified hashes. The
critical coverage report is also `ADVISORY`/report-only and completes without
runtime or inventory failure. The optional 1,000-iteration refcount override
and the conditional released-v1.7-binary gate were not promoted into Phase 11.

## 20. PostgreSQL local availability

`LOCAL_POSTGRES: AVAILABLE`. The existing healthy PostgreSQL 16 service was
reused; no PostgreSQL installation was provisioned. Backend contracts,
integration, adversarial, maintenance advisory-lock, smoke, snapshot,
legacy-compatibility, and benchmark rows all used it successfully.

## 21. Skip audit

```text
EXPECTED_ENVIRONMENTAL: 0
HOSTED_ONLY_BY_CONTRACT: macOS/Windows native runtime and hosted security/quality
PLATFORM_NOT_LOCAL: macOS/Windows native execution (cross-build passed locally)
NOT_APPLICABLE: optional 1000-iteration refcount override; conditional released-v1.7 binary gate
ADVISORY: four timing observations; critical coverage report
UNEXPECTED: 0
```

Short integration legitimately omits long-run cases, but every mandatory
omitted family was executed in its dedicated row. The smoke script's opt-in
large-file sub-gate and nested codec-variant helper remained optional; both
top-level codec smoke matrices executed and passed.

## 22. Retry history

Three unchanged-code environmental retries and two command-environment
corrections are retained:

- Default `go mod tidy -diff` could not write the read-only user Go cache:
  `TRANSIENT_LOCAL_INFRASTRUCTURE`; the same command passed with task caches.
- A sandboxed PostgreSQL socket attempt was denied:
  `TRANSIENT_LOCAL_INFRASTRUCTURE`; the unchanged test passed with authorized
  localhost access.
- The first short integration run reached pre-existing root storage owned by
  another user and returned invalid repository identity:
  `ENVIRONMENTAL_BOUND`; the exact failing selector and then the complete
  suite passed with documented task-owned storage directories.
- The checklist's refcount command exited zero but skipped without
  `COLDKEEP_LONG_RUN=1`; audit detected the skip and both codec rows were
  re-executed with the repository's documented long-run switch, producing
  real PASS events.
- A final focused chunk-benchmark command named a not-yet-created task GOTMPDIR:
  `TRANSIENT_LOCAL_INFRASTRUCTURE`; after creating that directory, the
  unchanged selector passed.
- The repeated post-document remote CI-enforcement query reached GitHub's API
  rate limit after all local invariants passed: `TRANSIENT_LOCAL_INFRASTRUCTURE`.
  The same full remote audit had already passed before documentation, the diff
  contains no CI files, and the supported post-document `--local-only` audit
  passed unchanged.

No deterministic source or test failure was retried, and no implementation or
test was edited.

## 23. Post-test hygiene

After exact generated-file cleanup, `git status --short --untracked-files=all`
is empty. Repository scans find zero `repository.lock`, `.coldkeep-control`,
Phase 11 cross-build binaries, or coverage profiles. The pre-existing ignored
user data identified before the gate remains untouched and was never used as
test storage.

## 24. Blocker closure preservation

All fresh local results remain consistent with the Phase 9 closure matrix and
Phase 10 documentation closure:

```text
P2_ARCH_001: CLOSED
P3_COR_001: CLOSED
P3_COR_002: CLOSED
P3_COR_003: CLOSED
P4_BE_001: CLOSED
P5_COORD_001: CLOSED
P2_DOC_001: CLOSED
KNOWN_OPEN_V1_BLOCKERS: 0
FORMAL_V1_X_CLOSURE: PENDING_LATER_PHASE
```

## 25. Documentation-only change audit

The Phase 11 diff contains only this report, the companion execution matrix,
and the six authorized current tracker files. Production Go, test Go, schema,
migration, dependency, CI implementation, validator implementation, version,
and historical evidence semantics remain unchanged.

## 26. Release-state validation

The post-document gate records passing diff, validation-matrix, release-state
fixture, development-state, local CI-enforcement, relative-link, and CKRS
checks. The full remote enforcement audit passed before the documentation-only
diff; its post-document repeat was rate-limited as recorded above. The
resulting topology is Phases 0–11 Complete, Phase 12 Next, and Phases 13–16 Not
started, with development state and zero violations.

## 27. Commit/push identity

The documentation commit subject is
`docs: record v1.13.13 full local release gate`. The exact final Phase 11 SHA
is the commit containing this self-identifying report and matrix on
`release/v1.13.13`; post-commit verification requires local/origin equality,
ahead/behind `0/0`, a clean worktree, and no local or remote `v1.13.13` tag.
Automatically triggered hosted runs are recorded after push but are owned
formally by Phase 12.

## 28. Final verdict

```text
PHASE_11_LOCAL_GATE: PASS
STARTING_SHA: ab6aa49f54b34ed8a4610058a16b63ce4eab7d29
FULL_GO_TEST: PASS
RACE_GATE: PASS
BUILD_GATE: PASS
VET_GATE: PASS
LINT_GATE: PASS
PLAIN_CODEC_GATE: PASS
AES_GCM_GATE: PASS
ADVERSARIAL_GATE: PASS
RESTORE_LOCAL_PROOF: PASS
VERIFY_LOCAL_PROOF: PASS
CATALOG_LOCAL_PROOF: PASS
COORDINATION_LOCAL_PROOF: PASS
DARWIN_CROSS_BUILD: PASS
WINDOWS_CROSS_BUILD: PASS
LOCAL_POSTGRES: AVAILABLE
POSTGRES_LOCAL_PROOF: PASS
MANDATORY_LOCAL_GATES_EXECUTED: 44
MANDATORY_LOCAL_GATES_FAILED: 0
UNEXPECTED_SKIPS: 0
POST_TEST_REPOSITORY_ARTIFACTS: 0
KNOWN_OPEN_V1_BLOCKERS: 0
PRODUCTION_GO_CHANGES: 0
TEST_GO_CHANGES: 0
CI_IMPLEMENTATION_CHANGES: 0
FORMAL_V1_X_CLOSURE: PENDING_LATER_PHASE
```

Phase 11 passes. Phase 12 — Hosted Exact-Head Security and Quality Gate — is
next in `BUILD` mode and is not started here.
