# Coldkeep v1.13.15 Phase 6 — Hosted Candidate and Pre-Tag Governance Proof

**Mode:** `BUILD`
**Status:** Terminal exact-head certification pending
**Observed:** `2026-08-27`
**Pre-remediation head:** `840defd8e2e09e0e14c07ecf5b0f42d22c98beff`

## Authorized blocker reconciliation

The exact-head Codacy inventory contained 29 findings grouped into 11 root
causes. Twenty-seven findings were individually dispositioned and the two
remaining workflow-integrity findings were remediated by replacing only the
mutable Docker action tags with the exact commits for their existing v4.2.0
versions:

```text
docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0
docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0
```

No action upgrade, product behavior, dependency, container UID, schema,
storage, public CLI/API, or v2 change was made. The complete per-ID inventory
is recorded in
[`v1.13.15-phase6-codacy-inventory.csv`](v1.13.15-phase6-codacy-inventory.csv).

## Codacy disposition proof

The official Codacy Cloud CLI was installed outside the repository and used
with a non-persisted account API token:

```text
@codacy/codacy-cloud-cli: 1.9.0
npm integrity: sha512-INLe42I6e3XUnkpDLTiqXjijfdhXi9mtafMACMn5vUM9MfvUBBtaxA5A+7fjPwSICffmAtiuDBgqP2iSTHtvkQ==
runtime: Node v22.23.2 (task-owned /tmp installation)
authenticated GitHub organization: franchoy / remote identifier 7415595
repository: gh/franchoy/coldkeep
```

`codacy issue --help` exposed the supported reasons
`AcceptedUse|FalsePositive|NotExploitable|TestCode|ExternalCode`. PR findings
use a numeric `resultDataId` selector in the CLI, which the CLI maps to the
32-character issue ID before invoking the documented individual issue-state
operation. The archived PR response supplied and verified every one-to-one
mapping before mutation.

Exactly 27 individual operations succeeded with comments and zero failures:

| Root cause | Count | Reason | Rationale |
|---|---:|---|---|
| RC01 | 2 | `AcceptedUse` | Explicit governance checks remain auditable; threshold-only complexity is non-blocking. |
| RC02 | 5 | `AcceptedUse` | Generic agent-document heuristics conflict with repository-specific governance terminology and structure. |
| RC03 | 6 | `AcceptedUse` | Safety invariants are intentionally unconditional and must not gain escape hatches. |
| RC04 | 1 | `FalsePositive` | `PRE_RELEASE_CHECKLIST.md` exists. |
| RC05 | 1 | `AcceptedUse` | Bind-mounted storage UID compatibility makes a non-root conversion a release-design change. |
| RC06 | 5 | `TestCode` | Trusted test-harness subprocess use; three findings are duplicate annotations. |
| RC07 | 1 | `TestCode` | Test-only Windows fixture complexity has no production impact. |
| RC08 | 1 | `AcceptedUse` | The base and toolchain are pinned; Debian packages intentionally remain security-updateable. |
| RC09 | 4 | `TestCode` | Tests necessarily inspect validated native Windows ABI buffers; two findings are duplicates. |
| RC10 | 1 | `AcceptedUse` | Bounds validation and exact allocation precede the necessary native ABI view. |
| RC11 | 2 | `FIX` | Mutable v4.2.0 action tags replaced by their exact v4.2.0 commits. |

No bulk issue operation, PR-wide ignore-all, pattern disablement, file ignore,
or repository suppression was used. Codacy reports disposition changes after a
new analysis; the final exact-head analysis is therefore the terminal proof
that the 27 dispositions persist and the two fixed findings are absent.

## CodeQL inventory

The repository-restricted GitHub credential authenticated as `franchoy`, User
ID `7415595`. Complete pagination of
`/repos/franchoy/coldkeep/code-scanning/alerts?state=open&per_page=100`
returned:

```text
OPEN_CODEQL_ALERTS_TOTAL: 0
OPEN_CODEQL_ALERTS_UNIQUE: 0
CODEQL_ALERT_INVENTORY: PASS
```

No alert state was changed. The inventory must be repeated after the final
tracked commit.

## Release-tag governance

Ruleset `14537086` was read, archived, projected to its live writable fields,
changed, reread, and compared semantically. The update changed only:

```text
refs/tags/"v*" -> refs/tags/v*
bypass_actors: [] -> User 7415595 / always
```

All other semantics remained byte-equivalent in the canonical writable
object, including `CI Required Gate`, live integration ID `15368`, strict
status-check policy, create-time enforcement, and deletion,
non-fast-forward, creation, and update rules. The pre-change response SHA256
is `c75ca0e07acf5562ff0a2f9031c8d77cc1b49e4d57f9fa623334eaa3e181a8d4`.
The live-derived PUT object SHA256 is
`1d11c85a3a4232c1021716bd23b2616ce75665b5d7d5f1db88c9a896bf37b7fb`.
The authenticated reread reports `current_user_can_bypass: always` and raw
`refs/tags/v*`, which matches `refs/tags/v1.13.15`; `exempt` is absent.

The authenticated rule-suite endpoint was also queried with exact ref
`refs/tags/v1.13.14` and complete pagination. It returned an empty array, so
no historical evaluated rule suite is available to archive; that absence is
recorded without claiming the malformed current include protected the old tag.

The complete canonical authenticated responses are archived in
[`v1.13.15-phase6-ruleset-14537086-before.json`](v1.13.15-phase6-ruleset-14537086-before.json)
and
[`v1.13.15-phase6-ruleset-14537086-after.json`](v1.13.15-phase6-ruleset-14537086-after.json).

## Immutable releases and historical identity

Repository immutable releases changed from
`{"enabled":false,"enforced_by_owner":false}` to
`{"enabled":true,"enforced_by_owner":false}` and the authenticated reread
proved enablement before any v1.13.15 draft creation.

The final exact-head gate must reprove the frozen v1.13.14 values:

```text
tag object: a996b25b562de69749f41c3af56626aeb5d44e33
peeled target: caac44d459609f89f2c971cb7b07a8678bd52d2c
release ID: 377376616
identity SHA256: 03d1aa57b46c2e340fc4d33876d0f197c558a4c106aea3e2798c30914914dbd6
baseline whole-file SHA256: 139d93f855d0185996fff54fc5c2008a26844ee6d33ec0aef484bc6778ecf4cc
```

## Terminal exact-head gate

Before commit, the bounded candidate passed YAML parsing, diff-integrity and
exact-pin checks, both ruleset archive comparisons, the development
release-state validator, governance validator, local CI-enforcement audit, all
165 Python discovery tests, and `go test -count=1 ./...` with Go 1.26.7 and
`GOTOOLCHAIN=local`. The definitive Go run used task-owned module, build, and
temporary caches; no dependency or repository file changed.

This tracked evidence is finalized before terminal certification. The commit
containing it supersedes `840defd8...` and must receive full CI, CI Required
Gate, CodeQL workflow and zero-alert inventory, exact-SHA Codacy closure,
Linux/macOS/Windows remote exact-candidate installation, v1.13.14 identity
proof, and open/unmerged PR proof. Run IDs and terminal observations are
reported without another tracked commit; any subsequent tracked change
invalidates certification and repeats the full gate.

```text
PHASE_6: TERMINAL_EXACT_HEAD_CERTIFICATION_PENDING
PHASE_7: NOT_STARTED
PR_115: OPEN_UNMERGED
MERGE: NOT_AUTHORIZED
TAG: NOT_AUTHORIZED
RELEASE: NOT_AUTHORIZED
V2_IMPLEMENTATION: NOT_STARTED
```
