# Coldkeep v1.13.9 Phase List

**Release:** `v1.13.9 — Snapshot Mutation Boundary: Create / Delete / Restore`
**Status:** Active
**Branch:** `release/v1.13.9`

## Phase 0A — Release-state closure and v1.13.9 activation

**Status:** Complete

Phase 0A:

- confirmed the working tree was clean before edits;
- confirmed the released `main` baseline at `584d6e8db8f54f4fdf5f4785aad698c71cad66dd`;
- created `release/v1.13.9` from the released `main` state;
- inspected the annotated `v1.13.8` tag;
- inspected recent `main` history;
- verified PR #101 merge state and required PR checks;
- verified the published `v1.13.8` GitHub release;
- verified that the remote `release/v1.13.8` branch was deleted;
- reconciled the active `v1.13.8` release records to released state;
- created the `v1.13.9` scope, phase list, and validation checklist;
- changed the source-reported version from `1.13.8` to `1.13.9`.

The only allowed observable code-behavior change in Phase 0A is the version
reporting change to `1.13.9`.

## Phase 0B — Restore destination trust-boundary correction

**Status:** Complete

Phase 0B addresses inherited finding
`CK-1139-P0-01 — Outer symlink-prefix restore compatibility`.

Phase 0B implementation:

- replaced full absolute-path symlink rejection with explicit trusted-root
  validation plus below-root symlink checks;
- added storage-level `TrustedRoot` propagation without changing public CLI or
  engine restore contracts;
- aligned by-ID live restore destination derivation with dry-run semantics;
- updated snapshot restore planning/execution to propagate trusted roots for
  original, prefix, and override modes;
- removed test-only `EvalSymlinks` masking from restore success-path helpers;
- added outer-alias and inner-symlink coverage across path-safety, storage,
  engine, and snapshot restore tests.

## Phase 0C — Stored-path batch compatibility correction

**Status:** Complete

Phase 0C closes inherited findings:

- `CK-1139-P0-02 — Stored-path fail-fast summary compatibility`
- `CK-1139-P0-03 — All-invalid stored-path initialization ordering`

Phase 0C implementation:

- removed request-length summary normalization from stored-path batch remove;
- made stored-path batch summaries derive only from emitted item results;
- added engine-owned preflight classification for stored-path batch remove;
- returned terminal invalid-only stored-path batch results before repository
  initialization;
- preserved CLI routing, JSON payload keys, human output, and request/result
  contract shapes while restoring pre-v1.13.8 invalid-batch initialization
  ordering.

## Phase 0D — Corrective CI enforcement and baseline freeze

**Status:** Complete

Phase 0D closes inherited finding:

- `CK-1139-P0-04 — Required cross-platform enforcement`

Phase 0D implementation:

- made `cross-platform` an explicit dependency of `ci-required`;
- made the required-gate result assertion reject non-success cross-platform
  results;
- expanded native cross-platform workflow coverage to include pathsafe,
  storage, engine, and snapshot restore alias/trusted-root tests;
- added local CI-audit enforcement for cross-platform dependency and success
  assertion;
- added audit regression tests for missing `cross-platform` dependency and
  missing required-result enforcement;
- added direct `release/**` CI push validation and an in-repo CodeQL workflow
  so release branches have a permanent remote-validation route without a
  temporary PR;
- collected local focused, full normal, full race, vet, lint, and local
  script-gate evidence for the corrective baseline;
- verified native Ubuntu, macOS, and Windows release-branch jobs;
- verified deterministic PostgreSQL G6 coverage in both codec jobs, including
  all six isolated subcases;
- verified CodeQL for `actions`, `go`, and `python`;
- verified Codacy on the exact executable head
  `c3a5a537d9dfad197f35c258ab78a75526bad788`;
- recorded the same-SHA adversarial rerun truthfully; and
- froze the executable Phase 0 baseline at
  `c3a5a537d9dfad197f35c258ab78a75526bad788`.

PR #103 was opened prematurely during Phase 0 remote validation and is now
closed without merge. The intended workflow remains one release branch, direct
release-branch remote validation, and a single final release PR after all
phases and the full pre-release gate are green.

Phase 0 is complete.

- Phase 0A: complete
- Phase 0B: complete
- Phase 0C: complete
- Phase 0D: complete

## Phase 1 — Snapshot mutation inventory and behavior freeze

**Status:** Complete

Phase 1 implementation:

- froze the current snapshot mutation baseline in
  `v1.13.9-snapshot-mutation-inventory.md`;
- verified that `snapshot create`, `snapshot delete`, and `snapshot restore`
  remain direct CLI and snapshot-package owned candidate operations;
- corrected lineage deletion semantics to distinguish normal `ON DELETE SET NULL`
  parent deletion from compatibility-only missing-parent metadata states;
- recorded exact human output and JSON success contracts for create, delete
  preview, delete live, and restore;
- mapped create, delete, and restore behavior matrices, validation order,
  common error and exit behavior, backend parity evidence, dependency
  direction, and unresolved contract gaps;
- traced high-risk behaviors to exact tests or explicit uncovered follow-up
  requirements; and
- preserved Phase 1 as docs-only work with no runtime, test, schema, or
  workflow changes.

Phase 1 is complete.

- Phase 1: complete
- Phase 2 is authorized and next.

## Phase 2 — Snapshot create contract design

**Status:** Complete

Phase 2 implementation:

- created the snapshot create contract document at
  `v1.13.9-snapshot-create-contract.md`;
- froze the final target `SnapshotCreateRequest` without a caller-selected
  `Type`;
- froze the final target `SnapshotCreateResult` with committed
  `SnapshotID`, `Type`, `PathsCount`, `FilesInserted`, `Label`, and `ParentID`
  only;
- removed speculative create-result warnings from the target contract;
- froze engine-owned ID generation semantics, normalization rules, and the
  instance-owned generator seam;
- froze `PathsCount` as raw request-filter count and `FilesInserted` as exact
  committed membership-row count from the mutation itself;
- froze the target create transaction boundary, validation ordering, error
  categories, and DB-only engine dependency boundary;
- froze the future CLI translation target, including removal of CLI-owned ID
  generation and post-create count SQL after later routing;
- recorded the canonical Phase 2 artifact filename and the discrepancy from the
  earlier Phase 1 predicted `...snapshot-create-contract-design.md` filename;
- enumerated the SQLite/PostgreSQL parity requirements, contract test plan, and
  candidate migration impact for later implementation phases; and
- preserved Phase 2 as docs-only work with no runtime, schema, test, or
  workflow changes.

Phase 2 is complete.

- Phase 2: complete
- Phase 3 is authorized and next.

## Phase 3 — Snapshot delete preview/live contract design

**Status:** Complete

Phase 3 implementation:

- created the snapshot delete contract document at
  `v1.13.9-snapshot-delete-contract.md`;
- froze the final target `SnapshotDeleteRequest` with explicit `Mode` instead
  of contradictory `Force` and `DryRun` booleans;
- froze the final target `SnapshotDeleteResult` with normalized `SnapshotID`,
  explicit `Deleted`, and preview/live separation through `Preview`;
- replaced `sql.NullString` parent exposure with explicit
  `none`/`present`/`missing` parent-state semantics;
- froze preview counts as `int64` membership/reference metrics only, with exact
  `TotalFiles`, `UniqueFiles`, and `SharedFiles` meanings and no reclaim
  promise;
- froze warning ownership outside the engine contract so current
  `lineage_breakage` rendering derives from raw preview child facts;
- froze the preview read-only catalog-only boundary and the execute
  metadata-only transaction contract;
- froze execute-result honesty so successful delete later requires actual
  snapshot-row deletion, not commit success alone;
- froze the smallest domain-adaptation path, future CLI translation target,
  JSON/human compatibility rules, backend parity requirements, and contract
  test plan;
- recorded the canonical Phase 3 artifact filename and the discrepancy from the
  earlier Phase 1 predicted `...snapshot-delete-contract-design.md` filename;
  and
- preserved Phase 3 as docs-only work with no runtime, schema, test, or
  workflow changes.

Phase 3 is complete.

- Phase 3: complete

## Phase 4 — Snapshot restore selection and destination contract design

**Status:** Complete

Phase 4 implementation:

- created the snapshot restore contract document at
  `v1.13.9-snapshot-restore-contract.md`;
- froze the final target `SnapshotRestoreRequest` with explicit positional
  `Paths`, restore-specific `Selection`, explicit `Destination`, `Overwrite`,
  and metadata mode;
- froze restore-specific selection semantics without reusing the narrower
  read-side `engine.SnapshotQuery`;
- froze repeated exact-path and prefix preservation at the future engine
  boundary while preserving the current distinction between positional
  exact-path missing errors and query-only exact filters;
- froze the final destination mode enum and destination object for `original`,
  `prefix`, and `override`;
- froze original-mode current-working-directory ownership in the future CLI
  translation layer and removed any future engine dependency on `os.Getwd()`;
- froze lexical output-path identity separately from trusted canonical safety
  paths, preserving outer-alias acceptance and inner-symlink rejection;
- replaced `Strict` and `NoMetadata` booleans in the target contract with one
  explicit metadata mode and restore-specific structured metadata warnings;
- froze override-mode eligibility as one positional exact path only, with no
  query-only or directory-prefix broadening;
- froze the final renderer-neutral `SnapshotRestoreResult` with normalized
  `SnapshotID`, explicit `DestinationMode`, raw positional
  `RequestedPathsCount`, exact `RestoredFiles`, lexical `OutputTarget`,
  lexical `OutputPaths`, and structured warnings only;
- kept CLI `type`, timing, human output, and JSON envelope ownership outside
  the future engine result;
- froze empty-selection success, positional exact-path missing-error behavior,
  and non-atomic partial-failure semantics explicitly;
- froze the smallest honest domain-adaptation path, dependency boundary,
  immutability/concurrency expectations, backend parity requirements, and
  contract test plan;
- recorded the canonical Phase 4 artifact filename and the discrepancy from the
  earlier Phase 1 predicted `...snapshot-restore-contract-design.md`
  filename; and
- preserved Phase 4 as docs-only work with no runtime, schema, test, or
  workflow changes.

Phase 4 is complete.

- Phase 4: complete

## Phase 5 — Snapshot mutation request/result contract cleanup

**Status:** Complete

Phase 5 implementation:

- replaced the provisional snapshot mutation candidate request/result shapes in
  `internal/engine/candidates.go` with the frozen Phase 2, Phase 3, and
  Phase 4 contract surfaces;
- removed speculative create-result warnings and contradictory delete/restore
  request booleans from the candidate contract layer;
- introduced the frozen delete mode, delete parent-state, and delete preview
  result types;
- introduced the frozen restore-specific selection, destination, metadata, and
  warning types without modifying the active read-side `SnapshotQuery` or the
  active stored-path restore contract;
- preserved repeated restore exact-path and prefix selectors without narrowing
  them to one value;
- updated comments to describe the mutation contracts as frozen but inactive
  v1.13.9 candidate surfaces rather than active engine ownership;
- added exact structural reflection tests, enum-value tests, slice/pointer
  shape tests, representability tests, backend-neutrality checks, and
  non-activation guards for the cleaned mutation contracts;
- verified that the active `Engine` interface still excludes
  `SnapshotCreate`, `SnapshotDelete`, and `SnapshotRestore`, and that active
  `DefaultEngine` methods still do not consume the cleaned request types;
- created the Phase 5 artifact
  `v1.13.9-snapshot-mutation-request-result-cleanup.md`; and
- preserved Phase 5 as contract cleanup only with no CLI, snapshot-domain,
  schema, storage, or runtime routing changes.

Phase 5 is complete.

- Phase 5: complete
- Phase 6 is authorized and next.

## Phase 6 — Snapshot create engine activation

**Status:** Complete

Phase 6 implementation:

- activated `SnapshotCreate` on the real `Engine` interface;
- implemented `DefaultEngine.SnapshotCreate` against the frozen Phase 2 create
  contract without changing CLI routing;
- introduced the instance-owned snapshot ID generator seam on
  `DefaultEngine` with the secure default `snap-<16 lowercase hex>` format;
- moved create request copying, ID normalization/generation, label/parent
  trimming, type derivation, parent-plus-paths rejection, self-parent
  rejection, and request-only path validation to the engine boundary;
- kept the snapshot-domain create transaction as the execution core while
  adding a result-returning `CreateSnapshotWithOptionsResult` path;
- made engine-side `FilesInserted` come from the committed mutation itself
  rather than a later query;
- preserved the existing error-only snapshot-domain wrapper for direct callers;
- added focused engine activation, generator, rollback, immutability, duplicate
  ID, parent validation, and snapshot-domain result tests;
- updated active-interface and candidate-only guards so snapshot create is
  active while snapshot delete and snapshot restore remain inactive;
- preserved production CLI `snapshot create` routing through
  `createSnapshotPhase`, including CLI-owned ID generation and the CLI
  post-create `COUNT(*)` query;
- created the Phase 6 artifact
  `v1.13.9-snapshot-create-engine-activation.md`; and
- preserved Phase 6 scope without activating snapshot delete, snapshot
  restore, or broader CLI mutation routing.

Phase 6 is complete.

- Phase 6: complete
- Phase 7: complete
- Phase 8: complete
- Phase 9: complete
- Phase 10 is authorized and next.

## Phase 9 — Route CLI `snapshot delete` through `Engine.SnapshotDelete`

**Status:** Complete

Phase 9 implementation:

- routed production CLI `snapshot delete --dry-run` through
  `Engine.SnapshotDelete` with `Mode=preview`;
- routed production CLI `snapshot delete --force` through
  `Engine.SnapshotDelete` with `Mode=execute`;
- preserved preview-wins precedence when both preview and force flags are
  present;
- preserved explicit false behavior and other pre-initialization CLI validation
  before repository or engine initialization;
- removed the production CLI preview call to
  `snapshotDeleteLineagePreviewPhase`;
- removed the production CLI execute call to `deleteSnapshotPhase`;
- kept warning rendering CLI-owned and derived from preview children rather
  than engine presentation objects;
- preserved preview human output and preview JSON compatibility by projecting
  engine raw facts into the existing CLI shape;
- preserved execute human output and sparse live JSON compatibility without
  loading preview data during execute;
- added focused CLI routing, parity, validation-order, missing-parent, sparse
  execute JSON, and translation-preservation tests;
- kept `snapshot create` routed through `Engine.SnapshotCreate` unchanged;
- kept `snapshot restore` on its existing direct CLI/domain route unchanged;
- created the Phase 9 artifact
  `v1.13.9-snapshot-delete-cli-routing.md`; and
- preserved Phase 9 scope without activating restore or changing snapshot
  delete engine semantics.

Phase 9 is complete.

- Phase 9: complete
- Phase 10: complete
- Phase 11 is authorized and next.

## Phase 10 — Snapshot restore engine activation

**Status:** Complete

Phase 10 implementation:

- activated `SnapshotRestore` on the real `Engine` interface;
- implemented `DefaultEngine.SnapshotRestore` against the frozen Phase 4
  restore contract without changing production CLI routing;
- added focused engine request preparation for snapshot ID normalization,
  explicit destination validation, metadata validation, restore-specific query
  adaptation, and caller-owned slice/pointer copying;
- kept `snapshot.RestoreSnapshot` as the execution core rather than duplicating
  restore planning or payload reconstruction inside the engine;
- added an explicit original-root option for engine-owned restore so the
  engine path never depends on `os.Getwd()`;
- preserved legacy implicit-CWD restore behavior only for existing direct
  snapshot callers that do not supply an explicit original root;
- kept lexical destination/output identity separate from trusted-root safety
  validation for original, prefix, and override destinations;
- added structured restore metadata warnings without changing current direct
  CLI human or JSON output;
- preserved non-atomic restore behavior, override eligibility, stored-path
  safety checks, and best-effort versus strict metadata semantics;
- updated active-interface, contract-surface, engine-activation, snapshot-domain,
  and CLI non-routing preservation tests;
- kept `snapshot create` and `snapshot delete` active and unchanged;
- kept production CLI `snapshot restore` on `restoreSnapshotPhase`;
- created the Phase 10 artifact
  `v1.13.9-snapshot-restore-engine-activation.md`; and
- preserved Phase 10 scope without routing production CLI restore through the
  engine.

Phase 10 is complete.

- Phase 10: complete
- Phase 11: complete
- Phase 12 is authorized and next.

## Phase 11 — Snapshot restore CLI routing

**Status:** Complete

Phase 11 implementation:

- routed production CLI `snapshot restore` through `Engine.SnapshotRestore`;
- preserved CLI ownership of parsing, destination-mode parsing, top-level
  validation, duration, and human/JSON output projection;
- mapped positional restore paths to `SnapshotRestoreRequest.Paths` without
  collapsing them into query selectors;
- mapped restore query flags to `SnapshotRestoreRequest.Selection` while
  preserving repeated exact `--path` selectors and repeated `--prefix`
  selectors in order;
- introduced CLI-local explicit current-working-directory discovery for
  original-mode restore and passed that lexical root into the engine;
- kept the engine path free of `os.Getwd()` ownership;
- preserved prefix and override destination mapping as explicit lexical
  destination paths on the engine request;
- preserved metadata-mode compatibility through explicit best-effort, strict,
  and none engine request mapping;
- preserved query-only restore JSON `type=full` compatibility by keeping the
  CLI projection based on positional requested-path count;
- added focused restore-routing tests for pre-initialization validation,
  original-mode explicit-root forwarding, and selector separation/repetition;
- updated prior boundary-preservation tests so create, delete, and restore all
  reflect the active engine ownership boundary;
- kept `restoreSnapshotPhase` available for lower-level compatibility tests and
  direct domain callers while removing it from the production CLI route;
- created the Phase 11 artifact
  `v1.13.9-snapshot-restore-cli-routing.md`; and
- preserved Phase 11 scope without changing snapshot-domain restore semantics,
  create/delete routing, schema, storage format, GC, or workflows.

Phase 11 is complete.

- Phase 11: complete
- Phase 12 is authorized and next.

## Phase 12 — Snapshot mutation routing closure and boundary hardening

**Status:** Complete

Phase 12 implementation:

- audited the production CLI snapshot mutation routes after create, delete,
  and restore all became engine-routed operations;
- confirmed production CLI `snapshot create` routes through
  `Engine.SnapshotCreate`;
- confirmed production CLI `snapshot delete` routes through
  `Engine.SnapshotDelete`;
- confirmed production CLI `snapshot restore` routes through
  `Engine.SnapshotRestore`;
- classified remaining direct snapshot-domain seams as compatibility-only
  lower-level test and non-CLI caller seams, not production CLI bypasses;
- tightened compatibility-seam comments in `cmd/coldkeep/main.go` so they no
  longer imply pending candidate activation;
- removed the stale `restoreSnapshotPhase` keepalive artifact after confirming
  it was unnecessary;
- strengthened restore routing tests so production CLI restore explicitly fails
  if the legacy `restoreSnapshotPhase` seam is touched;
- corrected stale restore test wording to reflect active engine routing rather
  than legacy direct-seam expectations;
- created the Phase 12 artifact
  `v1.13.9-snapshot-mutation-routing-closure.md`; and
- preserved Phase 12 scope without changing snapshot mutation semantics, CLI
  parsing, human or JSON output, engine contracts, schema, storage format, GC,
  or workflows.

Phase 12 is complete.

- Phase 12: complete
- Phase 13 is authorized and next.

## Phase 13 — Repair and recovery boundary decision scope definition

**Status:** Complete

Phase 13 implementation:

- confirmed that Phase 12 closed the snapshot mutation routing boundary and
  left production CLI `snapshot create`, `snapshot delete`, and
  `snapshot restore` engine-owned;
- verified that the remaining direct snapshot-domain seams are compatibility-only
  lower-level hooks rather than production CLI bypasses;
- reconciled the missing checked-in `v1.13.9` Phase 13 definition against the
  broader `v1.13.x` release-train intent;
- defined the exact `v1.13.9` Phase 13 scope as a docs-only tracker-definition
  pass rather than a runtime implementation phase;
- created the Phase 13 artifact
  `v1.13.9-repair-recovery-boundary-scope-definition.md`;
- froze the next implementation phase as `Repair and Recovery Boundary Decision`;
- froze the next implementation phase as production-boundary work that may touch
  `internal/engine`, `internal/maintenance`, `internal/recovery`, and CLI
  ownership tests only if that later phase explicitly requires it;
- kept the current Build pass docs-only with no engine contract changes, no
  snapshot create/delete/restore semantic changes, no CLI output changes, no
  parser changes, no schema or storage changes, and no workflow changes; and
- preserved PR #103 as permanently closed with no new or reopened PR.

Phase 13 acceptance criteria:

- the `v1.13.9` tracker defines an explicit Phase 13 title;
- the `v1.13.9` tracker defines an explicit Phase 13 artifact name;
- the `v1.13.9` tracker defines the next implementation phase after Phase 13;
- the `v1.13.9` tracker makes clear that Phase 13 itself is docs-only;
- the `v1.13.9` tracker records that no runtime code changes land in Phase 13;
- the release-train handoff is consistent with the checked-in
  `v1.13.x` release train; and
- no PR is opened or reopened.

Phase 13 is complete.

- Phase 13: complete
- Phase 14 is authorized and next.

## Phase 14 — Repair and Recovery Boundary Decision

**Status:** Complete

Phase 14 implementation:

- created the canonical Phase 14 artifact
  `v1.13.9-repair-recovery-boundary-decision.md`;
- classified current repair/recovery surfaces across CLI, engine candidates,
  maintenance, recovery, verify, snapshot, and storage/container layers;
- recorded the active ownership split where `doctor` and `repair` remain
  CLI-owned orchestration over lower-layer corrective primitives;
- recorded that active engine ownership currently stops at `Verify` and
  `GarbageCollect`, while `Repair*` and `Recover*` remain candidate-only
  contract shapes;
- defined the ownership decision table for corruption detection, metadata and
  payload verification, missing/orphan detection, derived-metadata rebuild,
  lineage or membership repair, storage/container index repair, recovery from
  surviving payloads, repair planning, repair execution, and rendering;
- froze `v1.13.9` scope so repair/recovery remain a docs-only boundary
  decision rather than a runtime activation phase;
- kept future engine repair/recovery ownership explicitly deferred until a later
  phase proves honest unsupported boundaries or a smaller activation path;
- kept storage rebuild and quarantine helpers classified as lower-layer
  primitives rather than public repair APIs;
- defined forbidden automatic repairs and corrective-path safety invariants,
  including no silent destructive repair, no hidden corruption masking, and no
  restore broadening;
- recorded PostgreSQL/backend compatibility constraints for any future
  corrective-path work; and
- preserved PR #103 as permanently closed with no new or reopened PR.

Phase 14 acceptance criteria:

- the `v1.13.9` tracker defines the exact Phase 14 title
  `Repair and Recovery Boundary Decision`;
- the `v1.13.9` tracker defines
  `v1.13.9-repair-recovery-boundary-decision.md` as the canonical Phase 14
  artifact;
- the checked-in artifact contains a current inventory, ownership decision
  table, in-scope versus out-of-scope decisions, and deferred follow-up scope;
- the tracker makes clear that Phase 14 does not implement runtime repair or
  recovery behavior;
- the tracker records Phase 15 as next and not started;
- no runtime code changes land in Phase 14; and
- no PR is opened or reopened.

Phase 14 is complete.

- Phase 14: complete
- Phase 15 is next and remains not started.

## Phase 15 — Repair and Recovery Boundary Honesty Scope Definition

**Status:** Complete

Phase 15 implementation:

- created the canonical Phase 15 artifact
  `v1.13.9-repair-recovery-boundary-honesty-scope-definition.md`;
- reconciled the checked-in `v1.13.9` tracker gap after Phase 14 left Phase 15
  authorized but not explicitly defined;
- defined Phase 15 itself as a docs-only tracker-definition pass rather than a
  runtime repair/recovery activation phase;
- froze the next implementation phase as
  `Repair and Recovery Boundary Honesty Proof`;
- limited the intended follow-up to explicit unsupported-boundary proof,
  CLI-ownership honesty, and lower-layer ownership evidence rather than
  corrective-path redesign;
- recorded that Phase 16 may touch CLI ownership tests, engine candidate-only
  boundary evidence, and release artifacts only if that later phase explicitly
  requires it;
- kept `doctor` and `repair` ownership unchanged as CLI-owned orchestration
  over lower-layer recovery, maintenance, verify, and storage/container
  primitives;
- kept engine `Repair*` and `Recover*` surfaces explicitly candidate-only and
  deferred;
- kept the current Build pass docs-only with no engine contract changes, no
  repair/recovery behavior changes, no verify changes, no CLI output changes,
  no schema or storage changes, and no workflow changes; and
- preserved PR #103 as permanently closed with no new or reopened PR.

Phase 15 acceptance criteria:

- the `v1.13.9` tracker defines the exact Phase 15 title
  `Repair and Recovery Boundary Honesty Scope Definition`;
- the `v1.13.9` tracker defines
  `v1.13.9-repair-recovery-boundary-honesty-scope-definition.md` as the
  canonical Phase 15 artifact;
- the `v1.13.9` tracker defines the next implementation phase after Phase 15
  as `Repair and Recovery Boundary Honesty Proof`;
- the `v1.13.9` tracker makes clear that Phase 15 itself is docs-only;
- the checked-in Phase 15 artifact limits later work to boundary-honesty or
  unsupported-surface proof rather than repair/recovery activation;
- no runtime code changes land in Phase 15; and
- no PR is opened or reopened.

Phase 15 is complete.

- Phase 15: complete
- Phase 16 is authorized and next.

## Phase 16 — Repair and Recovery Boundary Honesty Proof

**Status:** Complete

Phase 16 implementation:

- created the canonical Phase 16 artifact
  `v1.13.9-repair-recovery-boundary-honesty-proof.md`;
- strengthened active `Engine` interface tests to prove repair/recovery methods
  remain absent while snapshot create/delete/restore remain active;
- broadened the absent-method proof to cover future-only corrective names such
  as `SnapshotRepair`, `RepairPlan`, `SnapshotRecover`, and
  `RecoveryPlan`;
- preserved candidate-only repair/recovery request/result types as future
  contracts rather than active engine ownership;
- added CLI boundary proof that `doctor` does not construct an engine
  repair/recovery API and remains direct CLI orchestration over lower-layer
  recovery, verify, and audit seams;
- added CLI boundary proof that `repair` does not construct an engine
  repair/recovery API and remains direct CLI orchestration over lower-layer
  maintenance repair seams;
- tightened boundary comments so they describe these repair/recovery seams as
  intentional CLI-owned boundaries in `v1.13.9`;
- kept lower-layer recovery, maintenance, verify, and storage corrective
  helpers classified as internal primitives rather than public engine APIs;
- kept the current Build pass limited to proof-only test and docs work with no
  runtime repair/recovery, verify, CLI, schema, storage, or workflow changes;
  and
- preserved PR #103 as permanently closed with no new or reopened PR.

Phase 16 acceptance criteria:

- the `v1.13.9` tracker defines the exact Phase 16 title
  `Repair and Recovery Boundary Honesty Proof`;
- the `v1.13.9` tracker defines
  `v1.13.9-repair-recovery-boundary-honesty-proof.md` as the canonical
  Phase 16 artifact;
- the checked-in proof shows active `Engine` snapshot mutation methods remain
  present while repair/recovery engine methods remain absent;
- the checked-in proof shows `doctor` and `repair` remain CLI-owned rather
  than engine-routed corrective APIs;
- the checked-in proof shows lower-layer corrective helpers remain internal
  primitives rather than active engine contracts;
- no runtime repair/recovery, verify, snapshot, schema, storage-format, or
  workflow behavior changes land in Phase 16; and
- no PR is opened or reopened.

Phase 16 is complete.

- Phase 16: complete
- Phase 17 is authorized and next.

## Planned later phases

Later `v1.13.9` phases remain pending. They may address:

- the Phase 17
  `SQLite/PostgreSQL Compatibility Gate Hardening` follow-up after Phase 16;
- preview/live mutation semantics;
- retention-safety and restore/delete refusal boundaries;
- release-hardening and backend/parity follow-up for the now-routed restore CLI path;
- final release validation and handoff.
