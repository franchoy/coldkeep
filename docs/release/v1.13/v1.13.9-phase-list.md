# Coldkeep v1.13.9 Phase List

**Release:** `v1.13.9 — Snapshot Mutation Boundary: Create / Delete / Restore`
**Status:** Active
**Branch:** `release/v1.13.9`

## Phase 0A — Release-state closure and v1.13.9 activation

**Status:** Complete

Phase 0A:

- confirmed the working tree was clean before edits;
- confirmed the released `main` baseline at `584d6e8db8f54f4fdf5f4785aad698c71cad66dd`;
- created `release/v1.13.9` from the released `main` state;
- inspected the annotated `v1.13.8` tag;
- inspected recent `main` history;
- verified PR #101 merge state and required PR checks;
- verified the published `v1.13.8` GitHub release;
- verified that the remote `release/v1.13.8` branch was deleted;
- reconciled the active `v1.13.8` release records to released state;
- created the `v1.13.9` scope, phase list, and validation checklist;
- changed the source-reported version from `1.13.8` to `1.13.9`.

The only allowed observable code-behavior change in Phase 0A is the version
reporting change to `1.13.9`.

## Phase 0B — Restore destination trust-boundary correction

**Status:** Complete

Phase 0B addresses inherited finding
`CK-1139-P0-01 — Outer symlink-prefix restore compatibility`.

Phase 0B implementation:

- replaced full absolute-path symlink rejection with explicit trusted-root
  validation plus below-root symlink checks;
- added storage-level `TrustedRoot` propagation without changing public CLI or
  engine restore contracts;
- aligned by-ID live restore destination derivation with dry-run semantics;
- updated snapshot restore planning/execution to propagate trusted roots for
  original, prefix, and override modes;
- removed test-only `EvalSymlinks` masking from restore success-path helpers;
- added outer-alias and inner-symlink coverage across path-safety, storage,
  engine, and snapshot restore tests.

## Phase 0C — Stored-path batch compatibility correction

**Status:** Complete

Phase 0C closes inherited findings:

- `CK-1139-P0-02 — Stored-path fail-fast summary compatibility`
- `CK-1139-P0-03 — All-invalid stored-path initialization ordering`

Phase 0C implementation:

- removed request-length summary normalization from stored-path batch remove;
- made stored-path batch summaries derive only from emitted item results;
- added engine-owned preflight classification for stored-path batch remove;
- returned terminal invalid-only stored-path batch results before repository
  initialization;
- preserved CLI routing, JSON payload keys, human output, and request/result
  contract shapes while restoring pre-v1.13.8 invalid-batch initialization
  ordering.

## Phase 0D — Corrective CI enforcement and baseline freeze

**Status:** Active

Phase 0D closes inherited finding:

- `CK-1139-P0-04 — Required cross-platform enforcement`

Phase 0D local implementation:

- made `cross-platform` an explicit dependency of `ci-required`;
- made the required-gate result assertion reject non-success cross-platform
  results;
- expanded native cross-platform workflow coverage to include pathsafe,
  storage, engine, and snapshot restore alias/trusted-root tests;
- added local CI-audit enforcement for cross-platform dependency and success
  assertion;
- added audit regression tests for missing `cross-platform` dependency and
  missing required-result enforcement;
- collected local focused, full normal, full race, vet, lint, and local
  script-gate evidence for the corrective baseline.

Phase 0D remains open until native GitHub CI, Codacy, CodeQL, and any other
required remote evidence are verified and recorded.

## Planned later phases

Later `v1.13.9` phases remain pending. They may address:

- snapshot create boundary definition and routing;
- snapshot delete boundary definition and routing;
- snapshot restore boundary definition and routing;
- preview/live mutation semantics;
- retention-safety and restore/delete refusal boundaries;
- final release validation and handoff.
