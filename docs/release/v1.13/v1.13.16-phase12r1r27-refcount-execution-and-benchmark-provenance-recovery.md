# Phase 12R1R27 — Refcount execution and benchmark provenance recovery

**Mode:** BUILD
**Status:** Authorized — local implementation and bounded recovery only
**Base:** `98b90c0fa8e90032b8294d9cd87e6d7b3197f507`

## Recovery disposition

R1R26 rejected candidate push readiness on two independent evidence defects:
the four attached/detached plain/AES-GCM refcount matrix requirements skipped
because `COLDKEEP_LONG_RUN` was unset, and the detached benchmark bundle
recorded a hard-coded image digest that contradicted the service it used.
The retained integrity results, informational timing observations, six CK-014
event profiles, 22 required test records, and unaffected gates remain
requirement-specific evidence. Historical records remain immutable, including
the skipped invocations and erroneous provenance fields.

The benchmark defect is a distinct, unnumbered release-tooling/provenance root
owned by this recovery. It is not absorbed into CK-V11316-014, does not reopen
CK-V11316-015, and changes no finding number or counter. The archive remains an
auditable evidence corpus; omission of a complete offline rebuild environment
is `NON_BLOCKING_OFFLINE_REPRODUCTION_LIMITATION`.

## Exact implementation contract

The benchmark producer must establish and retain:

```text
OBSERVED_DATABASE_SERVICE
= PROVENANCE_DOCUMENT_SERVICE
= EFFECTIVE_BENCHMARK_CONNECTION
```

The collector must fail closed while observing the running Docker container,
its configured immutable pull reference and RepoDigests, the registry pull
descriptor, selected platform manifest, local image configuration, mapped
PostgreSQL endpoint, and actual server version. It must reject a remote Docker
context as proof of a local-loopback mapping. Its credential-free records must
be retained and checksummed so later validation needs neither the live
container nor fresh registry access. Collection and the required post-run
identity comparison occur outside measured benchmark intervals.

New aggregates use evidence policy v3 and release bundles use bundle format
v2. Current local, hosted, runner-reuse, and bundle acceptance must require
the new provenance. Historical policy-v2/v1 bundles remain checksum-auditable
historical evidence only. No current path may fall back to historical
acceptance. Workloads, four profiles, two integrity samples, command ceilings,
advisory timing policy, candidate/binary binding, and atomic promotion remain
unchanged.

The local checklist must set `COLDKEEP_LONG_RUN=1` on every designated
refcount invocation itself. After the final implementation SHA is frozen,
exactly four serialized attached/detached plain/AES-GCM executions run the
unchanged `TestRefCountContainmentStressMatrix` with its normal 25 iterations
and all four source-defined children. Two complete new benchmark bundles are
then generated, one attached and one fresh-detached. No historical timing or
provenance file is transplanted.

## Commit and execution boundary

Exactly two append-only local commits are authorized:

1. `docs: authorize v1.13.16 refcount and benchmark provenance recovery`
2. `fix: bind benchmark evidence to observed database execution identity`

The first commit changes exactly the eight authority paths recorded in the
source/test allowlist. The second changes only the eleven implementation paths
recorded there. Deterministic RED evidence remains external; no artificial
missing-helper RED or third evidence commit is permitted.

After commit 2, run only the affected attached/fresh-detached tooling,
governance, identity, four refcount, and two bundle recovery scope. Preserve
unaffected R1R25 evidence by its governed-byte and prerequisite applicability.
Any mandatory test, capture, checker, provenance, benchmark, or validation
failure stops without automatic repair or retry.

No push, hosted execution, finding closure/numbering, product/schema/
application-test/module/dependency change, full Profile A restart, tag,
release, Phase 13 work, or extra commit is authorized.

```text
CK_V11316_014: OPEN
CK_V11316_015: CLOSED_CERTIFIED
CK_V11316_007: OPEN_UNCHANGED
FINDINGS_CONFIRMED: 15
FINDINGS_CLOSED: 13/15
CK014_CANDIDATE_PUSH_READINESS: REJECTED_PENDING_SEPARATE_ACCEPTANCE_AUDIT
V1_X_TECHNICAL_CORRECTNESS_CLOSURE: WITHHELD
PHASE_13: NOT_AUTHORIZED
```
