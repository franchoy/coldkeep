# Coldkeep v1.13.13 Coordination Platform Evidence Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `5 — Repository Coordination Audit`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `5a4f1f76ee807b5cf4075782d2a13c4e37bdbf67`

An unexecuted platform-specific subprocess case is not marked as a pass. The
frozen claim requires native runtime on Linux, macOS, and Windows, with
representative Linux subprocess proof for process death and live GC.

| Invariant/test | Linux | macOS | Windows | PostgreSQL | Same-process/subprocess | Production path | Explicit bound | Classification | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Canonical identity and aliases | Local tests pass | Portable source; hosted package coverage | Portable source plus drive-letter normalization | N/A | Same-process | `ResolveIdentity` | Cooperative legitimate aliases | PROVEN | Relative, absolute, `..`, separators, symlinks, and trailing separators converge; native artifact remains authoritative for case aliases. |
| Control namespace | Local tests pass | Portable source/hosted lifecycle | Portable source/hosted lifecycle | N/A | Same-process | `PrepareControlNamespace` | Local filesystems only | PROVEN | Final re-resolution rejects identity change. |
| Process reservation | Local race tests pass | Shared Go implementation | Shared Go implementation | N/A | Same-process | production global registry | Identity-hash keyed, non-reentrant | PROVEN | Separate Coordinator objects share the registry. |
| Native primitive | `flock`, local runtime pass | Darwin `flock`, exact-head hosted runtime required | `CreateFile` + `LockFileEx`, exact-head hosted runtime required | N/A | Same-process/native handles | production native lock | Supported local filesystems | PROVEN_WITH_EXPLICIT_BOUND | Mechanisms differ; outcome is exclusive and fail-fast. |
| Production Coordinator lifecycle | Local Linux pass | Exact-head hosted pass required | Exact-head hosted pass required | N/A | Same-process | `NewCoordinator` | Supported native OSes | PROVEN_WITH_EXPLICIT_BOUND | Acquisition, metadata, release, and reacquisition use production components. |
| Owner diagnostics | Local pass | Portable source/hosted Coordinator | Portable source/hosted Coordinator | N/A | Same-process | `owner.json` publication/removal | Diagnostic, never authority | PROVEN | Windows replacement may transiently remove the diagnostic while native lock remains held. |
| CLI outer lifecycle | Local Linux/race pass | Shared source | Shared source | Current production session opened inside lease | Same-process | `executeCLICommand` | Participating commands only | PROVEN except finding | Recovery, dispatch, session cleanup, then lease release; `simulate gc` is the one bypass. |
| Independent-process contention | Exact-head G6 hosted pass required; historical proof exists | Not separately executed | Not separately executed | Required G6 service | Subprocess | real holder plus real CLI contender | Representative Linux proof | PROVEN_WITH_EXPLICIT_BOUND | Busy contender writes no logical-file row and successor succeeds. |
| Killed-holder release | Exact-head G6 hosted pass required; historical proof exists | Native handle lifecycle only | Native handle lifecycle only | Required by fixture, not by lock primitive | Subprocess | production Coordinator holder | Linux subprocess proof; kernel semantics on supported OSes | PROVEN_WITH_EXPLICIT_BOUND | Stale owner remains diagnostic; first successor reacquires. |
| Live-GC repository exclusion | Exact-head G6 hosted pass required | Not separately executed | Not separately executed | PostgreSQL live GC | Subprocess | real CLI GC and Store | Linux/PostgreSQL representative proof | PROVEN_WITH_EXPLICIT_BOUND | Outer native lease excludes Store while GC is server-observed in preflight. |
| Advisory same-session ownership | Local infrastructure unavailable; exact-head hosted pass required | Not separately executed | Not separately executed | Four required maintenance tests | Same-process, multiple DB sessions | production `RunGCWithDB` | PostgreSQL/Linux hosted evidence | PROVEN_WITH_EXPLICIT_BOUND | Acquisition and unlock backend PID match; uncertain sessions are discarded. |
| SQLite supported coordination | Local SQLite refusal/dry-run tests pass | Shared source | Shared source | N/A | Same-process | outer native lease plus SQLite transactions | Live GC explicitly refused | PROVEN_WITH_EXPLICIT_BOUND | No daemon serialization is required in v1. |
| Fail-fast errors | Local source/race tests pass | Shared source/hosted native | Shared source/hosted native | Advisory contention explicit | Same-process and subprocess | CLI stable error classifier | No wait/retry/shared mode | PROVEN | No fallback to weaker coordination. |
| Unsupported platform | N/A | N/A | N/A | N/A | Build-specific | `native_lock_unsupported.go` | FreeBSD/other unclaimed OSes | PROVEN_WITH_EXPLICIT_BOUND | Returns `ErrRepositoryLockUnsupported`; no runtime claim. |
| Cross-host/network filesystem | Not claimed | Not claimed | Not claimed | Advisory lock does not broaden repository identity | Cross-host excluded | No v1 production surface | v3 expansion | PROVEN_WITH_EXPLICIT_BOUND | NFS, SMB, NAS, cloud sync, and distributed operation remain post-v1. |

## Local evidence summary

| Evidence file | Command family | Pass events | Skip events | Fail events |
| --- | --- | ---: | ---: | ---: |
| `/tmp/coldkeep-phase5-evidence/core.json` | full coordination, CLI, and maintenance packages | 783 | 34 | 0 |
| `/tmp/coldkeep-phase5-evidence/focused-race.json` | coordination/lifecycle/native/SQLite/advisory selectors under `-race` | 128 | 5 | 0 |
| `/tmp/coldkeep-phase5-evidence/adversarial-local.json` | three G6 process selectors under `-race` | package PASS | 3 | 0 |

The core 34 skips are PostgreSQL/infrastructure-gated maintenance and
benchmark cases. The focused five are the named advisory-session tests. The
three G6 process tests skip because the configured PostgreSQL test gate is
absent. `pg_isready` at `127.0.0.1:5432` returned no response; Phase 5 did not
install or start a database.
