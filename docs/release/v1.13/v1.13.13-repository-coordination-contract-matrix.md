# Coldkeep v1.13.13 Repository Coordination Contract Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `5 — Repository Coordination Audit`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `5a4f1f76ee807b5cf4075782d2a13c4e37bdbf67`

This matrix classifies every frozen `V1R-COORD-*` requirement from current
source. Historical completion evidence supplements, but does not replace, the
source trace. Requirements are not expanded to hostile same-user interference,
network filesystems, or identical subprocess test shapes on every platform.

| Requirement ID | Coordination invariant | Claimed scope | Production implementation | Failure mode | Release behavior | Platform/backend | Tests | Evidence strength | Finding | Classification | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `V1R-COORD-001` | Same-process, same-host, local-filesystem repository coordination | Cooperative Coldkeep processes using one supported local repository | `internal/coordination`; top-level CLI `WithLease`; application caller contract | Native contention fails fast; repeated in-process acquisition is explicit; `simulate gc` bypasses the lease | Outer lease normally spans recovery, session/Engine work, cleanup, and native release | Linux, macOS, Windows; current production PostgreSQL repository | identity, registry, native, Coordinator, CLI lifecycle, and Linux process tests | Strong except one production operation-coverage defect | `P5-COORD-001` | **BLOCKER** | Shared `simulate gc` can open/bootstrap/migrate/read the repository without the outer lease. |
| `V1R-COORD-002` | Native Unix/Windows lock lifecycle and production Coordinator behavior | Supported native local filesystems; exclusive, fail-fast, non-reentrant | Unix `flock`; Windows `LockFileEx`; shared process registry; diagnostic owner metadata | Busy, unsupported, permission/path, invalid identity, and internal I/O remain distinguishable | Idempotent; owner removal, native unlock/close, then registry release; native errors remain authoritative | Linux/Darwin source; Windows source; hosted native runtime required on all three | full coordination package; focused race; cross-platform named selectors | Current source plus local Linux and hosted cross-platform runtime | — | **PROVEN** | Different native mechanisms provide the same high-level contract. |
| `V1R-COORD-003` | Independent-process contention, killed-holder release, live-GC exclusion, and PostgreSQL advisory-session ownership | Representative Linux subprocess proof; native lifecycle on all supported OSes; PostgreSQL live GC | Persistent native lock artifact; kernel-owned handles; Linux G6 subprocess tests; dedicated `*sql.Conn` advisory owner | Contender receives Busy before mutation; session/handle loss releases kernel/server ownership; uncertain advisory cleanup discards session | Explicit release or automatic process/session cleanup; successor reacquires | Linux subprocess/PostgreSQL; macOS/Windows native runtime without duplicate subprocess shape | G6 contention, killed holder, live GC; four required advisory-session tests | Strong within explicitly reaffirmed platform/backend bounds | — | **PROVEN_WITH_EXPLICIT_BOUND** | macOS/Windows subprocess duplication and SQLite live GC are not claimed. |
| `V1R-COORD-004` | Cross-host, NFS, SMB, NAS, multi-daemon, cloud, replication, and distributed coordination | Post-v1 only | No v1 implementation; native repository lease is path/artifact scoped; PostgreSQL advisory lock is only an inner GC barrier | Unsupported scope must not be presented as safe | Not applicable to v1 release lifecycle | Network/distributed filesystems and cross-machine deployments | Documentation/source boundary and unsupported-platform implementation | Authoritative scope proof | — | **PROVEN_WITH_EXPLICIT_BOUND** | Correctly classified `V3_NETWORK_EXPANSION`; absence is not a v1 blocker. |

## Relevant cross-platform requirements

| Requirement ID | Classification | Evidence boundary |
| --- | --- | --- |
| `V1R-XPLAT-001` | **PROVEN_WITH_EXPLICIT_BOUND** | Linux/macOS/Windows native and production Coordinator runtime are required at the exact Phase 5 SHA; representative independent-process, killed-holder, and live-GC subprocess evidence is Linux-only by the frozen claim. |
| `V1R-XPLAT-003` | **PROVEN_WITH_EXPLICIT_BOUND** | Unsupported platforms fail through `ErrRepositoryLockUnsupported`; FreeBSD remains optional future scope and receives no runtime claim. |

`V1R-XPLAT-002` remains the Phase 3 restore/path requirement and is not
reclassified by this coordination audit.
