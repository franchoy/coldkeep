# Coldkeep v1.13.10 Release Gate

**Release:** `v1.13.10 — v1.x Closure Integrity and CI Runtime Hygiene`
**Status:** Passed — awaiting publication

## Release identity

- Version: `v1.13.10`
- Title: `v1.x Closure Integrity and CI Runtime Hygiene`
- Branch: `release/v1.13.10`
- Execution date: `2026-07-18` in Europe/Madrid
- Source version: `1.13.10`
- Lifecycle state: `pre-release`

The exact candidate commit is the commit containing this gate artifact and is
reported by Git and the release pull request evidence. This document does not
guess or embed its own commit SHA.

## Scope closure

Phases 0–7 closed the v1.13.9 release record, activated the v1.13.10 identity,
reconciled the release train, corrected engine-contract documentation, added
and enforced the release-state validator, migrated CI artifact uploads to the
Node 24-compatible action release, and froze the v1.x/v2.0 handoff. Phase 8
completed the final local pre-release gate and release-state transition.

No runtime feature, schema, migration, storage format, repository format, or
backend-default change was introduced by Phase 8.

## Phase completion

Phases 0 through 8 are complete. The active scope, phase list, validation
checklist, release train, root README, release README, and changelog agree that
v1.13.10 is ready for release. No phase remains `Next` or `Not started`.

## Source and behavior summary

- Commit `8e932b0` activated source, test, and pre-release-checklist version
  identity `1.13.10`.
- Commit `d7df4e7` corrected engine ownership comments and test terminology
  without changing interfaces, assertions, or runtime behavior.
- Commit `0a2592e` implemented CKRS001–CKRS019, isolated fixture tests, blocking
  CI release-state validation, and CI-audit enforcement.
- Commit `53f8ad7` migrated exactly five artifact uploads to
  `actions/upload-artifact@v7` without changing artifact behavior.
- Commit `43ae85f2` corrected PostgreSQL release-gate test fixtures only: the
  legacy migration assertion now expects its required compatibility mapping and
  the shared fixture binds a Go boolean. It changed no runtime, schema,
  migration, storage, or repository behavior.

## Documentation and release-state validation

The complete local gate runs `git diff --check`, documentation guards, the
44-test release-state fixture suite, lifecycle validation, and exact authorized
diff and tag reviews. The final local result is `pre-release` with active
version `1.13.10` and zero violations.

## Go quality and test evidence

The complete local gate runs module consistency, tracked-Go formatting,
ShellCheck, golangci-lint, go vet, builds, deterministic chunk coverage, and
plain and AES-GCM package race matrices.

## Backend and migration evidence

PostgreSQL 16 is provisioned through the repository Compose service. The gate
runs short, stress, long-run, refcount-containment, adversarial, smoke, legacy
compatibility, deterministic G6, and focused G14–G17 coverage under their
documented codec conditions. Existing SQLite migration evidence remains covered
by the package suites; no broad dual-runtime parity claim is made.

## CLI contract evidence

The gate builds the CLI and verifies exact human and JSON version output for
`1.13.10`, then runs plain and AES-GCM smoke validation and the snapshot
release gate.

## Benchmark and coverage evidence

The gate runs the required AES-GCM small-dataset benchmark matrix for `none`
and `zstd` at workers 1 and 4, checking each result against its v1.9 baseline.
One identical rerun is permitted only for an initial hard threshold failure;
any repeated or inconsistent hard failure blocks this release. Critical
coverage is report-only and adds no invented threshold.

## CI and workflow evidence

`scripts/audit_ci_enforcement.sh --local-only` verifies required job
aggregation, release-state enforcement, cross-platform coverage, PostgreSQL
adversarial enforcement, exact upload-artifact v7 usage, and CodeQL workflow
structure. Local Linux path-safety and storage-restore selectors supplement,
but do not replace, native PR CI on macOS and Windows.

## Known non-blocking deferrals

Early v2.0 retains explicit decisions for `StoreRequest` narrowing or
splitting, snapshot read-side ownership and result shapes, repair/recovery
activation, daemon operation classes, serialization and barriers,
restore-versus-GC coordination, emergency direct-engine policy, SQLite-first
default transition, job identity and cancellation, and repository locking.

Later v2.x retains local daemon/API/UI/scheduling and other local
productization. LAN/NAS, centralized server mode, multi-user authorization,
cloud/object storage, distributed repositories, replication, and cross-machine
coordination remain v3.x or later.

## External post-PR and post-tag evidence

The following evidence does not yet exist and is not claimed locally:

- pull-request CI, CodeQL, or Codacy result;
- pull-request merge evidence or main-branch validation;
- annotated `v1.13.10` tag or tag/release workflow result;
- GitHub release publication or timestamp; and
- remote release-branch deletion.

These facts must be recorded only after the corresponding external operations
complete. No tag or publication is authorized by this gate.

## Pull-request authorization

The `release/v1.13.10` branch is authorized for one pull request to `main`.
No tag or release publication is authorized until the pull request is merged
and the required main-branch validation is green.

## Final pre-release verdict

`READY FOR SINGLE RELEASE PR`

## Final verdict

`READY FOR SINGLE RELEASE PR`
