# Coldkeep v1.13.3 — Release Gate

## Release metadata

- release: `v1.13.3 — Read-Side Contract Cleanup`
- branch: `release/v1.13.3`
- release type: `read-side contract cleanup / docs-tests release gate`
- final status: `ready for PR review`
- date of validation: `2026-06-12`
- validated branch head before gate-doc update:
  `f239f2938918c958ad1ef6dff780bd1c44936277`

## Historical gate record

- original blocked gate commit: `7f2cf18`
- original blocker: repo-wide `gofmt` audit reported
  `cmd/coldkeep/read_side_cli_preservation_test.go` as unformatted
- formatting remediation commit: `dd5e7eb`
  (`test: format read-side CLI preservation tests`)
- first rerun blocked-gate doc commit: `39edd0b`
- first rerun blocker: PostgreSQL catalog contract tests shared one configured
  database and contaminated each other under the broader gate run
- PostgreSQL blocker-remediation commit: `c807123`
  (`fix: stabilize PostgreSQL catalog contract checks`)
- second rerun blocked-gate doc commit: `c00bf1a`
- second rerun blocker: `TestAdversarialG6ConcurrentStoresSameFileConvergeDeterministically/aes-gcm`
  failed under `COLDKEEP_LONG_RUN=1 go test -race -count=1 ./tests/adversarial/...`
- concurrent-store blocker-remediation commit: `f239f29`
  (`fix: stabilize concurrent store convergence`)

## Final release statements

- `v1.13.3` remains documentation-complete and test-hardening complete through
  Phase 9.
- Phase 10 reran from the beginning on remediation commit `f239f29`.
- The rerun progressed beyond the earlier formatting, PostgreSQL isolation, and
  G6 adversarial blockers.
- `PRE_RELEASE_CHECKLIST.md` Profile A sections 1-3 and final local checks were
  executed.
- The benchmark matrix completed. `workers=1` baselines passed. The local
  `workers=4` uncompressed regression check reported a hard fail and the local
  `workers=4` compressed regression check reported warnings. Per the checklist's
  local-variance guidance for virtualized hardware, those `workers=4` results
  are recorded as local environment variance rather than a release blocker.
- `v1.13.3` is ready for PR review.
- No production Go behavior changed.
- No CLI behavior changed.
- No human output changed.
- No JSON output changed.
- No JSON error envelope changed.
- No exit code changed.
- No command routing changed.
- No engine routing changed.
- No schema or migration changed.
- No storage or repository format changed.
- No backend default changed.
- No script or CI file changed.
- The only repo-tracked non-doc changes since the prior blocked rerun are:
  - test-only PostgreSQL contract-backend isolation in
    `internal/catalog/backend_contract_backends_test.go`
  - internal concurrent-store correctness hardening in `internal/storage/store.go`
  - matching storage regression coverage in `internal/storage/store_test.go`
  - adversarial test isolation in
    `tests/adversarial/g6_concurrent_operations_adversarial_test.go`

## Release artifacts

Release docs:

- `docs/release/v1.13/v1.13.3-scope.md`
- `docs/release/v1.13/v1.13.3-phase-list.md`
- `docs/release/v1.13/v1.13.3-validation-checklist.md`
- `docs/release/v1.13/v1.13.3-read-side-contract-inventory.md`
- `docs/release/v1.13/v1.13.3-stats-contract-map.md`
- `docs/release/v1.13/v1.13.3-inspect-contract-map.md`
- `docs/release/v1.13/v1.13.3-snapshot-show-contract-map.md`
- `docs/release/v1.13/v1.13.3-snapshot-diff-contract-map.md`
- `docs/release/v1.13/v1.13.3-backend-dependency-review.md`
- `docs/release/v1.13/v1.13.3-read-side-contract-summary.md`
- `docs/release/v1.13/v1.13.3-v1.13.4-handoff.md`
- `docs/release/v1.13/v1.13.3-release-gate.md`

Key test artifacts:

- `internal/engine/read_side_taxonomy_boundary_test.go`
- `internal/observability/read_side_taxonomy_boundary_test.go`
- `cmd/coldkeep/read_side_taxonomy_boundary_test.go`
- `cmd/coldkeep/read_side_cli_preservation_test.go`
- `internal/catalog/backend_contract_backends_test.go`
- `internal/storage/store_test.go`
- `tests/adversarial/g6_concurrent_operations_adversarial_test.go`

## Validation results

| Command | Result | Notes |
| --- | --- | --- |
| `git status -sb` | passed | clean start on `release/v1.13.3` before the rerun |
| `git branch --show-current` | passed | branch confirmed as `release/v1.13.3` |
| `git fetch origin` | passed | branch sync rechecked |
| `git log --oneline --decorate -n 16` | passed | rerun started from head `f239f29` |
| `git diff --check` | passed | pre-gate and final local checks clean |
| `git ls-files '*.go' \| xargs gofmt -l` | passed | repo-wide gofmt audit returned no files |
| `go vet ./...` | passed | rerun pre-gate vet clean |
| `golangci-lint run ./...` | passed | rerun pre-gate lint clean; `0 issues.` |
| `go test -count=1 ./cmd/coldkeep` | passed | targeted read-side rerun |
| `go test -count=1 ./internal/engine` | passed | targeted read-side rerun |
| `go test -count=1 ./internal/observability` | passed | targeted read-side rerun |
| `go test -count=1 ./internal/snapshot` | passed | targeted read-side rerun |
| `go test -count=1 ./internal/catalog` | passed | targeted read-side rerun |
| `go test -count=1 ./internal/cli/...` | passed | targeted read-side rerun |
| `go test -count=1 ./...` | passed | full rerun suite passed |
| `go test -race -count=1 ./...` | passed | full rerun race suite passed |
| `docker compose up -d coldkeep_postgres` | passed | PostgreSQL service running for Profile A |
| `docker compose exec -T coldkeep_postgres pg_isready -U coldkeep -d coldkeep` | passed | PostgreSQL accepting connections |
| `docker compose exec -T coldkeep_postgres psql -U coldkeep -d coldkeep -c "SELECT 1;"` | passed | PostgreSQL reachability confirmed |
| `bash scripts/clean_test_storage.sh` | passed | section 2 |
| `go mod tidy && git diff --exit-code` | passed | section 2; no repo drift |
| `bash -n scripts/*.sh` | passed | section 2 |
| `bash scripts/check_smart_quotes.sh` | passed | section 2 |
| `shellcheck $(find scripts/ -maxdepth 1 -name '*.sh' ! -name 'critical_coverage.sh')` | passed | section 2 |
| `scripts/validate_validation_matrix.sh` | passed | section 2 |
| `bash scripts/check_versioned_row_writers.sh` | passed | section 2 |
| `COLDKEEP_CODEC=plain go test -race -count=1 ./cmd/... ./internal/...` | passed | section 2 |
| `COLDKEEP_CODEC=aes-gcm COLDKEEP_KEY="$COLDKEEP_KEY" go test -race -count=1 ./cmd/... ./internal/...` | passed | section 2 |
| `go test -race -count=1 ./internal/chunk/benchmark -run 'TestFastCDCBetterThanV1_SmallModifications\|TestFastCDCBetterThanV1_ShiftedData\|TestChunkerDeterminism\|TestChunkerDeterminism_RunDatasetTwice\|TestChunkCoverage\|TestDefaultDatasetsDeterministicAcrossCalls'` | passed | section 2 |
| `go build ./...` | passed | section 2 |
| `scripts/audit_ci_enforcement.sh --local-only` | passed | section 2 |
| `go build -o coldkeep ./cmd/coldkeep` | passed | section 2 |
| `go test -race -count=1 -short ./tests/integration/...` | passed | section 3; both codecs |
| `go test -race -count=1 ./tests/integration/...` | passed | section 3; both codecs |
| `COLDKEEP_LONG_RUN=1 go test -race -count=1 ./tests/integration/... -run 'TestStoreGCVerifyRestoreDeleteLoopStability\|TestRandomizedLongRunLifecycleSoak\|TestSnapshotRetentionChurnLongRun'` | passed | section 3; both codecs |
| `go test -race -count=1 ./tests/integration/... -run 'TestRefCountContainmentStressMatrix'` | passed | section 3; both codecs |
| `COLDKEEP_LONG_RUN=1 go test -race -count=1 ./tests/adversarial/...` | passed | section 3; both codecs |
| `go test -race -count=1 ./tests/adversarial/... -run 'TestAdversarialG14\|TestAdversarialG15\|TestAdversarialG16\|TestAdversarialG17'` | passed | section 3; both codecs |
| `scripts/smoke.sh` with `COLDKEEP_SMOKE_RESET_DB=1`, `COLDKEEP_STORAGE_DIR="$PWD/.ci-storage/<codec>"`, and `COLDKEEP_SMOKE_SCHEMA_MESSAGE_GATE=1` | passed | section 3 smoke matrix for `plain` and `aes-gcm` |
| `./coldkeep benchmark run --dataset small --workers 1 --output json` | passed | benchmark matrix; uncompressed |
| `./coldkeep benchmark run --dataset small --workers 4 --output json` | passed | benchmark matrix executed; result later classified as local variance |
| `COLDKEEP_COMPRESSION=zstd ./coldkeep benchmark run --dataset small --workers 1 --output json` | passed | benchmark matrix; compressed |
| `COLDKEEP_COMPRESSION=zstd ./coldkeep benchmark run --dataset small --workers 4 --output json` | passed | benchmark matrix executed; result later classified as local variance |
| `python3 scripts/validate_regression_thresholds.py check benchmark-none-w1.json ... --mode uncompressed` | passed | workers=1 local baseline clean |
| `python3 scripts/validate_regression_thresholds.py check benchmark-none-w4.json ... --mode uncompressed` | completed with local hard fail | treated per checklist as non-blocking local `workers=4` variance on virtualized hardware |
| `python3 scripts/validate_regression_thresholds.py check benchmark-zstd-w1.json ... --mode compressed` | passed | workers=1 local baseline clean |
| `python3 scripts/validate_regression_thresholds.py check benchmark-zstd-w4.json ... --mode compressed` | completed with warnings | treated per checklist as non-blocking local `workers=4` variance |
| `go test -race -count=1 ./tests/integration/... -run 'TestPhase2PostMigrationStoreRestoreSnapshotRegressionIntegration'` | passed | legacy compatibility |
| `go test ./internal/pathsafe/... -count=1` | passed | cross-platform local approximation |
| `go test ./internal/storage -run 'RestoreCrossPlatform\|RestoreSeam' -count=1` | passed | cross-platform local approximation |
| `mkdir -p artifacts && scripts/critical_coverage.sh --report --csv-output artifacts/critical-coverage-report.csv` | passed | critical coverage report generated |
| `bash scripts/run_snapshot_release_gate.sh --count 1` | passed | snapshot contract gate |
| `bash scripts/audit_ci_enforcement.sh` | passed | full local + remote CI audit |
| `gh auth status` | passed | GitHub CLI authenticated as `franchoy` |
| `gh repo view` | passed | GitHub API access confirmed |

## PRE_RELEASE_CHECKLIST.md Profile A

Profile used:

- `Profile A - Pre-PR CI-parity gate`

Profile A status:

- `passed`

What happened:

- Historical blocked gate records remain at `7f2cf18`, `39edd0b`, and `c00bf1a`.
- The formatting blocker was remediated in `dd5e7eb`.
- The PostgreSQL catalog-contamination blocker was remediated in `c807123`.
- The concurrent-store adversarial blocker was remediated in `f239f29`.
- Phase 10 reran from the beginning on `f239f29`.
- Branch-state checks, formatting checks, targeted package tests, full suites,
  section 1, section 2, section 3, and final local checks completed.
- The benchmark matrix completed. `workers=1` results passed. Local
  `workers=4` variance was recorded and interpreted under the checklist's
  explicit local-variance policy.

Section status:

- Section 1: passed
- Section 2: passed
- Section 3: passed
- Final local checks: passed

Docker and PostgreSQL result:

- passed for section 1 environment setup and remained usable through the rerun

Benchmark matrix result:

- completed
- `workers=1`: passed
- `workers=4`: local variance recorded; not treated as a release blocker per
  checklist guidance

Smoke matrix result:

- passed for both codecs

Legacy compatibility result:

- passed

Cross-platform approximation result:

- passed

Audit and release-gate script result:

- `scripts/audit_ci_enforcement.sh --local-only`: passed
- `scripts/audit_ci_enforcement.sh`: passed
- `scripts/run_snapshot_release_gate.sh --count 1`: passed

## PR result

- PR creation is executed after this gate record is committed and pushed
- the PR URL is recorded in the follow-up Phase 10 closeout update

## Final decision

`ready for PR review`

Reason:

- The formatting blocker from `7f2cf18` was fixed in `dd5e7eb`.
- The PostgreSQL catalog-contract contamination blocker from `39edd0b` was
  fixed in `c807123`.
- The G6 concurrent-store blocker from `c00bf1a` was fixed in `f239f29`.
- The full rerun completed `PRE_RELEASE_CHECKLIST.md` Profile A.
- All required gate categories passed, and the benchmark matrix variance was
  recorded and interpreted per the checklist's explicit local-policy note.
