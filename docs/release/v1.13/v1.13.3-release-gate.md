# Coldkeep v1.13.3 — Release Gate

## Release metadata

- release: `v1.13.3 — Read-Side Contract Cleanup`
- branch: `release/v1.13.3`
- release type: `read-side contract cleanup / docs-tests release gate`
- final status: `blocked, not PR-ready`
- date of validation: `2026-06-12`
- validated branch head before gate-doc update:
  `d3d0f70d4056ef55f3b8df590891d7fdd55556f0`

## Final release statements

- `v1.13.3` remains documentation-complete and test-hardening complete through
  Phase 9.
- Phase 10 did not complete the final pre-PR gate.
- `v1.13.3` is not PR-ready.
- No production Go behavior changed.
- No CLI behavior changed.
- No human output changed.
- No JSON output changed.
- No JSON error envelope changed.
- No exit code changed.
- No command routing changed.
- No engine routing changed.
- No schema or migration changed.
- No storage or repository format changed.
- No backend default changed.
- SQLite compatibility expectations remain unchanged.
- PostgreSQL compatibility expectations remain unchanged.

## Release artifacts

Release docs:

- `docs/release/v1.13/v1.13.3-scope.md`
- `docs/release/v1.13/v1.13.3-phase-list.md`
- `docs/release/v1.13/v1.13.3-validation-checklist.md`
- `docs/release/v1.13/v1.13.3-read-side-contract-inventory.md`
- `docs/release/v1.13/v1.13.3-stats-contract-map.md`
- `docs/release/v1.13/v1.13.3-inspect-contract-map.md`
- `docs/release/v1.13/v1.13.3-snapshot-show-contract-map.md`
- `docs/release/v1.13/v1.13.3-snapshot-diff-contract-map.md`
- `docs/release/v1.13/v1.13.3-backend-dependency-review.md`
- `docs/release/v1.13/v1.13.3-read-side-contract-summary.md`
- `docs/release/v1.13/v1.13.3-v1.13.4-handoff.md`
- `docs/release/v1.13/v1.13.3-release-gate.md`

Key test artifacts:

- `internal/engine/read_side_taxonomy_boundary_test.go`
- `internal/observability/read_side_taxonomy_boundary_test.go`
- `cmd/coldkeep/read_side_taxonomy_boundary_test.go`
- `cmd/coldkeep/read_side_cli_preservation_test.go`

## Validation results

| Command | Result | Notes |
| --- | --- | --- |
| `git status -sb` | passed | clean start state: `## release/v1.13.3...origin/release/v1.13.3` |
| `git branch --show-current` | passed | current branch was `release/v1.13.3` |
| `git fetch origin` | passed after escalation | initial sandbox attempt failed with `cannot open '.git/FETCH_HEAD': Read-only file system`; rerun with approved escalation succeeded |
| `git status -sb` | passed | branch remained clean and synced after fetch |
| `git log --oneline --decorate -n 12` | passed | latest branch head before Phase 10 doc update was `d3d0f70` |
| `git diff --check` | passed | no formatting diff before gate updates |
| `gofmt -l $(git ls-files '*.go')` | failed via safe equivalent | rerun as `git ls-files '*.go' \| xargs gofmt -l`; output was `cmd/coldkeep/read_side_cli_preservation_test.go` |
| `go vet ./...` | passed | exit code `0` |
| `golangci-lint run ./...` | passed | `0 issues.` |

Blocked gate detail:

- blocked command: `gofmt -l $(git ls-files '*.go')`
- substitute attempted: `git ls-files '*.go' | xargs gofmt -l`
- substitute equivalence: equivalent format audit for the Phase 10 gate
- failure reason: the equivalent command reported an unformatted tracked file,
  `cmd/coldkeep/read_side_cli_preservation_test.go`
- release consequence: required formatting gate incomplete, so the release
  remains blocked and PR opening is not acceptable in Phase 10

Commands not run because the gate stopped at the required formatting blocker:

- targeted read-side package tests
- `go test -count=1 ./...`
- `go test -race -count=1 ./...`
- `PRE_RELEASE_CHECKLIST.md` Profile A section 1
- `PRE_RELEASE_CHECKLIST.md` Profile A section 2
- `PRE_RELEASE_CHECKLIST.md` Profile A section 3
- `bash scripts/audit_ci_enforcement.sh --local-only`
- `bash scripts/audit_ci_enforcement.sh`
- `bash scripts/run_snapshot_release_gate.sh --count 1`
- benchmark matrix
- smoke matrix
- legacy compatibility
- cross-platform local approximation
- PR creation

## PRE_RELEASE_CHECKLIST.md Profile A

Profile used:

- `Profile A - Pre-PR CI-parity gate`

Profile A status:

- `blocked before section 1`

What happened:

- Phase 10 started with the required branch-state confirmation and pre-gate
  formatting/static checks.
- `git diff --check`, `go vet ./...`, and `golangci-lint run ./...` passed.
- The required Go formatting audit failed because
  `cmd/coldkeep/read_side_cli_preservation_test.go` was reported as
  unformatted.
- Because a required gate step failed, Profile A was not executed further.

Section status:

- Section 1: not started due earlier required gate failure
- Section 2: not started due earlier required gate failure
- Section 3: not started due earlier required gate failure
- Final local checks after Profile A: not reached

Docker and PostgreSQL result:

- not attempted because the gate stopped before Profile A section 1

Benchmark matrix result:

- not attempted because the gate stopped before Profile A section 3

Smoke matrix result:

- not attempted because the gate stopped before Profile A section 3

Legacy compatibility result:

- not attempted because the gate stopped before Profile A section 3

Cross-platform approximation result:

- not attempted because the gate stopped before Profile A section 3

Audit and release-gate script result:

- not attempted because the gate stopped before Profile A sections 2-3

## PR result

- PR not opened
- reason: required Profile A gate incomplete after the formatting check failed
- `gh auth status`, `gh repo view`, and `gh pr create` were not attempted after
  the gate was blocked

## Final decision

`blocked, not PR-ready`

Reason:

- A required Phase 10 gate step failed.
- The equivalent `gofmt` audit reported
  `cmd/coldkeep/read_side_cli_preservation_test.go` as unformatted.
- Under the accepted Phase 10 policy, any required Profile A failure blocks PR
  opening and prevents Phase 10 from being marked complete.

Next step:

- reformat the reported Go file in a later implementation step
- rerun the full Phase 10 gate from a clean branch state
- only open the PR after the complete gate is green
