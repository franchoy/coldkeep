# v1.13.2 Phase List

## Phase 0 - Release Scope and Baseline Docs

Status: `complete`

Objective: create the `v1.13.2` baseline documents that lock scope, define the release phases,
and set validation expectations before any code, test, or taxonomy-primitive work begins.

Allowed changes:

- `docs/release/v1.13/` baseline release docs

Disallowed changes:

- Go code changes
- test changes
- script changes
- CI changes
- schema or migration changes
- CLI, JSON, or exit-code changes
- runtime or backend behavior work

Expected files:

- `docs/release/v1.13/v1.13.2-scope.md`
- `docs/release/v1.13/v1.13.2-phase-list.md`
- `docs/release/v1.13/v1.13.2-validation-checklist.md`

Validation commands:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" docs/release/v1.13/v1.13.2-scope.md docs/release/v1.13/v1.13.2-phase-list.md docs/release/v1.13/v1.13.2-validation-checklist.md`
- `git status -sb`

Acceptance criteria:

- the three `v1.13.2` baseline docs exist
- scope clearly states taxonomy baseline, not behavior migration
- `git diff --check` is clean
- no Go files change
- no tests change
- no scripts or CI files change

Completion note: complete. Files created: `docs/release/v1.13/v1.13.2-scope.md`,
`docs/release/v1.13/v1.13.2-phase-list.md`, and
`docs/release/v1.13/v1.13.2-validation-checklist.md`. This phase was docs-only. No runtime
behavior changed. No tests changed. No scripts changed. No CI changed. Next phase: Phase 1
error-surface inventory.

## Phase 1 - Current Error-Surface Inventory

Status: `pending`

Objective: inventory the current engine, catalog, and adjacent CLI error surfaces so later phases
can classify existing behavior from observed repo truth rather than assumptions.

Allowed changes:

- release docs
- inventory notes
- focused documentation updates tied to observed current behavior

Disallowed changes:

- broad runtime behavior changes
- new error framework implementation
- broad routing changes
- schema, format, or backend behavior changes

Expected files:

- `docs/release/v1.13/v1.13.2-phase-list.md`
- inventory artifacts created during Phase 1

Validation commands:

- `git status -sb`
- `git diff --check`
- targeted docs sanity checks for new inventory artifacts

Acceptance criteria:

- active and near-active error surfaces are explicitly inventoried
- unsupported and deferred boundaries are not conflated
- missing `errors.go` files, if still absent, are recorded honestly

## Phase 2 - Define Taxonomy Names and Meanings

Status: `pending`

Objective: define the baseline names and meanings for the `v1.13.2` taxonomy using the Phase 1
inventory as the source of truth.

Allowed changes:

- release docs
- taxonomy definition notes
- narrow comments or assertions if needed to anchor terminology

Disallowed changes:

- broad code-backed error refactors
- CLI behavior changes
- exit-code or JSON changes

Expected files:

- `docs/release/v1.13/` `v1.13.2` release docs
- narrowly scoped supporting notes or comments if required

Validation commands:

- `git status -sb`
- `git diff --check`
- targeted package tests if any Go files change

Acceptance criteria:

- taxonomy names are explicit and non-overlapping
- unsupported, deferred, validation, domain, invariant, and runtime meanings are distinct
- code-backed versus doc-backed semantics remain explicit

## Phase 3 - Engine Taxonomy Primitives

Status: `pending`

Objective: introduce narrow engine-level taxonomy primitives where current behavior is already
stable enough to classify without broadening runtime support.

Allowed changes:

- `internal/engine`
- narrowly scoped supporting packages
- focused engine tests
- release docs

Disallowed changes:

- broad engine routing
- new engine operations
- CLI contract changes
- schema, storage, or backend-default changes

Expected files:

- `internal/engine`
- targeted engine tests
- `docs/release/v1.13/` `v1.13.2` docs

Validation commands:

- `git status -sb`
- `git diff --check`
- `go test -count=1 ./internal/engine`
- additional targeted package tests as touched

Acceptance criteria:

- engine taxonomy primitives stay narrow and explicit
- `ErrNotImplemented` remains distinct from validation, domain, invariant, and runtime failures
- no new active runtime support is implied

## Phase 4 - Catalog Taxonomy Parity

Status: `pending`

Objective: align catalog deferred and near-active error semantics with the `v1.13.2` baseline
without claiming completion of deferred catalog ownership.

Allowed changes:

- `internal/catalog`
- focused catalog tests
- release docs

Disallowed changes:

- implementing deferred catalog graph, placement, restore-plan, or GC-plan behavior
- backend narrowing
- broad catalog refactors

Expected files:

- `internal/catalog`
- focused catalog contract tests
- `docs/release/v1.13/` `v1.13.2` docs

Validation commands:

- `git status -sb`
- `git diff --check`
- `go test -count=1 ./internal/catalog`
- additional targeted package tests as touched

Acceptance criteria:

- catalog deferred behavior remains explicit and honest
- catalog classification work stays parity-focused rather than implementation-expanding
- cross-backend expectations remain preserved

## Phase 5 - Validation vs Unsupported Boundary Tests

Status: `pending`

Objective: add focused protection proving malformed requests are treated as validation failures and
unsupported modes remain distinct unsupported boundaries.

Allowed changes:

- `internal/engine`
- `internal/catalog` if directly relevant
- focused tests
- release docs

Disallowed changes:

- broad request/result redesign
- CLI flag or output changes
- broad validation-ownership migration

Expected files:

- focused engine or catalog tests
- `docs/release/v1.13/` `v1.13.2` docs

Validation commands:

- `git status -sb`
- `git diff --check`
- `go test -count=1 ./internal/engine`
- `go test -count=1 ./internal/catalog`
- additional targeted package tests as touched

Acceptance criteria:

- malformed-request cases are distinguishable from unsupported modes
- unsupported boundaries do not degrade into generic validation failures
- current CLI behavior remains preserved

## Phase 6 - Domain vs Invariant vs Runtime Classification Inventory

Status: `pending`

Objective: record and, where stable, protect the distinctions between domain failures,
correctness or integrity failures, and runtime or environment failures.

Allowed changes:

- release docs
- focused tests where current classification can be protected safely
- narrow supporting code only where the existing behavior is already stable

Disallowed changes:

- broad invariant ownership migration
- storage or database behavior changes
- broad recovery or repair implementation work

Expected files:

- `docs/release/v1.13/` `v1.13.2` docs
- focused tests or narrow supporting code if needed

Validation commands:

- `git status -sb`
- `git diff --check`
- targeted package tests as touched

Acceptance criteria:

- domain, invariant, and runtime or environment failures are not conflated in release docs
- any code-backed distinctions are explicitly test-protected
- gaps remain documented honestly where classification is still only doc-backed

## Phase 7 - CLI Translation Preservation Tests

Status: `pending`

Objective: prove that taxonomy cleanup does not accidentally change current CLI-visible
translation, output, or exit-code behavior.

Allowed changes:

- `cmd/coldkeep`
- focused preservation tests
- narrow supporting comments
- release docs

Disallowed changes:

- new CLI features
- command flag changes
- JSON output changes
- exit-code changes

Expected files:

- `cmd/coldkeep`
- targeted CLI preservation tests
- `docs/release/v1.13/` `v1.13.2` docs

Validation commands:

- `git status -sb`
- `git diff --check`
- `go test -count=1 ./cmd/coldkeep`
- additional targeted package tests as touched

Acceptance criteria:

- taxonomy work preserves existing CLI translation behavior
- no accidental JSON or exit-code drift is introduced
- CLI-owned paths remain honest where ownership is still transitional

## Phase 8 - Documentation and v1.13.3 Handoff

Status: `pending`

Objective: summarize what `v1.13.2` classified, what remains doc-backed versus code-backed, and
how that prepares the read-side contract cleanup in `v1.13.3`.

Allowed changes:

- `docs/release/v1.13/`

Disallowed changes:

- starting `v1.13.3` implementation
- broad release-train redesign
- unrelated cleanup

Expected files:

- `docs/release/v1.13/` summary and gate docs for `v1.13.2`

Validation commands:

- `git status -sb`
- `git diff --check`
- docs sanity checks for `v1.13.2` artifacts

Acceptance criteria:

- the release docs clearly record what was classified
- code-backed versus doc-backed distinctions remain explicit
- the `v1.13.3` handoff is clear and does not overclaim completeness

## Phase 9 - Final Validation and Release Gate

Status: `pending`

Objective: run the final validation gate, record actual results honestly, and confirm whether the
`v1.13.2` taxonomy baseline is ready for review.

Allowed changes:

- release-gate documentation
- narrowly scoped fixes required to address real validation failures within approved release scope

Disallowed changes:

- unrelated cleanup disguised as gate work
- undocumented compatibility drift
- broad implementation growth beyond `v1.13.2` scope

Expected files:

- `docs/release/v1.13/` `v1.13.2` release-gate artifacts
- narrow in-scope fixes only if required by validation

Validation commands:

- `git status -sb`
- `git diff --check`
- `gofmt -l $(git ls-files '*.go')`
- `go vet ./...`
- `go test -count=1 ./...`
- `go test -race -count=1 ./...`
- `golangci-lint run ./...`
- `scripts/audit_ci_enforcement.sh`
- `PRE_RELEASE_CHECKLIST.md` Profile A

Acceptance criteria:

- the release gate records actual commands and actual outcomes
- unavailable validation, if any, is recorded honestly
- compatibility commitments remain preserved
- the final gate result is explicit and evidence-backed
