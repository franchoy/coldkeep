# Coldkeep v1.13.15 Release Gate

**Status:** Phase 5 local gate passed; hosted/publication gates pending
**Release readiness:** Not certified

## Blocking conditions

1. All 15 findings and four root invariants are reconciled with durable proof.
2. Go 1.26.7 and `GOTOOLCHAIN=local` are exact across release-critical jobs.
3. Ordinary `govulncheck@v1.7.0 ./...` reports no reachable vulnerability.
4. Windows native-string boundary tests pass without panic or truncation.
5. Existing unit, integration, race, backend, codec, compression, layout,
   fault, adversarial, and native-platform contracts pass.
6. The complete Python discovery suite passes.
7. Devcontainer safety/idempotence and official pinned product images pass.
8. Local source installation passes on Linux, macOS, and Windows.
9. Public exact-candidate installation passes before tagging.
10. Public `@v1.13.15` installation passes after tag CI and before publication.
11. v1.13.14 canonical identity remains byte-for-byte equal by identity hash.
12. The active tag ruleset matches raw `refs/tags/v*`, retains all four
    protections, and uses only authenticated `franchoy` User/`always` bypass.
13. The actual v1.13.15 tag rule suite proves the expected audited behavior.
14. Immutable releases are enabled before draft creation; the published tag
    and assets are GitHub-immutable, while title/body are Coldkeep-frozen by
    canonical evidence.
15. Phase 7 separately authorizes merge/tag/publication and Phase 10 proves
    zero unique commits before deleting both temporary branches.

## Publication order

```text
local source install
-> remote exact-candidate install
-> protected merge and exact-main proof
-> annotated tag
-> tag CI and actual rule-suite proof
-> remote @v1.13.15 install
-> final draft validation
-> immutable source-only publication
-> protected closure PR
-> zero-unique-commit branch cleanup
```

Any scope expansion, identity drift, unexpected dependency movement, private
security impact, registry ambiguity, governance mismatch, remote-install
failure, or unverifiable attestation is blocking and returns the train to Plan
mode.

## Final verdict

Pending.
