# Coldkeep v1.13.15 Release Gate

**Release:** v1.13.15 — Final v1.x Security, Reproducibility, and Operational Closure
**Status:** Passed — pre-publication prerequisites complete
**Branch:** recovery/v1.13.15-phase9-tag-certification-path

## Preserved merge and completed recovery boundaries

PR #115 merged candidate 90122c52172764a73e165b1bae63b2e54887839b
normally as protected merge 7c1e0466986aff156b3990c37f19fcd301639063,
with its ordered parents and reviewed tree preserved. PR #116 then merged the
bounded Phase 8R validator recovery normally as protected main
67a32788c0d7cf5ab02af7199b99d80236613977. Exact-main recertification passed,
so Phases 0–8 are Complete.

Phase 9R durably materialized the canonical 1,815-byte publication body at
SHA-256
477796fc1c44151ddc77825559c48876c49ab742540586a190abc2c878eea357.
It merged normally through PR #117, and exact protected main
5c6e37d065c163f7050e015762e1a4c657e7565a passed recertification. The lost
Phase 7 digest remains historical evidence and is not attributed to the
replacement body.

## Phase 9S tag-certification automation recovery

Phase 9S is a nonnumeric pre-tag recovery. It keeps the existing exact-SHA
public-install proof for non-tag events and adds a semantic-tag path that:

- uses the event tag name as the Go module and install query;
- independently resolves both the ordinary and peeled tag refs from the public
  repository without checkout;
- rejects lightweight tags by requiring the peeled ref;
- binds Go module Origin.Hash to the independently peeled commit;
- proves the semantic module version and installed binary build metadata; and
- runs on Linux, macOS, and Windows.

CodeQL also gains the same `v*` push trigger while retaining main, release,
PR, schedule, dispatch, all three languages, and its aggregate gate. The CI
enforcement audit and mutation tests freeze both contracts.

Phase 9 remains Next and no tag operation is authorized until Phase 9S merges
normally and its exact resulting protected main passes complete
recertification. Exact merge SHA, hosted run IDs, alert pagination, and Codacy
results belong to the non-recursive execution report.

## Publication and final closure boundary

Phase 9 remains separately authorized and is the only owner of tag, tag CI,
tagged installation, immutable source-only publication, attestation, and
V1_PRODUCT_BASELINE_SHA. Phase 10 remains separately authorized and is the
only owner of post-publication closure, the final v1.x closure bundle,
terminal agent/provider authority, V1_REPOSITORY_CLOSURE_SHA, exact-final-main
recertification, and cleanup of every temporary release/recovery/closure
branch.

No Phase 9 or Phase 10 operation is performed by Phase 9S.

## Final verdict

**PASS — PRE-PUBLICATION PREREQUISITES COMPLETE**

The current lifecycle remains `merged-not-tagged`, with Phases 0–8 Complete,
Phase 9 Next, and Phase 10 Not started. After the Phase 9S protected merge and
exact-final-main recertification pass, that resulting SHA becomes the only
candidate eligible for a separate Phase 9 Build authorization. v1.13.15
remains untagged, unpublished, operationally open, and unauthorized for v2
implementation.
