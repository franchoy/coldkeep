# Coldkeep v1.13.8 Version and Documentation Reconciliation

**Release:** `v1.13.8 — Restore / Remove Contract Split and Stored-Path Boundary`
**Phase:** Phase 0B
**Status:** Complete
**Branch:** `release/v1.13.8`

## Purpose

Reconcile active source version metadata, project documentation, changelog
history, ownership comments, and release-train numbering before corrective
migration work begins.

## Pre-reconciliation stale state

Before Phase 0B:

- source version constants and unit test still reported `1.11.0`;
- README status and current-focus text still advertised v1.12 planning;
- README and ARCHITECTURE still required PostgreSQL schema v15 in active text;
- restore/remove ownership comments still pointed to `v1.13.7`;
- snapshot mutation ownership comments and metadata still pointed to `v1.13.8`;
- repair/recovery ownership comments and metadata still pointed to `v1.13.9`;
- some thin-wrapper ownership text still pointed to `v1.13.11`;
- the active release train still duplicated `v1.13.12`;
- `CHANGELOG.md` was missing all `v1.12.x` and `v1.13.0`-`v1.13.7` entries.

## Source version correction

Record:

- previous reported version: `1.11.0`
- new reported version: `1.13.8`
- version constant file: `internal/version/version.go`
- unit-test correction: `internal/version/version_test.go`
- built human output: `coldkeep version 1.13.8`
- built JSON output: `1.13.8`

## Release-gate version assertion

Profile A now requires the built CLI to report exactly `1.13.8` in human and
JSON modes.

## README reconciliation

Record:

- active status changed from stale v1.12 planning to v1.13.8 in progress;
- v1.13.7 release outcome summarized;
- v1.13.8 focus summarized;
- PostgreSQL remains current normal runtime;
- no normal SQLite runtime is claimed;
- required PostgreSQL schema corrected from v15 to v16.

## Architecture reconciliation

Record the active schema requirement correction from v15 to v16.

## Changelog reconciliation

Record all added v1.12.x and v1.13.0-v1.13.7 entries.

## Ownership reconciliation

| Concern | Previous owner text | Correct owner |
|---|---|---|
| restore/remove stored paths | v1.13.7 | v1.13.8 |
| snapshot mutations | v1.13.8 | v1.13.9 |
| repair/recovery | v1.13.9 | v1.13.10 |
| backend compatibility gate | v1.13.11 | v1.13.11 |
| CLI thin-wrapper proof | v1.13.11 where stale | v1.13.12 |
| final v1.x/v2 handoff | duplicated v1.13.12 | v1.13.13 |

## Historical documents intentionally unchanged

Historical release records retain the planning truth that existed when they
were written.

Only active source comments, candidate ownership metadata, active project
documentation, and the current release train were corrected.

## Behavior impact

- Reported CLI version changes to `1.13.8`.
- No command behavior changes.
- No JSON shape changes.
- No exit-code changes.
- No schema or migration changes.
- No backend changes.
- No storage or repository format changes.
- No engine routing changes.

## Phase 0C input

The next checkpoint must add failing zero-reference migration coverage before
changing migration behavior.

## Immediate blockers

Expected:

`None found.`
