# Coldkeep v1.13.8 Corrective Baseline Freeze

**Release:** `v1.13.8 — Restore / Remove Contract Split and Stored-Path Boundary`
**Phase:** Phase 0D
**Status:** Complete
**Branch:** `release/v1.13.8`
**Validated corrective commit:** `023fb94deeabf1a489629a56b102dabe6e2c7c12`

## Purpose

Freeze a validated corrective baseline after closing v1.13.7 records,
reconciling release metadata, and correcting zero-reference migration behavior.

## Phase 0 outcomes

### Phase 0A

- closed the v1.13.7 release records;
- captured exact PR, merge, tag, and release provenance;
- activated `release/v1.13.8`.

### Phase 0B

- changed the reported version to `1.13.8`;
- added the Profile A exact version assertion;
- reconciled README, ARCHITECTURE, and CHANGELOG active state;
- corrected release-train ownership and numbering drift.

### Phase 0C

- fixed the zero-reference resurrection defect;
- gated SQLite compatibility backfill on pre-v6 state;
- gated PostgreSQL compatibility backfill on pre-v6 state;
- preserved genuine legacy compatibility behavior;
- reconciled the stale verification fixture explicitly.

## Frozen migration contract

| Pre-migration state | Expected result |
|---|---|
| genuine pre-v6 logical file | synthetic compatibility mapping created |
| current `ref_count=0`, zero mappings | preserved |
| current `ref_count>0`, zero mappings | preserved as detectable mismatch |
| current one mapping | preserved |
| current multiple mappings | preserved |

## Verification ownership

- migration no longer repairs current-schema mismatches;
- verify remains responsible for reporting them;
- the stale verification fixture was corrected explicitly;
- verification production behavior did not change.

## Validation evidence

- SQLite focused regressions: PASS
- SQLite race regressions: PASS
- remove/reopen regression: PASS
- corrected verification fixture direct run: PASS
- verification package and race suite: PASS
- live PostgreSQL normal and race validation: PASS using the local dockerized PostgreSQL service on `127.0.0.1:5432`
- related semantic package tests and race suites: PASS for `internal/db`, `internal/storage`, `internal/verify`, `internal/graph`, `internal/gc`, and `internal/maintenance`
- snapshot package tests and race suite: PASS
- snapshot release gate: PASS
- relocation guard and race guard: PASS
- plain short integration race suite: PASS
- AES-GCM short integration race suite: PASS
- version contract: PASS for exact human and JSON `1.13.8`
- `go mod tidy`: PASS with no repo diff
- formatting: PASS
- `git diff --check`: PASS
- `go vet ./...`: PASS
- `golangci-lint run ./...`: PASS
- `go test -count=1 ./...`: PASS
- `go test -race -count=1 ./...`: PASS

## Scope-freeze decision

`The inherited corrective baseline is complete and green.`

- Phase 0 is complete;
- no inherited v1.13.7 correctness blocker remains open;
- `v1.13.8` may proceed to restore contract inventory;
- new work must remain inside the approved `v1.13.8` scope;
- one branch and one final PR remain mandatory.

## Deferred work

- full restore/remove contract inventory;
- stored-path taxonomy;
- engine boundary implementation;
- CLI routing migration;
- final Profile A gate.

## Impact statement

- Source reports version `1.13.8`.
- Valid zero-reference state survives migration and reopen.
- Genuine legacy pre-v6 compatibility remains.
- Current mismatches remain visible to verification.
- No schema version changed.
- No backend default changed.
- No storage or repository format changed.
- No CLI, JSON, or exit behavior changed.
- No engine routing changed during Phase 0.

## Immediate blockers

None found.
