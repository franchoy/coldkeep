# v1.13.x Release Train Proposal

## Purpose and Release-Train Rules

This document records the proposed `v1.13.x` release train derived from the completed `v1.13.0`
inventories. It is documentation/inventory only. It does not start implementation work, change Go
code, change tests, alter schema or migrations, change CLI behavior, JSON output, exit codes,
storage format, repository format, default backend behavior, engine routing, catalog
implementation, CI behavior, or scripts.

Release-train rules:

- The proposal is derived from completed `v1.13.0` inventories, not aspirational implementation
  work.
- Releases must stay small, correctness-first, and phase-scoped.
- Request/result contracts must not be frozen before deferred boundaries, error taxonomy, and
  validation ownership are clarified.
- Daemon/API/UI/v2 implementation remains out of scope for `v1.13.x`.
- All phases remain on one release branch. Remote validation uses
  release-branch workflows. A single pull request is opened only after the
  complete release phase list and pre-release gate are green.

Sequencing rules:

- Do not freeze request/result contracts before explicit deferred boundaries are documented.
- Do not route more operations through engine before error taxonomy and validation ownership are
  clear.
- Do not move catalog ownership before dependency direction and backend parity expectations are
  clear.
- Do not switch default SQLite behavior in `v1.13` unless a later release proves portability and
  parity gates.
- Do not remove PostgreSQL compatibility.
- Do not treat CLI thin-wrapper proof as complete while repair/recovery/snapshot mutation paths
  remain CLI-owned.
- Keep daemon/API/UI/v2 implementation out of `v1.13.x`.

## Proposed v1.13.x Release Train

### `v1.13.0 — Engine Stabilization Baseline & Contract Inventory`

- Release title: `v1.13.0 — Engine Stabilization Baseline & Contract Inventory`
- Primary objective: establish the full stabilization baseline without changing runtime behavior.
- Inventory findings that justify it: Phases 1-8 show incomplete engine ownership, provisional
  contracts, deferred catalog boundaries, backend parity gaps, and mixed invariant ownership.
- Included scope: docs-only inventories, baselines, and release-train proposal.
- Excluded scope: all behavior changes and all implementation work.
- Expected code areas: none; docs only.
- Expected tests: none added; normal release validation only.
- Compatibility risk: `low`
- Behavior-change risk: `low`
- Backend risk: `low`
- Recommended validation gate: documentation-only release gate with no Go/test/schema/script drift.
- Acceptance criteria: all Phase 0-10 release docs exist and the branch remains docs-only.
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.1 — Explicit Deferred-Contract Boundaries`

- Release title: `v1.13.1 — Explicit Deferred-Contract Boundaries`
- Primary objective: convert implicit or mixed deferrals into explicit, documented runtime
  boundaries.
- Inventory findings that justify it:
  - Phase 3 recorded 20 deferred/fallback paths, including 7 candidate-only operations and 4
    partial-routing seams.
  - Phase 8 found only 2 of 18 invariants clearly safe for daemon/API callers without CLI
    assumptions.
- Included scope:
  - formalize explicit unsupported/error boundaries for deferred engine and catalog paths
  - keep candidate-only operations clearly non-active
  - close misleading “looks active but isn’t complete” seams first
- Excluded scope:
  - new engine operations
  - new catalog implementations
  - routing snapshot mutations, repair, or recovery
- Expected code areas:
  - `internal/engine`
  - `internal/catalog`
  - `cmd/coldkeep`
- Expected tests:
  - explicit `ErrNotImplemented` / unsupported-mode tests
  - route-preservation tests for partial paths
- Compatibility risk: `low`
- Behavior-change risk: `medium`
- Backend risk: `low`
- Recommended validation gate: targeted engine/catalog contract tests plus full standard release
  gate
- Acceptance criteria:
  - deferred and unsupported paths fail explicitly and consistently
  - candidate-only operations cannot be mistaken for active runtime support
  - partial routes no longer imply hidden completeness
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.2 — Engine Error Taxonomy Baseline`

- Release title: `v1.13.2 — Engine Error Taxonomy Baseline`
- Primary objective: define consistent machine-meaningful engine error categories before further
  contract freeze.
- Inventory findings that justify it:
  - Phase 2 reported `stable: 0` and broad reliance on inherited domain errors.
  - Phase 8 showed fail-closed invariants are difficult to freeze without clear engine-level error
    semantics.
- Included scope:
  - error taxonomy baseline for active and near-active engine operations
  - invariant/error category alignment
- Excluded scope:
  - broad routing expansion
  - daemon/API transport error design
- Expected code areas:
  - `internal/engine`
  - `internal/invariants`
  - relevant CLI parity tests
- Expected tests:
  - error classification tests
  - result/error contract tests
- Compatibility risk: `medium`
- Behavior-change risk: `medium`
- Backend risk: `low`
- Recommended validation gate: engine contract suite, JSON/exit-code parity checks, full release
  gate
- Acceptance criteria:
  - active engine paths classify errors consistently
  - invariant failures are distinguishable from unsupported and domain passthrough errors
  - error semantics are precise enough to support later contract cleanup
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.3 — Read-Side Contract Cleanup: Stats / Inspect / Snapshot Show / Snapshot Diff`

- Release title: `v1.13.3 — Read-Side Contract Cleanup: Stats / Inspect / Snapshot Show / Snapshot Diff`
- Primary objective: separate read-side engine contracts from current CLI/render/domain result
  shapes.
- Inventory findings that justify it:
  - Phase 2 found active read-side contracts provisional and domain-leaking.
  - Phase 1 and 3 found `inspect` bypassing engine and `snapshot show` / `snapshot diff` still
    partial or narrowed.
- Included scope:
  - clean request/result boundaries for `Stats`, `Inspect`, `SnapshotShow`, and `SnapshotDiff`
  - resolve obvious query narrowing and wrapper-shape issues
- Excluded scope:
  - mutating operations
  - snapshot create/delete/restore
- Expected code areas:
  - `internal/engine`
  - `internal/observability`
  - `internal/snapshot`
- Expected tests:
  - read-side contract tests
  - CLI parity tests
  - query-behavior preservation tests
- Compatibility risk: `medium`
- Behavior-change risk: `medium`
- Backend risk: `medium`
- Recommended validation gate: targeted read-side parity suite and full release gate
- Acceptance criteria:
  - read-side engine contracts are no longer wrapper-only or accidentally renderer-shaped
  - `inspect` no longer overstates engine ownership
  - `snapshot show` and `snapshot diff` no longer silently narrow caller expectations
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.4 — Read-Side Dependency Direction and Result Ownership Review`

- Release title: `v1.13.4 — Read-Side Dependency Direction and Result Ownership Review`
- Primary objective: review and tighten read-side dependency direction and result ownership
  without changing public behavior.
- Inventory findings that justify it:
  - Phase 8 accepted the current backend and dependency state for `v1.13.3`, but recorded mixed
    ownership as provisional.
  - `stats`, `inspect`, `snapshot show`, and `snapshot diff` still rely on wrapper-shaped or
    mixed ownership seams across engine, observability, snapshot, renderer, and CLI layers.
- Included scope:
  - decide engine-owned versus non-engine-owned result boundaries for active read-side surfaces
  - review direct observability and snapshot-package ownership that still bypasses a unified seam
  - review renderer coupling and CLI JSON assembly boundaries
  - refresh backend-compatibility evidence while preserving current behavior first
- Excluded scope:
  - CLI redesign
  - JSON redesign
  - exit-code changes
  - schema or storage changes
  - snapshot mutation implementation
  - broad routing migration unless explicitly scoped and test-protected
- Expected code areas:
  - `internal/engine`
  - `internal/observability`
  - `internal/snapshot`
  - `cmd/coldkeep`
- Expected tests:
  - read-side ownership and dependency-direction tests
  - CLI preservation tests
  - backend-parity review updates
- Compatibility risk: `medium`
- Behavior-change risk: `medium`
- Backend risk: `medium`
- Recommended validation gate: targeted read-side preservation suite plus full release gate
- Acceptance criteria:
  - result ownership decisions are explicit for the active read-side surfaces
  - direct CLI, renderer, observability, and snapshot couplings are reviewed honestly rather than
    implied away
  - no public behavior, JSON, exit-code, or routing drift is introduced while ownership seams are
    reviewed
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.5 — Release Record Closure and Engine-Level Invariant Ownership`

- Release title: `v1.13.5 — Release Record Closure and Engine-Level Invariant Ownership`
- Primary objective: close the final `v1.13.4` release-record gap, then review critical
  validation and invariant authority below CLI-only paths where possible.
- Inventory findings that justify it:
  - Phase 8 found 12 of 18 invariants only partially protected below CLI.
  - Phase 4 showed CLI still owns sequencing, validation, destructive policy, and recovery framing.
- Release-train note:
  - Phase 0 starts with post-release cleanup because final `v1.13.4` included two narrow
    production hardenings after the initial ownership review.
- Included scope:
  - duplicate critical CLI validation inside engine where engine is or will be the boundary
  - add engine-level invariant tests for restore/remove/verify/packed-legacy-sensitive areas
- Excluded scope:
  - snapshot mutation routing
  - repair/recovery routing decisions
- Expected code areas:
  - `internal/engine`
  - `cmd/coldkeep`
  - invariant-focused tests
- Expected tests:
  - engine-level invariant tests
  - routed CLI parity tests
- Compatibility risk: `medium`
- Behavior-change risk: `medium`
- Backend risk: `medium`
- Recommended validation gate: invariant-focused test subset plus full release gate
- Acceptance criteria:
  - critical engine-facing validations no longer rely only on CLI parsing
  - invariant ownership is explicit for active engine paths
  - lower-layer runtime protection is improved for future non-CLI callers
- Must happen before v2.0 or may be deferred: `must`

**Release status update:** released, merged to `main`, and tagged as `v1.13.5`.

## v1.13.6 — Catalog Contract Expansion and Backend Parity Review

**Status:** Released

Release outcome:

- catalog/backend contract inventory completed
- snapshot membership reviewed
- logical/physical mapping reviewed
- placement and restore-plan assumptions reviewed
- reachability and GC eligibility reviewed
- SQLite/PostgreSQL parity posture reviewed
- existing catalog/backend evidence classified as sufficient
- no new Phase 8 guard tests justified
- AES-GCM integration harness issue remediated in test code
- pull request CI passed
- main CI passed
- tagged from `main` as `v1.13.6`

Runtime impact:

- no production code changed
- no public behavior changed
- no schema or storage format changed

## v1.13.7 — SQLite-First Repository Portability Baseline

**Status:** Released

Focus:

- repository initialization inventory
- backend-selection inventory
- SQLite catalog-location review
- repository-root and portability assumptions
- WAL/SHM/checkpoint considerations
- repository move/reopen contract
- portability evidence classification
- SQLite/PostgreSQL compatibility review
- minimal portability smoke-test design
- narrow test-only guards if justified
- backend-direction documentation consistency
- v1.13.8 handoff preparation

Non-goals:

- no schema rewrite
- no storage format change
- no repository format change
- no backend default change
- no SQLite-only assumption
- no PostgreSQL compatibility removal
- no public output change

Release outcome to date:

- current repository initialization and backend defaults inventoried
- future SQLite catalog placement contract defined
- complete catalog-plus-payload portability contract defined
- existing portability evidence classified
- SQLite/PostgreSQL compatibility constraints reviewed
- narrow relocation guard designed
- schema-reopen physical-mapping defect discovered
- SQLite and PostgreSQL migration backfills corrected
- migration-idempotency regressions added
- direct rename/reopen/verify/restore guard added
- exact restored-byte equality proved
- documentation consistency reviewed

Current runtime posture:

- PostgreSQL remains the normal local runtime backend
- SQLite remains simulated/test-only in production startup
- no backend-default transition occurred
- no user-facing repository-move feature was added

Final release state:

- PR #99 passed all required checks;
- merged into `main`;
- post-merge `main` CI passed;
- tagged from the released main state as `v1.13.7`;
- GitHub release published;
- release branch deleted.

### `v1.13.8 — Restore / Remove Contract Split and Stored-Path Boundary`

**Status:** Released

- Release title: `v1.13.8 — Restore / Remove Contract Split and Stored-Path Boundary`
- Primary objective: stop overloading one contract surface with both by-ID and stored-path
  semantics.
- Inventory findings that justify it:
  - Phase 2 found `RestoreRequest` and `RemoveRequest` too CLI-shaped.
  - Phase 3 and 4 fixed the contract split and field taxonomy needed to route
    stored-path restore/remove through dedicated engine methods honestly.
- Included scope:
  - split by-ID vs stored-path contract boundaries
  - narrow active by-ID request/result surfaces to truthful semantics
  - activate dedicated stored-path restore/remove engine methods
  - route stored-path CLI restore/remove through the engine without changing
    public behavior
  - harden destination-safety, remove reachability, and backend invariants
- Excluded scope:
  - snapshot restore mutation work
  - repair/recovery
- Expected code areas:
  - `internal/engine`
  - `cmd/coldkeep`
  - `internal/storage`
- Expected tests:
  - contract-shape tests
  - restore/remove parity tests
  - destination-safety and fail-closed tests
- Active branch:

`release/v1.13.8`

Phase 0 began with inherited v1.13.7 post-release corrections before
restore/remove contract work started.
- Compatibility risk: `high`
- Behavior-change risk: `medium`
- Backend risk: `medium`
- Recommended validation gate: restore/remove invariant suite plus full release gate
- Acceptance criteria:
  - by-ID and stored-path responsibilities are no longer blurred by one misleading contract
  - stored-path boundaries are routed honestly through dedicated engine methods
  - restore/remove safety semantics are preserved while contract shape becomes daemon/API-safe
- Must happen before v2.0 or may be deferred: `must`

Final release state:

- PR #101 merged into `main`
- required PR checks passed
- post-merge `main` CI passed
- tagged from released `main` as `v1.13.8`
- GitHub release published
- release branch deleted

### `v1.13.9 — Snapshot Mutation Boundary: Create / Delete / Restore`

**Status:** Active

- Release title: `v1.13.9 — Snapshot Mutation Boundary: Create / Delete / Restore`
- Primary objective: decide and stabilize engine ownership for snapshot mutation commands.
- Inventory findings that justify it:
  - Phases 1, 3, and 4 show snapshot create/delete/restore still CLI/domain-owned candidate
    operations.
  - Phase 8 marks snapshot retention and delete safety as high-risk invariants.
- Included scope:
  - snapshot create/delete/restore boundary decision
  - preview/live mutation semantics
  - snapshot mutation request/result cleanup
- Excluded scope:
  - recovery/repair
  - daemon/API work
- Expected code areas:
  - `internal/engine`
  - `internal/snapshot`
  - `cmd/coldkeep`
- Expected tests:
  - snapshot mutation parity tests
  - retention-safety tests
  - request/result contract tests
- Active branch:

`release/v1.13.9`
- Compatibility risk: `high`
- Behavior-change risk: `high`
- Backend risk: `medium`
- Recommended validation gate: snapshot mutation + retention suite and full release gate
- Acceptance criteria:
  - snapshot mutation ownership is explicit and test-backed
  - delete/restore safety semantics are no longer hidden in CLI-only policy
  - snapshot mutation contracts are honest enough to support later handoff planning
- Must happen before v2.0 or may be deferred: `must`

Phase 0 is complete on `release/v1.13.9`. The executable Phase 0 baseline is
`c3a5a537d9dfad197f35c258ab78a75526bad788`. Required native cross-platform CI,
deterministic PostgreSQL G6 coverage, CodeQL, and exact-head Codacy evidence
were all verified.

Phase 1 is now complete on `release/v1.13.9`. The snapshot mutation inventory
freezes current create, delete, and restore ownership, exact output contracts,
lineage-delete semantics, backend evidence, and unresolved routing gaps without
activating any engine mutation method.

Phase 2 is complete.

`v1.13.9` Phase 2 created
`docs/release/v1.13/v1.13.9-snapshot-create-contract.md` as the canonical
snapshot-create contract artifact, replacing the earlier predicted
`...snapshot-create-contract-design.md` filename. It froze the final target
create request/result boundary, engine-owned ID generation seam,
mutation-derived `FilesInserted` ownership, DB-only engine dependency
expectations, future CLI translation target, and backend parity/test
requirements without activating snapshot create or changing runtime behavior.

Phase 3 is complete.

`v1.13.9` Phase 3 created
`docs/release/v1.13/v1.13.9-snapshot-delete-contract.md` as the canonical
snapshot-delete contract artifact, replacing the earlier predicted
`...snapshot-delete-contract-design.md` filename. It froze the final target
delete mode enum, preview/live request and result separation, explicit parent
state contract, membership-reference count semantics, warning ownership outside
the engine, preview read-only and execute metadata-only boundaries,
affected-row delete honesty, future CLI translation target, and backend
parity/test requirements without activating snapshot delete or changing runtime
behavior.

Phase 4 is complete.

`v1.13.9` Phase 4 created
`docs/release/v1.13/v1.13.9-snapshot-restore-contract.md` as the canonical
snapshot-restore contract artifact, replacing the earlier predicted
`...snapshot-restore-contract-design.md` filename. It froze the final target
restore request/result boundary, restore-specific selection contract,
destination-mode and output-target semantics, CLI-owned original-mode CWD
responsibility, lexical-versus-trusted-path safety distinction, metadata-mode
and structured-warning contract, override eligibility, partial-failure truth,
future dependency boundary, and backend parity/test requirements without
activating snapshot restore or changing runtime behavior.

Phase 5 is authorized and next.

### `v1.13.10 — Repair and Recovery Boundary Decision`

- Release title: `v1.13.10 — Repair and Recovery Boundary Decision`
- Primary objective: decide whether repair/recovery belong in engine, remain separate maintenance
  boundaries, or stay explicitly deferred.
- Inventory findings that justify it:
  - Phases 1-4 show repair and recovery are candidate-only and highly sensitive direct paths.
  - Phase 8 marks repair/recovery invariants high-risk and unsafe to over-assume for daemon/API
    callers.
- Included scope:
  - ownership decision
  - explicit boundary or explicit deferral
  - corrective-path safety framing
- Excluded scope:
  - broad recovery redesign
  - daemon/service workflow
- Expected code areas:
  - `internal/engine`
  - `internal/maintenance`
  - `internal/recovery`
- Expected tests:
  - fail-safe corrective-path tests
  - ownership/unsupported-boundary tests
- Compatibility risk: `high`
- Behavior-change risk: `high`
- Backend risk: `high`
- Recommended validation gate: corrective-path safety suite plus full release gate
- Acceptance criteria:
  - repair and recovery no longer sit in ambiguous “maybe engine-owned” territory
  - corrective paths either remain explicitly deferred or gain honest, tested ownership
  - v2 planning can distinguish emergency-only paths from stable boundaries
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.11 — SQLite/PostgreSQL Compatibility Gate Hardening`

- Release title: `v1.13.11 — SQLite/PostgreSQL Compatibility Gate Hardening`
- Primary objective: turn current backend-compatibility documentation into stronger ongoing proof.
- Inventory findings that justify it:
  - Phase 7 found parity strong only in limited areas; many rows remain partial, weak, absent, or
    unclear.
- Included scope:
  - stronger parity gates where contracts claim compatibility
  - explicit backend-specific behavior checks
  - no default backend switch
- Excluded scope:
  - PostgreSQL removal
  - SQLite-default product switch
- Expected code areas:
  - `internal/db`
  - `internal/catalog`
  - selected `internal/engine` tests
  - CI workflow coverage
- Expected tests:
  - dual-backend catalog tests
  - targeted engine/backend parity tests
  - integration harness parity checks
- Compatibility risk: `medium`
- Behavior-change risk: `low`
- Backend risk: `high`
- Recommended validation gate: dual-backend parity suite must pass before release
- Acceptance criteria:
  - backend contracts that claim compatibility have explicit ongoing proof
  - backend-specific differences remain documented instead of accidental
  - SQLite-first direction stays preparatory until parity and portability are proven
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.12 — CLI Thin-Wrapper Proof and Remaining Coupling Review`

- Release title: `v1.13.12 — CLI Thin-Wrapper Proof and Remaining Coupling Review`
- Primary objective: prove what is truly thin and explicitly classify remaining direct CLI
  ownership.
- Inventory findings that justify it:
  - Phase 4 and Phase 8 show CLI still owns important sequencing, validation, destructive policy,
    and recovery behavior in several paths.
- Included scope:
  - final coupling review
  - thin-wrapper proof for routed commands
  - explicit classification of remaining direct CLI ownership as acceptable, emergency-only, or
    deferred
- Excluded scope:
  - new major routing migrations
  - daemon/API implementation
- Expected code areas:
  - `cmd/coldkeep`
  - `internal/engine`
  - related docs and parity tests
- Expected tests:
  - CLI parity and text/JSON stability checks
  - direct-coupling audit checks
- Compatibility risk: `medium`
- Behavior-change risk: `low`
- Backend risk: `medium`
- Recommended validation gate: CLI parity suite and full release gate
- Acceptance criteria:
  - routed commands have credible thin-wrapper proof
  - remaining direct CLI ownership is classified honestly
  - the release train does not overclaim wrapper thinness before boundary work is complete
- Must happen before v2.0 or may be deferred: `must`

### `v1.13.13 — v1.x / v2.0 Handoff Gate`

- Release title: `v1.13.13 — v1.x / v2.0 Handoff Gate`
- Primary objective: decide explicitly whether the repo is ready to start `v2.0` work without
  lying about boundary completeness.
- Inventory findings that justify it: all prior releases are preconditions for an honest handoff,
  and Phase 8 showed current daemon/API safety is far from complete.
- Included scope:
  - final readiness review against handoff requirements
  - explicit mandatory vs deferred remainder
- Excluded scope:
  - implementation of daemon/API/UI
  - last-minute stabilization hidden inside the gate
- Expected code areas:
  - release docs
  - validation/gate artifacts
- Expected tests:
  - full release gate only
- Compatibility risk: `low`
- Behavior-change risk: `low`
- Backend risk: `medium`
- Recommended validation gate: full required release gate with recorded outputs
- Acceptance criteria:
  - v2 readiness is claimed only if the handoff requirements are truly met
  - remaining deferred or emergency-only paths are explicit
  - no hidden behavior work is smuggled into the handoff gate
- Must happen before v2.0 or may be deferred: `must`

## v1.13.x Non-Goals

- no daemon
- no local API
- no UI
- no network/NAS/cloud work
- no multi-user work
- no storage format change unless required for correctness
- no default backend switch until explicitly proven safe
- no PostgreSQL removal
- no broad refactor for aesthetics

## v2.0 Handoff Requirements

`v2.0` can begin only when:

- deferred engine operations are explicit and safe
- engine error taxonomy exists
- engine request/result contracts are no longer accidentally CLI-shaped
- critical invariants are engine/domain/catalog protected, not CLI-only
- dependency direction is CI-enforced for key boundaries
- catalog contracts are honest about implemented vs deferred ownership
- SQLite-first direction has a documented portability baseline
- PostgreSQL compatibility gates remain active
- remaining CLI direct coupling is classified as acceptable, emergency-only, or deferred

## Mandatory vs Deferrable Before v2.0

Mandatory before `v2.0`:

- `v1.13.1` through `v1.13.13`

Potentially deferrable within `v1.x`, but only if `v2.0` is also deferred:

- none of the core stabilization releases are optional for an honest `v2.0` start
- the only planned nuance is delivery order in the back half of the train
- `v1.13.10` and `v1.13.11` could potentially swap order if later evidence shows compatibility
  gating must harden before thin-wrapper proof, but the current Phase 7 and Phase 8 findings
  support the recommended order

## Bottom Line

The inventories justify keeping the full v1.13.0-v1.13.13 train intact.

`v1.13` is not a brief cleanup series. It is a substantial stabilization train whose purpose is to
make boundary ownership, deferred scope, invariant protection, backend compatibility, and contract
shape honest enough that any later `v2.0` work begins from proven seams rather than from CLI-era
assumptions.
