# Coldkeep v1.13.13 Phase 12 Hosted Exact-Head Security and Quality Gate

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `12 — Hosted Exact-Head Security and Quality Gate`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting candidate:** `123976440053264362c2b93041ade43aed3788cf`

## 1. Purpose

Phase 12 proves the immutable Phase 11 candidate against the complete hosted
CI, required-check, backend, codec, platform, adversarial, security, external-
quality, and repository-protection contract. It performs no remediation and
does not declare formal v1.x closure, merge, tag, or publication authority.

## 2. Starting candidate SHA

Entry fetch and identity checks proved `release/v1.13.13`, local/origin exact
at `123976440053264362c2b93041ade43aed3788cf`, ahead/behind `0/0`, a clean
worktree, and no local or remote `v1.13.13` tag. Phases 0–11 were Complete,
Phase 12 was Next, and Phases 13–16 were not started.

## 3. Entry identity

```text
BRANCH: release/v1.13.13
LOCAL_HEAD: 123976440053264362c2b93041ade43aed3788cf
ORIGIN_HEAD: 123976440053264362c2b93041ade43aed3788cf
AHEAD_BEHIND: 0/0
WORKTREE: CLEAN
V1.13.13_TAG: ABSENT_LOCAL_AND_REMOTE
```

## 4. Authority loaded

The complete Phase 11 report and execution matrix, Phase 9 proof and blocker
matrix, Phase 10 reconciliation, current phase list/checklist, root validation
matrix, current CI workflow, current CodeQL workflow, and live protection
configuration were read. They retain all six technical roots and
`P2-DOC-001` as closed, with zero known open v1 blockers.

## 5. Existing hosted runs adopted

The already-triggered candidate runs were adopted without a redundant rerun:

- [CI run 32498130286](https://github.com/franchoy/coldkeep/actions/runs/32498130286), attempt 1, `SUCCESS`.
- [CodeQL run 32498130293](https://github.com/franchoy/coldkeep/actions/runs/32498130293), attempt 1, `SUCCESS`.

Both runs report the exact candidate SHA. The companion
[hosted inventory](v1.13.13-phase12-hosted-check-inventory.md) records all 29
jobs/checks and the [security-quality matrix](v1.13.13-phase12-security-quality-evidence-matrix.md)
maps each mandatory gate.

## 6. Complete check inventory

The exact commit exposes 29 GitHub check runs: 25 CI jobs and four CodeQL jobs.
All are terminal `SUCCESS`; SHA mismatches, failed, cancelled, and pending
required checks are zero. GitHub legacy commit status contains no separate
contexts; the current Check Runs API is authoritative.

## 7. CI workflow result

CI run `32498130286` completed successfully on attempt 1. Every actual workflow
job succeeded. Conditional diagnostic uploads were correctly skipped on
success; the AES-GCM correctness row's separate plain-only PostgreSQL package
step was inapplicable by workflow design. Unexpected hosted skips are zero.

## 8. CI Required Gate

`CI Required Gate` job `96826170071` succeeded after evaluating quality,
correctness, stress, long-run, adversarial, smoke, legacy, benchmark integrity,
timing advisory, and cross-platform dependencies.

## 9. Quality result

Job `quality` succeeded. Its blocking steps passed release-state fixtures and
real-state validation, module cleanliness, formatting, smart quotes, shell
syntax and ShellCheck, validation-matrix and row-writer policy, candidate lint
contract, golangci-lint v2.6.2, vet, both package codec tiers, benchmark tests,
build, local CI audit, and CLI build.

```text
QUALITY_GATE: PASS
LINT: PASS
VET: PASS
RELEASE_STATE_HOSTED: PASS
```

## 10. PostgreSQL/backend proof

The plain correctness matrix ran the required PostgreSQL internal contracts
and required named pass-event parser. Catalog typed errors, snapshot-label
parity, placement/restore/GC planning, mutation cardinality, Engine routes,
maintenance advisory sessions, and container row locking passed. Integration,
stress, long-run, adversarial, smoke, legacy, and benchmark jobs also used the
healthy PostgreSQL 16 service where configured.

```text
POSTGRESQL_HOSTED: PASS
SQLITE_POSTGRES_PARITY: PASS
```

## 11. Codec proof

Plain and AES-GCM package, correctness, stress, long-run, adversarial, and
smoke surfaces all succeeded. The plain-only package-contract step is the
single workflow authority for backend-specific named PostgreSQL events and is
not an unexpected codec skip.

```text
PLAIN: PASS
AES_GCM: PASS
UNEXPECTED_CODEC_SKIPS: 0
```

## 12. Linux native proof

The Ubuntu native job passed coordination, path safety, storage restore,
Engine restore, and snapshot restore runtime tests. Required exact/no-replace
publication and retained-parent behavior remain consistent with Phase 9.

## 13. macOS native proof

The macOS native job passed the same runtime families, including the Darwin
exclusive-rename/link publication and retained-parent semantics covered by
the current tests.

## 14. Windows native proof

The Windows native job passed the same runtime families, including retained
object/HANDLE publication and reparse-safe confinement semantics covered by
the current tests.

## 15. Stress proof

Both `integration-stress (plain)` and `integration-stress (aes-gcm)` passed
their complete race-enabled PostgreSQL integration suites.

## 16. Long-run proof

Both codec variants passed the dedicated store/GC/verify/restore/delete,
randomized lifecycle, and snapshot-retention long-run selectors.

## 17. Adversarial proof

Both codec variants passed deterministic PostgreSQL G6, full G1–G17 JSON-
verified execution, required independent-process G6 pass events, and explicit
G14–G17 tail selectors. Failure-only diagnostic steps were correctly skipped.

## 18. Benchmark integrity

Hard profiles `none-w1`, `none-w4`, `zstd-w1`, and `zstd-w4` all succeeded and
verified their artifact checksums. All four timing-advisory profiles also
completed successfully under their informational contract; no advisory result
was promoted to a hard release threshold.

## 19. Smoke, legacy, and coverage

Plain and AES-GCM smoke jobs passed. PostgreSQL legacy compatibility passed.
The critical coverage report completed under its report-only semantics; no new
coverage threshold was introduced.

## 20. CodeQL analyses

Go, Python, and Actions analyses all succeeded on exact candidate run
`32498130293`.

## 21. CodeQL Aggregate

`CodeQL Aggregate` job `96821674594` succeeded after its three analysis
dependencies passed.

## 22. Code-scanning/security finding state

The open code-scanning alert query for `refs/heads/release/v1.13.13` returned
zero records. No alert was dismissed, suppressed, or ignored.

```text
NEW_CODEQL_SECURITY_FINDINGS: 0
```

## 23. Codacy exact-head state

The release branch was enabled during the separately authorized pre-Phase-12
governance repair. Normal branch enablement analyzed the candidate without a
recovery or explicit reanalysis trigger. Codacy reports exact analyzed SHA,
`analyzed=true`, `quality.isUpToStandards=true`, and `newIssues=0`. Complete
cursor exhaustion for both `onlyPotential=false` and `onlyPotential=true`
returned one analyzed page and zero records each.

```text
ANALYZED_SHA: 123976440053264362c2b93041ade43aed3788cf
ANALYSIS_COMPLETE: YES
UP_TO_STANDARDS: YES
ADDED_REGULAR_ISSUES: 0
ADDED_POTENTIAL_ISSUES: 0
CODACY_RECOVERY_USED: NO
CODACY_REANALYSIS_USED: NO
```

## 24. Branch-protection/required-check state

The supported remote CI-enforcement audit passes. Live GitHub evaluation gives
`main` deletion, non-fast-forward, pull-request, and strict `CI Required Gate`
rules, without linear history. The existing candidate release branch evaluates
exactly `non_fast_forward` and `required_linear_history` from active ruleset
`21155858`; it has no deletion, pull-request, or status-check precondition.
The hotfix ruleset `21155863` contains only non-fast-forward. No bypass actor
exists. Legacy main protection and merge policy remain unchanged.

```text
REQUIRED_CHECK_POLICY: PASS
HOSTED_REQUIRED_CHECKS: PASS
BRANCH_PROTECTION: PASS
```

## 25. Retry/attempt history

Candidate CI and CodeQL each passed on attempt 1. Codacy completed from normal
branch enablement. There was no unchanged-SHA workflow retry, recovery,
reanalysis, deterministic failure, or suppressed finding.

## 26. Candidate hosted verdict

```text
CANDIDATE_CI: PASS
CANDIDATE_REQUIRED_GATE: PASS
CANDIDATE_CODEQL: PASS
CANDIDATE_CODEQL_AGGREGATE: PASS
CANDIDATE_LINUX: PASS
CANDIDATE_MACOS: PASS
CANDIDATE_WINDOWS: PASS
CANDIDATE_POSTGRESQL: PASS
CANDIDATE_PLAIN: PASS
CANDIDATE_AES_GCM: PASS
CANDIDATE_ADVERSARIAL: PASS
CANDIDATE_STRESS: PASS
CANDIDATE_LONG_RUN: PASS
CANDIDATE_BENCHMARK_INTEGRITY: PASS
CANDIDATE_EXTERNAL_QUALITY: PASS
```

## 27. Repository change audit

Phase 12 adds this report, the hosted inventory, and the security-quality
matrix, then updates only the six authorized current trackers. It changes no
production/test Go, schema, migration, dependency, CI implementation,
validator, version, Codacy setting, or GitHub protection setting.

## 28. Tracker advancement

Current trackers advance to Phases 0–12 Complete, Phase 13 — Formal v1.x
Closure Decision — Next in `PLAN` mode, and Phases 14–16 Not started. Phase 13
is not started here.

## 29. Final evidence-head validation

The documentation commit produced by this phase must pass exact-head CI,
`CI Required Gate`, CodeQL, CodeQL Aggregate, Codacy, and required-check
inventory after push. Its run URLs and exact SHA are intentionally recorded in
the final Phase 12 response rather than recursively changing this document.

## 30. Phase 12 verdict

The immutable candidate hosted gate passes. Formal v1.x closure remains
`PENDING_PHASE_13`; merge, tag, GitHub Release, and v2 implementation are not
authorized by this phase.
