# v1.13.16 Phase 4R2 Hosted Snapshot Path Recovery

**Release:** v1.13.16 — Snapshot Retention Integrity, Observability Truth, and Final v1.x Closure
**Phase:** 4R3 — Recovery candidate pending exact-head hosted certification
**Mode:** BUILD

## Failed candidate evidence

```text
ORIGINAL_PHASE_4_IMPLEMENTATION_HEAD: 7b0700ada6c20464a0c52c785730804562ddfe82
ORIGINAL_PHASE_4_CI: 33253782716
ORIGINAL_PHASE_4_CI_RESULT: FAILED
ORIGINAL_REQUIRED_GATE: FAILED
ORIGINAL_CODEQL: 33253782723
ORIGINAL_CODEQL_RESULT: PASS
ORIGINAL_CODEQL_JOBS: 4/4 PASS
ORIGINAL_CODEQL_ALERTS: 0
```

The original Phase 4 implementation evidence remains immutable historical
candidate evidence. Its local implementation claims are not erased, but its
hosted-completion claim is superseded because the exact-head CI run failed.

## Failure reconciliation

```text
ROOT_A_PRODUCT_DEFECT: REJECTED
ROOT_A_FIXTURE_DEFECT: CONFIRMED
ROOT_B_CALLER_CAPTURE_ROOT: CONFIRMED
PRODUCTION_RECOVERY_CHANGE_REQUIRED: NO
```

The five macOS/Windows failures were caused by snapshot tests overwriting a
canonical Store-produced `physical_file.path` with a raw temporary-path
spelling. Real Store already applies absolute, clean, symlink-resolved
host-native identity. Phase 4R3 preserves the strict catalog invariant and
changes no production runtime path.

The integration, adversarial, and benchmark failures were caller defects.
Snapshot-create CLI invocations must run from their intended source root.
Benchmark snapshot scenarios must use their scenario-local data root and an
explicit relative selector so shared-database cases do not capture unrelated
earlier sources.

## Authorized recovery

Exactly three linear recovery commits are authorized:

1. `docs: authorize v1.13.16 hosted snapshot path recovery`
2. `fix: align snapshot callers and fixtures with capture roots`
3. `docs: record v1.13.16 hosted snapshot path recovery`

The complete code/test boundary is frozen in the source/test allowlist. No
schema, migration, dependency, workflow, storage, Engine, CLI, snapshot
production, query, restore, or repository-format change is authorized.

The Phase 3 RED harness remains untracked and immutable:

```text
RED_TEST_DIFF_BYTES: 17788
RED_TEST_DIFF_SHA256: 6b006f9024e8ea45b3551d02d8f9d2cdeef961238924b073473c8b6a947c444c
```

## Recovery commit and local certification

```text
R1_AUTHORITY_SHA: 7ce47a912c1f7c683e295f73efe51afba7339179
R2_RECOVERY_SHA: 40bffbc9016f20ae6b36dda1e634bf1114b22b57
ROOT_A_PRODUCT_DEFECT: REJECTED
ROOT_A_FIXTURE_DEFECT: FIXED
ROOT_B_CALLER_CAPTURE_ROOT: FIXED
PRODUCT_RUNTIME_CHANGE: NONE
RED_HARNESS: UNCHANGED
LOCAL_RECOVERY_VALIDATION: PASS
DARWIN_NATIVE: PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
WINDOWS_NATIVE: PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
```

The R2 commit changed exactly six fixture/caller paths, two native test paths,
two benchmark paths, and one test-helper path. It changed no production
runtime, schema, migration, dependency, workflow, repository-format, or Phase
5 selector-accounting path.

Local validation used Go 1.26.7 with `GOTOOLCHAIN=local` and external caches.
The modified snapshot, benchmark, and test-helper packages passed. The complete
integration and adversarial packages passed `-race -count=1 -short` against
separate disposable PostgreSQL databases for both plain and AES-GCM codecs.
The direct `benchmark run --dataset small` returned a valid nine-case envelope,
and the `none-w1`, `none-w4`, `zstd-w1`, and `zstd-w4` hard integrity profiles
each returned `BENCHMARK_INTEGRITY_PASS`; their checksums and affected
`snapshot-creation`/`gc-after-churn` snapshot states verified.

Linux native tests and `go vet ./...` passed. Windows/amd64 and Darwin/amd64
snapshot test packages compile-tested successfully, but this Linux execution
does not claim native Darwin or Windows runtime certification. Exact-head
hosted execution remains mandatory.

A temporary detached worktree at exact R2 SHA, without the uncommitted RED
harness, passed the modified packages, plain PostgreSQL race/short integration
and adversarial packages, full `go test -count=1 ./...`, development and auto
release-state validation under release-PR context, governance, all 215 Python
tests, local CI enforcement, and `git diff --check`. The temporary worktree was
then removed without pruning unrelated worktrees.

## Current disposition

```text
PHASE_4: RECOVERY_CANDIDATE_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
PHASE_5: NOT_AUTHORIZED
CK-V11316-001: OPEN_RECOVERY_CANDIDATE_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
CK-V11316-002: OPEN_RED_REPRODUCED
FINDINGS_CLOSED: 0/7
```

Local committed-tree validation has passed. The R3 exact head must still pass
hosted CI, Required Gate, native Darwin/Windows execution, CodeQL, and open-
alert certification before the committed conditional Phase 4 predicate is
satisfied. Phase 5 requires a separate explicit operation.
