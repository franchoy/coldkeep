# Coldkeep v1.13.13 Phase 9 Remediation Regression and Cross-Platform/Backend Proof

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `9 — Remediation Regression and Cross-Platform/Backend Proof`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `215ece977588bfd41675fd346ffc39dc6c1f3252`
**Test-proof commit:** `dfff9bcf7453a73800987b5db47db40eedb3442a`
**Proof-harness correction commit:** `088741fbfb0d3efbffec4cd031824e8cbc54dc0f`
**Final Phase 9 identity:** the commit containing this evidence document

## 1. Purpose and entry identity

Phase 9 independently proves the six blocker implementations frozen by Phase
7 and delivered by Phase 8. The entry gate fetched branch and tag state and
proved branch, local HEAD, and origin all equalled the authorized starting
SHA, ahead/behind was `0/0`, the worktree was clean, no local or remote
`v1.13.13` tag existed, and the active topology was Phases 0–8 Complete,
Phase 9 Next, and Phases 10–16 Not started.

## 2. Frozen proof authority

The controlling sources are the [Phase 7 design](v1.13.13-phase7-remediation-design-and-test-contract-freeze.md),
[regression contract matrix](v1.13.13-remediation-regression-contract-matrix.md),
[Phase 8 allowlist](v1.13.13-phase8-source-test-allowlist.md),
[Phase 8 report](v1.13.13-phase8-minimum-v1x-blocker-remediation.md), and
[implementation status](v1.13.13-phase8-blocker-implementation-status.md).
Every root started `IMPLEMENTED_PENDING_PROOF`; no generic green suite or
Phase 8 run was accepted as Phase 9 closure evidence.

## 3. Phase boundary

Production implementation was frozen. Phase 9 changed seven test files only,
then added this evidence and current tracker reconciliation. It changed no
public API, runtime implementation, schema, migration, repository format,
version, dependency, validator, or CI implementation.

## 4. Frozen inventory reconciliation

All 34 `P7-*` regression IDs were mapped individually in the
[execution matrix](v1.13.13-phase9-proof-execution-matrix.md). Existing Phase
8 tests supplied executable proof for 30 IDs. Four families needed stronger
test-only proof: complete five-method deadline identity, exhaustive packed
placement faults on both backends, deterministic independent-process
`simulate gc` ownership, and native retained-parent/publication proof selected
by the existing cross-platform workflow. No production proof seam was missing.

## 5. Phase 9 test-only additions

Test commit `dfff9bcf7453a73800987b5db47db40eedb3442a` contains exactly seven proof
files: three existing tests extended and four focused files added. They are
classified `NEW_FROZEN_PROOF` or `MECHANICAL_PROOF_FIXTURE`; none changes an
expected product semantic. The additions cover cancellation/deadline identity,
dual-backend placement failure and truthful counts, Linux independent-holder
coordination, native restore publication/parent/metadata behavior, direct
Darwin primitive execution, and PostgreSQL integration routes.

The first hosted execution found two assumptions in those new proof files,
not product defects. Correction commit
`088741fbfb0d3efbffec4cd031824e8cbc54dc0f` accepts Windows' native
fail-closed refusal to relocate an open retained parent and validates the
established `simulation` JSON envelope for successful `simulate gc`. It
changes no production source or expected product behavior.

## 6. No-production-change audit

Comparison with `215ece977588bfd41675fd346ffc39dc6c1f3252` reports zero changed
production Go files. The only Go delta is in `_test.go` files. There are zero
allowlist/proof-scope violations and no newly discovered production defect.

## 7. Catalog typed-error source proof

`translateServiceError` is applied exactly once at each frozen Service
boundary: `FindLogicalFile`, `FindPhysicalFilesForLogicalFile`, `FindSnapshot`,
`ListSnapshots`, and `LoadReachabilityRoots`. Existing typed errors pass
through; cancellation, deadline, and wrapped causes remain discoverable; other
query, scan, and iteration failures become stable operation failures; and
normal lookup absence remains successful absence.

## 8. Catalog typed-error execution proof

SQLite and deterministic fault-driver tests pass query, scan, iteration,
absence, cancellation, deadline, unknown-failure, and cause-preservation
contracts for all five methods. Exact-head hosted PostgreSQL backend contracts
pass the same public boundary. No raw database-error return path remains at a
frozen aggregate Service boundary.

## 9. P2-ARCH-001 closure

Source, fault, SQLite, PostgreSQL, and negative evidence satisfy
`P7-CAT-001`–`006`. `P2-ARCH-001` is `CLOSED`.

## 10. Snapshot-label source proof

Both limited and unlimited Catalog branches use
`LOWER(label) LIKE LOWER($2)`. There is no backend-specific predicate,
`ILIKE`, collation, Unicode-normalization, or CLI-local filtering path.
Ordering, wildcard interpretation, limit behavior, DTOs, and projection are
unchanged.

## 11. Snapshot-label execution proof

The `Alpha`, `alpha-two`, `BETA`, and `unrelated` opposite-case fixture passes
SQLite and exact-head PostgreSQL Catalog execution. Engine and CLI projection
tests preserve the identical ordered set and limited result.

## 12. P4-BE-001 closure

Source plus SQLite/PostgreSQL Catalog, Engine, and CLI evidence satisfies
`P7-LBL-001`–`004`. `P4-BE-001` is `CLOSED`.

## 13. Packed file-deep source proof

`verifyFileChunkHashes` loads the complete recipe through
`Catalog.LoadChunkPlacements`. Every placement is visited; packed entries
consume verified stored blocks and independently validate table/range,
segment size, reconstructed size, and chunk hash. Packed-block caching does
not skip per-placement validation. The former legacy-only deep path is not
reachable.

## 14. Packed file-deep execution proof

SQLite and exact-head PostgreSQL proofs cover healthy legacy, packed, mixed,
and migration-companion recipes; missing, incomplete, conflicting, and
malformed placement; legacy and packed payload corruption; packed-segment and
chunk-hash corruption; mixed packed-subset corruption; and truthful attempted
placement counts. Plain and AES-GCM hosted integration/adversarial jobs pass.

## 15. P3-COR-003 closure

All `P7-VFY-001`–`007` source, backend, fault, adversarial, count, and negative
contracts pass. `P3-COR-003` is `CLOSED`.

## 16. Simulate-GC source proof

The canonical CLI policy maps only valid `simulate gc` to exclusive
`OperationSimulateGC`. Acquisition precedes startup recovery and application
open; ownership covers plan, projection, session cleanup, and lease release.
Isolated simulation, benchmark, help, version, init, invalid syntax, and
unknown commands retain their frozen safe bypasses. No second valid shared
`simulate gc` route bypasses coordination.

## 17. Simulate-GC execution proof

Policy, lifecycle order, cleanup, safe exceptions, Linux race, and a
stdio-synchronized independent holder all pass. The holder proof observes a
fail-fast busy result with no recovery/dispatch activity and successful
reacquisition after release. Exact-head PostgreSQL integration proves the real
CLI shared route under the same native repository identity; it does not claim
cross-host coordination.

## 18. P5-COORD-001 closure

All `P7-GCC-001`–`005` requirements pass, including PostgreSQL route and Linux
independent-process proof. `P5-COORD-001` is `CLOSED`.

## 19. Exact restore source and negative proof

Planned exact destinations remain exact through storage and secure
installation. No lower layer appends a basename after an exact plan. Native
no-overwrite paths use exclusive rename or the frozen atomic link fallback and
otherwise fail closed. Ordinary handle-relative rename is reachable only for
intentional overwrite. Production `Stat` uses are planning or fail-closed
parent-consistency checks, not check-then-replace publication.

```text
UNSAFE_NO_REPLACE_FALLBACKS: 0
EXACT_DESTINATION_REINTERPRETATION_PATHS: 0
```

## 20. Linux exact-publication proof

Linux runtime tests pass `renameat2(RENAME_NOREPLACE)`, atomic `linkat`
fallback, unsupported-primitive fail closed, final-window destination race,
intentional overwrite, sync/cleanup faults, and complete storage integration.
The required race executions pass.

## 21. macOS and Windows exact-publication proof

Hosted macOS executes the real secure installer plus direct
`RenameatxNp(RENAME_EXCL)` and `Linkat` primitives. Hosted Windows executes
retained-object `NtSetInformationFile` publication with both replacement
modes. Both platforms pass destination-exists, overwrite, cleanup, and the
cross-platform production storage proof; no platform reopens the temporary
object by pathname.

## 22. P3-COR-001 closure

All `P7-RST1-001`–`006` route, platform, race, fault, and negative contracts
pass. `P3-COR-001` is independently `CLOSED`.

## 23. Retained-parent confinement proof

Creation, publication, metadata, and cleanup are relative to retained parent
and exact-object handles. Deterministic Linux and hosted macOS tests replace
the validated parent namespace before publication and prove no write or
cleanup is redirected to the replacement target. Hosted Windows proves the
native retained parent prevents that relocation while held, then publishes
through the retained handles without a pathname reopen or outside write. Path
revalidation is a fail-closed consistency signal. The proof makes no claim
against arbitrary hostile relocation of an already-open Unix directory
object.

## 24. P3-COR-002 closure

All `P7-RST2-001`–`006` source, native platform, fault, race, cleanup, and
negative contracts pass independently from atomic publication.
`P3-COR-002` is `CLOSED`.

## 25. Restore metadata compatibility

Post-publication metadata targets the retained published object. Strict
metadata failure returns an error while correct published bytes remain
visible; best-effort failure preserves warning behavior. A metadata failure
does not remove correct published content. Linux mode/time and hosted native
platform projections remain inside the frozen bounds.

## 26. Complete local proof

The eight required package suites and `go test -count=1 ./...` pass. The
minimum race set passes. Focused Catalog, verify, coordination, G5/G7,
secure-installer, storage, and AES-GCM selectors pass or record the expected
local PostgreSQL-only skips. `go vet ./...` passes. `golangci-lint v2.6.2`
passes with a fresh task-specific cache; an initial run reused stale local
analysis and emitted 35 unchanged baseline findings, which disappeared on
the clean-cache rerun and were not hidden through source/config changes.
Darwin amd64/arm64 and Windows amd64 secure-installer and storage test binaries
cross-compile successfully. Local PostgreSQL was unavailable and was not
provisioned.

## 27. Hosted exact-head proof

The final commit containing this file passed branch CI, `CI Required Gate`,
CodeQL, and `CodeQL Aggregate`. Required Linux/macOS/Windows runtime,
PostgreSQL Catalog/label/packed-verify/simulate-GC, and plain/AES-GCM jobs are
green at that exact identity. The run and job URLs are recorded in the final
Phase 9 handoff.

Hosted attempt history is retained rather than hidden:

| Attempt | Identity | Result | Classification and disposition |
| --- | --- | --- | --- |
| 1 | `66b21e34084e5a901be5c3033b1eea3e540ebd08` | CI failed; CodeQL and Aggregate passed | Windows correctly denied relocation of the retained open parent, while the new test required relocation; both PostgreSQL codec jobs rejected the successful `simulation` envelope by applying a generic command-envelope helper. Both were `TEST_HARNESS_DEFECT`; production source and frozen semantics were unchanged. |
| 2 | final evidence identity | Required CI, Required Gate, CodeQL, and Aggregate passed | The narrow successor proof accepts the Windows fail-closed behavior and validates the established simulation schema. |

## 28. Strategic verdict

```text
PHASE_9_PROOF: COMPLETE
P2_ARCH_001: CLOSED
P3_COR_001: CLOSED
P3_COR_002: CLOSED
P3_COR_003: CLOSED
P4_BE_001: CLOSED
P5_COORD_001: CLOSED
BLOCKERS_CLOSED: 6
BLOCKERS_REMAINING: 0
PRODUCTION_GO_CHANGES: 0
PHASE_9_TEST_ONLY_CHANGES: 7
SQLITE_PROOF: PASS
POSTGRESQL_PROOF: PASS
LINUX_RESTORE_PROOF: PASS
MACOS_RESTORE_PROOF: PASS
WINDOWS_RESTORE_PROOF: PASS
RACE_PROOF: PASS
FAULT_ADVERSARIAL_PROOF: PASS
NEGATIVE_SOURCE_PROOF: PASS
ALLOWLIST_OR_PROOF_SCOPE_VIOLATIONS: 0
PUBLIC_API_CHANGE: NO
SCHEMA_CHANGE: NO
MIGRATION_CHANGE: NO
NEW_DEPENDENCY: NO
CI_IMPLEMENTATION_CHANGE: NO
V1_X_BLOCKER_REMEDIATION: CLOSED
```

Phase 9 closes the frozen remediation roots; it does not declare final v1.x
closure, merge, tag, publish, or begin Phase 10. Phase 10 is authorized next
in `BUILD` mode for documentation and current-state reconciliation only.
