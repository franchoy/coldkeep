# Coldkeep v1.13.14 Phase 16 — Backend, Codec, Compression, and Storage-Layout Proof

**Release:** `v1.13.14 — Final v1.x Correctness Remediation and Closure Certification`

**Status:** Complete

**Branch:** `release/v1.13.14`

**Entry SHA:** `b1cc057503649a3d78f0426bf1b859b83f79cc92`

**Verdict:** `PASS — BACKEND_CODEC_COMPRESSION_LAYOUT_MATRIX_PROVEN`

## 1. Entry and trusted Phase 15 evidence

Phase 16 entered on the exact Phase 15 commit
`test: prove v1.13.14 integrated regression closure`. Local and remote heads
were equal, divergence was `0/0`, and the worktree was clean. The local and
remote `v1.13.14` tag and GitHub release were absent. The repository-local
`cmd/coldkeep/storage` path and generated control lock were absent.

The exact entry retained successful hosted evidence:

- CI run `32865224503`, exact head `b1cc057503649a3d78f0426bf1b859b83f79cc92`;
- CI Required Gate job `97865786214`;
- CodeQL run `32865224347`, exact head
  `b1cc057503649a3d78f0426bf1b859b83f79cc92`;
- CodeQL Aggregate job `97859191155`.

No baseline drift or hard-stop condition was found.

## 2. Authorities and inventory method

The Phase 2 design, implementation, regression, allowlist, and compatibility
matrices remain binding. The Phase 15 record and its 21-row matrix remain the
integrated-regression authority. Phase 16 neither reinterprets supported
combinations nor changes an owner-phase closure decision.

Before executing tests, current evidence for every required cell was classified
as `DIRECT_CURRENT_TEST`, `HOSTED_MATRIX_ONLY`,
`MISSING_DIRECT_PROOF`, or `NOT_APPLICABLE_BY_CONTRACT`. The inventory found
direct current owner/backend/layout/transform tests for every required cell.
Hosted Phase 15 plain, AES-GCM, PostgreSQL, legacy, benchmark, compression,
stress, and cross-platform jobs were retained only as complementary evidence.

```text
DIRECT_CURRENT_TEST_REQUIRED_CELLS: 73
HOSTED_MATRIX_ONLY_REQUIRED_CELLS: 0
MISSING_DIRECT_PROOF: 0
NOT_APPLICABLE_BY_CONTRACT: EXPLICITLY_JUSTIFIED
TEST_CODE_ADDED: NO
```

The 73 direct required cells are the independently derived sum of 20 backend,
16 codec, 16 compression, and 21 layout cells. No arbitrary Cartesian total is
used. The authoritative row-level derivation, fixtures, exact commands, and
not-applicable reasons are in the
[compatibility proof matrix](v1.13.14-compatibility-proof-matrix.md).

## 3. Applicable compatibility matrix

The matrix covers the 10 Phase-16-owned rows:

```text
CK-V1-AUD-001
CK-V1-AUD-004
CK-V1-AUD-006
CK-V1-AUD-009
CK-V1-AUD-010
CK-V1-AUD-011
CK-V1-AUD-012
CK-V1-AUD-013
CK-V1-AUD-014
CK-V1-AUD-017
```

Backend applies to all 10. Codec and compression apply to the eight
data-bearing or configuration rows; AUD-010 and AUD-012 are metadata-only.
Legacy/packed/mixed apply only where physical placement participates. AUD-014
requires packed construction plus legacy preservation, not a distinct mixed
resolver. AUD-017 controls new packed writes; legacy and mixed are not separate
init-result states, while existing-block preservation is proved directly.

## 4. Finding-by-finding deep proof

### CK-V1-AUD-001

`TestVerifyResultFastCountsOnlyExecutedStages` and its standard/full/deep,
system/file, repeated-reference, skipped/quarantine, compressed, legacy-only,
and mixed companions passed. `TestEngineReadVerifyAcrossBackends` passed on
SQLite and PostgreSQL. Four actual Store/Restore transform tuples proved plain,
AES-GCM, none, and zstd readability. Counters remained execution-derived;
legacy block and packed `storage_block` identities were distinct; repeated or
skipped work earned no credit; verification failure returned a zero public
result.

### CK-V1-AUD-004

The Engine primary regression and maintenance physical-Stat, overflow,
partial-result, active-accounting, packed-parity, and mixed-GC fixtures passed.
SQLite exercised dry-run only. PostgreSQL exercised dry-run and live GC.
Expected bytes came from fixture filesystem sizes before invocation. The
successful physical population agreed with affected count, filenames, and
bytes; active removals were included; no mixed physical unit was double
credited; failed physical work remained uncredited.

### CK-V1-AUD-006

Overwrite false preserved known target bytes and overwrite true atomically
installed exact snapshot bytes. Backend selector/restore contracts, four
codec/compression Store/Restore tuples, and supported legacy, packed, and mixed
snapshot fixtures passed. Snapshot and catalog state remained unchanged by
restore.

### CK-V1-AUD-009

SnapshotShow, SnapshotDiff, and SnapshotRestore accepted, rejected, normalized,
and matched the same selector cases on SQLite and PostgreSQL. Representation
fixtures showed that plain/AES-GCM, none/zstd, and legacy/packed/mixed physical
placement did not change the selected logical paths. Invalid selectors returned
`invalid_argument` and wrote no output.

### CK-V1-AUD-010

The metadata-only SQLite/PostgreSQL contract passed: Limit followed query and
change filtering, Total remained the raw diff, Matched and Summary described
the complete pre-limit population, and Entries were the deterministic first N.
Codec, compression, and layout are not applicable by the frozen contract.

### CK-V1-AUD-011

Verify, GC dry-run, Recover, Restore, and RestoreStoredPath used the selected
default or explicit ContainerDir. SQLite and PostgreSQL owner suites, the
PostgreSQL RestoreStoredPath default fixture, four transform tuples, and
legacy/packed/mixed route fixtures passed. All I/O remained beneath the
selected root; the global default contract did not change.

### CK-V1-AUD-012

Direct persisted `snapshot_file` membership remained the independent source of
truth. Empty, list, limited list, show, filtered show, limited show, graph,
tree, and lineage projections passed on SQLite and PostgreSQL. Codec,
compression, and layout are not applicable to this metadata-only result.

### CK-V1-AUD-013

The explicit plain, explicit AES-GCM, environment plain, environment AES-GCM,
unset-to-AES-GCM, invalid-environment, and valid-explicit-over-invalid-env table
passed. Actual plain/AES-GCM and none/zstd stores restored exact known bytes on
SQLite and PostgreSQL. No repository codec state, default change, or accidental
compression coupling was introduced.

### CK-V1-AUD-014

The complete frozen environment table passed for new/legacy precedence,
1/2/3, unsupported, zero/negative, empty, numeric-prefix parsing,
overflow-like values, and unset state. Store and Stats consumed the same
effective packed target on SQLite and PostgreSQL; Store warnings were unchanged
and Stats stayed silent. Plain/AES-GCM and none/zstd were represented. The
legacy fixture proved the packed target did not rewrite legacy placement.

### CK-V1-AUD-017

Init accepted none and zstd with supported levels including 1, 3, and 9, and
rejected invalid level relationships. Plain+none, plain+zstd, AES-GCM+none, and
AES-GCM+zstd stores restored exact bytes. The transform-order fixture proved
compression before encryption; the activation-switch fixture proved new writes
only and no existing-block migration. SQLite owner tests and PostgreSQL init /
Store / Restore paths passed.

Every row has `DEEP_COMPATIBILITY_PROOF: PASS_PHASE_16`.

## 5. Fixture independence and placement validity

- Verify expectations were derived from known fixture stage and authoritative
  placement identities.
- GC expected bytes came from filesystem Stat before GC, never metadata totals.
- Snapshot FileCount came from persisted membership and backend fixtures.
- Restore comparisons used known source bytes.
- Codec tuples were explicit and validated through persisted/readable behavior.
- Block-target expectations came from the Phase 12 frozen environment table.
- Legacy and mixed tests used the repository's supported v1.7/v1.8 fixture and
  upgrade mechanisms; no invalid rows were synthesized merely to label a
  layout.

Legacy and packed identities remained distinct, mixed verification received no
duplicate reference credit, mixed GC received no duplicate physical credit,
and restore always followed authoritative placement.

## 6. Preservation rows and boundaries

AUD-002, AUD-003, AUD-007, AUD-008, AUD-015, AUD-016, and all four process
gates retained their owner regressions through the required package sweeps.
Their closure state did not change.

AUD-005 remains outside compatibility multiplication:

```text
FINDINGS_CLOSED: 16/17
CK_V1_AUD_005: STILL_OPEN_PARTIAL
CK_V1_AUD_005_LIFECYCLE: LIFECYCLE_DEFERRED_PHASE18_26
PROCESS_GATES_COMPLETE: 4/4
```

No candidate-truth or publication-truth reconciliation was performed. Root
README was not changed.

Phase 17 retains cancellation, race, fault, adversarial, and native
cross-platform multiplication. Phase 16 did not begin that work.

## 7. Local validation and GATE-001

The required local owner-package sweep, PostgreSQL scratch-schema sweep,
legacy/mixed integration set, codec/compression tuple tests, and compression
activation tests passed. Environment-changing fixtures were serialized and
used `t.Setenv` or isolated process environments. PostgreSQL fixtures used
auto-bootstrap, scratch schemas, the maintenance database, and repository-
standard `DB_*` variables.

The documentation candidate passed:

```text
git diff --check: PASS
go vet ./...: PASS
golangci-lint v2.6.2 --new-from-rev=b1cc057503649a3d78f0426bf1b859b83f79cc92: PASS (0 issues)
go test -count=1 ./...: PASS
release-state self-tests: PASS (48/48)
release-state development: PASS (active_version=1.13.14, violations=0)
release-state auto: PASS (resolved development, violations=0)
validation-matrix audit: PASS
snapshot evidence-name validation: PASS (19 tracked declarations)
benchmark evidence inventory: PASS (0 staging directories)
release-linearity: PASS (no post-base merge)
CI-enforcement local/live audit: PASS
```

The candidate lint is intentionally entry-relative because Phase 16 changes no
Go source or tests; unrelated pre-existing full-tree test lint is outside this
phase's allowlist and was not rewritten.

`cmd/coldkeep/storage` was absent before and after each high-level matrix and
remained absent after the full suite without cleanup. No GATE-001 runtime
artifact recurred.

## 8. Scope audit

```text
PRODUCTION_CODE_CHANGED: NO
PUBLIC_API_CHANGED: NO
PUBLIC_REQUEST_SHAPE_CHANGED: NO
PUBLIC_RESULT_SHAPE_CHANGED: NO
JSON_CONTRACT_CHANGED: NO
CLI_INVOCATION_CONTRACT_CHANGED: NO
EXIT_NUMBERS_CHANGED: NO
SCHEMA_CHANGED: NO
MIGRATION_CHANGED: NO
STORAGE_FORMAT_CHANGED: NO
DEFAULT_BACKEND_CHANGED: NO
CODEC_SEMANTICS_CHANGED: NO
COMPRESSION_SEMANTICS_CHANGED: NO
SUPPORTED_LAYOUT_CHANGED: NO
DEPENDENCY_CHANGED: NO
WORKFLOW_CHANGED: NO
GOVERNANCE_CHANGED: NO
BENCHMARK_THRESHOLDS_CHANGED: NO
ROOT_README_CHANGED: NO
AUD_005_CLOSED: NO
PHASE_17_IMPLEMENTED: NO
```

No Go test addition was necessary because the direct current inventory already
covered all required cells. Phase 2 matrices, Phase 3–15 historical evidence,
and Phase 10 GC designs remain unchanged.

## 9. Publication acceptance and verdict

The immutable Phase 16 candidate is accepted only after the exact commit
`test: prove v1.13.14 compatibility matrix` is pushed normally and its exact
head passes CI, CI Required Gate, CodeQL, and CodeQL Aggregate. The execution
report records the immutable commit SHA and run/job identifiers; pending or
failed hosted checks cannot satisfy this record and do not require a
self-referential documentation amendment.

```text
PHASE16_OWNER_ROWS: 10
PHASE16_OWNER_ROWS_PROVEN: 10/10
BACKEND_REQUIRED_CELLS: 20
BACKEND_PROVEN_CELLS: 20
CODEC_REQUIRED_CELLS: 16
CODEC_PROVEN_CELLS: 16
COMPRESSION_REQUIRED_CELLS: 16
COMPRESSION_PROVEN_CELLS: 16
LAYOUT_REQUIRED_CELLS: 21
LAYOUT_PROVEN_CELLS: 21
MISSING_DIRECT_PROOF: 0
UNJUSTIFIED_NOT_APPLICABLE: 0
PHASE_16: PASS — BACKEND_CODEC_COMPRESSION_LAYOUT_MATRIX_PROVEN
PHASE_17: NOT_STARTED_READY_FOR_SEPARATE_AUTHORIZATION
```
