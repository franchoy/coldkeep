# v1.13.4 Phase List

## Phase 0 — Branch Creation, Scope Declaration, and Baseline Hygiene

Status: `complete`

Objective: create the `release/v1.13.4` branch, add the baseline release
documents, and correct stale `v1.13.3` summary wording so the new release
starts from an accurate completed historical record.

Allowed changes:

- `docs/release/v1.13/` baseline release docs
- branch creation and upstream tracking
- focused wording fixes in historical release docs

Disallowed changes:

- Go code changes
- test changes
- script changes
- CI changes
- schema or migration changes
- CLI, JSON, or exit-code changes
- runtime or backend behavior work
- broad routing or ownership refactors

Expected files:

- `docs/release/v1.13/v1.13.4-scope.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`
- `docs/release/v1.13/v1.13.4-validation-checklist.md`
- `docs/release/v1.13/v1.13.3-read-side-contract-summary.md`

Acceptance criteria:

- the `release/v1.13.4` branch exists locally and on `origin`
- the three `v1.13.4` baseline docs exist
- stale `v1.13.3` pre-release wording is removed or intentionally rewritten
- scope clearly states read-side ownership review, not behavior migration
- Phase 10 explicitly exists for `PRE_RELEASE_CHECKLIST.md` Profile A and PR
  opening
- `git diff --check` is clean
- no Go files change
- no tests change
- no scripts or CI files change

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "pending Phase 10|not PR-ready|Production Go behavior unchanged|Phase 10 pending" docs/release/v1.13/v1.13.3-read-side-contract-summary.md`
- final `git status -sb`

Completion note: complete. Created the `release/v1.13.4` branch and added
`docs/release/v1.13/v1.13.4-scope.md`,
`docs/release/v1.13/v1.13.4-phase-list.md`, and
`docs/release/v1.13/v1.13.4-validation-checklist.md`. Updated
`docs/release/v1.13/v1.13.3-read-side-contract-summary.md` so it now reflects
that `v1.13.3` completed its release gate, included release-gate blocker
remediations, and carried the production storage correctness fix. This phase
was docs-only. No runtime behavior changed. No tests changed. No scripts
changed. No CI changed. Next phase: Phase 1 read-side ownership baseline
inventory.

## Phase 1 — Read-Side Ownership Baseline Inventory

Status: `complete`

Objective: inventory the current ownership seams for `stats`, `inspect`,
`snapshot show`, and `snapshot diff`, covering CLI entrypoints, engine
entrypoints, dependencies, result owners, renderers, JSON assembly paths,
taxonomy boundaries, backend-specific behavior, and existing tests.

Allowed changes:

- release docs
- ownership inventory notes
- focused documentation updates tied to observed current behavior

Disallowed changes:

- production behavior changes
- broad request/result refactors
- new engine or catalog behavior
- CLI or renderer output changes

Expected files:

- `docs/release/v1.13/v1.13.4-read-side-ownership-inventory.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- all four target read-side surfaces are explicitly inventoried
- current ownership and dependency seams are recorded honestly
- backend-specific and test-protection coverage is documented
- missing or ambiguous seams are recorded instead of guessed

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- final `git status -sb`

Completion note: complete. Inventory document created:
`docs/release/v1.13/v1.13.4-read-side-ownership-inventory.md`. Discovery
commands run: targeted read-side ownership searches for `stats`, `inspect`,
`snapshot show`, and `snapshot diff`, plus focused reads of
`cmd/coldkeep/main.go`, `internal/engine`,
`internal/observability`, `internal/snapshot`, and
`internal/cli/render`, along with the `v1.13.3` summary, handoff, and backend
review docs. Branch-state finding: the branch remained `release/v1.13.4` and
clean, while `git fetch origin` was blocked by the environment because
`.git/FETCH_HEAD` was not writable; the existing local tracking ref and local
history were used as the available sync evidence. Ownership findings recorded
honestly: `stats` remains engine-wrapped but observability-result-owned;
`inspect` remains direct `CLI -> observability`; `snapshot show` remains a
mixed metadata/file/stats seam with CLI-owned JSON assembly; and
`snapshot diff` remains a mixed CLI/engine/snapshot seam with CLI-owned
filtering and JSON field decisions. This phase was docs-only. No runtime
behavior changed. No tests changed. No scripts changed. No CI changed. Next
phase: Phase 2 stats result ownership review.

## Phase 2 — Stats Result Ownership Review

Status: `complete`

Objective: review the `stats` path and decide whether the current
`engine.StatsResult` wrapper over `observability.StatsResult` remains
acceptable for `v1.13.x`, and whether any narrow test or cleanup is needed to
reduce contract risk without behavior drift.

Allowed changes:

- release docs
- focused contract notes
- narrow tests or cleanup only if required by a real contract risk

Disallowed changes:

- broad result-type rewrites
- stats behavior changes
- JSON or human output changes
- exit-code changes
- backend behavior changes

Expected files:

- `docs/release/v1.13/v1.13.4-stats-result-ownership.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- `stats` result ownership and renderer dependency expectations are explicit
- backend-sensitive assumptions are documented honestly
- taxonomy boundaries remain preserved
- any code change is narrow and justified by a discovered contract risk

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- targeted tests if code changes
- final `git status -sb`

Completion note: complete. Review document created:
`docs/release/v1.13/v1.13.4-stats-result-ownership.md`. Discovery commands
run: targeted `stats` ownership searches plus focused reads of
`cmd/coldkeep/main.go`, `internal/engine/types.go`,
`internal/engine/default_engine.go`, `internal/observability/{options,types,stats}.go`,
`internal/cli/render/stats.go`, and the current `stats`-related CLI, engine,
observability, renderer, maintenance, and equivalence tests. Branch-state
finding: the branch remained `release/v1.13.4` and clean, while
`git fetch origin` was blocked again because `.git/FETCH_HEAD` was not
writable; the existing local tracking ref and local history were used as the
available sync evidence. Ownership decision: the current `stats` seam is
acceptable for `v1.13.x` as a transitional seam. `engine.StatsResult`
continues to wrap observability-owned stats data, the renderer continues to
consume observability-shaped results directly, and no correctness or contract
bug justified a refactor in this release. This phase was docs-only. No runtime
behavior changed. No tests changed. No scripts changed. No CI changed. Next
phase: Phase 3 inspect dependency-direction review.

## Phase 3 — Inspect Dependency-Direction Review

Status: `complete`

Objective: review the `inspect` path, including whether the direct
`CLI -> observability` production path remains acceptable for `v1.13.x`, which
package clearly owns inspect results, and whether unsupported and not-found
paths are sufficiently protected.

Allowed changes:

- release docs
- focused contract notes
- narrow tests only if current contract coverage is insufficient

Disallowed changes:

- inspect behavior changes
- broad observability refactors
- engine routing changes
- CLI output changes

Expected files:

- `docs/release/v1.13/v1.13.4-inspect-dependency-direction.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- `inspect` ownership is described without overstating engine authority
- supported targets and error boundaries are explicit
- dependency-direction concerns are documented for later releases if retained

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- targeted tests if added
- final `git status -sb`

Completion note: complete. Review document created:
`docs/release/v1.13/v1.13.4-inspect-dependency-direction.md`. Discovery
commands run: targeted `inspect` dependency-direction searches plus focused
reads of `cmd/coldkeep/main.go`, `internal/observability/{inspect,options,types,errors}.go`,
`internal/cli/render/inspect.go`, `internal/engine/types.go`,
`internal/engine/default_engine.go`, and the current CLI, engine,
observability, renderer, and service tests that protect inspect behavior.
Branch-state finding: the branch remained `release/v1.13.4` and clean, while
`git fetch origin` was blocked again because `.git/FETCH_HEAD` was not
writable; the existing local tracking ref and local history were used as the
available sync evidence. Dependency-direction decision: the current direct
`CLI -> observability` inspect path remains acceptable for `v1.13.x` as a
documented transitional seam. Supported public CLI targets remain unchanged,
`EntityPhysicalFile` remains internal-only, and no correctness or contract bug
justified a routing change in this release. This phase was docs-only. No
runtime behavior changed. No tests changed. No scripts changed. No CI changed.
Next phase: Phase 4 snapshot show ownership review.

## Phase 4 — Snapshot Show Ownership Review

Status: `complete`

Objective: review `snapshot show` ownership across metadata, file listing,
stats, CLI JSON assembly, renderer behavior, query/filter logic, and backend
compatibility while preserving current public behavior.

Allowed changes:

- release docs
- focused contract notes
- narrow tests only if the current contract is underprotected

Disallowed changes:

- public JSON field changes
- count changes
- routing changes
- broad snapshot refactors

Expected files:

- `docs/release/v1.13/v1.13.4-snapshot-show-ownership.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- ownership of metadata, file-list, stats, renderer, and CLI JSON seams is
  explicit
- missing and empty snapshot behavior is documented
- backend compatibility assumptions are recorded honestly

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- targeted tests if added
- final `git status -sb`

Completion note: complete. Review document created:
`docs/release/v1.13/v1.13.4-snapshot-show-ownership.md`. Discovery commands
run: targeted `snapshot show` ownership searches plus focused reads of
`cmd/coldkeep/main.go`, `internal/engine/default_engine.go`,
`internal/cli/render/snapshot_list_show.go`, and the current CLI, engine,
snapshot, renderer, and routing tests that protect snapshot show behavior.
Branch-state finding: the branch remained `release/v1.13.4` and clean, while
`git fetch origin` was blocked again because `.git/FETCH_HEAD` was not
writable; the existing local tracking ref and local history were used as the
available sync evidence. Ownership decision: the current `snapshot show` seam
remains acceptable for `v1.13.x` as a documented mixed transitional seam.
Metadata and stats/count paths remain engine-backed, file listing remains
snapshot-package-backed, and CLI JSON assembly remains active. No correctness
or contract bug justified consolidating these seams in this release. This
phase was docs-only. No runtime behavior changed. No tests changed. No scripts
changed. No CI changed. Next phase: Phase 5 snapshot diff query/result
ownership review.

## Phase 5 — Snapshot Diff Query/Result Ownership Review

Status: `pending`

Objective: review `snapshot diff` ownership across CLI query parsing, engine
narrowing, snapshot package diffing, summary and detailed paths, `--filter`
post-processing, counts, result types, rendering, and JSON assembly.

Allowed changes:

- release docs
- focused contract notes
- narrow tests only if the current contract is underprotected

Disallowed changes:

- snapshot diff semantic changes
- JSON shape changes
- broad narrowing refactors
- public CLI behavior changes

Expected files:

- `docs/release/v1.13/v1.13.4-snapshot-diff-ownership.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- ownership of query parsing, narrowing, result shaping, filtering, and output
  seams is explicit
- summary versus detailed behavior is preserved
- matched-versus-total count ownership is documented

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- targeted tests if added
- final `git status -sb`

## Phase 6 — Renderer and JSON Assembly Boundary Review

Status: `pending`

Objective: review whether human rendering and JSON assembly boundaries are
explicit enough for `stats`, `inspect`, `snapshot show`, and `snapshot diff`,
and document what remains intentionally CLI-owned versus deferred.

Allowed changes:

- release docs
- focused contract notes
- narrow tests only if needed to catch accidental drift

Disallowed changes:

- renderer output redesign
- JSON envelope redesign
- broad CLI abstraction work

Expected files:

- `docs/release/v1.13/v1.13.4-renderer-json-boundary-review.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- human renderer ownership is explicit for each surface
- JSON success and error envelope ownership is explicit
- retained CLI JSON assembly is marked intentional where applicable
- deferred work is clearly assigned to later releases

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- targeted tests if added
- final `git status -sb`

## Phase 7 — Dependency-Direction Guard Tests

Status: `pending`

Objective: add narrow tests that protect intended dependency boundaries and
ownership seams without forcing a broad refactor.

Allowed changes:

- test files
- release docs
- narrow supporting production changes only if a test exposes a real bug

Disallowed changes:

- broad refactors
- CLI behavior changes unrelated to a correctness fix
- speculative test additions with no protection value

Expected files may include:

- `cmd/coldkeep/read_side_dependency_direction_test.go`
- `internal/engine/read_side_result_ownership_test.go`
- `internal/observability/read_side_result_ownership_test.go`
- `internal/snapshot/read_side_result_ownership_test.go`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- added tests protect real ownership or dependency decisions
- no public output drift is introduced
- any production change is narrow and bug-justified

Validation checklist:

- `git status -sb`
- `git diff --check`
- `gofmt -l $(git ls-files '*.go')`
- targeted package tests
- final `git status -sb`

## Phase 8 — Backend Compatibility and Package Dependency Review

Status: `pending`

Objective: review SQLite/PostgreSQL compatibility implications of the read-side
ownership decisions, including catalog contract tests, backend behavior,
temporary database isolation, and parity assumptions.

Allowed changes:

- release docs
- backend-focused tests if current behavior is underprotected
- narrow supporting fixes only if a real backend contract bug is discovered

Disallowed changes:

- SQLite-only assumptions
- backend default changes
- schema or storage changes
- speculative compatibility rewrites

Expected files:

- `docs/release/v1.13/v1.13.4-backend-compatibility-review.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- SQLite/PostgreSQL parity assumptions are documented honestly
- no compatibility regressions are introduced
- any backend-sensitive code change is narrow and justified

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- targeted backend validation if needed
- final `git status -sb`

## Phase 9 — Final Ownership Decision and v1.13.5 Handoff

Status: `pending`

Objective: summarize what ownership seams were reviewed, which are now stable,
which remain provisional, what tests protect them, whether production code
changed, and what remains deferred to later releases.

Allowed changes:

- release docs
- release-gate draft updates

Disallowed changes:

- final release gate execution
- PR opening
- merge, tag, or release creation

Expected files:

- `docs/release/v1.13/v1.13.4-read-side-ownership-summary.md`
- `docs/release/v1.13/v1.13.4-v1.13.5-handoff.md`
- `docs/release/v1.13/v1.13.4-release-gate.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- summary clearly distinguishes stable versus provisional seams
- tests and production-change status are recorded honestly
- release-gate doc remains a draft only

Validation checklist:

- `git status -sb`
- `git diff --check`
- `rg -n "TODO|TBD|FIXME" <changed-doc-files>`
- final `git status -sb`

## Phase 10 — PRE_RELEASE_CHECKLIST.md Profile A, Final Validation, and PR Opening

Status: `pending`

Objective: execute the full `PRE_RELEASE_CHECKLIST.md` Profile A gate, record
results honestly, and open the PR from `release/v1.13.4` to `main` only if the
required validation passes.

Allowed changes:

- release docs
- release-gate evidence updates
- narrow bug-fix changes only if a required validation exposes a real issue

Disallowed changes:

- merge
- tag creation
- GitHub release creation
- skipping required gate steps without an equivalent recorded substitute

Expected files:

- `docs/release/v1.13/v1.13.4-release-gate.md`
- `docs/release/v1.13/v1.13.4-phase-list.md`

Acceptance criteria:

- `PRE_RELEASE_CHECKLIST.md` Profile A is executed or an equivalent substitute
  is honestly recorded
- branch-state, formatting, vet, lint, targeted tests, full tests, race tests,
  backend-sensitive validation, integration matrix, adversarial matrix, smoke
  matrix, benchmark matrix, legacy compatibility, cross-platform
  approximation, snapshot release gate, CI enforcement audit, and final clean
  branch state are all recorded honestly
- PR is opened only if the gate passes

Validation checklist:

- `git status -sb`
- `git diff --check`
- `gofmt -l $(git ls-files '*.go')`
- `go vet ./...`
- `golangci-lint run ./...`
- targeted read-side tests
- `go test -count=1 ./...`
- `go test -race -count=1 ./...`
- backend-sensitive validation required by the release
- integration matrix
- adversarial matrix
- smoke matrix
- benchmark matrix
- legacy compatibility
- cross-platform approximation
- `bash scripts/run_snapshot_release_gate.sh --count 1`
- `bash scripts/audit_ci_enforcement.sh --local-only`
- `bash scripts/audit_ci_enforcement.sh`
- final `git status -sb`
