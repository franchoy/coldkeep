# Coldkeep v1.13.14 Phase 21 — Independent Full Local Exact-Candidate Release Gate

**Status:** Complete

**Branch:** `release/v1.13.14`

**Exact candidate SHA:** `89a1890ad90a6778e0e19062a7eff0327482005a`

**Execution date:** 2026-08-26 UTC

**Mode:** Independent local release gate; documentation-only success record

## 1. Purpose and boundary

Phase 21 independently executed the complete owner-promoted local Profile B
gate against the immutable Phase 20 candidate. Earlier phase and hosted results
were context only and were not substituted for fresh local execution.

This phase does not alter the candidate, certify hosted release readiness,
merge the release branch, create a tag, publish a release, close
`CK-V1-AUD-005`, or begin Phase 22. Its only tracked success-path change is
this record.

## 2. Exact-candidate entry proof

The gate ran in a detached worktree at the exact Phase 20 SHA. A connection
loss removed the original Phase-21-owned temporary directory; the stale
worktree registration and only that temporary worktree were recreated from the
same exact SHA. The normal release worktree remained untouched until every
local gate had passed.

```text
DETACHED_HEAD: 89a1890ad90a6778e0e19062a7eff0327482005a
ENTRY_WORKTREE: CLEAN
IMMUTABLE_CANDIDATE_CORE_MANIFEST_ENTRIES: 1397
IMMUTABLE_CANDIDATE_CORE_MANIFEST_SHA256: 984221fc5dc0e9d2d76d97cfb2c334bd7a52733682d818cb839d7c5caacac4da
ENTRY_CORE_MANIFEST_BYTE_FOR_BYTE: PASS
POST_GATE_CORE_MANIFEST_BYTE_FOR_BYTE: PASS
CORE_SURFACE_CHANGED: NO
```

The filtered `git ls-tree -r --full-tree` stream matched the tracked immutable
candidate-core manifest byte-for-byte at entry and after the complete gate.

## 3. Owner-authorized environment repair

The local validation environment was repaired outside the repository before
execution. Go and lint caches and the task-owned tool directory were rooted
under `/home/codespace/.cache/coldkeep-phase21-tools`; no tool or environment
setup was added to the repository.

```text
GO: go version go1.25.10 linux/amd64
DOCKER_COMPOSE: Docker Compose version v2.40.3
PSQL: psql (PostgreSQL) 16.15 (Ubuntu 16.15-0ubuntu0.24.04.1)
JQ: jq-1.7
SHELLCHECK: ShellCheck 0.9.0
PYTHON: Python 3.12.1
BASH: GNU bash 5.2.21
GOLANGCI_LINT: golangci-lint v2.6.2, built with go1.25.10
REQUIRED_TOOLS_PRESENT: YES
```

Every Go command, including the lint invocation, used exact
`GOTOOLCHAIN=go1.25.10`. The pre-existing Go 1.26.1 and golangci-lint 2.11.3
installations were not replaced.

## 4. PostgreSQL isolation

PostgreSQL 16.15 was provided through the repository's Compose service. Gate
families used fresh, task-specific databases, including dedicated manual,
uncached-sweep, and full-race databases. Auto-bootstrap and the tracked v16
schema path were enabled where the checklist requires them. The benchmark
bundle records database image digest
`sha256:33f923b05f64ca54ac4401c01126a6b92afe839a0aa0a52bc5aeb5cc958e5f20`.

No pre-existing repository data was reused as test storage.

## 5. Profile A quality gate

The complete Section 2 quality-equivalent surface passed:

- clean-test-storage preflight;
- `go mod tidy -diff` with no module-file change;
- tracked Go formatting, Bash syntax, smart-quote, and ShellCheck checks;
- validation-matrix and versioned-row-writer validators;
- golangci-lint v2.6.2 with zero issues and `go vet ./...`;
- plain and AES-GCM `go test -race -count=1 ./cmd/... ./internal/...`;
- required chunk benchmark selectors;
- `go build ./...`, local CI-enforcement audit, evidence lifecycle fixtures,
  snapshot evidence-name validation, benchmark inventory, and release
  linearity validation;
- all 48 release-state validator fixtures; and
- exact CLI human and JSON version `1.13.14`.

The real release-state check passed with `state=development`, active version
`1.13.14`, and zero violations under the supported release-PR GitHub context.
The raw detached-worktree context is deliberately rejected by the validator
and was not misrepresented as a candidate failure.

## 6. Plain codec matrix

The plain-codec Profile A matrix passed:

| Gate | Result | Elapsed |
| --- | --- | ---: |
| Short integration correctness | Pass | 227.689s |
| Full integration stress | Pass | 289.735s |
| Dedicated long-run selectors | Pass | 58.221s |
| Real 25-iteration refcount containment matrix | Pass, all plain/AES-GCM worker subprofiles | 56.142s |
| Full long-run adversarial G1–G17 | Pass | 320.351s |
| Focused G14–G17 tail | Pass | 17.100s |
| Smoke | Pass | 21.271s |

## 7. AES-GCM codec matrix

The AES-GCM Profile A matrix passed with the gate key supplied only through the
process environment:

| Gate | Result | Elapsed |
| --- | --- | ---: |
| Short integration correctness | Pass | 237.215s |
| Full integration stress | Pass | 301.171s |
| Dedicated long-run selectors | Pass | 59.443s |
| Real 25-iteration refcount containment matrix | Pass, all plain/AES-GCM worker subprofiles | 51.530s |
| Full long-run adversarial G1–G17 | Pass | 320.716s |
| Focused G14–G17 tail | Pass | 15.872s |
| Smoke | Pass | 18.303s |

No secret value is retained in this record or the benchmark evidence.

## 8. Named backend, recovery, and coordination proofs

Fresh race-enabled named rows passed for PostgreSQL mutation cardinality,
round-trip storage under both codecs, shared-chunk removal, startup recovery,
and all three independent-process G6 selectors under both codec invocations.
The required selectors reported explicit pass events rather than skips.

The four maintenance advisory-session selectors passed, including dedicated
session ownership, operation-failure release, cleanup-failure discard, and
single-connection live-GC refusal.

## 9. Legacy, snapshot, coverage, and cross-platform local proof

The legacy post-migration store/restore/snapshot regression passed. The
snapshot release gate passed all nine selected tests. The critical-coverage
report completed as report-only advisory evidence with no enforced threshold.

Linux pathsafe/storage checks passed. Secure-install test binaries were
cross-compiled and format-verified for Darwin amd64, Darwin arm64, and Windows
amd64. These compile checks do not substitute for Phase 22 native hosted
runtime evidence.

## 10. Supplementary Section 4

Section 4 remained supplementary and non-blocking under the frozen contract,
but was executed in full. The exact command
`go test -p 1 ./tests/... -count=1 -v -timeout 20m` passed.

## 11. Benchmark integrity and timing advisory

All four hard integrity profiles returned `BENCHMARK_INTEGRITY_PASS`, and all
profile and bundle checksum manifests verified:

| Profile | Integrity | Timing advisory |
| --- | --- | --- |
| `none-w1` | Pass | Within reference; 0 crossings |
| `none-w4` | Pass | Valid informational warning; 6 crossings |
| `zstd-w1` | Pass | Within reference; 0 crossings |
| `zstd-w4` | Pass | Valid informational warning; 2 crossings |

The `none-w4` observations crossed historical absolute thresholds for
GC-after-churn duration/throughput, store-many-small-files
duration/throughput, and store-mixed-dataset duration/throughput. The
`zstd-w4` observations crossed store-many-small-files duration and throughput.
The evaluator produced complete, schema-valid advisory reports and the frozen
policy makes these historical timing observations informational. The hard
candidate-integrity rows remained green.

The exact-SHA bundle was validated, atomically promoted, and retained at
`.release-evidence/v1.13.14/89a1890ad90a6778e0e19062a7eff0327482005a/`.
Inventory validation reports zero staging directories and a clean ordinary
untracked-inclusive worktree.

## 12. Doctor, bootstrap, and clean-install gates

Sections 5–8 passed against fresh PostgreSQL state:

- Doctor succeeded in human and JSON modes.
- The validation matrix was reviewed and validated.
- Auto-bootstrap enabled succeeded at schema version 16.
- Auto-bootstrap disabled against a missing schema failed as expected with an
  actionable schema-version error.
- `docker compose down -v` removed only the Phase-21 test volume.
- A fresh Compose PostgreSQL service and candidate container build passed
  candidate `stats` and `doctor` checks.

## 13. CLI and batch contract gates

Sections 9–10 passed. Doctor and verify success JSON retained the documented
envelopes. An invalid verify level returned exit 2, `USAGE`, and
`INVALID_ARGUMENT` on the documented error stream. Focused command, internal,
integration, batch, and transitional-API guardrail tests passed.

Local evidence assertions were aligned to the published command names, fields,
and stdout/stderr contract before classification; these were harness assertion
corrections, not retries of a failing candidate behavior.

## 14. Physical graph and GC safety gate

Section 11 passed. Two stored samples established a real physical graph. A
deliberately introduced refcount drift caused live GC refusal with
`GC_REFUSED_INTEGRITY` and caused verify to return invariant exit 3. Repair
updated exactly one refcount, after which verify and GC dry-run recovered.

Stored-path removal, remaining-refcount, chunk live-refcount, and batch
contracts passed. An unsafe stored path was rejected as a usage error with
exit 2. The G10–G13 physical-file and audited-GC contracts remained intact.

## 15. Snapshot CLI and retention lifecycle

Sections 15–17 passed through a complete manual lifecycle using two snapshots,
`pre-gc-gate` and `post-gc-gate`:

- create, list, show, stats, and diff succeeded; the diff reported one added
  file;
- removal was refused while either snapshot retained the logical file;
- GC reported two retained roots before snapshot deletion;
- restore to a task-owned external destination produced the expected file;
- delete without force returned usage exit 2;
- `--force --dry-run` remained read-only and left the snapshot present;
- deleting only the first snapshot preserved the retention refusal;
- deleting the second allowed both stored paths to be removed;
- GC retained roots fell from two to zero, and final verify passed.

Focused snapshot selector, query, filter, range, error, and delete-policy tests
also passed. The tracked-source snapshot evidence validator found all 19
required declarations. The local pull-request template requirement was
reviewed; Phase 21 neither creates nor edits a pull request description.

## 16. Final uncached and race sweeps

The required final full-repository sweeps used separate fresh PostgreSQL
databases:

```text
go test -count=1 ./...       PASS  real 7m59.362s
go test -race -count=1 ./... PASS  real 11m22.556s
```

The race sweep included `tests/adversarial` in 343.593s and
`tests/integration` in 335.084s. No package failed.

## 17. Section 18 final sign-off

The final sign-off revalidated:

- tracked-source snapshot evidence names;
- benchmark manifests, checksums, exact-SHA promotion, ignored inventory, and
  zero staging directories;
- branch-relative release linearity with no release-local merges;
- Doctor, validation-matrix, bootstrap on/off, clean-install, CLI, batch,
  physical graph, snapshot, and retention results;
- local CI-enforcement invariants;
- all 48 release-state fixtures and the supported real development-state
  context with zero violations;
- Go formatting, Bash syntax, smart quotes, ShellCheck, module tidiness, and
  `git diff --check`; and
- the 1,397-entry immutable candidate-core manifest byte-for-byte.

Post-test cleanup removed only Phase-21-generated storage, locks, binaries,
cross-build outputs, and Python bytecode. No `storage`, `.ci-storage`,
`cmd/coldkeep/storage`, `repository.lock`, test binary, `.pyc`, or
`__pycache__` artifact remains in the detached worktree. The ignored
contractual benchmark evidence is intentionally retained.

## 18. Skip and advisory audit

```text
EXPECTED_ENVIRONMENTAL_SKIPS: 0
UNEXPECTED_SKIPS: 0
HOSTED_ONLY: native macOS/Windows runtime and hosted security/governance
ARCHIVED_NON_GATING: checklist Sections 12–14
SUPPLEMENTARY_NON_BLOCKING: Section 4 (executed and passed)
ADVISORY: critical coverage report; four timing observations
NOT_PROMOTED: optional 1000-iteration refcount override; conditional v1.7 binary gate
```

Short suites intentionally omit long-run cases, but every promoted long-run
family ran in its dedicated row. The smoke script's large-file opt-in and
nested codec helper remained optional; both complete top-level codec smoke
matrices ran and passed.

## 19. Execution corrections and failure classification

The connection reset, detached-worktree recreation, supported release-state
context selection, local JSON/stream assertion alignment, sandbox access to the
external Go cache, and final Python-bytecode cleanup were environmental or
harness corrections. They changed no tracked candidate file and did not mask a
deterministic test failure.

No actual gate failed after the environment was proven. No product, test,
script, workflow, schema, dependency, candidate manifest, or existing release
document was edited.

## 20. Phase decision

```text
PHASE_21: COMPLETE
INDEPENDENT_FULL_LOCAL_EXACT_CANDIDATE_RELEASE_GATE: PASS
EXACT_CANDIDATE_SHA: 89a1890ad90a6778e0e19062a7eff0327482005a
IMMUTABLE_CANDIDATE_CORE_MANIFEST_ENTRIES: 1397
IMMUTABLE_CANDIDATE_CORE_MANIFEST_SHA256: 984221fc5dc0e9d2d76d97cfb2c334bd7a52733682d818cb839d7c5caacac4da
CORE_SURFACE_CHANGED: NO
PHASE_22: NOT_STARTED
```

Hosted runs triggered by the documentation commit, if any, are not Phase 21
acceptance evidence. Phase 22 requires separate authorization and remains not
started.
