# v1.13.16 Phase 6R9R2 — Bounded Benchmark-Evidence Signal Cleanup Harness Recovery

**Mode:** BUILD
**Status:** Implemented; branch-worktree certified; clean committed-tree and detached certification pending
**Base:** `53f64e130139ecb21f9b426cb2092e7851f82006`
**Finding:** CK-V11316-011, S2 CI/test reliability blocker

## Entry and diagnosis

Phase 6R9 completed its one-file throughput isolation and passed both the
branch-worktree and clean committed-tree matrices. Detached certification then
stopped only in the unchanged
`TestBenchmarkEvidenceLifecycleLeavesCleanWorktree/signal_cleans_unique_same-filesystem_staging`
subtest. The old failed full-suite command was not retried, no Phase 6R9 push
occurred, and its three commits remain immutable.

Phase 6R9R1 classified the timeout as `COMBINATION`. The primary component is
`TEST_FIFO_SIGNAL_SYNCHRONIZATION_DEFECT`; the secondary component is
`TEST_CHILD_PROCESS_LIFECYCLE_BOUNDING_DEFECT`. The exact original blocking
parent call was not recovered. The test reads readiness without a local bound,
never establishes or writes the release FIFO, sends SIGINT, accepts generic
non-success, and waits without unconditional exact-child cleanup. Bash may
therefore remain in the artificial release-FIFO open while INT-trap dispatch is
deferred.

Retained source and failed-test residue agree with bounded external evidence:
11 of 240 signal-only iterations blocked, all 11 exited 130 and removed staging
after a release record was supplied, and 140 of 140 writer-established-before-
SIGINT differential iterations exited 130 without hangs or cleanup failures.
This establishes a test-harness defect. It does not establish a production
benchmark-script, product-runtime, detached-worktree, or CI-infrastructure
defect.

## Exact authority boundary

Exactly one executable/test path may change:

- `scripts/phase14_release_gate_test.go`

`scripts/release_benchmark_evidence.sh`, all other scripts and tests, the CI
workflow, production runtime, thresholds, modules, dependencies, database and
schema surfaces, containers, and historical Phase 6R9 evidence remain frozen.
If the recovery needs a second executable path, it stops.

The helper must remain portable and standard-library-only. It starts the child
once and, after successful start, owns exactly one `cmd.Wait` goroutine whose
stored result is observed through one completion channel. A deferred cleanup
owner must exist before the first blocking FIFO operation. Every readiness,
release-writer, signal/write, child-wait, failure-cleanup, and goroutine-join
phase is locally bounded.

The real-script proof uses a five-second overall context. An owned readiness
reader must return a complete non-empty staging path. Then exactly one owned
goroutine opens the release FIFO with ordinary `O_WRONLY`, proving Bash has
opened its read side, before `os.Interrupt` is sent. Immediately after SIGINT,
the harness writes one complete newline-terminated release record and closes
the writer. Success requires a non-nil `*exec.ExitError` with `ExitCode()`
exactly 130; generic non-zero, exit 0, exit 1, exit 143, an outer-deadline kill,
or SIGKILL-only termination is not evidence.

Normal success must prove the reported staging and final exact-SHA evidence
directory are absent; the child is waited and reaped; readiness and writer
goroutines are joined; descriptors are closed; and neither fallback unblocking
nor forced kill was used. On failure only, cleanup targets the exact child,
kills it if still live, may use `O_RDWR` open/close solely to unblock test-owned
FIFO opens, joins all helper goroutines, observes the same single Wait owner,
closes descriptors, and reports bounded diagnostics. Fallback cleanup can never
convert failure into success.

The same helper must serve a new top-level
`TestBenchmarkEvidenceSignalHarnessBoundsMissingReadiness`. A deterministic
Bash fixture never writes the expected readiness record; a short injected
context, recommended 500 milliseconds, must return a readiness-phase deadline
error and prove exact kill, wait, unblock, join, descriptor closure, and zero
test-owned residue. Neither test may add sleep, CI detection, or detached-
worktree detection. Windows and missing-`mkfifo` skips remain required, and the
scripts test package must compile for Windows and Darwin.

## External residue cleanup

The retained Phase 6R9 failed-test root
`/tmp/coldkeep-v11316-phase6r9-detached-gotmp/TestBenchmarkEvidenceLifecycleLeavesCleanWorktreesignal_cleans_3830410463`
was externally inventoried before deletion. It was a real current-user-owned
directory beneath the exact expected GOTMPDIR; its ready and release children
were FIFOs; fixture HEAD was
`a2e5b8e3d79114d3f23016c0bd3858aa54cd1c60`; the expected
`.staging-a2e5b8e3d79114d3f23016c0bd3858aa54cd1c60.YZNnTVD2` existed; the
final exact-SHA directory was absent; and no process, descriptor, current
working directory, or registered worktree owned it. Only that exact root was
removed. It and its related FIFO/staging content are absent, and the repository
remained unchanged.

## Required certification

Before the test commit: cross-compile `./scripts` for Windows amd64 and Darwin
amd64, then pass one ordinary, race, atomic-coverage real signal proof and one
negative proof. Branch stress then requires real-signal ordinary 200, race 100,
atomic 100, negative 50, parent ordinary 20 and race 10, plus the complete
scripts package ordinary and race. Every normal proof must log exit 130 with no
fallback, kill, deadline, process, FIFO, staging, fixture, or helper-goroutine
residue.

The accepted Phase 6R9 throughput semantics must remain intact: ordinary 20
and race 10 enforce their unchanged floors, atomic 10 performs real operations
and skips only the two numeric floors, full Phase 1 coverage preserves exactly
those skips, and critical coverage passes three independent reports with 17
data rows each. Storage ordinary/race, quality plain/AES-GCM, full Go/race/vet,
module, format, exact golangci-lint 2.9.0, shell, policy, governance, Phase 3,
and exactly 215 Python tests must pass. The same reduced matrices must pass on
the clean committed tree and a fresh detached exact-head worktree. Detached Go
commands alone use the established source-neutral `GOFLAGS=-buildvcs=false`
linked-worktree policy. The prior isolated PostgreSQL 12-of-12 matrix is
retained; no PostgreSQL provisioning is authorized because no product, script,
storage, integration, adversarial, or backend path changes.

## Implementation and branch-worktree evidence

Authority `4105e8579974bc340a43b197f8b9504e71530eda` and test
`8784873e141bf1af102fbfa0573f3f5b43a1a5de` completed the exact one-file
implementation. `scripts/phase14_release_gate_test.go` has SHA-256
`e8b123a3b82e776a1b38ba62dbc66965b9a944c3e56f4570184593c50186be55`.
It owns one Wait goroutine, bounded ready and ordinary `O_WRONLY` release
handshakes, SIGINT followed by one newline-terminated release record and close,
exact exit 130, explicit normal-path joins, and failure-only two-second
fallback cleanup. The real proof remains bounded at five seconds and the
negative missing-readiness proof at 500 milliseconds.

The first unsharded 200-count command stopped at its three-minute package
budget after 123/123 completed signal iterations had passed with exact exit
130; iteration 124 was still in fixture preparation and had not entered the
signal helper. Its classification is
`VALIDATION_STRESS_AGGREGATION_TIMEOUT_BUDGET_DEFECT`, not a harness,
production, runtime, or CI defect. That command was not rerun and those 123
iterations remain supporting evidence only.

Corrected independently bounded branch shards passed ordinary 200/200, race
100/100, atomic coverage 100/100, and negative readiness 50/50. Parent
ordinary passed 20/20 and race 10/10; the complete scripts package passed
ordinary and race with 218 test identities, exact exit 130, the negative
harness, zero race reports, and no forced cleanup on success. Every shard and
package residue audit found no process, descriptor, FIFO, staging directory,
or fixture repository.

Phase 6R9 semantics remained exact: ordinary 20 and race 10 enforced the
unchanged floors; atomic 10 executed and measured each operation before 10
expected Store and 10 expected Restore skips; full Phase 1 coverage passed all
other tests with exactly those two skips. Critical coverage passed 3/3 with 17
populated unique rows per CSV. Storage ordinary/race, quality plain/AES-GCM,
full Go/race/vet, Windows and Darwin scripts-package compilation, module,
format, shell, policy, exact golangci-lint 2.9.0 with zero issues, current
authority, governance, Phase 3, and exactly 215 Python tests passed. The branch
worktree remained clean. Clean committed-tree and detached certification are
still required before the single push.

## Hosted truth and commit policy

The unchanged hosted workflow compiles and vets `./scripts` and runs
`TestCandidateLintGate`, but does not directly execute the benchmark lifecycle
parent or signal subtest. Direct CK-V11316-011 execution evidence is therefore
branch-worktree, clean committed-tree, and detached exact-head stress only.
After those pass, one push must produce fresh exact-head whole-candidate CI
36/36, CodeQL 4/4, and zero open CodeQL alerts. Hosted success must not be
described as direct signal-subtest execution.

Exactly three linear Phase 6R9R2 commits are authorized:

1. `docs: authorize v1.13.16 benchmark signal test recovery`
2. `test: bound benchmark evidence signal cleanup handshake`
3. `docs: record v1.13.16 benchmark signal test recovery`

No amend, rebase, reset, squash, merge, retry of an old run, fourth commit,
post-hosted evidence commit, or second push is authorized. A remote move,
allowlist breach, unbounded helper phase, wrong exit code, surviving resource,
validation failure, or mismatch between committed and detached results stops
the build. Phase 6 stays withheld, Phase 7 remains unauthorized, and v1.x
technical correctness closure remains withheld.

```text
BASE_HEAD: 53f64e130139ecb21f9b426cb2092e7851f82006
DETACHED_TIMEOUT_CLASSIFICATION: COMBINATION
PRIMARY_COMPONENT: TEST_FIFO_SIGNAL_SYNCHRONIZATION_DEFECT
SECONDARY_COMPONENT: TEST_CHILD_PROCESS_LIFECYCLE_BOUNDING_DEFECT
EXACT_ORIGINAL_BLOCKING_CALL: NOT_RECOVERED
PHASE_6R9R2: BRANCH_WORKTREE_CERTIFIED_COMMITTED_AND_DETACHED_PENDING
CK-V11316-009: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
CK-V11316-010: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
CK-V11316-011: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
FINDINGS_CONFIRMED: 11
FINDINGS_CLOSED: 1/11
DIRECT_CK_V11316_011_EXECUTION_EVIDENCE: BRANCH_PASS_COMMITTED_DETACHED_PENDING
HOSTED_DIRECT_SIGNAL_SUBTEST_EXECUTION: NOT_PRESENT_IN_CURRENT_WORKFLOW
PRIOR_ISOLATED_POSTGRESQL_MATRIX: RETAIN_PASS_12_OF_12
PHASE_6: WITHHELD
PHASE_7: NOT_AUTHORIZED
V1_X_TECHNICAL_CORRECTNESS_CLOSURE: WITHHELD
```
