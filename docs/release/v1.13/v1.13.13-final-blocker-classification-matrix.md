# Coldkeep v1.13.13 Final Blocker Classification Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `6 — Deferred-Item and Blocker Freeze`
**Status:** Frozen

`OPEN_FROZEN` means the finding is mandatory before v1.x closure and may be
closed only by the frozen Phase 7–9 workflow. `NOT_REQUIRED_FOR_V1_CLOSURE`
means the item has a named later owner and cannot expand the remediation set.

| Finding ID | Origin | Normative requirement | Original classification | Phase 6 classification | Why | Consequence | Required outcome | Workstream | Regression contract | Target phase | Closure status | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `P2-ARCH-001` | Phase 2 | `V1R-CAT-003` | `PARTIAL`; zero Phase 2 architecture blockers | `V1_X_BLOCKER` | Complete neutral typed Catalog errors are mandatory before v1 closure. Engine translation covers only two consumers and cannot satisfy the reusable aggregate Catalog contract; the three library-only methods have no authoritative deferral. | Direct Catalog callers cannot depend on stable, backend-neutral error codes or cancellation/cause meaning across the aggregate interface. | All five affected methods translate query/scan failures into stable Catalog errors while preserving cause, cancellation, and deadline meaning. | `WS-CATALOG-TYPED-ERRORS` | Query, scan, cancellation, deadline, unknown failure, and SQLite/PostgreSQL fault parity; no race requirement. | Phases 7–9 | `OPEN_FROZEN` | Promotion reconciles the audit verdict with the closure test; it does not rewrite Phase 2 history. |
| `P3-COR-001` | Phase 3 | `V1R-COR-001`, `V1R-COR-005`, `V1R-XPLAT-002` | `BLOCKER` | `V1_X_BLOCKER` | Atomic authoritative destination installation is mandatory, backend-independent, and has no platform or productization bound. | No-overwrite can replace a final-window entrant; an exact planned destination can be reinterpreted as a directory. | Exact destinations remain exact; no-overwrite is atomically non-replacing; overwrite remains intentional; dry-run and live destination meaning agree. | `WS-RESTORE-DESTINATION` | Final-window, exact-existing-directory, selectors, overwrite, cleanup/fault, and Linux/macOS/Windows proof. | Phases 7–9 | `OPEN_FROZEN` | One finding with two recorded subcases; distinct from confinement. |
| `P3-COR-002` | Phase 3 | `V1R-COR-004`, `V1R-XPLAT-002` | `BLOCKER` | `V1_X_BLOCKER` | Destination confinement must hold at mutation time; preflight path checks provide no authoritative bound for parent replacement. | A checked parent can be replaced by a symlink and redirect restored bytes or metadata outside the trusted root. | Installation and directory creation remain confined when a parent identity changes after validation. | `WS-RESTORE-DESTINATION` | Deterministic parent-swap/adversarial tests across every selector and Linux/macOS/Windows, plus cleanup/fault proof. | Phases 7–9 | `OPEN_FROZEN` | Shares implementation design with `P3-COR-001` but retains an independently testable blocker identity. |
| `P3-COR-003` | Phase 3 | `V1R-COR-006`, `V1R-COR-007` | `BLOCKER` | `V1_X_BLOCKER` | Deep verification is a fail-closed v1 claim for packed, legacy, and mixed repositories; no backend/product bound excludes packed placements. | Packed-only corruption can report success and mixed files can leave packed chunks unchecked. | Verify every authoritative placement, fail closed on missing/conflicting placement, and report truthful checked counts. | `WS-VERIFY-PACKED-DEEP` | SQLite/PostgreSQL legacy, packed, and mixed corruption/missing/conflict fixtures; no race requirement. | Phases 7–9 | `OPEN_FROZEN` | One route-level verification root, not duplicated per backend or repository shape. |
| `P4-BE-001` | Phase 4 | `V1R-BE-003` | `BLOCKER` | `V1_X_BLOCKER` | Comparable shared-backend behavior is mandatory. Current-file search and the pre-Catalog snapshot route deliberately use `LOWER(...) LIKE LOWER(...)`; native `LIKE` introduced an undocumented divergence. | The same supported snapshot-list request returns different sets on SQLite and PostgreSQL. | Limited and unlimited routes use narrow ASCII case-insensitive substring matching and return identically ordered results on both backends. | `WS-BACKEND-LABEL-SEMANTICS` | Opposite-case fixtures through Catalog, Engine, and CLI on SQLite/PostgreSQL, limited and unlimited, with identical ordering. | Phases 7–9 | `OPEN_FROZEN` | The policy follows prior Coldkeep behavior, not SQLite's database default; Unicode/locale normalization is out of scope. |
| `P5-COORD-001` | Phase 5 | `V1R-COORD-001` | `BLOCKER` | `V1_X_BLOCKER` | Same-host cooperative repository ownership is mandatory. `simulate gc` opens the shared application path and may perform schema work plus planning without the lease; no simulation-name exception exists. | A normal Coldkeep process can enter shared schema/planning work while another process owns the repository. | Acquire before shared schema/application work and retain ownership through planning and cleanup while keeping genuinely isolated simulations lease-free. | `WS-SIMULATE-GC-COORDINATION` | Policy, startup order, real-holder contention, safe exceptions, PostgreSQL subprocess, and relevant race proof. | Phases 7–9 | `OPEN_FROZEN` | Schema and plan exposure are consequences of one policy/routing root. |
| `P2-DOC-001` | Phase 2 | Current documentation truth; not a technical normative gap | `DOCUMENTATION_DEBT` | `DOCUMENTATION_DEBT` | Stale pre-v1.13.12 prose is contradicted by current source and therefore cannot downgrade architecture. | Readers may encounter obsolete current-state descriptions. | Reconcile the nonhistorical engine-boundary plan without changing historical evidence. | none | Documentation review and link/current-truth validation. | Phase 10 | `NOT_REQUIRED_FOR_V1_CLOSURE` | Phase 10 owns the edit; it is not a Phase 7–9 implementation workstream. |

## Totals

```text
FINAL_V1_BLOCKERS: 6
REMEDIATION_WORKSTREAMS: 5
DOCUMENTATION_DEBT_ITEMS: 1
AMBIGUOUS_FINDINGS: 0
```
