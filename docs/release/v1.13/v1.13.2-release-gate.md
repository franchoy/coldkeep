# Coldkeep v1.13.2 — Release Gate

## Release metadata

- release: `v1.13.2 — Engine Error Taxonomy Baseline`
- branch: `release/v1.13.2`
- release type: `engine/catalog taxonomy baseline / narrow primitives / tests-docs`
- final status: `ready for PR review`
- date of validation: `2026-06-07`
- validated branch head before ready-gate doc update:
  `011250f2acac1b0f15290028aee7a5b5ea92a8b9`

## Final release statements

- `v1.13.2` establishes the first engine/catalog error taxonomy baseline.
- It does not introduce a broad error framework.
- It does not change public CLI behavior.
- It does not change JSON output.
- It does not change exit codes.
- It does not change command routing.
- It does not change engine routing.
- It does not implement deferred operations.
- It does not change schema/migrations.
- It does not change storage/repository format.
- It does not change backend defaults.
- It preserves PostgreSQL compatibility.

## Release artifacts

Release docs:

- `docs/release/v1.13/v1.13.2-scope.md`
- `docs/release/v1.13/v1.13.2-phase-list.md`
- `docs/release/v1.13/v1.13.2-validation-checklist.md`
- `docs/release/v1.13/v1.13.2-error-surface-inventory.md`
- `docs/release/v1.13/v1.13.2-error-taxonomy.md`
- `docs/release/v1.13/v1.13.2-domain-invariant-runtime-map.md`
- `docs/release/v1.13/v1.13.2-error-taxonomy-summary.md`
- `docs/release/v1.13/v1.13.2-release-gate.md`

Key code/test artifacts:

- `internal/engine/errors.go`
- `internal/engine/errors_test.go`
- `internal/engine/unsupported_modes_test.go`
- `internal/catalog/errors.go`
- `internal/catalog/errors_test.go`
- `internal/catalog/service_test.go`
- `internal/catalog/backend_contract_test.go`
- `internal/engine/validation_vs_unsupported_test.go`
- `cmd/coldkeep/cli_translation_preservation_test.go`

## Phase completion table

| Phase | Name | Status | Main artifact/result | Behavior impact |
| --- | --- | --- | --- | --- |
| 0 | Release Scope and Baseline Docs | complete | scope, phase list, and validation docs | none; docs-only baseline |
| 1 | Current Error-Surface Inventory | complete | `v1.13.2-error-surface-inventory.md` | none; current surfaces inventoried |
| 2 | Define Taxonomy Names and Meanings | complete | `v1.13.2-error-taxonomy.md` | none; taxonomy language defined |
| 3 | Engine Taxonomy Primitives | complete | `engine.IsUnsupported(err)` | none; narrow primitive and tests only |
| 4 | Catalog Taxonomy Parity | complete | `catalog.IsDeferred(err)` | none; narrow primitive and tests only |
| 5 | Validation vs Unsupported Boundary Tests | complete | `validation_vs_unsupported_test.go` | none; boundary tests only |
| 6 | Domain vs Invariant vs Runtime Classification Inventory | complete | `v1.13.2-domain-invariant-runtime-map.md` | none; mapping only |
| 7 | CLI Translation Preservation Tests | complete | `cli_translation_preservation_test.go` | none; CLI preservation tests only |
| 8 | Documentation and v1.13.3 Handoff | complete | `v1.13.2-error-taxonomy-summary.md` | none; docs-only closeout |
| 9 | Final Validation and Release Gate | complete | `v1.13.2-release-gate.md` | none; release-gate evidence and decision only |

## Key changes summary

- created release scope and validation docs
- inventoried current error surfaces
- defined formal taxonomy language
- added narrow `engine.IsUnsupported(err)` primitive
- added narrow `catalog.IsDeferred(err)` primitive
- preserved `errors.Is` compatibility for engine/catalog sentinels
- kept engine and catalog sentinels distinct
- added validation-vs-unsupported boundary tests
- added CLI translation preservation tests
- mapped domain/invariant/runtime/catalog/storage/environment surfaces
- created `v1.13.3` handoff summary

## Code-backed taxonomy baseline

| Category | v1.13.2 status | Mechanism |
| --- | --- | --- |
| Unsupported | code-backed narrowly | `engine.IsUnsupported(err)` |
| Deferred | code-backed narrowly | `catalog.IsDeferred(err)` |
| Validation | protected by boundary tests, no unified helper | engine validation-vs-unsupported tests |
| Domain | mapped and representative CLI path preserved, no unified helper | domain/state docs plus CLI preservation test |
| Invariant | mapped and representative CLI path preserved, existing wrappers retained | existing invariant wrappers plus CLI preservation test |
| Runtime | mapped, no unified helper | runtime/origin mapping docs |
| Catalog | partially through catalog deferred sentinel and mapping | `catalog.IsDeferred` plus catalog-origin map |
| Storage | mapped, no unified helper | storage-origin map |
| External / Environment | documented/release-gate category | release docs and gate evidence |
| CLI Translation | protected by tests | CLI translation preservation tests |
| Unknown / Needs later decision | documented only | inventory, taxonomy, and mapping docs |

## Boundary protection summary

- `engine.ErrNotImplemented` remains stable.
- `engine.IsUnsupported` recognizes wrapped `engine.ErrNotImplemented`.
- `catalog.ErrNotImplemented` remains stable.
- `catalog.IsDeferred` recognizes wrapped `catalog.ErrNotImplemented`.
- `engine.IsUnsupported(catalog.ErrNotImplemented)` is false.
- `catalog.IsDeferred(engine.ErrNotImplemented)` is false.
- unsupported active engine modes remain unsupported.
- malformed engine requests are not unsupported.
- CLI JSON does not expose taxonomy helper names or taxonomy fields.
- CLI exit-code behavior remains unchanged.

## Remaining intentional gaps

- no full taxonomy framework
- no unified validation helper
- no unified domain helper
- no unified invariant helper
- no unified runtime helper
- no general catalog-origin helper
- no general storage-origin helper
- no public CLI taxonomy fields
- no JSON error-shape change
- no exit-code change
- no read-side contract cleanup yet
- no snapshot diff/query cleanup yet
- no restore/remove contract split yet
- no snapshot mutation boundary implementation
- no repair/recovery boundary implementation
- no deferred catalog method implementation

## v1.13.3 handoff

- focus is read-side contract cleanup for `stats / inspect / snapshot show / snapshot diff`
- use the `v1.13.2` taxonomy baseline
- preserve public CLI behavior unless explicitly scoped
- preserve JSON output
- preserve exit codes
- avoid mutation surface implementation
- avoid broad routing changes
- distinguish domain not-found from invariant inconsistency
- avoid overclassifying provisional result shapes

## Validation results

| Command | Result | Notes |
| --- | --- | --- |
| `git status -sb` | passed | clean pre-edit state: `## release/v1.13.2...origin/release/v1.13.2` |
| `git diff --check` | passed | no output before doc edits |
| `gofmt -l $(git ls-files '*.go')` | passed via safe equivalent | the direct shell-substitution form mis-executed under the sandbox wrapper; rerun safely as `git ls-files '*.go' \| xargs gofmt -l` produced no output |
| `go test -count=1 ./internal/engine` | passed | exit code `0`; Phase 3 and Phase 5 engine coverage remained green |
| `go test -count=1 ./internal/catalog` | passed | exit code `0`; Phase 4 catalog parity coverage remained green |
| `go test -count=1 ./cmd/coldkeep` | passed | exit code `0`; Phase 7 CLI preservation coverage remained green |
| `go test -count=1 ./internal/cli/...` | passed | exit code `0`; `internal/cli/render` suite passed |
| `go test -count=1 ./...` | passed | run with `GOCACHE=/tmp/gocache GOMODCACHE=/tmp/gomodcache`; repo-wide suite passed |
| `go test -race -count=1 ./...` | passed | run with `GOCACHE=/tmp/gocache GOMODCACHE=/tmp/gomodcache`; repo-wide race suite passed |
| `go vet ./...` | passed | run with `GOCACHE=/tmp/gocache GOMODCACHE=/tmp/gomodcache`; exit code `0` |
| `golangci-lint run ./...` | passed | `0 issues.` |
| `docker compose up -d coldkeep_postgres` | passed | PostgreSQL container started and reached healthy state on `127.0.0.1:5432` |
| PostgreSQL reachability check | passed | `psql` returned `SELECT 1` successfully against the local service |
| `scripts/run_snapshot_release_gate.sh --count 1` | passed | ended with `[snapshot-gate] PASS` |
| `scripts/audit_ci_enforcement.sh` | passed | local workflow invariant checks and remote protection-policy audit both passed |
| `gh auth status` | passed | valid `github.com` auth for `franchoy`; repo/workflow scopes available |
| `curl -I https://api.github.com` | passed | returned `HTTP/2 200` |
| legacy compatibility gate | passed | `go test -race -count=1 ./tests/integration/... -run 'TestPhase2PostMigrationStoreRestoreSnapshotRegressionIntegration'` passed |
| local cross-platform approximation | passed | `go test ./internal/pathsafe/... -count=1` and `go test ./internal/storage -run "RestoreCrossPlatform|RestoreSeam" -count=1` both passed |
| plain-codec CI-matrix local equivalent | passed | integration short, integration full, long-run subset, refcount containment, adversarial suite, explicit `G14`-`G17`, and `scripts/smoke.sh` all passed |
| AES-GCM CI-matrix local equivalent | passed | integration short, integration full, long-run subset, refcount containment, adversarial suite, explicit `G14`-`G17`, and `scripts/smoke.sh` all passed |
| benchmark matrix | passed after investigation | initial first-pass threshold checks failed for uncompressed `workers=1` and `workers=4`, and warning-level compressed `workers=1`; focused reruns for `workers=1` uncompressed, `workers=1` compressed, and `workers=4` uncompressed all passed with no regressions; compressed `workers=4` passed on the first run |
| docs `TODO/TBD/FIXME` check | passed with expected historical matches | current `v1.13.2` docs only matched documented command strings in existing release docs/checklists |

Release-specific targeted validation notes:

- engine unsupported primitive tests: covered by `go test -count=1 ./internal/engine`
- catalog deferred primitive tests: covered by `go test -count=1 ./internal/catalog`
- validation-vs-unsupported tests: covered by `go test -count=1 ./internal/engine`
- CLI translation preservation tests: covered by `go test -count=1 ./cmd/coldkeep`

## PRE_RELEASE_CHECKLIST.md Profile A

Profile used:

- `Profile A - Pre-PR CI-parity gate`

Profile A status:

- `passed`

What ran:

- Section 1:
  - `docker compose up -d coldkeep_postgres`
  - checklist-compatible PostgreSQL/AES key environment exports
  - local PostgreSQL reachability check via `psql`
- Section 2:
  - `bash scripts/clean_test_storage.sh`
  - `GOCACHE=/tmp/gocache GOMODCACHE=/tmp/gomodcache go mod tidy && git diff --exit-code`
  - `bash -n scripts/*.sh`
  - `bash scripts/check_smart_quotes.sh`
  - `find scripts/ -maxdepth 1 -name '*.sh' ! -name 'critical_coverage.sh' -print0 | xargs -0 shellcheck`
  - `scripts/validate_validation_matrix.sh`
  - `bash scripts/check_versioned_row_writers.sh`
  - `GOCACHE=/tmp/gocache GOMODCACHE=/tmp/gomodcache go build ./...`
  - `bash scripts/audit_ci_enforcement.sh --local-only`
- Section 3:
  - plain-codec integration correctness/stress/long-run/refcount/adversarial/smoke path
  - AES-GCM integration correctness/stress/long-run/refcount/adversarial/smoke path
  - benchmark matrix
  - legacy compatibility gate
  - local cross-platform approximation
  - full `scripts/audit_ci_enforcement.sh`
  - `bash scripts/run_snapshot_release_gate.sh --count 1`
- Final local checks:
  - `git diff --check`
  - `git status -sb`

Benchmark investigation note:

- The initial first-pass benchmark threshold checks reported:
  - hard fail for uncompressed `workers=1`
  - hard fail for uncompressed `workers=4`
  - warnings for compressed `workers=1`
  - pass for compressed `workers=4`
- Focused reruns were then executed for the unstable combinations:
  - uncompressed `workers=1` rerun: passed with no regressions
  - compressed `workers=1` rerun: passed with no regressions
  - uncompressed `workers=4` rerun: passed with no regressions
- Final benchmark conclusion:
  - no reproducible benchmark regression remains for `v1.13.2`
  - the first-pass failures were treated as transient local variance or contaminated initial runs, not persistent branch regressions

Profile A conclusion:

- Profile A is green in this environment.

## Final decision

`ready for PR review`

Reason:

- The local code/test validation baseline is green.
- The missing environment-gated Profile A work completed successfully in a real validation environment.
- Docker/PostgreSQL setup completed successfully.
- The snapshot release gate passed.
- The full remote CI audit passed.
- The legacy compatibility and local cross-platform approximation checks passed.
- The benchmark matrix completed with clean reruns for the initially unstable combinations and no reproducible regression remained.
- `v1.13.2` preserved CLI behavior, JSON output, exit codes, routing, schema, storage format, repository format, and backend defaults.

Next step:

- open the PR for `release/v1.13.2`
- keep the benchmark investigation note with the release evidence so the first-pass variance and clean reruns remain traceable
