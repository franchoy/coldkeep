# v1.13.16 Phase 12R1R18 — Profile A Adversarial Timeout Recovery

**Status:** Authorized in Build mode; implementation and certification pending

## Classification

The exact-candidate Profile A restart at
`1bde7a301f4771b3056d9c418f8da308389c46ba` stopped at the first genuine
candidate failure when the full long-run adversarial race package reached Go's
implicit ten-minute package timeout while actively encoding zstd data.

```text
ADVERSARIAL_TIMEOUT_CLASSIFICATION=PACKAGE_WIDE_DEFAULT_TIMEOUT_EXPIRED_DURING_ACTIVE_FORWARD_PROGRESS
TIMEOUT_STATE=ACTIVE_FORWARD_PROGRESS
RECOVERY_CLASS=PROFILE_A_TIMEOUT_POLICY_OR_HARNESS_PROBLEM
CK015_PERFORMANCE_OR_LIVENESS_REGRESSION=NOT_ESTABLISHED
PRODUCT_DEFECT=NOT_ESTABLISHED
NEW_FINDING=NO
```

The retained stack contains no database-lock, repair-mutex, chunk-claim,
deadlock, livelock, or repeated-convergence signature. Historical successful
full-package evidence already reached 539.294 seconds. A controlled identical
focused comparison completed at both pre-R1R17 `cbc415e2...` and R1R17
`5e7fb399...`, with the latter approximately 6.7 percent faster. Healthy
harness execution does not enter the R1R17 zero-decision convergence path.
The original Profile A failure remains FAIL evidence and is not converted to
PASS by that diagnostic.

## Frozen recovery

The full long-run adversarial race package command receives an explicit finite
Go package timeout of 20 minutes in `PRE_RELEASE_CHECKLIST.md` and its
equivalent `.github/workflows/ci.yml` command. Twenty minutes is an
orchestration ceiling, not a product-performance guarantee: it is roughly
twice the largest retained successful duration, materially above the observed
ten-minute boundary, and remains below the independent unchanged 60-minute
hosted job ceiling.

The timeout applies only to that exact full-package gate. Focused selectors
and unrelated Go suites do not inherit it. Iterations, internal plain/AES-GCM
enumeration, compression, `COLDKEEP_LONG_RUN=1`, race execution,
Store/Remove/GC/snapshot/corruption/Verify cadence, and correctness assertions
remain unchanged.

Local CI enforcement must require the exact 20-minute value in both the
checklist and workflow commands and reject omission, unlimited timeout, wrong
value, or local/hosted disagreement. No production, adversarial-test, schema,
dependency, API, storage-format, workload, or finding change is authorized.

## Exact scope and history

Executable/checklist paths are limited to:

- `PRE_RELEASE_CHECKLIST.md`
- `.github/workflows/ci.yml`
- `scripts/audit_ci_enforcement.sh`
- `scripts/audit_ci_enforcement_test.go`

Authority/evidence documentation is limited to this record, the R1R17 record,
and the v1.13 README, phase list, release state, remediation tracker, scope,
source/test allowlist, and validation checklist. Every other path, including
the release-gate record, is frozen.

Create exactly four append-only commits:

1. `docs: authorize v1.13.16 adversarial timeout recovery`
2. `test: preserve explicit adversarial timeout policy`
3. `fix: make adversarial long-run timeout explicit`
4. `docs: record v1.13.16 adversarial timeout recovery certification`

Commit 2 must be RED only because explicit timeout enforcement is absent.
No amend, squash, reset, rebase, recreation, push, hosted execution, or Phase
13 work is authorized.

## Certification boundary

At clean commit 3, run the audit regressions; complete scripts ordinary/race,
vet, formatting, exact golangci-lint 2.9.0, Bash/ShellCheck, governance,
release-state, local-CI, linearity, diff, scope, and clean-tree gates; then run
the affected harness and complete adversarial long-run race package with the
explicit timeout for both codec profiles using fresh PostgreSQL 16.15 database
and storage identities. Repeat the complete recovery certification from a
fresh detached worktree and prove every non-allowlisted path byte-identical to
`1bde7a301f4771b3056d9c418f8da308389c46ba`.

Only complete attached and detached success permits commit 4. Profile A then
restarts from command 1 at the exact evidence commit. Earlier Profile A PASS
evidence is not reusable, and any genuine candidate failure is terminal.

```text
BUILD_AUTHORIZATION=AUTHORIZED_LOCAL_ONLY
PUSH=NOT_AUTHORIZED
HOSTED_CERTIFICATION=NOT_STARTED
PHASE_13=NOT_AUTHORIZED
```
