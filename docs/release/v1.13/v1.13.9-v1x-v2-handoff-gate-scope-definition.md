# v1.13.9 Phase 20 — v1.x / v2.0 Handoff Gate Scope Definition

Starting SHA: `fdda95e516798dba9b6e4ff1c7060c42290c4c9c`

## Summary

Phase 20 closes the remaining tracker-definition gap after Phase 19 defined
the broader handoff direction but still left the next concrete `v1.13.9` phase
unnamed and unbounded.

This phase is docs-only. It does not implement the handoff review, does not
change runtime behavior, and does not change any CLI, engine, schema, storage,
script, or workflow behavior.

Its purpose is to define the exact checked-in Phase 20 scope and the next
concrete follow-up phase using the current release documents as the source of
truth.

## Defined next concrete phase

The next concrete phase after this scope-definition pass is:

`Phase 21 — v1.x / v2.0 Handoff Gate`

That later phase is where the actual readiness review may happen. Phase 20
itself does not start it.

## Intended role of the later handoff gate

If the later handoff-gate phase proceeds from this definition, its intended
role is limited to:

- final readiness review against the checked-in `v2.0` handoff requirements
- explicit mandatory versus deferred remainder
- honest statement of whether `v2.0` work can begin
- final release-gate documentation and validation artifacts only if the
  checked-in tracker explicitly requires them there

That later pass must not hide runtime behavior work inside a supposed handoff
review and must not overclaim readiness.

## Current boundaries preserved by this pass

Phase 20 does not change the already checked-in baseline that:

- snapshot create/delete/restore routing and thin-wrapper proof follow-up still
  remain separate later work
- remaining stabilization tasks in `v1.13.9` must stay explicit
- no release PR work starts unless a later checked-in phase explicitly
  authorizes pre-release closure

## Forbidden in Phase 20

Phase 20 does not:

- claim `v2.0` readiness
- start the actual handoff review
- start pre-release closure or release PR work
- change CLI output or JSON contracts
- change parser behavior
- change engine contracts
- change snapshot, backend, verify, repair, or handoff-related runtime behavior
- change schema, migrations, storage format, scripts, or workflows
- begin any runtime implementation phase
- open or reopen a pull request

## Acceptance criteria

Phase 20 is complete when:

- the checked-in `v1.13.9` tracker defines an explicit Phase 20 title
- the checked-in `v1.13.9` tracker defines this artifact as the canonical
  Phase 20 record
- the checked-in `v1.13.9` tracker explicitly marks Phase 20 docs-only
- the checked-in Phase 20 artifact defines the next concrete phase after
  Phase 20
- the checked-in Phase 20 artifact records that no readiness-review or
  pre-release closure implementation starts in this pass
- no runtime code changes land

## Validation

Because this phase is docs-only, the local validation gate is intentionally
narrow:

- `git diff --check`
- release-doc consistency grep across:
  - `docs/release/v1.13/v1.13.9-phase-list.md`
  - `docs/release/v1.13/v1.13.9-validation-checklist.md`
  - `docs/release/v1.13/v1.13.x-release-train.md`
  - `docs/release/v1.13/v1.13.9-v1x-v2-handoff-gate-scope-definition.md`

## Handoff

The checked-in `v1.13.9` tracker now defines the concrete handoff-gate follow-up,
but it does not begin that review in this phase.

Any later readiness-review or pre-release closure work must remain an explicit
follow-up Build pass and must not infer runtime changes from memory.
