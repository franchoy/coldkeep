# Phase 12R1R39 — CK-015 proof-enforcement gap correction

## Authority and purpose

The unchanged R1R38 candidate
`236a6198e26310dc5b42b97ab82002f85c3b69bf` retains correct CK-015 runtime
wrappers and earlier named storage proofs, but a repository-native negative
matrix confirmed that its CI audit accepts sixteen isolated changes that
disable required evidence completeness, stale-file refusal, immediate
`PIPESTATUS` capture, or Go-failure propagation. R1R39 corrects that audit and
its regression coverage. It is separate prospective authority, not a third
R1R38 commit and not a new storage finding.

R1R39 authorizes exactly two append-only local commits from the unchanged
R1R38 candidate:

1. `docs: authorize v1.13.16 CK-015 enforcement-gap correction`; and
2. `ci: harden CK-015 proof wrapper enforcement`.

The first commit changes exactly the nine authority paths frozen below. The
second changes exactly the two audit/test paths frozen below. No workflow,
Profile A checklist, event checker/profile, product source, storage test,
database, schema, migration, module, dependency, public API, storage format,
benchmark, or other implementation path enters scope.

CK-V11316-015 stays open. CK-V11316-014 stays closed certified,
CK-V11316-007 stays open, and the aggregate remains 15 confirmed and 13/15
closed. No push, hosted run, finding closure, Phase 12 completion, Phase 13,
merge, tag, or release is authorized.

## Confirmed enforcement gap

At candidate `236a6198...`, the complete repository
`scripts/audit_ci_enforcement.sh --local-only` passed its unchanged positive
control and also accepted all sixteen independently invalid fixtures: four
mutations in each hosted/local SQLite/PostgreSQL wrapper. The invalid fixtures
removed Go stderr from the active completeness enumeration, returned zero from
the Go-failure branch, delayed the `PIPESTATUS` snapshot with an intervening
pipeline, or disabled the stale-target condition while retaining its message.

Those zero exits are enforcement bypass evidence, not successful release
gates. No additional repository predicate rejected a mutation. The current
runtime wrappers are not shown defective by this finding and are frozen.

The retained confirmation package verifies locally at 866,865 bytes and
SHA-256
`5bd7fc29f06476c89eaeda3f5fd8bbed69d8b9417f2545c4e9560de451172902`.
Its 203-entry payload manifest and seventeen per-case records pass the R1R39
entry gate. Its report says an aggregate runner receipt was absent when the
report was frozen, while the hash-frozen archive contains a later `0` receipt
bound to the same persistent runner command. This stale reporting observation
does not replace the complete per-case terminal evidence and is preserved as
an explicit process qualification.

## Bounded active wrapper grammar

Retain the existing wrapper extraction and shared
`check_ck015_proof_wrapper` entry point. Add a narrow validator for the frozen
CK-015 wrapper forms. Acceptance must depend on active executable structure,
not isolated strings or ordered fragments. The validator must require exactly
one recognized instance of each protected region, reject unsupported or
duplicate critical structures, and enforce their active order:

1. the exact six-variable stale-target loop with active
   `[ -e "$target" ]`, wrapper-specific refusal, and `exit 1` before metadata
   creation and execution;
2. the intended Go/tee logical pipeline immediately followed by the complete
   `PIPESTATUS` snapshot and exact Go/capture assignments;
3. the real checker and status-record commands immediately followed by their
   status captures, plus active fail-closed metadata-write handling;
4. after `evidence_status=0`, the exact six-variable regular/readable
   completeness loop and active status/metadata consistency checks; and
5. one contiguous Go → capture → checker → status-write → evidence precedence
   chain assigning the corresponding value to the actual return status.

Comments, quoted strings, here-documents, never-called functions, inactive
enclosures, duplicate decoys, early successful exits, and later status resets
must not satisfy or defeat the protected grammar. This is not authority for a
general shell parser or unrelated audit redesign.

Hosted wrappers retain their unconditional final `exit "$status"`. Local
Profile A wrappers retain `set -e` and exit only inside the nonzero-status
conditional so successful execution falls through to the next check. The
validator must recognize both forms without changing the checklist.

## Regression contract

The full local-audit fixture framework must retain the unchanged positive
control and reject all sixteen A–D mutations with property-specific
diagnostics. Each transformation must be isolated to its selected wrapper and
must not use a startup, parser, or missing-dependency failure as a substitute.

Add bounded activity, uniqueness, ordering, decoy/inactive, and local-return
regressions sufficient to prove the validator is not another string-presence
check. Rename existing readability-predicate tests accurately; they remain
useful but do not claim missing-file behavior.

For each of the four unchanged wrapper forms, run a paired synthetic tooling
fixture: one success control and one missing-Go-stderr case. The controlled Go
stub accepts only expected version/test forms, emits a deterministic complete
synthetic event stream, and never invokes a real Go toolchain. The genuine
event checker must succeed. The fault case then synchronously deletes only its
owned Go-stderr record while leaving the other five records and unrelated
diagnostic evidence. Require the wrapper to fail with the missing target. For
local wrappers, a continuation sentinel must prove success falls through and
failure does not.

Generated fixtures and stubs remain test-owned or external. No tracked helper,
testdata, parser package, workflow, checklist, or checker change is permitted.

## Exact path allowlists and commit topology

Commit 1 has sole parent
`236a6198e26310dc5b42b97ab82002f85c3b69bf`, subject exactly
`docs: authorize v1.13.16 CK-015 enforcement-gap correction`, and changes
exactly:

- `docs/release/v1.13/v1.13.16-phase12r1r39-ck015-proof-enforcement-gap-correction.md`
- `docs/release/v1.13/README.md`
- `docs/release/v1.13/v1.13.16-phase-list.md`
- `docs/release/v1.13/v1.13.16-release-state.md`
- `docs/release/v1.13/v1.13.16-remediation-tracker.md`
- `docs/release/v1.13/v1.13.16-scope.md`
- `docs/release/v1.13/v1.13.16-source-test-allowlist.md`
- `docs/release/v1.13/v1.13.16-validation-checklist.md`
- `docs/release/v1.13/v1.13.16-phase12r1r38-ck015-hosted-named-proof-wiring-recovery.md`

Commit 2 has commit 1 as its sole parent, subject exactly
`ci: harden CK-015 proof wrapper enforcement`, and changes exactly:

- `scripts/audit_ci_enforcement.sh` with mode `100755`; and
- `scripts/audit_ci_enforcement_test.go` with mode `100644`.

All seven preceding local commits remain byte-identical and in order. A
successful R1R39 Build has nine linear local commits after recorded release
base `3d47aa84e91933758e4d3da469302f26dc196a3a`. No amend, rebase, squash,
reset, third R1R39 commit, separate RED commit, evidence commit, or
post-certification documentation commit is authorized.

## Tooling validation and evidence applicability

Before commit 2, require focused and complete `./scripts` Go tooling tests,
all sixteen negative audit cases, paired synthetic behavior, current Python
tooling tests, separate `bash -n "$file"` results for every canonical shell
script, ShellCheck, formatting, vet, exact retained golangci-lint 2.9.0,
applicable release/governance validation, and the actual local-only audit.

After commit 2, certify its clean exact SHA in the attached tree and exactly
one fresh detached worktree. Both contexts repeat the fresh tooling,
regression, static, Python, audit, identity, scope, mode, lineage, and
cleanliness gates. Detached Go commands use `-buildvcs=false` from their first
invocation and a new retained synthetic local-PR event when the supported
release context requires it.

The original attached/detached SQLite 7/7 and PostgreSQL 5/5 R1R38 recordings
remain earlier runtime evidence only because workflow, checklist, checker,
profiles, production code, and storage tests are byte-frozen. Preserve their
original SHA, time, context, toolchain, and historical qualification; never
relabel them as R1R39 executions. R1R39 runs real Go tooling tests but no new
storage/database proof, full product suite, full Profile A, codec/database
cycle, container, or benchmark.

A genuine mandatory failure after commit 2 stops certification without amend,
edit, retry, repair commit, or detached continuation.

## Historical and stop boundary

Preserve without reinterpretation:

```text
ORIGINAL_R1R38_CANDIDATE_AUDIT=BLOCKED_PRESERVED
R1R38_PROVENANCE_RECOVERY=COMPLETE_WITH_HISTORICAL_QUALIFICATION
HISTORICAL_SETUP_FAILURE_RAW_RECORDS=UNAVAILABLE
HISTORICAL_EVENT_INPUT=UNAVAILABLE
ORIGINAL_PROCESS_COMPLIANCE=NOT_RETROACTIVELY_ESTABLISHED
R1R38_ENFORCEMENT_GAP=CONFIRMED_AT_236A6198
PHASE_12R1R39=AUTHORIZED_LOCAL_BUILD
CK_V11316_015=OPEN
CK_V11316_014=CLOSED_CERTIFIED
CK_V11316_007=OPEN_UNCHANGED
FINDINGS_CONFIRMED=15
FINDINGS_CLOSED=13/15
PUSHES=0
HOSTED_CERTIFICATION=NOT_STARTED
V1_X_TECHNICAL_CORRECTNESS_CLOSURE=WITHHELD
PHASE_12=NEXT
PHASE_12_MODE=PLAN
PHASE_13=NOT_AUTHORIZED
```

Stop after the external local-Build handoff for separate owner review. No
candidate acceptance, push, hosted execution, finding closure, historical
cleanup, merge, tag, release, Phase 12 completion, or Phase 13 action follows.
