# Coldkeep v1.13.13 Engine Operation Ownership Matrix

**Release:** `v1.13.13 — Final v1.x and v2 Handoff Gate`
**Phase:** `2 — Engine/Catalog Ownership Audit`
**Status:** Complete
**Branch:** `release/v1.13.13`
**Starting HEAD:** `ec1f6305e417e7b9a26e9664054975751d9ea854`

This is the complete mechanically enumerated active `engine.Engine` surface at
the Phase 2 starting commit. `PROVEN` requires both an implementation and
production routing evidence; a type or implementation alone is insufficient.
PostgreSQL tests supplement, but do not establish, ownership.

| Engine method | Request | Result | Implementation | CLI/API consumer | Application path | Lower owners | Catalog contracts | Validation owner | Error boundary | Routing evidence | Backend evidence | Compatibility adapter | Classification | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Stats` | `StatsRequest` | `StatsResult` | `default_engine.go` | `runStatsCommand` | `application.Open` → `Engine` | observability queries; Engine projection | None | Engine request and lower query validation | deferred `TranslateError`; neutral trace/value DTOs | `TestStatsAndInspectRouteThroughTypedEngineResults` plus production call | shared DB abstraction; backend suite supplemental | `statsResultFromEngine` presentation projection | PROVEN | CLI never executes the stats query |
| `Inspect` | `InspectRequest` | `InspectResult` | `default_engine.go` | `runInspectCommand` | same | observability inspection; Engine projection | None | Engine entity/query validation | deferred translation; stable typed not-found/validation | typed routing and no-fallback tests | shared DB abstraction; backend suite supplemental | `inspectResultFromEngine` presentation projection | PROVEN | Domain enums are converted outside Engine |
| `Verify` | `VerifyRequest` | `VerifyResult` | `default_engine.go` | `runVerifyCommand` | same | verify and storage readers | None | CLI syntax only; Engine/lower integrity validation | deferred translation preserves invariant/cancellation | system/file JSON and text routing tests | active backend contracts are supplemental | renderer projection only | PROVEN | Correctness execution is behind Engine |
| `SnapshotList` | `SnapshotListRequest` | `SnapshotListResult` | `default_engine.go` | snapshot list command | same | snapshot reads; Engine tree projection | `ListSnapshots`; `LoadSnapshotGraph` for tree | Engine option validation; Catalog graph invariants | deferred translation deterministically maps Catalog errors | list/tree Catalog-consumption tests and CLI routing | SQLite/PostgreSQL graph tests supplemental | snapshot presentation conversion | PROVEN | Tree mode consumes Catalog graph; no parallel planner |
| `SnapshotShow` | `SnapshotShowRequest` | `SnapshotShowResult` | `default_engine.go` | snapshot show command | same | snapshot metadata reads | `FindSnapshot` | Engine selector validation | deferred typed translation | CLI JSON routing and no-fallback tests | shared Catalog contracts supplemental | snapshot presentation conversion | PROVEN | Exact operation ownership traced |
| `SnapshotStats` | `SnapshotStatsRequest` | `SnapshotStatsResult` | `default_engine.go` | snapshot stats command | same | snapshot statistics | Snapshot metadata contracts | Engine selector validation | deferred typed translation | CLI JSON routing | shared DB abstraction supplemental | lineage enum projection | PROVEN | Neutral Engine result |
| `SnapshotDiff` | `SnapshotDiffRequest` | `SnapshotDiffResult` | `default_engine.go`; routing helpers | snapshot diff command | same | snapshot diff query/planner | Snapshot metadata contracts | Engine selector/mode validation | deferred typed translation | detailed and summary CLI routing tests | shared backend tests supplemental | diff enum projection | PROVEN | Fast and detailed routes remain Engine-owned |
| `SnapshotCreate` | `SnapshotCreateRequest` | `SnapshotCreateResult` | `snapshot_create.go` | snapshot create command | same | snapshot mutation service | Snapshot metadata contracts | Engine normalizes and validates full/partial/parent requests | deferred translation preserves conflicts/invariants | full, partial, empty, parented routing tests | mutation backend contracts supplemental | result rendering only | PROVEN | Secure ID generation and sequencing are Engine-owned |
| `SnapshotDelete` | `SnapshotDeleteRequest` | `SnapshotDeleteResult` | `snapshot_delete.go` | snapshot delete command | same | snapshot deletion service | Snapshot/reachability metadata | Engine validates selector and preview/execute mode | deferred typed translation | preview and execute routing tests | mutation backend contracts supplemental | result rendering only | PROVEN | No CLI deletion fallback |
| `SnapshotRestore` | `SnapshotRestoreRequest` | `SnapshotRestoreResult` | `snapshot_restore.go` | snapshot restore command | same | snapshot planning plus storage restore | restore-plan/placement reached through storage | Engine validates selectors/options; lower layer validates installation | deferred typed translation | explicit-original-root routing tests | mutation/restore backend tests supplemental | result rendering only | PROVEN_WITH_EXPLICIT_BOUND | Ownership is proven; Phase 3 still audits restore-install correctness |
| `GarbageCollect` | `GarbageCollectRequest` | `GarbageCollectResult` | `default_engine_routing_helpers.go` | live `gc` command | same | maintenance GC | `LoadGCPlanMetadata` in production planner | Engine mode/retention request; planner validates metadata | deferred typed translation | dry-run human/JSON and production call evidence | backend GC-plan tests supplemental | result rendering only | PROVEN | Engine direct SQL prohibition is not invented; no competing Catalog-owned decision found |
| `PlanGarbageCollection` | `GarbageCollectionPlanRequest` | `GarbageCollectionPlanResult` | `default_engine_routing_helpers.go` | live-repository simulate-GC | same | GC planner and projection | `LoadGCPlanMetadata` | Engine exclusions; Catalog validates plan metadata | deferred typed translation | production simulate call and helper source trace | backend GC-plan tests supplemental | warnings/result projection | PROVEN | Catalog metadata remains authoritative |
| `Store` | `StoreRequest` | `StoreResult` | `default_engine_routing_helpers.go` | file store command | same | storage payload writer | None of the four planning contracts | Engine source/codec request; storage correctness validation | deferred typed translation | file human/JSON routing tests | mutation backend contracts supplemental | result rendering only | PROVEN | Storage owns payload bytes |
| `StoreFolder` | `StoreFolderRequest` | `StoreFolderResult` | `default_engine_routing_helpers.go` | folder store command | same | storage folder writer | None | Engine request/options; storage path/content validation | deferred typed translation | stored-path routing tests and production call | mutation backend contracts supplemental | result rendering only | PROVEN | No CLI folder execution |
| `ListFiles` | `ListFilesRequest` | `ListFilesResult` | `current_files.go` | files list command | same | Catalog metadata query | `ListCurrentFiles` | Engine pagination validation | deferred Catalog-to-Engine translation | current-file routing tests | SQLite/PostgreSQL Catalog tests supplemental | result rendering only | PROVEN | Catalog query directly adopted by Engine |
| `SearchFiles` | `SearchFilesRequest` | `SearchFilesResult` | `current_files.go` | files search command | same | Catalog metadata query | `SearchCurrentFiles` | Engine filter/page validation | deferred Catalog-to-Engine translation | current-file routing tests | SQLite/PostgreSQL Catalog tests supplemental | result rendering only | PROVEN | Catalog query directly adopted by Engine |
| `GetConfiguration` | `GetConfigurationRequest` | `GetConfigurationResult` | `repository_configuration.go` | config get command | same | Catalog configuration metadata | `GetRepositoryConfiguration` | Engine key normalization | deferred Catalog-to-Engine translation | production call and contract tests | SQLite/PostgreSQL Catalog tests supplemental | rendering only | PROVEN | Neutral configuration values |
| `SetConfiguration` | `SetConfigurationRequest` | `SetConfigurationResult` | `repository_configuration.go` | config set command | same | Catalog configuration mutation | `SetRepositoryConfiguration` | Engine key/value normalization | deferred Catalog-to-Engine translation | production call and contract tests | SQLite/PostgreSQL Catalog tests supplemental | rendering only | PROVEN | Mutation does not depend on CLI parsing |
| `Repair` | `RepairRequest` | `RepairResult` | `repair.go` | repair command | same | maintenance repair operations | Metadata services below Engine | Engine target/fail-fast validation | deferred translation; per-target typed results | production call and backend typed-error tests | backend suite supplemental | result rendering only | PROVEN | Phase 3 audits repair correctness, not ownership |
| `Recover` | `RecoverRequest` | `RecoverResult` | `recover.go` | startup recovery and recovery command seam | same | recovery service | Metadata services below Engine | Engine dependency validation; recovery validates journal/state | deferred translation preserves recovery causes | startup seam and adapter source trace | backend suite supplemental | `runRecoveryThroughEngine` report projection | PROVEN | Startup timing stays CLI outer coordination; corrective work is Engine-owned |
| `Doctor` | `DoctorRequest` | `DoctorResult` | `doctor.go` | doctor command | same | recovery, schema, verify, audit | Metadata services below Engine | Engine verify-level and dependency validation | ordered typed composite result/error | doctor ownership and CLI projection tests | backend suite supplemental | `executeDoctorEngine` compatibility seam | PROVEN | Ordered recovery/schema/verify/audit sequence is Engine-owned |
| `Remove` | `RemoveRequest` | `RemoveResult` | `default_engine.go`; routing helpers | by-ID remove command | same | storage/catalog mutation | Metadata services below Engine | Engine IDs/dry-run validation | deferred translation; typed per-item results | human/JSON/dry-run routing tests | mutation backend contracts supplemental | rendering only | PROVEN | By-ID surface is deliberately distinct |
| `RemoveStoredPaths` | `RemoveStoredPathsRequest` | `RemoveStoredPathsResult` | `default_engine.go`; `remove_stored_paths.go` | stored-path remove commands | same | storage/recovery metadata mutation | Metadata services below Engine | Engine target/dependency validation | deferred translation; stable partial-batch errors | dedicated single/batch routing tests | mutation backend contracts supplemental | `removeStoredPathWithEngine` projection | PROVEN | Legacy command shape cannot bypass Engine |
| `Restore` | `RestoreRequest` | `RestoreResult` | `default_engine.go`; routing helpers | by-ID restore command | same | storage restore | `LoadRestorePlanMetadata` and placement through storage | Engine IDs/destination options; lower install validation | deferred translation; typed per-item results | human/JSON/dry-run routing tests | mutation/restore backend tests supplemental | rendering only | PROVEN_WITH_EXPLICIT_BOUND | Ownership is proven; restore-install semantics remain unresolved for Phase 3 |
| `RestoreStoredPath` | `RestoreStoredPathRequest` | `RestoreStoredPathResult` | `restore_stored_path.go` | stored-path restore command | same | storage restore | `LoadRestorePlanMetadata` and placement through storage | Engine selector/destination mode; lower install validation | deferred translation preserves conflict/invariant meaning | dedicated routing and safety tests | PostgreSQL failure tests supplemental | `restoreStoredPathWithEngine` projection | PROVEN_WITH_EXPLICIT_BOUND | Exhaustive destination-mode panic is an impossible-state assertion, not a stub |

## Mechanical result

```text
ENGINE_OPERATION_COUNT:
25

ENGINE_IMPLEMENTATION_COUNT:
25

PROMISED_PRODUCTION_STUBS:
0

ENGINE_ARCHITECTURE:
COMPLETE
```

The count excludes private helpers. `snapshotDiffSummaryFastPath`, repair and
restore helpers, and other unexported receiver methods are implementation
details, not additional contract operations.
