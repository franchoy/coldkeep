# v1.13.16 Phase 6R3 Migration-Compatibility Reconciliation

**Release:** v1.13.16 — Snapshot Retention Integrity, Observability Truth, and Final v1.x Closure
**Phase:** 6R3 — Migration-compatibility test reconciliation and final Phase 6 recertification
**Mode:** BUILD
**Status:** Authorized; implementation next
**Findings:** CK-V11316-001 and CK-V11316-008

## Entry evidence

```text
FAILED_EVIDENCE_HEAD: 6bcac449df7a952ec7b95f0813c4d725497341d9
FAILED_CI: 33331967865
CODEQL: 33331967855 PASS 4/4
OPEN_CODEQL_ALERTS: 0
ROOT: stale migration-compatibility test expectations
PRODUCT_RUNTIME_DEFECT: NOT_ESTABLISHED
PRODUCT_RUNTIME_CHANGE: FORBIDDEN
AUTHORIZED_TEST_PATH: tests/integration/v18_migration_compatibility_integration_test.go
PHASE_6: CANDIDATE_WITHHELD_PENDING_COMPATIBILITY_RECONCILIATION
PHASE_7: NOT_AUTHORIZED
```

Exact-head CI `33331967865` passed quality, Linux, macOS, Windows, critical
coverage, legacy compatibility, all four benchmark-integrity jobs, and all
four timing-advisory jobs. PostgreSQL plain and AES-GCM correctness failed on
the same two compatibility assertions; Required Gate failed as a consequence.
CodeQL run `33331967855` passed Actions, Go, Python, and Aggregate, with zero
open alerts.

## Hosted failure reconciliation

### TestPhase7SnapshotCompatibilityIntegration

```text
TEST: TestPhase7SnapshotCompatibilityIntegration
ASSERTION: snapshot-retained legacy and packed stored-path removal must fail
OLD_EXPECTATION: RemoveFileByStoredPath returns a retained-by-snapshot error
NEW_FROZEN_EXPECTATION: stored-path unlink succeeds while snapshot membership and logical recipe remain available for post-GC restore
PRODUCT_CHANGE_REQUIRED: NO
```

The compatibility lifecycle must unlink the selected legacy and packed
current mappings, prove their `physical_file` rows are absent while their
`snapshot_file` roots and logical recipes remain, execute dry-run and live GC,
and preserve byte-identical restore of both snapshots. The independent by-ID
logical-delete guard remains covered by permanent tests and is not duplicated
here.

### TestPhase7MixedGCAfterUpgradeIntegration

```text
TEST: TestPhase7MixedGCAfterUpgradeIntegration
ASSERTION: every packed block classified dead from live_ref_count and pin_count alone is reclaimed
OLD_EXPECTATION: live_ref_count = 0 and pin_count = 0 completely define GC reclaimability
NEW_FROZEN_EXPECTATION: whole-dead, partial-live, and legacy-dead classifications use current OR pin OR snapshot reachability
PRODUCT_CHANGE_REQUIRED: NO
```

For assertion-only classification, snapshot reachability means that a block's
chunk occurs in a `file_chunk` recipe whose logical file is referenced by
`snapshot_file`. A whole-dead block has referenced chunks but no chunk retained
by the root union. A partial-live block has both retained and unretained chunks.
Legacy dead-chunk reclamation predicates must exclude snapshot-reachable
chunks. Counter-only assertions must remain counter-only.

No third substantive failure root was present. The plain and AES-GCM jobs
reported the same two assertions. Required Gate failure and skipped downstream
jobs were consequences, not independent roots.

## Exact boundary

The only mutable code/test path is:

- `tests/integration/v18_migration_compatibility_integration_test.go`

No product runtime, Engine, CLI, internal GC, schema, migration, workflow, or
dependency path may change. R1 and R3 documentation is limited to this record
and the eight current-authority surfaces named by the Phase 6R3 instruction.

Exactly three commits are authorized on top of failed evidence head
`6bcac449df7a952ec7b95f0813c4d725497341d9`:

1. `docs: authorize v1.13.16 migration compatibility reconciliation`
2. `test: reconcile migration compatibility with snapshot-only retention`
3. `docs: record v1.13.16 migration compatibility reconciliation`

No published Phase 6 commit may be rewritten. Phase 6 remains a candidate
withheld pending reconciliation, clean committed-tree recertification, and a
new exact-head hosted pass. Phase 7 is not authorized.
