# Coldkeep v1.13.7 Release Gate

**Release:** `v1.13.7 — SQLite-First Repository Portability Baseline`
**Phase:** Phase 10
**Status:** Released — Profile A and external release validation complete
**Branch:** `release/v1.13.7`
**Validated implementation commit:** `bb73189`

## Gate profile

`PRE_RELEASE_CHECKLIST.md Profile A — Pre-PR CI-parity gate`

## Scope freeze

No new product scope was introduced during the gate.

The gate reran validation only, recorded evidence outside the repository at
`/tmp/v1137-profile-a-20260619-132748`, and preserved the final implementation
surface already accepted on `bb73189`.

## Toolchain evidence

- Go: `go version go1.26.1 linux/amd64`
- `go.mod` / toolchain: `go 1.25`, `toolchain go1.25.10`
- Docker Compose: `v2.40.3`
- PostgreSQL client: `psql (PostgreSQL) 16.14`
- `jq`: `jq-1.7`
- `shellcheck`: `0.9.0`
- `golangci-lint`: local executable pass ran on `v2.11.3`; CI-pinned `v2.6.2`
  was installed successfully but panicked with `file requires newer Go version
  go1.26`, so the mismatch is recorded explicitly rather than hidden
- Python: `Python 3.12.1`
- Host/platform: `Linux codespaces-d67ffb 6.8.0-1052-azure x86_64`

## PostgreSQL environment

- `docker compose up -d coldkeep_postgres`: passed
- readiness (`pg_isready`): passed
- host-side connection (`psql ... SELECT 1;`): passed
- auto-bootstrap configuration: `COLDKEEP_DB_AUTO_BOOTSTRAP=true`
- schema path: `db/schema_postgres.sql`
- fixture credentials: deterministic local gate configuration only
- reconnect note: the codespace disconnect left a stale exited PostgreSQL
  container; the gate removed that broken container, restarted the service, and
  revalidated readiness before continuing

## Release-specific regressions

- SQLite legacy backfill: passed
- mapped-file preservation: passed
- multiple-mapping preservation: passed
- storage migration idempotency: passed
- live PostgreSQL schema rerun: passed
- SQLite repository relocation guard: passed
- targeted race equivalents: passed

## Quality-equivalent checks

| Check | Result |
|---|---|
| `go mod tidy` / clean diff | Passed |
| `gofmt` | Passed |
| shell syntax | Passed |
| smart quotes | Passed |
| shellcheck | Passed |
| validation matrix | Passed |
| versioned writer guard | Passed |
| golangci-lint | Passed with local `v2.11.3`; pinned `v2.6.2` panicked on `go1.26` file requirement |
| go vet | Passed |
| plain race package matrix | Passed on serialized rerun |
| AES-GCM race package matrix | Passed |
| chunk benchmark-contract tests | Passed |
| build | Passed |
| CI-enforcement audit | Passed |

## Required CI matrix

| Suite | Plain | AES-GCM |
|---|---:|---:|
| integration correctness | Pass | Pass |
| integration stress | Pass | Pass |
| integration long-run | Pass | Pass |
| ref-count containment | Pass | Pass |
| adversarial | Pass | Pass |
| focused adversarial | Pass | Pass |
| smoke | Pass | Pass |

## Benchmark matrix

| Compression | Workers | Result | Interpretation |
|---|---:|---|---|
| none | 1 | Pass | No regressions detected |
| none | 4 | Pass in GitHub CI | Local hard-fail variance cleared by authoritative PR CI |
| zstd | 1 | Pass | No regressions detected |
| zstd | 4 | Pass in GitHub CI | Local warning variance cleared by authoritative PR CI |

Local workers=4 variance was recorded explicitly:

- uncompressed `workers=4`: hard-fail report in
  `/tmp/v1137-profile-a-20260619-132748/regression-report-none-w4.json`
- compressed `workers=4`: warning report in
  `/tmp/v1137-profile-a-20260619-132748/regression-report-zstd-w4.json`

Authoritative PR CI resolved both workers=4 cases:

- uncompressed `workers=4`: passed in `benchmark-matrix (none)`
- zstd `workers=4`: passed in `benchmark-matrix (zstd)`

## Legacy compatibility

Focused PostgreSQL-backed migration/store/restore/snapshot regression passed:
`TestPhase2PostMigrationStoreRestoreSnapshotRegressionIntegration`.

## Cross-platform local approximation

- `internal/pathsafe`: passed
- `internal/storage` restore seam approximation: passed
- limitation: GitHub Actions remains responsible for macOS and Windows proof

## Critical coverage

- report generation: passed
- output location: `/tmp/v1137-profile-a-20260619-132748/coverage/critical-coverage-report.csv`
- review conclusion: generated successfully, no unexplained Tier 1 package had
  zero or near-zero coverage, and current coverage remained report-only per the
  script output

## Snapshot release gate

Passed: `scripts/run_snapshot_release_gate.sh --count 1`

## Final repository checks

- `git diff --check`: passed
- no executable changes were kept during the gate
- clean working tree before gate documentation: passed
- branch/remote alignment before gate documentation: `release/v1.13.7` aligned
  with `origin/release/v1.13.7` at `bb73189`

## Profile A decision

Profile A passed.

The release branch is authorized to receive its documentation-only gate
record and then open the single pull request to main.

## PR CI completion

PR #99 completed with all required checks green.

Confirmed successful outcomes include:

- `quality`
- `correctness-matrix (plain)`
- `correctness-matrix (aes-gcm)`
- `Cross-platform validation (ubuntu-latest)`
- `Cross-platform validation (macos-latest)`
- `Cross-platform validation (windows-latest)`
- `benchmark-matrix (none)`
- `benchmark-matrix (zstd)`
- `CodeQL`
- `Codacy Static Code Analysis`

## Remaining external validation

- GitHub Actions provided the authoritative required-check result for PR #99
- merge remains subject to the controlled post-CI merge verification flow
- tagging and release publication remain out of scope for this phase

## Pull-request authorization

One pull request may now be opened from `release/v1.13.7` to `main`.

No additional release branch or parallel PR is authorized.

## Release status

Not merged.
Not tagged.
Not released.

## Final external and release closure

The local Profile A gate validated implementation commit `bb73189`.

Additional documentation and Codacy remediation commits were added afterward.
The final pull-request head was validated by the complete required PR CI suite.

Final authoritative release evidence:

- Local Profile A implementation commit: `bb73189`
- Final pull-request head: `5f5506179a6395304c90f4b6fd938b5d8d259305`
- PR merge commit: `3813dc32f85c72641dd516dfa41c439d2b4c916c`
- Released main commit: `3813dc32f85c72641dd516dfa41c439d2b4c916c`
- Annotated tag target: `3813dc32f85c72641dd516dfa41c439d2b4c916c`
- PR required checks: passed
- Post-merge main CI: passed
- Codacy: passed
- CodeQL/code scanning: passed

This distinction preserves the provenance of both:

1. the local pre-PR Profile A gate;
2. the final executable state validated by PR and post-merge CI.

## Final release state

- Merged.
- Tagged as `v1.13.7`.
- GitHub release published.
- Release branch deleted.
- Released.
