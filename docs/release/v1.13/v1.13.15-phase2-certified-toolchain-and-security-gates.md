# Coldkeep v1.13.15 Phase 2 — Certified Toolchain and Security Gates

**Mode:** `BUILD`
**Status:** Complete
**Observed:** `2026-08-27T12:21:00Z`
**Findings:** `CK-V11315-001`, `CK-V11315-002`, `CK-V11315-003`,
`CK-V11315-009`, `CK-V11315-014`

## Toolchain contract

The module retains the Go 1.25 language line, canonically spelled `go 1.25.0`
for compatibility with `x/sys v0.44.0`, and now declares:

```text
toolchain go1.26.7
```

Every `actions/setup-go` use in required CI, CodeQL, and manual benchmark
calibration pins `1.26.7`, sets `check-latest: false`, and runs beneath a
workflow-level `GOTOOLCHAIN: local`. The repository audit fails if any of those
exact pins or switching controls drift.

## Security and validation gates

Required CI now includes a blocking native `ubuntu-latest` and
`windows-latest` matrix running ordinary-output:

```text
go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...
```

The aggregate `CI Required Gate` consumes and rejects a non-successful or
skipped vulnerability result. The audit rejects structured JSON/SARIF/OpenVEX
gate semantics and failure suppression.

The quality job now executes the canonical complete Python suite:

```text
python3 -m unittest discover -s scripts -p 'test_*.py' -v
```

The native Windows cross-platform row directly runs the Phase 1
`TestWindowsRenameBuffer` boundary family, so successful hosted execution will
close the remaining direct-execution condition for findings `001` and `014`.

The exact-head attempt at `1f8982db36b56c8da2595b726af490b1e53eb879`
correctly failed before certification:

- native Windows proved that `windows.MAX_LONG_PATH` includes the terminating
  NUL; the production name boundary is therefore `MAX_LONG_PATH - 1`, or
  32,767 UTF-16 code units;
- the prior `golangci-lint v2.6.2` binary was built with Go 1.25 and refused a
  Go 1.26.7 target.

The boundary constant was corrected without changing the surrounding rename
design. The linter moved narrowly to v2.9.0, the first official release whose
changelog explicitly declares Go 1.26 support. Its official release checksum
was verified, the binary reports that it was built with Go 1.26.0, and the
fail-closed candidate lint gate reports zero issues. A trial of current
v2.12.2 was rejected because it introduced four new diagnostics outside the
Phase 2 source allowlist; no suppression or product-source expansion was used.

Upstream support evidence:
<https://github.com/golangci/golangci-lint/blob/main/CHANGELOG.md#v290>.

## Local exact-toolchain proof

The host default is Go 1.26.1, so it was not treated as certification. The
downloaded Go 1.26.7 distribution was invoked with its matching `GOROOT` and
`GOTOOLCHAIN=local`; its binary reported:

```text
go version go1.26.7 linux/amd64
GOTOOLCHAIN: local
go mod tidy -diff: PASS
go mod verify: PASS
go vet ./...: PASS
complete Python discovery: 160/160 PASS
ordinary govulncheck v1.7.0: PASS, 0 reachable vulnerabilities
golangci-lint v2.9.0 candidate gate: PASS, 0 issues
workflow YAML parse: PASS
local CI-enforcement audit: PASS
```

The ordinary scanner also reports one vulnerability in a required module that
the program does not appear to call; this is not mislabeled as zero vulnerable
modules. The blocking reachable-symbol result is zero.

An initial direct-binary attempt without matching `GOROOT` was discarded after
the compiler correctly rejected mixed Go 1.26.1/1.26.7 standard-library
objects. No result from that invalid environment is acceptance evidence.

## Hosted acceptance

Exact candidate `86424ff53d368922fcf43fe3d5e2e66a78aaf3d8` proved:

- CI run `33071668442` passed its complete graph;
- Required Gate job `98521581833` passed;
- Linux vulnerability job `98515324423` passed;
- Windows vulnerability job `98515324756` passed;
- native Windows boundary job `98515324790` passed;
- quality job `98515324821` passed the complete Python suite and all local
  enforcement checks;
- CodeQL run `33071668348` passed its Go, Python, Actions, and aggregate jobs.

The regression graph included correctness, coverage, benchmark integrity,
timing advisory, legacy compatibility, stress, smoke, long-run, and adversarial
rows for the applicable codec matrices. No required row was skipped or failed.

```text
PHASE_2_IMPLEMENTATION: COMPLETE
PHASE_2_HOSTED_CERTIFICATION: COMPLETE
FINDINGS_CLOSED: 5/15
V2_IMPLEMENTATION: NOT_STARTED
```
