# v1.13.1 Phase List

## Phase 0 - Release Scope and Baseline Docs

Objective: create the `v1.13.1` baseline documents that lock scope, define the release phases, and
set validation expectations before any code or test work begins.

Expected touched area:

- `docs/release/v1.13/`

Explicit non-goals:

- Go code changes
- test changes
- schema or migration changes
- CLI, JSON, or exit-code changes
- any routing, catalog, or backend behavior work

Acceptance framing:

- the three `v1.13.1` baseline docs exist
- scope clearly states what `v1.13.1` does and does not claim
- `git diff --check` is clean and no Go files change

## Phase 1 - Deferred Boundary Target Matrix

Status note: complete. Target matrix recorded in
`docs/release/v1.13/v1.13.1-deferred-boundary-targets.md`.

Objective: convert the `v1.13.0` inventory findings into a narrow target matrix that maps which
deferred, partial, candidate-only, and misleading boundaries `v1.13.1` will harden.

Expected touched area:

- release docs
- inventory-to-phase planning notes

Explicit non-goals:

- runtime behavior changes
- engine routing expansion
- catalog implementation expansion

Acceptance framing:

- every in-scope boundary target is explicit
- out-of-scope deferred areas remain explicitly deferred
- the matrix stays aligned with the `v1.13.x` release train

## Phase 2 - Engine Explicit Unsupported Boundary Tests

Status note: complete. Targets covered: `CK-1131-BND-001`, `CK-1131-BND-002`, and
`CK-1131-BND-003`. Production code was unchanged; this phase strengthened engine-package tests and
documented existing explicit unsupported behavior rather than changing runtime behavior.

Objective: add targeted tests proving that deferred engine paths fail explicitly and consistently
instead of implying hidden support.

Expected touched area:

- `internal/engine`
- focused engine tests

Explicit non-goals:

- new engine operations
- broad request/result redesign
- CLI behavior changes

Acceptance framing:

- explicit unsupported engine paths are covered by targeted tests
- failure mode stays intentional and consistent
- no new active runtime support is implied

## Phase 3 - Catalog Deferred-Method Boundary Tests

Status note: complete. Deferred surfaces covered: `LoadSnapshotGraph`, `LoadChunkPlacements`,
`LoadRestorePlanMetadata`, and `LoadGCPlanMetadata`. Production code was unchanged; this phase
strengthened SQLite and existing cross-backend tests to document explicit deferred behavior,
nil-result non-success, and no-obvious-mutation expectations. Backend parity was checked through
the existing catalog backend-contract harness only.

Objective: add targeted tests proving that deferred catalog methods remain explicit and safe rather
than looking complete because interfaces and types already exist.

Expected touched area:

- `internal/catalog`
- focused catalog contract tests

Explicit non-goals:

- implementing deferred catalog graph, placement, restore-plan, or GC-plan methods
- backend narrowing
- broad catalog refactors

Acceptance framing:

- deferred methods stay explicitly deferred
- backend expectations remain honest across supported backends
- tests reinforce boundary clarity rather than implementation growth

## Phase 4 - Candidate-Only Operation Guardrails

Status note: complete. Candidate surfaces covered: `SnapshotCreate`, `SnapshotDelete`,
`SnapshotRestore`, `Repair`, and `Recover`, with adjacent documentation preserved for simulation
and benchmark tooling as intentionally outside active engine ownership. Production code behavior
was unchanged; this phase added candidate-only comments and engine-package guardrail tests only.
Later full implementation ownership remains `v1.13.8` for snapshot mutation operations, `v1.13.9`
for repair/recovery, and `v2.x` for simulation or benchmark tooling surfaces.

Objective: harden the places where candidate-only operations can be mistaken for active engine
support.

Expected touched area:

- `internal/engine`
- possibly adjacent CLI-preservation tests

Explicit non-goals:

- activating snapshot mutation operations in engine
- activating repair or recovery in engine
- changing successful CLI workflows

Acceptance framing:

- candidate-only operations cannot be mistaken for active runtime support
- guardrails remain narrow and explicit
- existing command behavior stays preserved

## Phase 5 - Partial Routing Boundary Hardening

Status note: complete. Partial-routing surfaces covered: `inspect`, `snapshot show`, `snapshot
diff`, `restore`, and `remove`. Production behavior and CLI behavior were unchanged; this phase
added seam comments plus targeted command-path tests only. Later cleanup ownership remains
`v1.13.3` for read-side `inspect` / `snapshot show` / `snapshot diff` cleanup and `v1.13.7` for
restore/remove split cleanup.

Objective: make partial engine routes explicit where today they can be mistaken for full workflow
 ownership.

Expected touched area:

- `internal/engine`
- targeted route-preservation tests

Explicit non-goals:

- broad engine routing
- moving entire CLI workflows behind engine
- hidden contract expansion

Acceptance framing:

- partial routes no longer imply full workflow completeness
- boundary hardening is narrow and local
- CLI-visible behavior remains preserved

## Phase 6 - Request/Result Support-Surface Clarification

Status note: complete. Request/result surfaces clarified: `VerifyResult`, `SnapshotQuery`,
`StoreRequest`, `RestoreRequest`, `RemoveRequest`, `SnapshotListRequest`, `SnapshotListResult`,
`SnapshotShowResult`, `SnapshotDiffRequest`, and `SnapshotDiffResult`. Production behavior was
unchanged; this phase added contract comments and focused engine-package shape tests only. Later
cleanup ownership remains `v1.13.2` for error taxonomy baseline, `v1.13.3` for read-side
contract cleanup, `v1.13.5` for validation/invariant ownership, `v1.13.7` for restore/remove
contract split, `v1.13.8` for snapshot mutation boundaries, `v1.13.9` for repair/recovery
boundaries, and `v1.13.11` for remaining thin-wrapper review.

Objective: clarify where current request/result shapes overstate active support or hide narrowed
behavior.

Expected touched area:

- `internal/engine`
- possibly `internal/catalog`
- targeted contract tests

Explicit non-goals:

- freezing broad daemon/API-ready contracts
- changing JSON output
- broad type-system cleanup

Acceptance framing:

- supported versus deferred semantics are clearer
- request/result shapes no longer misleadingly overclaim active support
- later releases still have room for deeper contract cleanup

## Phase 7 - CLI Boundary Preservation Tests

Status note: complete. CLI boundary surfaces covered: stored-path `restore`, stored-path
`remove`, stored-paths batch `remove`, snapshot create/delete/restore, `repair`, and
doctor/recovery. Production behavior and CLI behavior were unchanged; this phase added targeted
CLI-preservation tests plus short transitional-ownership comments only. Later cleanup ownership
remains `v1.13.7` for restore/remove split cleanup, `v1.13.8` for snapshot mutation boundaries,
`v1.13.9` for repair/recovery decisions, and `v1.13.11` for remaining CLI thin-wrapper proof.

Objective: prove that v1.13.1 boundary hardening does not accidentally change existing CLI-facing
behavior.

Expected touched area:

- command-path tests
- engine/CLI parity coverage

Explicit non-goals:

- new CLI features
- command flag changes
- output or exit-code changes

Acceptance framing:

- boundary hardening preserves current CLI behavior
- CLI-owned paths stay intentionally preserved where still required
- regressions are caught before release gate

## Phase 8 - Documentation and v1.13.2 Handoff Notes

Status note: complete. Summary document created at
`docs/release/v1.13/v1.13.1-boundary-summary.md`. Production code and runtime behavior were
unchanged; this phase was docs-only and recorded the `v1.13.2` handoff baseline explicitly.
Later ownership remains aligned with the existing `v1.13.x` release train.

Objective: record what `v1.13.1` clarified, what remains deferred, and how that sets up the
`v1.13.2` error-taxonomy baseline safely.

Expected touched area:

- `docs/release/v1.13/`

Explicit non-goals:

- starting `v1.13.2` implementation
- rewriting prior inventory docs
- broad release-train redesign

Acceptance framing:

- the release docs clearly record the hardened boundaries
- remaining deferred areas are still explicit
- handoff notes set up `v1.13.2` without overstating what `v1.13.1` completed

## Phase 9 - Final Validation and Release Gate

Status note: pending remaining Profile A Section 3 tail. Local validation, docs sanity checks,
full remote `scripts/audit_ci_enforcement.sh`, and Profile A Sections 1-2 all passed in
`docs/release/v1.13/v1.13.1-release-gate.md`. The original Section 3 run hit a local
uncompressed `workers=1` benchmark hard fail, but benchmark investigation showed that failure does
not reproduce: three valid reruns passed with zero violations, and `git diff main...HEAD -- '*.go'`
confirmed no runtime logic changes relative to `main`. Profile A is still not green because the
original shell exited before the remaining Section 3 tail checks ran. Runtime behavior remained
unchanged; this phase stayed release-gate documentation and evidence only.

Objective: run the final validation gate, record the actual results honestly, and confirm the
release stayed within its narrow explicit-boundary scope.

Expected touched area:

- `docs/release/v1.13/`
- validation evidence

Explicit non-goals:

- late unscoped fixes
- undocumented behavior changes
- replacing missing validation with assumptions

Acceptance framing:

- required validations are run and recorded honestly
- any unavailable validation is stated explicitly
- the release gate confirms `v1.13.1` did not broaden scope beyond explicit boundary hardening
