# v1.13.16 Phase 12R1R5 Atomic Store Repair Authority

**Mode:** BUILD
**Boundary:** Local authority, RED, implementation, and attached/detached certification only
**Finding:** CK-V11316-015

## Accepted finding

CK-V11316-015 is an S1 correctness finding:

`FAILED_DUPLICATE_STORE_CAN_IRREVERSIBLY_INVALIDATE_EXISTING_RECIPE`

When an existing completed object temporarily loses required physical data, a
duplicate Store may fail after changing that object's status/retry state and
deleting its recipe. Returning the exact physical bytes then cannot restore the
original mapping. A failed repair must never worsen the old authoritative
graph.

CK-V11316-014 remains an external proposed S2 and is not repository-confirmed
or included in repository totals. CK-V11316-007 remains open and unchanged.

```text
FINDINGS_CONFIRMED: 14
FINDINGS_CLOSED: 12/14
CK-V11316-014: EXTERNAL_PROPOSED_S2_NOT_REPOSITORY_CONFIRMED
CK-V11316-015: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
```

## Frozen implementation contract

Repair-on-Store remains required for an invalid `COMPLETED` candidate whose
logical identity and ordered recipe are intact and trustworthy. Repair is
copy-on-write from the perspective of authoritative recoverability:

```text
STAGED_CONTAINER_PHYSICAL_PATH=FINAL_IMMUTABLE_CONTAINER_PATH
PUBLICATION_FILESYSTEM_RENAME=NONE
PUBLICATION_FILESYSTEM_COPY=NONE
PUBLICATION_FILESYSTEM_DELETE=NONE
PUBLICATION_AUTHORITY_CHANGE=DATABASE_TRANSACTION_ONLY
RETIRED_LEGACY_EXTENT_IDENTITY=PHYSICAL_CONTAINER_AND_OFFSET
CK015_REPAIR_RECIPE=VALIDATED_BUT_NOT_REPLACED
FILE_CHUNK_DURING_PREPARATION=UNCHANGED
FILE_CHUNK_DURING_PUBLICATION=UNCHANGED
```

Schema v17 adds durable attempt-owned staging and retired physical-accounting
state. Staged container bytes are written, synced, closed, directory-synced,
sealed, and validated at their final immutable pathname while the container is
quarantined and unreferenced. One bounded database transaction is the sole
authority switch. Old physical extents remain accounted for until safe
whole-block or whole-container collection.

For every retained packed block, encoded membership must equal active plus
retired membership exactly. Container payload occupancy must equal the
disjoint ordered union of active legacy extents, retired legacy extents, and
packed storage-block extents. Historical packed chunk IDs and legacy block or
chunk IDs are opaque provenance values, never current ownership.

The existing OS-backed exclusive repository lease excludes another compliant
process's recovery for the complete repair. PostgreSQL publication also shares
GC's advisory exclusion, locks and revalidates the complete authoritative
fingerprint, and requires zero affected Restore pins. Any material difference
aborts without changing the old graph.

## Exact Build allowlist

Existing production/schema files:

- `db/schema_sqlite.sql`
- `db/schema_postgres.sql`
- `internal/db/migrations.go`
- `internal/maintenance/constants.go`
- `internal/container/container.go`
- `internal/storage/store.go`
- `internal/storage/restore.go`
- `internal/recovery/system_recovery.go`
- `internal/verify/verify_repository.go` (only retired packed membership
  verification against the exact active-plus-retired union)
- `internal/verify/verify_system.go`
- `internal/maintenance/stats.go`
- `internal/maintenance/gc.go`
- `internal/gc/plan.go`

New production files:

- `internal/db/repository_mutation_lock.go`
- `internal/container/repair_staging.go`
- `internal/fsx/syncdir.go`
- `internal/fsx/syncdir_linux.go`
- `internal/fsx/syncdir_darwin.go`
- `internal/fsx/syncdir_windows.go`
- `internal/storage/store_repair.go`

Existing mutable tests:

- `internal/db/migrations_test.go`
- `internal/db/schema_backend_contract_test.go`
- `internal/db/migration_backend_contract_test.go`
- `internal/container/container_faultfs_test.go`
- `internal/storage/phase1_compat_matrix_test.go` (only the mechanically stale
  schema-version expectation may change from 16 to 17)
- `internal/storage/store_interleaving_rebuild_test.go`
- `internal/storage/restore_test.go`
- `internal/recovery/system_recovery_test.go`
- `internal/verify/verify_system_test.go`
- `internal/maintenance/stats_test.go`
- `internal/maintenance/gc_test.go`
- `internal/maintenance/gc_packed_test.go`
- `internal/gc/plan_test.go`

New tests:

- `internal/fsx/syncdir_test.go`
- `internal/fsx/syncdir_linux_test.go`
- `internal/fsx/syncdir_darwin_test.go`
- `internal/fsx/syncdir_windows_test.go`
- `internal/container/repair_staging_test.go`
- `internal/storage/store_atomic_repair_test.go`
- `internal/storage/store_atomic_repair_postgres_test.go`
- `internal/recovery/store_repair_recovery_test.go`
- `internal/verify/verify_retired_membership_test.go`
- `internal/maintenance/store_repair_gc_test.go`
- `internal/gc/retired_occupancy_plan_test.go`
- `cmd/coldkeep/repository_coordination_repair_test.go`
- `cmd/coldkeep/repository_coordination_repair_linux_test.go`
- `tests/integration/atomic_store_repair_integration_test.go`
- `tests/adversarial/atomic_store_repair_crash_test.go`

Implementation documentation may change only `ARCHITECTURE.md`,
`docs/internal/storage_contract_v1_9.md`, and the eight Phase 12R1R5 authority
documents. `AGENTS.md`, workflows, modules, dependencies, public interfaces,
container format, unrelated tests/scripts, CK-V11316-007, CK-V11316-014, and
Phase 13 are prohibited.

## Schema-v17 compatibility-test allowlist recovery

The initial Build stopped correctly with:

`SCHEMA_V17_COMPATIBILITY_TEST_ALLOWLIST_OMISSION`

Schema v17 was already frozen by CK-V11316-015, while the pre-existing Phase 1
compatibility matrix still hard-coded schema version 16. Updating only that
completed schema-version expectation to 17 is mechanically required by the
authorized migration. This is an allowlist omission, not a product defect, new
finding, or design change. No other assertion, fixture, backend contract, test
name, or test semantic is reopened.

## Retired packed-membership Verify allowlist recovery

The resumed Build stopped correctly with:

`RETIRED_PACKED_MEMBERSHIP_VERIFY_PRODUCTION_ALLOWLIST_OMISSION`

R1R5 already froze retired packed membership as authoritative physical
accounting. Implementation validation proved that repository-level packed
membership comparison is owned by `internal/verify/verify_repository.go`; the
initial production allowlist named `verify_system.go` but omitted that actual
owner. The former file is therefore authorized only to compare each decoded
packed directory against the exact union of active and retired membership and
to reject missing, unexplained, duplicate, overlapping, or invalid entries.
Retired embedded chunk IDs remain opaque physical provenance and never become
active chunk ownership. This is a mechanical allowlist recovery, not a new
finding or architecture expansion.

## Local commit and execution boundary

Exactly seven local commits are authorized:

1. `docs: authorize v1.13.16 atomic store repair`
2. `test: reproduce atomic store repair failures`
3. `docs: authorize v1.13.16 schema compatibility test update`
4. `docs: authorize v1.13.16 retired membership verification`
5. `fix: make completed-object repair atomic`
6. `fix: satisfy atomic repair lint gate`
7. `docs: record atomic store repair local certification`

The second commit must retain the four exact RED regressions and must never be
pushed while failing. The fifth commit makes the frozen contract GREEN. The
sixth handles only the checked assertion required by exact golangci-lint 2.9.0
and changes only `internal/storage/store_repair.go`. The seventh is permitted
only after complete attached and fresh-detached local certification under exact
Go 1.26.7 and isolated PostgreSQL 16.15.

## Append-only lint correction recovery

The first exact golangci-lint 2.9.0 execution at implementation commit
`39d139dcc2cb71e50fd5a0de8cf9fe6b3fcd6302` found one `errcheck` issue in the
private `completedRepairLock` helper. The initial tooling stop was
`EXACT_GOLANGCI_LINT_TOOL_AVAILABILITY_MISMATCH`; after exact tooling became
available, the missing correction-commit authority was
`APPEND_ONLY_LINT_CORRECTION_COMMIT_AUTHORIZATION_GAP`. Neither stop established
a new product defect or finding.

Commit `c080dbd912502683ec06066d02a9e8aa2e9370f4` (`fix: satisfy atomic repair
lint gate`) changes only `internal/storage/store_repair.go`. It explicitly
checks the private `sync.Map` type assertion and preserves the prior invariant
failure behavior without suppression, public behavior change, or architecture
change.

## Exact-head local certification

The lint-corrected implementation head is
`c080dbd912502683ec06066d02a9e8aa2e9370f4`. Its complete attached and fresh
detached local certification passed:

```text
b0872912f64b08483d30d7e306ddcf7a042793b2 docs: authorize v1.13.16 atomic store repair
f8a5cdbc96ed07d2e294c6ac2f572b2758fb9887 test: reproduce atomic store repair failures
650ec76780fed1f663e918dec0801793eb6abf0e docs: authorize v1.13.16 schema compatibility test update
f4f47530d0f37fcce6843e0914677c8c5f915512 docs: authorize v1.13.16 retired membership verification
39d139dcc2cb71e50fd5a0de8cf9fe6b3fcd6302 fix: make completed-object repair atomic
c080dbd912502683ec06066d02a9e8aa2e9370f4 fix: satisfy atomic repair lint gate
```

- focused storage, container, fsync, recovery, Verify, maintenance, GC,
  adversarial, and integration packages;
- complete `go test -count=1 ./...` and complete `go test -race -count=1 ./...`;
- `go vet ./...`, 215 Python authority tests, release-state validation on the
  attached release branch, governance validation, and local CI-enforcement
  audit;
- Darwin and Windows fsync cross-builds;
- PostgreSQL 16.15 affected-package ordinary and race runs through the
  authorized database gate in both attached and detached worktrees; and
- the frozen SQLite/PostgreSQL, packed/legacy, plain/AES-GCM,
  none/compression, migration, failure-injection, crash/recovery, and
  concurrency coverage carried by those focused and complete suites.

The official golangci-lint 2.9.0 archive SHA-256 was
`493aaaca2eba6c8bcef847d92716bbd91bbac4b22cdbb0ab5b6a581b32946091`.
The extracted Linux-amd64 executable SHA-256 was
`9d3691b2e2410f1fe06b6cb72c734e4cd15eebbcb5d5c5f08ac0901c693fdbd9`;
it reported version 2.9.0 built with Go 1.26.0. With the exact Go 1.26.7
binary bound through `PATH` and `GOTOOLCHAIN=local`, the canonical candidate
lint gate passed with zero issues in both attached and detached worktrees.

The isolated database service reported PostgreSQL 16.15 from local image object
`sha256:5f71c21b69a7977b82247582e2e731ed76bdebaadb7dd7945ed76bcc9ed06632`,
whose repository digest was
`postgres@sha256:bb3e1a57e5407e0a5280b4211980a5e537f4abd234a87014ac979849a78dd825`.
It used a uniquely named task-owned container, loopback-only random port, and
tmpfs data storage.

The deterministic tracked-file manifest contained 1,555 entries. Attached
entry/exit and detached entry/exit serializations were byte-identical and each
had SHA-256
`b57a2a7c4d2999ff2d8a44fef718aff3fa1287606fcdbe778b4fb06c29472eef`.
Both worktrees were clean at the certification boundary. The temporary
PostgreSQL container and detached worktree were removed afterward.

CK-V11316-015 is implemented and locally certified but remains open pending
exact-head hosted certification. CK-V11316-014 remains external and excluded
from repository totals, CK-V11316-007 remains open unchanged, and Phase 13
remains unauthorized.

```text
PUSH: NOT_AUTHORIZED
HOSTED_CERTIFICATION: NOT_STARTED
HOSTED_RECONCILIATION_COMMIT: NOT_AUTHORIZED
PHASE_13: NOT_AUTHORIZED
```

## Phase 12R1R10 append-only recovery authority

The later exact-candidate Profile A run at `de4a7821...` exposed one authorized
test reconciliation and CK-V11316-015 regressions. The missing authoritative
recipe remains fail-closed. Trustworthy pre-existing ABORTED chunks and
repairable semantic corruption retain Store-driven copy-on-write repair, now
with exactly one logical retry increment and one increment per distinct
existing chunk whose status or placement is actually repaired. Unchanged,
new, failed, and rolled-back chunk outcomes increment zero. Exact container
occupancy must be observed before sealing-state mutation. This recovery does
not add a finding or change schema, recipe authority, or storage format.

## Phase 12R1R13 append-only contention recovery authority

A chunk claimed `PROCESSING` by another Store is a transient contention state,
not by itself corrupt recipe evidence. The authorized correction releases the
completed-repair mutex and all publication/database locks before using the
existing bounded chunk-claim wait, then fully revalidates from current state.
If staging already exists, successful safe cleanup deletes only the losing
attempt's quarantined staging and attempt row before waiting; cleanup failure
stops and preserves positive ownership for startup recovery. No new persisted
attempt status, schema change, finding, or retry-accounting exception is
authorized.

## Phase 12R1R15 append-only PostgreSQL publication recovery authority

R1R15 completes the original atomic-publication locking invariant. The
publication transaction locks active packed placement rows, distinct affected
chunk rows, and active legacy placement rows in deterministic order before it
trusts the final fingerprint. A competing Store displacement causes rollback
without authority mutation and uses R1R13 only after cleanup and lock release.
The exact status/retry CAS is defensive and zero rows after locked validation
is terminal. This adds no schema change or finding.

## Phase 12R1R17 append-only pre-plan convergence authority

R1R17 completes the pre-publication convergence edge of the original atomic
repair contract. A stale zero-entity plan cannot publish or fail from its old
snapshot; current state is classified again under serialization and within
the existing contention deadline. Canonical stability signatures are
observation-only and cover recipe, distinct chunk, placement/accounting,
container/extent, validation-reason, and zero-disposition state. Atomic
publication and fail-closed boundaries remain unchanged.

## Phase 12R1R17 append-only local certification evidence

The final bounded convergence implementation is retained at
`5e7fb3996804c5a8c0173044bdc8ac6f1de9e498`. Deterministic SQLite/PostgreSQL
convergence, the unchanged eight-worker stress contract, complete attached,
and restarted fresh-detached certification passed ordinary and race execution
with exact scope and byte-identity proof. This evidence does not close
CK-V11316-015: the fresh evidence-head Profile A restart, push authority, and
hosted exact-head certification remain outstanding.
