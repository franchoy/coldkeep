# v1.13.16 Phase 12R1R7 Schema-v17 Backward-Version Fence

**Mode:** BUILD
**Finding:** CK-V11316-015
**Authority:** Local-only; push and hosted execution prohibited
**Status:** Local Build complete; attached and fresh-detached exact-head certification passed

## Decision

Historical schema-16 binaries at
`2212ddd2981f3535fb71f38232bda6c1b7b35e97` silently accept a
`schema_version.version` value above 16. This becomes release-blocking because
CK-V11316-015 introduces schema-17 repair-attempt and retired-occupancy
semantics that those binaries do not understand. It is a completion
requirement of CK-V11316-015, not a new finding.

The repository-side fence retains the `schema_version` table but changes its
committed schema-17 representation to exactly one integer-primary-key column,
`catalog_version`, with exactly one row containing `17`. The legacy `version`
column is absent. Historical binaries therefore find the table and fail on
their mandatory old-column query before schema application, recovery, session
construction, or command execution. Downgrade is unsupported.

```text
PRE_V17_BINARY_NEWER_SCHEMA_BEHAVIOR: SILENT_ACCEPT
FINDING_BOUNDARY: CK015_COMPATIBILITY_REQUIREMENT
NEW_FINDING: NOT_REQUIRED
SCHEMA_V17_OLDER_BINARY_ACCESS: TECHNICALLY_REJECTED
SCHEMA_V17_DOWNGRADE: UNSUPPORTED
SCHEMA_V17_METADATA_CARDINALITY: EXACTLY_ONE_ROW
SCHEMA_V17_METADATA_VALUE: 17
ZERO_SEMANTIC_REPOSITORY_MUTATION: REQUIRED
```

## Transaction and metadata contract

The v16-to-v17 migration begins a backend transaction before metadata
inspection, derives the legacy current version using the historical backend
rule, validates it, completes every required migration, renames `version` to
`catalog_version`, deletes all version rows, inserts only `17`, validates the
final representation, and commits. Any failure restores the complete original
legacy column and rows.

SQLite accepts one or more distinct integer legacy rows in the range 1..17
and derives the current version with `MAX(version)`. PostgreSQL accepts exactly
one integer legacy row in that range; multiple PostgreSQL rows are malformed
because its canonical primary-key update sequence is singleton. A normalized
v16 repository has exactly one `version=16` row on either backend. The local,
unpushed legacy-column `version=17` candidate is accepted only for
transactional normalization to the same fenced singleton.

Fresh bootstrap produces the fenced singleton. Reopen rejects zero or
multiple rows, both or neither recognized column, unexpected metadata columns,
an invalid column type or key, a fenced value below 17, and either
representation above 17.

## Exact executable allowlist

Production, schema, and script paths:

- `db/schema_sqlite.sql`
- `db/schema_postgres.sql`
- `internal/db/migrations.go`
- `internal/db/schema_version.go`
- `internal/repository/capabilities/derive.go`
- `scripts/smoke.sh`

Existing tests:

- `internal/db/migrations_test.go`
- `internal/db/migration_backend_contract_test.go`
- `internal/db/schema_backend_contract_test.go`
- `internal/db/schema_version_test.go`
- `internal/repository/capabilities/derive_test.go`
- `internal/storage/phase1_compat_matrix_test.go`
- `internal/storage/store_atomic_repair_test.go`
- `tests/adversarial/g6_concurrent_operations_adversarial_test.go`
- `tests/integration/integration_test.go`
- `tests/integration/read_path_regression_test.go`

New proof paths:

- `tests/integration/schema_v17_backward_fence_integration_test.go`
- `scripts/run_schema_v17_backward_fence_gate.sh`
- `scripts/testdata/schema_v17_pre_v17_sqlite_probe_test.go`

Implementation documentation may change only `ARCHITECTURE.md` and
`docs/internal/storage_contract_v1_9.md`. Authority documentation is limited
to this record, the v1.13 README, phase list, release state, remediation
tracker, scope, source/test allowlist, and validation checklist.

The exhaustive tracked-source audit found no other production database-column
consumer. `internal/engine/doctor.go` delegates to the shared schema inspector;
CLI and coordination `schema_version` fields are unrelated output/control JSON
schemas. `scripts/smoke.sh` is the one additional direct SQL consumer found by
the audit and is included above. Any further production path is a Plan stop.

## RED, GREEN, and certification

The RED commit must compile and fail only because the current candidate still
uses the legacy column and permits historical access. GREEN must prove fresh,
v16, multi-row SQLite legacy, singleton PostgreSQL legacy, unfenced local v17,
fenced reopen, malformed/future rejection, and rollback behavior on both
backends. Exact historical source is built only under `/tmp`; its database-open
and full CLI paths must reject fenced v17 with zero semantic repository
mutation. The normal transient coordination lifecycle is permitted only when
it leaves no residual owner artifact; incidental control-directory timestamps
are excluded.

Because executable/schema bytes change after `c080dbd...`, its prior local
certification is supporting evidence only. The new implementation SHA requires
complete attached and fresh-detached exact-head certification, including
SQLite/PostgreSQL 16.15 compatibility, historical probes, full Go/race,
golangci-lint 2.9.0, vet, governance, Python/local-CI, clean-tree, and tracked
manifest proof before the evidence commit.

Exactly four append-only local commits are authorized:

1. `docs: authorize v1.13.16 backward version fence`
2. `test: reproduce schema v17 backward compatibility gap`
3. `fix: fence schema v17 from older binaries`
4. `docs: record schema v17 backward fence local certification`

## Local exact-head certification

The fence implementation commit is
`e2c2b922e2a4a3f72aef621ea8ff8ab8ccc5fa29`. Certification used Go 1.26.7
with `GOTOOLCHAIN=local`, PostgreSQL 16.15, and golangci-lint 2.9.0. The
linter archive SHA-256 is
`493aaaca2eba6c8bcef847d92716bbd91bbac4b22cdbb0ab5b6a581b32946091`;
the executable SHA-256 is
`9d3691b2e2410f1fe06b6cb72c734e4cd15eebbcb5d5c5f08ac0901c693fdbd9`.

Both the attached branch and a fresh detached worktree at that exact SHA
passed:

- the complete SQLite/PostgreSQL backward-fence matrix, including bootstrap,
  reopen, legacy-row normalization, malformed/future rejection, and rollback;
- the exact `2212ddd2981f3535fb71f38232bda6c1b7b35e97` historical SQLite
  database-open and PostgreSQL full-CLI oracle, with byte/semantic equality
  and no residual coordination owner;
- PostgreSQL 16.15 `internal/db` ordinary and race suites;
- `go test -count=1 ./...` and `go test -race -count=1 ./...`;
- golangci-lint 2.9.0 with `0 issues`, `go vet ./...`, module verify/tidy-diff,
  tracked-Go formatting, diff, shell syntax/ShellCheck, smart-quote,
  validation-matrix, snapshot-evidence-name, governance, and local-CI checks;
- all 215 Python tests and the applicable local pathsafe/storage tests plus
  Darwin amd64/arm64 and Windows amd64 secure-installer cross-builds.

The attached and detached tracked manifests each contain 1,559 files and have
SHA-256
`bf120218516f618dccfe6e340551f9f22b6526e4ae3375433d3b401d1e8e3a88`.
Both trees were clean at exit. An initial detached location under `/tmp` was
discarded before certification because an unrelated `/tmp/.git` caused Go VCS
stamping to inspect the wrong repository; the complete detached matrix above
was restarted in a fresh clean-parent worktree and passed.

```text
CK_V11316_015: IMPLEMENTED_PENDING_EXACT_HEAD_HOSTED_CERTIFICATION
FINDINGS_CONFIRMED: 14
FINDINGS_CLOSED: 12/14
PUSH: NOT_AUTHORIZED
HOSTED_CERTIFICATION: NOT_STARTED
HOSTED_RECONCILIATION_COMMIT: NOT_AUTHORIZED
PHASE_13: NOT_AUTHORIZED
```

No amendment, squash, reset, rebase, dependency or workflow change, push,
hosted execution, tag, release, CK-V11316-007/014 remediation, or Phase 13 work
is authorized.

## Phase 12R1R8 append-only certification supersession

After this record was committed, the mandatory
`scripts/check_versioned_row_writers.sh` gate was found to report the
authorized `INSERT INTO chunk_block_refs` statement in
`internal/storage/store_repair.go` as though it inserted into `chunk`. The
shared matcher lacked a complete table-identifier boundary.

The product/schema implementation and the substantive R1R7 attached/detached
evidence above remain valid. This record is nevertheless not final local
certification until the separately authorized Phase 12R1R8 guard correction
passes its dedicated regression and attached/detached tooling gates. Commit
`5ea8f3506337cc833fcac80a33eb88e98c994454` remains immutable historical
evidence and is superseded append-only; it is not amended or recreated.
