# Coldkeep v1.13.14 Validation Checklist

**Release:** `v1.13.14 — Final v1.x Correctness Remediation and Closure Certification`
**Status:** Active
**Branch:** `release/v1.13.14`

## Phase 0 — v1.13.13 Publication Truth Reconciliation and v1.13.14 Identity Activation

**Phase status:** Complete

- [x] Prove the annotated v1.13.13 tag, tag CI, stable release, and branch state.
- [x] Activate 1.13.14 and create required development control documents.
- [x] Freeze 17 findings and 4 release-process gates without runtime change.
- [x] Correct release ruleset `21155858` to accept authoritative main ancestry
  while retaining non-fast-forward protection and zero bypass actors.
- [x] Record evidence in the [Phase 0 record](v1.13.14-phase0-v1.13.13-publication-truth-reconciliation-and-v1.13.14-identity-activation.md).

## Phase 1 — Confirmed-Finding, Rejection, and Scope Freeze

**Phase status:** Complete

- [x] Confirm all 17 audit findings from current contract, implementation, and
  observable-consequence evidence.
- [x] Confirm all 4 release-process gates and retain `0/4` complete.
- [x] Freeze the rejection, duplicate/root-cause, compatibility, and owner-phase
  registers without implementing remediation.
- [x] Preserve exactly 21 numbered rows and `0/17` findings closed.
- [x] Record evidence in the [Phase 1 record](v1.13.14-phase1-confirmed-finding-rejection-and-scope-freeze.md).

## Phase 2 — Remediation Design and Regression-Contract Freeze

**Phase status:** Complete

- [x] Freeze implementation designs and regression contracts for all 17
  findings and 4 process gates without closing a row.
- [x] Freeze exact compatibility impact and owner-phase source/test allowlists
  for all 21 rows with no placeholder or unresolved semantic decision.
- [x] Preserve public shapes, schema, migrations, storage format, workflows,
  governance, runtime behavior, and the `0/17` and `0/4` counters.
- [x] Record evidence in the [Phase 2 record](v1.13.14-phase2-remediation-design-and-regression-contract-freeze.md)
  and link the four companion matrices.

## Phase 3 — Deterministic Remediation Test Seams

**Phase status:** Complete

- [x] Add only the nil-by-default, instance- or invocation-scoped seams frozen
  by Phase 2.
- [x] Replace mutable restore globals with per-`StorageContext` hooks and prove
  cross-context isolation under the race detector.
- [x] Preserve all finding behavior, public API, schema, storage, CLI, and
  release-process contracts; close no finding or gate.
- [x] Record evidence in the [Phase 3 record](v1.13.14-phase3-deterministic-remediation-test-seams.md).

## Phase 4 — Execution-Derived Verification Results

**Phase status:** Complete

- [x] Implement the frozen execution-derived verification ledger and result
  accounting for `CK-V1-AUD-001` using the Phase 3 observer seam.
- [x] Prove level-, target-, layout-, compression-, skip-, deduplication-, and
  failure-zero semantics with focused and race regression tests.
- [x] Remove the obsolete catalog-derived Engine result path without changing
  the public result, JSON, CLI, schema, storage, or caller-context contracts.
- [x] Record closure evidence in the
  [Phase 4 record](v1.13.14-phase4-execution-derived-verification-results.md).

## Phase 5 — Complete Caller Context and Cancellation Ownership

**Phase status:** Complete

- [x] Propagate the Engine caller context through Store, both Remove identities,
  both Restore identities, Verify, and every Doctor stage.
- [x] Stop new sequential batch dispatch after cancellation while preserving
  completed results and returning the existing cancelled classification.
- [x] Prove bounded independent cleanup for Store finalization/abort, Remove
  rollback, and Restore pins/temporary output with joined cleanup errors.
- [x] Preserve Phase 4 verification counters and the still-open Phase 6
  finalization-error contract.
- [x] Record closure evidence in the
  [Phase 5 record](v1.13.14-phase5-complete-caller-context-and-cancellation-ownership.md).

## Phase 6 — Single-File Store Finalization Error Propagation

**Phase status:** Complete

- [x] Reproduce false Engine success for both default- and explicit-codec Store
  requests when injected end-of-operation finalization fails.
- [x] Centralize exactly-once single-file finalization and implement the frozen
  four-case Store/finalization error matrix with `errors.Is`-stable chains.
- [x] Preserve Phase 5 cancellation/deadline classification, LocalWriter-owned
  quarantine and non-reuse, SimulatedWriter semantics, and StoreFolder
  ownership without beginning rollback remediation.
- [x] Record closure evidence in the
  [Phase 6 record](v1.13.14-phase6-single-file-store-finalization-error-propagation.md).

## Phase 7 — Rollback Close Failure and Quarantine Safety

**Phase status:** Complete

- [x] Reproduce the close-only rollback false-success baseline using the
  existing deterministic `faultfs` close/truncate seams.
- [x] Preserve truncate, close, verification, rollback, and quarantine causes
  through `errors.Is`-stable joined errors.
- [x] Retain exact pending/poisoned container identity until durable DB
  quarantine succeeds; refuse append and ignore commit acknowledgment while
  poison is unresolved.
- [x] Prove successful rollback stays quarantine-free, successful quarantine
  clears poison and prevents reuse, and failed quarantine retains retry state.
- [x] Preserve Phase 5 caller-context behavior and Phase 6 exactly-once Store
  finalization semantics.
- [x] Record closure evidence in the
  [Phase 7 record](v1.13.14-phase7-rollback-close-failure-and-quarantine-safety.md).

## Phase 8 — Snapshot Restore Overwrite and Shared Selector Contract

**Phase status:** Complete

- [x] Reproduce direct Engine overwrite=true behaving as overwrite=false for
  an existing target, then preserve false refusal and true atomic replacement.
- [x] Reproduce Restore accepting `Prefixes: ["docs"]` while Show/Diff reject
  it, then route all three methods through one private selector normalizer.
- [x] Prove exact/prefix/glob/regex/size/time/empty-query parity, prefix
  isolation, internal exact deduplication, and raw explicit-path accounting.
- [x] Prove direct Engine and CLI overwrite routing plus SQLite/PostgreSQL
  selector contracts without changing SnapshotDiff Limit or FileCount.
- [x] Record closure evidence in the
  [Phase 8 record](v1.13.14-phase8-snapshot-restore-overwrite-and-shared-selector-contract.md).

## Phase 9 — Snapshot Diff Limit and Snapshot Metadata File Counts

**Phase status:** Complete

- [x] Reproduce SnapshotDiff returning two entries for `Limit: 1`, then apply
  Limit only after query and change filtering while preserving raw Total and
  pre-limit Matched/Summary populations.
- [x] Reject negative diff Limit, preserve zero-as-unlimited and summary-mode
  semantics, and enforce deterministic stored-path ordering before slicing.
- [x] Reproduce false zero metadata for non-empty snapshots, then carry
  persisted membership through Find/List/Graph projections into Show and Tree
  without a new Catalog method, schema change, or N+1 query loop.
- [x] Prove empty, one/multi-file, limited/filtered show, lineage, graph/tree,
  SQLite/PostgreSQL, race, full-suite, and Phase 8 preservation contracts.
- [x] Preserve first-candidate CI failure `32821825718` and Required Gate
  failure `97723845725` as immutable evidence; do not treat successful
  first-candidate CodeQL as Phase 9 acceptance.
- [x] Reproduce the stale hosted integration assertion, then prove filtered
  raw Total against an equivalent unfiltered diff, Matched against unlimited
  returned Entries, and Summary against the complete filtered population in
  both plain and AES-GCM correctness tiers.
- [x] Require acceptance from CI, Required Gate, CodeQL, and CodeQL Aggregate
  at the exact follow-up commit recorded by the continuation execution report.
- [x] Record closure evidence in the
  [Phase 9 record](v1.13.14-phase9-snapshot-diff-limit-and-snapshot-metadata-file-counts.md).

## Phase 10 — GC Execution and Byte-Accounting Design Freeze

**Phase status:** Complete

- [x] Audit sealed dry-run, sealed live, and fully-dead-active live planning,
  transaction, filesystem, result, error, and advisory-lock ownership.
- [x] Verify active-container result accounting, independent worker
  transactions under one advisory-lock connection, and accumulated partial
  result representation from current source, tests, and public contracts.
- [x] Freeze bounded no-backlog scheduling, pass materialization and ordering,
  non-error affected/retained/skipped continuation, stop-new-dispatch, and
  plan-indexed result/error aggregation.
- [x] Freeze exactly-once safe-path physical `Stat`, dry/live byte credit,
  active accounting, surfaced `Remove` failure, negative size, and deterministic
  `int64` overflow behavior.
- [x] Freeze deterministic PostgreSQL `Workers=2` overlap with three pool
  connections and channel gates, smaller-pool serialization, and SQLite
  ceiling/order/stop semantics without changing production pool sizing.
- [x] Preserve `FINDINGS_CLOSED: 8/17`, `PROCESS_GATES_COMPLETE: 0/4`, and both
  GC findings as `CONFIRMED_OPEN_DESIGN_FROZEN`; make no implementation change.
- [x] Record evidence in the
  [Phase 10 record](v1.13.14-phase10-gc-execution-and-byte-accounting-design-freeze.md)
  and its scheduler and byte-accounting companion designs.

## Phase 11 — GC Workers and Accurate Reclaimed Bytes

**Phase status:** Complete

- [x] Reproduce ignored Workers, false-zero reclaimed bytes, and swallowed
  physical Remove failure before production changes.
- [x] Validate negative/zero/one/positive Workers and implement the bounded
  maintenance-owned no-backlog coordinator with deterministic pass/result/error
  ordering and stop-new-dispatch semantics.
- [x] Prove PostgreSQL Workers=2 overlap with three pool connections, natural
  serialization with a smaller valid pool, independent transactions, and
  SQLite dry-run ceilings without changing production pool sizing.
- [x] Capture physical size with exactly one safe-path `Stat`, credit dry-run
  plans and successful live sealed/active removals, and surface path, Stat,
  negative-size, Remove, cancellation, overflow, and unlock errors with
  truthful partial results.
- [x] Prove legacy/packed/mixed, none/zstd, plain/AES-GCM, PostgreSQL/SQLite,
  race, static, focused, full-suite, and generated-residue contracts.
- [x] Close only `CK-V1-AUD-003` and `CK-V1-AUD-004`, preserve `0/4` process
  gates, and record evidence in the
  [Phase 11 record](v1.13.14-phase11-gc-workers-and-accurate-reclaimed-bytes.md).

## Phase 12 — Configuration Defaults and Observability Parity

**Phase status:** Complete

- [x] Reproduce RestoreStoredPath's contradictory explicit-directory
  dependency while Verify, GC, Recover, and ordinary Restore use the default.
- [x] Route the frozen affected Engine operations through one private effective
  ContainerDir resolver and preserve explicit supplied values exactly.
- [x] Reproduce and remove the false StoreRequest repository-codec-default
  wording while preserving explicit request, `COLDKEEP_CODEC`, and `aes-gcm`
  runtime precedence.
- [x] Capture Store's full new/legacy environment table and warning behavior,
  reproduce the unsupported-positive Stats mismatch, and extract one pure
  Store-compatible block-target resolver.
- [x] Prove Store warning preservation, silent Stats parity, supported 1/2/3
  MiB targets, fallback construction boundaries, SQLite/PostgreSQL, codec,
  compression, static, focused, race, full-suite, and residue contracts.
- [x] Close only `CK-V1-AUD-011`, `CK-V1-AUD-013`, and `CK-V1-AUD-014`,
  preserve `0/4` process gates, and record evidence in the
  [Phase 12 record](v1.13.14-phase12-configuration-defaults-and-observability-parity.md).

## Phase 13 — CLI Help and Automation Contracts

**Phase status:** Complete

- [x] Reproduce the rejected positional README search example while proving
  the accepted `--name` parser shape and preserving positional exit-2 usage
  rejection.
- [x] Audit the established 0–4 runtime classifier, batch precedence, error
  class labels, and JSON error layers without changing runtime behavior.
- [x] Publish one complete process exit contract, invariant-over-execution-
  over-validation batch precedence, and distinct JSON field roles in root
  README and top-level help.
- [x] Expose none/zstd init forms, block-level pre-encryption ordering,
  new-writes-only scope, zstd-only levels, and range 1–9 while preserving the
  full accepted/rejected init matrix and test-owned `0600` `.env` output.
- [x] Close only `CK-V1-AUD-015`, `CK-V1-AUD-016`, and `CK-V1-AUD-017`, keep
  `CK-V1-AUD-005` partial, preserve `0/4` gates, and record evidence in the
  [Phase 13 record](v1.13.14-phase13-cli-help-and-automation-contracts.md).

## Phase 14 — Release-Gate Reproducibility Hardening

**Phase status:** Complete

- [x] Make `DefaultCLIEnv` require and canonicalize an absolute test-owned root,
  give every schema release-gate subtest a unique TempDir, and prove no
  repository-local control artifact is created.
- [x] Replace recursive evidence-name grep with the exact tracked-Git-source
  validator and prove missing, untracked, ignored, unreadable, and non-Git
  fixture behavior.
- [x] Inventory historical ignored evidence, preserve `.gitignore`, and
  implement external transient benchmark generation, exhaustive per-profile
  and top manifests, same-filesystem staging, atomic exact-SHA promotion,
  idempotent reuse, corrupt-existing refusal, signal cleanup, and explicit
  ignored-content/clean-worktree proof.
- [x] Add the strict branch-relative release-linearity validator, prove direct,
  inherited-merge, main-advance, local-merge, missing-ref, and unrelated graph
  cases, and preserve the Phase 0 active release ruleset with only
  `non_fast_forward` and zero bypass actors.
- [x] Close `CK-V1-GATE-001` through `CK-V1-GATE-004`, preserve findings at
  `16/17` and `CK-V1-AUD-005` as partial, record `PROCESS_GATES_COMPLETE: 4/4`,
  and publish the [Phase 14 record](v1.13.14-phase14-release-gate-reproducibility-hardening.md).
- [x] Preserve the intermediate GATE-001 hard stop, apply the owner-authorized
  `cmd/coldkeep/main_test.go` expansion only to the coordinated simulate-GC
  test, and prove the focused culprit, both PostgreSQL schema gates, and the
  complete uncached suite leave `cmd/coldkeep/storage` absent without cleanup.

## Phase 15 — Finding-by-Finding Integrated Regression Closure

**Phase status:** Complete

- [x] Inventory all 21 frozen rows and prove all 20 currently executable
  primary regressions exist in their owner packages.
- [x] Run every executable primary regression independently and reconcile its
  repository, filesystem, and error boundary to Phase 2 and owner evidence.
- [x] Record `CK-V1-AUD-005` as `LIFECYCLE_DEFERRED_NOT_CLOSED`, with Phases
  18/26 unchanged and zero current false-publication claims.
- [x] Run focused owner suites, PostgreSQL owner contracts, required race
  areas, all four process validators, and full-suite no-artifact proof.
- [x] Preserve `FINDINGS_CLOSED: 16/17`, `PROCESS_GATES_COMPLETE: 4/4`, and
  record `INTEGRATED_ROWS_RECONCILED: 21/21` in the [Phase 15 record](v1.13.14-phase15-finding-by-finding-integrated-regression-closure.md)
  and [matrix](v1.13.14-integrated-regression-closure-matrix.md).

## Phase 16 — Backend, Codec, Compression, and Storage-Layout Proof

**Phase status:** Complete

- [x] Inventory every applicable compatibility cell before adding tests and
  classify it as direct, hosted-only, missing, or frozen-contract N/A.
- [x] Prove all 10 owner rows across SQLite/PostgreSQL, plain/AES-GCM,
  none/zstd, and valid legacy/packed/mixed placement combinations.
- [x] Record 20/20 backend, 16/16 codec, 16/16 compression, and 21/21 layout
  cells with zero missing direct proof and zero unjustified N/A cells.
- [x] Preserve production behavior, test code, public shapes, schema, storage
  format, defaults, workflows, governance, root README, and AUD-005 lifecycle.
- [x] Publish the [Phase 16 record](v1.13.14-phase16-backend-codec-compression-storage-layout-proof.md)
  and [compatibility matrix](v1.13.14-compatibility-proof-matrix.md).

## Phase 17 — Cancellation, Race, Fault, Adversarial, and Cross-Platform Proof

**Phase status:** Complete

- [x] Inventory all 21 numbered rows and classify the 17 deep rows plus four
  frozen-contract/lifecycle exclusions before adding tests.
- [x] Prove caller cancellation/deadline propagation, truthful partial
  results, bounded cleanup, and error-cause preservation with deterministic
  existing seams.
- [x] Run complete owner/process and PostgreSQL race matrices with zero data,
  shared-result, goroutine, or global-state leaks.
- [x] Prove deterministic filesystem, transaction, cleanup, quarantine, and
  evidence-lifecycle faults without new production hooks.
- [x] Run both long-run-enabled PostgreSQL G1–G17 codec profiles and require
  exact-head native Linux/macOS/Windows success.
- [x] Record 10/10 cancellation, 17/17 race, 12/12 fault, 17/17 adversarial,
  and 15/15 native cells with zero missing direct proof or unjustified N/A.
- [x] Publish the [Phase 17 record](v1.13.14-phase17-cancellation-race-fault-adversarial-cross-platform-proof.md)
  and [runtime matrix](v1.13.14-adversarial-runtime-proof-matrix.md) without
  production, test, workflow, root README, or AUD-005 lifecycle changes.

## Phase 18 — Current Documentation and Release-State Reconciliation

**Phase status:** Complete

- [x] Reconcile root README, the active changelog entry, the v1.13 index, and
  every authorized current v1.13.14 release-control surface.
- [x] Preserve `FINDINGS_CLOSED: 16/17`, `PROCESS_GATES_COMPLETE: 4/4`,
  `INTEGRATED_ROWS_RECONCILED: 21/21`, and complete compatibility/runtime
  matrices without claiming final corrective closure.
- [x] Record `CK-V1-AUD-005` as still lifecycle-partial, with Phase 18
  candidate truth complete and Phase 26 publication truth pending.
- [x] Prove the release branch present, `v1.13.14` tag absent, stable GitHub
  release absent, historical evidence unchanged, and current contradictions
  absent in the [Phase 18 record](v1.13.14-phase18-current-documentation-and-release-state-reconciliation.md).

## Phase 19 — Formal Final v1.x Corrective Closure Decision

**Phase status:** Complete

- [x] Reconfirm all 17 findings, all 4 gates, all 21 integrated rows, and all
  8 frozen rejection outcomes in the [Phase 19 decision](v1.13.14-phase19-formal-final-v1x-corrective-closure-decision.md).
- [x] Record all A–L decision inputs as satisfied, with AUD-005 explicitly
  `SATISFIED_NON_BLOCKING_LIFECYCLE_PENDING` and still open partial.
- [x] Scope zero-blocker claims to the authoritative frozen v1.13.14
  corrective inventory and preserve `FINDINGS_CLOSED: 16/17`.
- [x] Approve entry into the final release lifecycle without claiming release
  readiness, merge, tag, publication, or final published closure.

## Phase 20 — Final Closure/Handoff Record and Immutable Candidate Freeze

**Phase status:** Complete

- [x] Record the final corrective handoff in the
  [Phase 20 record](v1.13.14-phase20-final-closure-handoff-and-immutable-candidate-freeze.md)
  without certifying release readiness, publication, or final closure.
- [x] Derive and track the exact 1,397-entry
  [candidate-core manifest](v1.13.14-immutable-candidate-core-manifest.txt) with
  SHA-256 `984221fc5dc0e9d2d76d97cfb2c334bd7a52733682d818cb839d7c5caacac4da`.
- [x] Preserve `FINDINGS_CLOSED: 16/17`, AUD-005 partial, all 4 gates, all 21
  integrated rows, and both deep proof matrices.
- [x] Bind terminal handoff/freeze activation to exact-head Phase 20 hosted
  acceptance and leave Phase 21 implementation separately authorized.

## Phase 21 — Independent Full Local Exact-Candidate Release Gate

**Phase status:** Complete

- [x] Execute the complete promoted local Profile B gate against exact
  candidate `89a1890ad90a6778e0e19062a7eff0327482005a`.
- [x] Pass plain/AES-GCM, race, stress, long-run, adversarial, smoke, legacy,
  benchmark-integrity, operator, snapshot, retention, and final sweep gates.
- [x] Preserve the 1,397-entry immutable candidate core byte-for-byte and
  publish the [Phase 21 record](v1.13.14-phase21-independent-full-local-exact-candidate-release-gate.md).

## Phase 22 — Hosted Exact-Head Security, Quality, and Governance Gate

**Phase status:** Complete

- [x] Reconcile exact-head CI run `32949760025` and Required Gate job
  `98123359910`, with all 25 required jobs successful.
- [x] Reconcile native Linux, macOS, and Windows, both correctness profiles,
  stress, long-run, adversarial, smoke, legacy, coverage, and all eight hosted
  benchmark jobs.
- [x] Reconcile CodeQL run `32949759949`, Aggregate job `98118569421`, live
  governance, release linearity, and immutable-core identity in the
  [Phase 22 record](v1.13.14-phase22-hosted-exact-head-security-quality-and-governance-gate.md).

## Phase 23 — Protected Release PR Merge

**Phase status:** Next

- Implementation state: `BLOCKED_PENDING_PHASE23R_RECERTIFICATION`.

### Phase 23R recovery ledger — State C

- [x] Preserve the frozen Phase 2/14/15/20–22 evidence and original Phase 20
  manifest byte-for-byte.
- [x] Add `--candidate-ref` to the release-linearity validator and use the
  normalized candidate commit for merge-base and post-base merge scans.
- [x] Fail closed unless a release PR is against `main`, uses a matching head
  ref, comes from `GITHUB_REPOSITORY`, supplies a lowercase full SHA, and that
  SHA resolves locally.
- [x] Prove synthetic merge `HEAD=M` with candidate `R2`, wrong base/repository,
  mismatched ref, malformed/missing event identity, invalid/unresolvable SHA,
  release push/tag `HEAD`, and non-release PR skip behavior.
- [x] Refreeze the repaired 1,397-entry core at SHA-256
  `58d2909d1fe490e3ce246c48b42d02ff8ea256877fec05f1c8c2972f83248023`.
- [x] Observe fresh PR #112 quality and Required Gate success with
  `PR_HEAD_SHA == AUDIT_LINEAGE_CANDIDATE_SHA` and a distinct synthetic
  `GITHUB_SHA`.
- [x] Repeat Phase 21 local certification in a detached exact-ref worktree and
  record the [Phase 23R local recertification](v1.13.14-phase23r-phase21-independent-local-recertification.md).
- [ ] Repeat Phase 22 hosted certification and final-head PR checks.

## Phase 24 — Post-Merge Reconciliation and Exact Final-Main Gate

**Phase status:** Not started

## Phase 25 — Annotated Tag, Tag CI, and Stable Publication

**Phase status:** Not started

## Phase 26 — Post-Publication Truth Reconciliation and Final Cleanup

**Phase status:** Not started
