# Coldkeep v1.13.14 Phase 23R — Phase 21 Independent Local Recertification

**Status:** Complete

**Recovery state:** C — real PR context accepted; local recertification passed;
hosted recertification pending

**Branch:** `release/v1.13.14`

**Exact refrozen candidate SHA:** `5a41e0c9db7f9a0f690ea4878190a69579b015d5`

**Execution date:** 2026-08-26 UTC

## 1. Purpose and boundary

This record closes the local half of Phase 23R recertification. It first records
the external State B proof that the repaired release-linearity path selected the
authoritative same-repository PR head, then records a fresh complete Phase 21
Profile B gate executed from a detached worktree at that exact repaired SHA.

It does not certify hosted Phase 22 readiness, merge PR #112, create a tag,
publish a release, begin Phase 24, or authorize Phase 23 execution. The original
Phase 2/14/15/20–22 evidence remains immutable historical authority for its
superseded candidate.

## 2. State B hosted PR-context proof

Commit `5a41e0c9db7f9a0f690ea4878190a69579b015d5` was pushed normally to the
existing release branch and PR #112. Its exact-head push and PR checks passed:

| Surface | Run | Required job | Result |
| --- | ---: | ---: | --- |
| Push CI | `32965485055` | `98172243578` | Success |
| Push CodeQL | `32965485094` | Aggregate `98167289859` | Success |
| PR CI | `32965490997` | quality `98166949266`; Required Gate `98171932537` | Success |
| PR CodeQL | `32965491041` | — | Success |

The PR quality log and exact pull ref established the required identity split:

```text
PR_HEAD_SHA: 5a41e0c9db7f9a0f690ea4878190a69579b015d5
PR_SYNTHETIC_MERGE_SHA: 48e9bcffbdfa7ecb004afd46557207891b8832af
AUDIT_LINEAGE_CANDIDATE_SHA: 5a41e0c9db7f9a0f690ea4878190a69579b015d5
QUALITY_TEST_TREE: 48e9bcffbdfa7ecb004afd46557207891b8832af
RELEASE_LINEARITY_TREE: 5a41e0c9db7f9a0f690ea4878190a69579b015d5
PR_HEAD_SHA_EQUALS_AUDIT_LINEAGE_CANDIDATE_SHA: YES
PR_HEAD_SHA_EQUALS_PR_SYNTHETIC_MERGE_SHA: NO
```

Checkout logged the synthetic merge as merging the repaired head into main.
The audit logged the authoritative same-repository head and the validator
reported both `RELEASE_LINEARITY_CANDIDATE_REF` and
`RELEASE_LINEARITY_CANDIDATE_SHA` as the exact repaired release SHA. This is
the missing real execution-context proof; GATE-004 therefore reclosed without
restoring the superseded Phase 21/22 certifications.

## 3. Detached entry and refrozen core

The independent gate ran at:

```text
DETACHED_WORKTREE: /tmp/ck-v11314-phase23r.0MOvYB/repo
DETACHED_HEAD: 5a41e0c9db7f9a0f690ea4878190a69579b015d5
ENTRY_WORKTREE: CLEAN
PHASE23R_CORE_MANIFEST_ENTRIES: 1397
PHASE23R_CORE_MANIFEST_SHA256: 58d2909d1fe490e3ce246c48b42d02ff8ea256877fec05f1c8c2972f83248023
ENTRY_CORE_MANIFEST_BYTE_FOR_BYTE: PASS
POST_GATE_CORE_MANIFEST_BYTE_FOR_BYTE: PASS
CORE_SURFACE_CHANGED: NO
```

The original Phase 20 manifest remains unchanged at SHA-256
`984221fc5dc0e9d2d76d97cfb2c334bd7a52733682d818cb839d7c5caacac4da`.

## 4. Toolchain and isolation

```text
GO: go version go1.25.10 linux/amd64
DOCKER_COMPOSE: Docker Compose version v2.40.3
PSQL: psql (PostgreSQL) 16.15 (Ubuntu 16.15-0ubuntu0.24.04.1)
JQ: jq-1.7
SHELLCHECK: ShellCheck 0.9.0
PYTHON: Python 3.12.1
BASH: GNU bash 5.2.21
GOLANGCI_LINT: v2.6.2 built with go1.25.10
```

Every Go command used `GOTOOLCHAIN=go1.25.10`. Go, lint, and XDG caches were
external to the worktree. PostgreSQL used the repository Compose service and
fresh Phase23R-owned databases; manual storage and restore destinations were
task-owned. No operator data was used.

## 5. Profile A and deterministic proof

The complete quality-equivalent surface passed: clean storage, module
tidiness, Go formatting, Bash syntax, smart-quote and ShellCheck guards,
validation-matrix and versioned-row-writer validators, pinned candidate-relative
lint with zero issues, vet, plain/AES-GCM race owner suites, chunk determinism,
build, local audit, process fixtures, evidence validators, all 48 release-state
fixtures, supported real PR state, release linearity, and CLI version `1.13.14`
in text and JSON modes.

Both plain and AES-GCM matrices passed fresh race-enabled correctness, stress,
long-run, 25-iteration refcount containment, full G1–G17 adversarial, focused
G14–G17, and smoke gates. Named PostgreSQL mutation-cardinality, both-codec
round-trip, remove/recovery, all independent-process G6, and all maintenance
advisory-session selectors reported explicit pass events rather than skips.

Legacy post-migration compatibility, Linux pathsafe/storage approximation, the
nine-test snapshot release gate, and Darwin amd64, Darwin arm64, and Windows
amd64 secure-install test-binary cross-compilation/format verification passed.
Native hosted operating-system execution remains owned by Phase 22.

## 6. Supplementary and benchmark evidence

The supplementary command
`go test -p 1 ./tests/... -count=1 -v -timeout 20m` passed with the database and
long-run gates enabled. Its only conditional skips were the unpromoted released
v1.7 binary fixture and two packing-dependent isolation assertions; there were
zero missing-environment skips. Required long-run and named families had
already executed explicitly in the promoted matrix.

All four hard benchmark integrity profiles passed and all profile/top checksum
manifests validated. The exact-SHA bundle was atomically promoted to
`.release-evidence/v1.13.14/5a41e0c9db7f9a0f690ea4878190a69579b015d5/`.
Inventory reported zero staging directories. Timing remained advisory:

| Profile | Integrity | Timing classification |
| --- | --- | --- |
| `none-w1` | Pass | Within reference |
| `none-w4` | Pass | Valid informational warning; 4 crossings |
| `zstd-w1` | Pass | Within reference |
| `zstd-w4` | Pass | Valid informational warning; 2 crossings |

The report-only critical coverage surface completed without an enforced
threshold.

## 7. Profile B operator and snapshot gates

Sections 5–11 passed against fresh state: Doctor text/JSON, guarantee matrix,
bootstrap enabled at schema v16, bootstrap-disabled fail-fast, destructive
task-owned clean install, CLI success/error envelopes, batch parser/execution
contracts, stored-path behavior, deliberate physical refcount drift, GC
refusal with `GC_REFUSED_INTEGRITY`, verify exit 3 with
`PHYSICAL_GRAPH_REFCOUNT_MISMATCH`, exact repair, healthy verify/GC recovery,
and both maintenance batch envelopes.

Sections 15–17 passed with snapshots `pre-gc-gate` and `post-gc-gate`. The
diff truthfully reported one added file; two retained logical roots blocked
current mapping removal; restore was byte-identical; delete without force
returned usage exit 2; force plus dry-run was read-only; deleting only the
first snapshot preserved retention; deleting both allowed both mappings to be
removed; retained roots fell to zero; and final verify passed. Invalid regex
and size range selectors returned usage exit 2. The tracked-source validator
found all 19 snapshot evidence declarations, and the pull-request template was
present and reviewed.

## 8. Final full sweeps and Section 18

Separate fresh databases produced:

```text
go test -count=1 ./...       PASS  real 8m29.863s
go test -race -count=1 ./... PASS  real 11m38.453s
```

The race sweep included `tests/adversarial` in 337.735s and
`tests/integration` in 316.139s. The uncached sweep included scripts in
235.311s, adversarial in 302.624s, and integration in 228.415s.

Final Section 18 revalidated the full CI-enforcement audit, exact PR-head
linearity, benchmark checksums/inventory, snapshot declarations, validation
matrix, all 48 release-state fixtures and the supported real PR state, Doctor,
Go/Bash formatting and syntax, ShellCheck, module tidiness, vet, candidate lint,
and `git diff --check`.

## 9. Harness corrections and residue proof

Discarded invocations exposed only harness/context omissions: the snapshot
gate initially used the wrong bootstrap variable; the supplementary umbrella
initially omitted database opt-in; early manual assertions over-specified
command/action JSON fields or sequenced removal before a stored-path check; and
the first global sweep omitted the standard AES-GCM fixture key. The detached
worktree also required a task-owned Git context shim for the audit fixture's
ordinary-release-push subcase. The shim reported `release/v1.13.14` only for
`git -C <exact detached root> symbolic-ref --quiet --short HEAD` and delegated
all other Git operations unchanged. After its scope was narrowed to the exact
candidate root, all audit and release-state fixtures passed. No correction
changed a tracked candidate file or masked a deterministic product failure.

Cleanup removed only Phase23R-owned databases, storage, binaries, reports,
cross-build outputs, restore destinations, smoke output, and Python bytecode.
The detached worktree is ordinary-clean; the only ignored inventory is the
authorized exact-SHA benchmark evidence. `cmd/coldkeep/storage`, repository
`storage`, `.ci-storage`, test binaries, `.pyc`, and `__pycache__` are absent.

## 10. State C decision

```text
CK_V1_GATE_004: RECLOSED_PHASE23R
V1_X_RELEASE_PROCESS_GATES_COMPLETE: 4/4
ACTUAL_PR_CONTEXT_REVALIDATION: PASS
CURRENT_IMMUTABLE_CANDIDATE_CORE_FREEZE: ACTIVE_PHASE23R
PREVIOUS_PHASE20_FREEZE: HISTORICAL_SUPERSEDED
LOCAL_RELEASE_READINESS_CERTIFICATION: PASS_PHASE23R_RECERTIFICATION
HOSTED_RELEASE_READINESS_CERTIFICATION: PENDING_PHASE23R_HOSTED_RECERTIFICATION
RELEASE_READINESS_CERTIFICATION: PENDING_PHASE23R_HOSTED_RECERTIFICATION
PHASE_23: NEXT
PHASE_23_EXECUTION: BLOCKED_PENDING_PHASE23R_RECERTIFICATION
PHASE_24: NOT_STARTED
```

Phase 22 hosted recertification must run against the exact head created by this
lifecycle-only record and must re-prove the same 1,397-entry core digest. PR
#112 remains open and unmerged.
